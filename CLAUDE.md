# CLAUDE.md — L6790A 단일단 PF LLC · TV(OLED) SMPS

## 한 눈에

**무엇을 만드나** — ST **L6790A**로 **단일단(Single-Stage) PF LLC** TV SMPS를 설계한다.
90–264 Vac 입력, **25 V / 26.3 A = 657.5 W** 출력, OLED 패널 구동용.
기존 2단(Boost PFC + LLC)의 **400 V 벌크 커패시터와 승압단을 통째로 없애는 것**이 목적이고,
그 대가로 출력에 **75 mF 커패시터 뱅크**가 붙는다. 이 맞바꿈이 프로젝트의 전부다.

**산출물은 넷** — ① 설계 가이드(배포용 문서) ② SMath 시트(정확한 수학 모델)
③ 교육자료(초보자용) ④ 수정된 ST 스프레드시트. 데이터시트·회로도·논문은 **입력 자료**다.

**지금 어디까지** — **설계 수치는 끝났고 전부 검증을 통과했다.** SMath 정본 시트(19개 절)와
수정된 스프레드시트와 참조 구현 `l6790.py` 세 곳의 값이 일치하며, 사용자가 SMath에서
F9까지 돌려 확인했다. 판정 지표 23개 전부 합격.

**다음에 할 일** — **설계값을 더 만질 일은 없다. 남은 것은 문서 작업이다.**
가이드 rev 2.0(9:1 예제 전면 교체)까지 끝났다 — 남은 문서 작업은 워크북 rev 0.7 정리와 교육자료 한국어판이다 (시트 §4).

**가장 많이 헷갈리는 것** — 이 폴더에는 사양이 다른 설계가 셋 떠다닌다.
PPT·스프레드시트 원본의 예제는 **240 W / 60 V**(가이드는 rev 2.0 부터 본 사양), ST 평가보드는 **670 W / 48 V**,
그리고 **우리 것은 657.5 W / 25 V**다. 숫자를 볼 때마다 어느 것인지 확인할 것 (시트 §1.2).

---

**이 문서는 새 세션의 시작점이다.** 이어서 작업하는 데 필요한 것만 담는다.
**읽는 순서:** 시트 §1 → 시트 §3 → 시트 §4. 도구를 쓰려면 그다음 시트 §8 → 시트 §9.

문서는 둘이고, 답하는 질문이 다르다:

| 파일 | 답하는 질문 | 누가 쓰나 |
| :--- | :--- | :--- |
| **`CLAUDE.md`** (이 파일) | 지금 값이 뭐고, 뭘 어떻게 하면 되나 | 사람 — 단 **시트 §3은 `snapshot.py`** |
| **`HISTORY.md`** | 왜 그렇게 정했나 — 근거·정오표·사고 기록 | 사람 |

**시트 §3만 기계가 쓰는 이유:** 손으로 옮겨 적다가 실제로 한 번 낡았다 — 시트가 71.4 mF일 때
문서는 80 mF라고 적혀 있었다. 지금은 `snapshot.py`가 쓰고 Stop hook이 신선도를 지킨다.

---

## 1. 프로젝트 정의

### 1.1 목표

STMicroelectronics **L6790A** 신규 컨트롤러로 **단일단(Single-Stage) PF LLC** TV SMPS를 설계한다.
기존 2단(Boost PFC + LLC) 구성의 400 V 벌크 커패시터와 승압단을 통째로 제거한다.

| 항목 | 값 |
| :--- | :--- |
| 입력 | 90–264 Vac (유니버설, topology morphing) · 라인 주파수 **47–63 Hz** |
| 출력 | **25 V / 26.3 A = 657.5 W** |
| 출력 리플 (2f_l, pk-pk) | **≤ 5 %** (1.25 V) |
| 홀드업 | **12 ms, 최저 19 V @ 100 Vac, 최악 라인 위상 기준** |
| 2차 정류 | 센터탭 + SR (N_rect = 1) |
| 용도 | TV SMPS — **OLED 패널 구동 전원** |
| 컨트롤러 | L6790A (SO16N, **DRAFT** 데이터시트 2026-04-30) |

### 1.2 기존 예제와 반드시 구분할 것

SMath 구시트·PPT·ST 스프레드시트 원본의 예제는 전부 **240 W / 60 V**이고(가이드는 rev 2.0 부터 본 사양 9:1 이며 240 W 는 부록 C.5·C.30·C.31 에만 남는다), ST 평가보드는
**670 W / 48 V**다. **셋 다 우리 사양이 아니다.**

| | 가이드 예제 | ST EVL6790_670W | **본 프로젝트** |
| :--- | ---: | ---: | ---: |
| 출력 | 240 W / 60 V / 4 A | 670 W / 48 V / 14 A | **657.5 W / 25 V / 26.3 A** |
| 2차 정류 | FB (N_rect=2) | CT (N_rect=1) | **CT (N_rect=1)** |
| 권선비 n | 2.5 | 3.2 | **6.0** |
| C_out | 4.08 mF | 18.8 mF | **75.2 mF** |

> **▷ 출력 전력이 EVL 보드와 거의 같고 반사 전압도 거의 같으므로(150 vs 154 V), 탱크와 1차측은
> EVL 보드를 거의 그대로 재사용할 수 있다. 달라지는 것은 2차측 전부와 출력 커패시터 뱅크다.**

### 1.3 아키텍처 판단 — 25 V 직출력 (확정, 2026-08-21)

ST 평가보드는 **48 V 중간 버스를 만든 뒤 후단 벅 3개**로 나눈다 (BUCK1 24 V/21 A → OLED).
즉 **ST 자신도 OLED 레일을 단일단 LLC로 직접 만들지 않는다.**
우리는 그 후단 벅을 없애는 구성이며, **그 대가가 75 mF 출력 뱅크다.**

사용자 확정 사항:

| 항목 | 확정값 |
| :--- | :--- |
| 아키텍처 | **25 V 직출력** (48 V + 후단 벅 안은 채택하지 않음) |
| 리플 허용치 | 최대 5 % (pk-pk) |
| 홀드업 | 12 ms · 최저 19 V · 최대 출력 전류 · @100 Vac |
| 홀드업 시작 위상 | **최악 위상 기준** — 리플 골(θ=45°)에서 AC가 끊기는 것이 최악이므로 `V_out − ΔV_pp/2`에서 시작 |
| 라인 주파수 | 47–63 Hz. **최악은 최소값이므로 시트 전체가 `f.line_min` = 47 Hz 기준** |
| 입력 브리지 | 양쪽 다 계산하고 시트에서 선택 (`s.br` = 1 동기 / 0 다이오드) |
| 1차 스위치 | **소자를 고르지 말 것.** 전압·전류·필요 R_ds(on)·허용 C_oss만 산정 |
| SMath | 사양 변경이 가능하도록 **전 입력을 변수로** |
| **VCC** | **외부 12 V 전원** — 트랜스포머에서 뽑지 않는다 (2026-09-01) |
| **보조권선** | **ZCD 감지 전용.** 코어당 1턴 × 3 직렬 = 유효 3턴, `k_aux` = 1.5, V_aux 37.5 V. VCC 를 대지 않으므로 `k_aux_max`(=VCC 25 V 한계)가 적용되지 않는다 |
| **권선비** | **세트 7.5 : 1 (유닛당 1차 5턴 : 2차 2턴) — 이것이 확정이다** (2026-09-21 사용자 정정). 벤더에 나간 사양서도, 배포용 AN(`an_pdf.AN_VARIANT`)도, 가이드의 예제 사슬도 전부 이 설계점 위에 있다. `n.T` 7.5 · `n` 6.024 · `λ` 0.55 · `L.m` 20 µH. **아래 9:1 서술은 2026-09-04 의 중간 단계다** — 그때는 2차를 1턴으로만 볼 수 있다고 보아 3의 배수뿐이라 판단했는데, 2026-09-07 에 **유닛당 5:2** 가 나오면서 7.5 가 실현됐다. **`CLAUDE.md` 시트 §3 과 빌더 기본 변형은 아직 9:1 이다 — 사용자 결정 대기.** 이하 원문: 세트 9 : 1 (유닛당 1차 3턴 : 2차 1턴) — 2026-09-04. 2차가 대전류라 1턴이 한계이고 3코어 직병렬이라 감을 수 있는 비가 3의 배수뿐이다. 본사가 지시한 n.T 7.47(2차 2턴)은 실현 불가. `n` = 7.4527 · `λ` = 0.4583 · `L_m` 24 µH. **`λ`를 같이 내려 1차 손실을 원설계 수준으로 유지**했다 |
| **OVP1** | 목표 27.5 V 대신 **28.21 V** — `R.ZCD_H` 을 E24 **680 kΩ** 로 (계산값 662.52 k, `R.ZCD_L` 19 kΩ). 계산값보다 큰 E24 를 골라 리플 꼭대기에서 떨어뜨린다. **`n.aux` = 3 기준이다** — 2턴 설계(`n.aux` 1.5)의 330 kΩ · 28.17 V 와 혼동하지 말 것 |

---

## 2. 자료 지도

| 경로 | 역할 | 상태 |
| :--- | :--- | :--- |
| **`Smath/L6790A_SingleStage_PF_LLC_Design_Guide.sm`** | **정본 시트.** 25 V/26.3 A 사양, 19개 절 전 범위, 노란 입력 셀 113개, **ZedGraph 판 7장**(게인 2 · 라인 위상 3 · 루프 2), 도면 3장, **인쇄물 A3 가로 43쪽** | **rev 1 완료 · SMath F9 실행 확인됨** |
| **`Smath/variants/L6790A_9to1.sm`** · `Smath/variants/L6790A_7p5to1.sm` · **`Smath/variants/L6790A_8to1.sm`** · `Smath/variants/L6790A_6to1.sm` | **설계점 4종.** 세트 권선비 9:1 · 7.5:1 · **8:1** · 6:1. `L6790_VARIANT` 환경변수로 `build_l6790_smath.py` 가 만든다(인자가 없으면 정본 자리에 쓴다). 셋 다 `mismatches: 0` · `VERDICT OK` · `check_theta 0` · `audit_rpn 0` · `check_trans_spec 0` (2026-09-09 재생성 — 오늘의 변경 전부 반영: 쪽 피치 1120 · 접힘 줄 간격 24 px · 스윕 2배 · 판 굵기(데이터 4.0/기준선 2.0) · 범례 글꼴 · `f.Max` 삭제와 y축 auto · `KEEP` 0.8). **셋 다 성립하며 성격이 다르다** — 2차는 6:1(`k.PSR` 1.195), 1차는 7.5:1, 코어는 7.5:1 만 EE6405 에 들어간다(`k.Ae` 1.098). **ZVS 는 9:1 이다** — 2026-09-08 정정: 예전에 "ZVS 는 6:1" 이라고 적어 두었는데 `k.ZVS` 는 9:1 2.408 > 7.5:1 2.091 > 6:1 2.073 이고, 부하까지 훑으면 격차가 더 벌어진다(시트 §4.2 항목 9). **2026-09-21 신설 — 8:1 (2 × 8:2).** 사용자 질문 "3 × 5:2 대신 2 × 8:2 면?" 에서 나왔다. `N.x` 가 빌더에 `3` 으로 박혀 있어 **변형 파라미터로 뺐다**(기존 셋은 3 그대로라 값이 하나도 안 움직인다). 코어를 하나 줄이는 대신 유닛당 1차가 8턴이고, `N.x` = 2 · `N.s` = 2 라 감을 수 있는 세트비가 **1.0 단위**가 된다(3 × 2 는 1.5 단위라 8 을 못 잡는다). 탱크는 7.5:1 과 같고(`C.r` 100 nF · `L.r` 11 µH · `L.m` 20 µH · λ 0.55) `n.T` 만 7.5 → 8 이다. **전기 판정은 전부 7.5:1 이상이다** — `k.ZVS` 2.091 → **2.364** · `k.ceil` 1.283 → **1.348** · `k.lam` 0.443 → **0.537** · `k.PSR` 1.063 → 1.033(둘 다 통과). `n.aux` 는 1.5 → **1** 이고(`N.aux` = `n.aux`·`N.s`/`N.x` 로 코어당 1턴을 유지), `R.ZCD_H` 는 330 → **220 kΩ**(계산 208.2 k 위의 E24), OVP1 28.93 V. **`B.pk` 는 182.1 mT 로 안 변한다** — `V.o_eff/(4 f.r N.s A.e)` 라 코어 개수도 `N.p` 도 안 들어간다. **대가는 자기 쪽이다** — `A.L` 413.3 → **242.2 nH**(갭이 커진다) · 코어당 2차 실효 9.45 → **14.37 A**(+52 %) · 나권 동선 +59 %. **`k.floor` 는 1.021 로 7.5:1 과 같으므로 시트 §4.2 항목 5b 의 `R.T` 경고가 그대로 적용된다** — 사양서가 개방 인덕턴스에 ±10 % 를 거는데 이 `k.floor` 가 버티는 저하는 4.0 % 뿐이다. 채택하려면 `R.T` 를 11 → 10 kΩ 으로 내릴 것. **2026-09-07 재검토:** 6:1 의 `C.T` 를 390 → **330 pF** 로 고쳤다(390 은 자기 설계 상한 375 pF 밖이었고 `k.CTd` 0.962 가 그것을 가리키고 있었다 — 가이드 C.34). 그리고 변형에 따라 달라지는 **주석 6개가 9:1 숫자를 문자열로 갖고 있어** 값에서 만들도록 고쳤다(C.33) | 2026-09-07 |
| **`Calculation Excel Sheet/variants/`** | **워크북과 벤더 사양서.** `L6790_workbook_{9to1,7p5to1,6to1}.xlsx` 와 `Transformer_Spec_{9to1,7p5to1,8to1,6to1}.xlsx`. **사양서는 4종이고 워크북은 3종이다** — 8:1 은 2026-09-21 에 시트·사양서만 만들었고 워크북은 없다. 워크북은 정본을 복사해 `Res. Tank Design F9·F43·F44·F45` 와 `Device Setting D21·F83·F91` 을 패치한 뒤 Excel 로 전체 재계산한 것이다. **`C.T` 는 `D21` 직접 입력이지 F열 선정 셀이 아니다** | 2026-09-07 |
| **`Smath/L6790A_gainzed.sm`** | **LLC 게인 곡선 — ZedGraph 판을 소스 보고 편집한 것.** `Reference/Smath Bode Plot Example/Zedgraph` 의 ZedGraph 5.1 소스가 있으니 추측할 필요가 없다. **`Scale.GetObjectData` 가 필드 순서를 못박는다** — `min·max·majorStep·minorStep·exponent·baseTic` 뒤 48바이트 지점에 `minAuto·maxAuto·majorStepAuto·minorStepAuto` 가 1바이트씩 온다. **TI 판은 `minAuto=maxAuto=0`** 이라 축이 고정돼 있었고, 그것이 곡선이 10 Hz~100 kHz 창 밖에 있어 안 보인 이유 전부였다. **`LinearScale` 과 `LogScale` 은 직렬화가 완전히 동일**(`base.GetObjectData` + `schema2=10`)해서 길이접두 붙은 타입 이름만 바꾸면 로그축이 선형축이 된다. 문자열도 길이접두를 같이 고치면 **길이 자유**로 바꿀 수 있어 제목·축이름·범례를 원하는 대로 쓴다. `GraphPane` 은 `xAxis→x2Axis→yAxisList→y2AxisList→curveList` 순이라 첫 scale 블록이 x 다. 이 조작들은 **`zedpane.py`** 에 있다. **시트 §9 에 트레이스 개수 시험이 있고 답이 나왔다 — 판이 가진 곡선 수는 한계가 아니다.** T1 은 **4곡선 판에 행렬 8개**, T2 는 1곡선 판에 3개, T3 는 4곡선 판에 2개를 준다. 각 트레이스가 높이 1·2·3… 의 **수평선**이라 판단할 것 없이 **선 개수를 세면 된다.** **결과: 8·3·2 — SMath 는 판의 곡선 목록을 입력 개수에 맞추고 저장 시 되쓴다.** 그래서 `harvest_pane.py` 로 8곡선 판을 떼어 `panes/transfer8.b64` 에 넣었다. `build_gainzed.py` · `check_sm.py --allow eval,augment,vectorize` | 확인 대기 |
| **`Smath/L6790A_gainladder.sm`** | **P19 는 그려지는데 gainplot 은 왜 안 그려지나 — 원인을 한 칸씩 좁히는 사다리 시트.** L1~L4 가 네 가지 차이(range 상한 리터럴/변수 · 문장 5/9 · 행렬 1/4 · 11점/91점)를 하나씩만 바꾸고, **각 단마다 플롯 앞에 숫자를 먼저 찍는다** — 빈 프레임이 "데이터 없음"인지 "화면 밖"인지는 그 숫자로만 갈린다. **L6 은 TI 앱노트 시트의 방식** — 인덱스 하나짜리 열벡터 두 개를 `eval(augment(vectorize(x),vectorize(y)))` 로 붙인다. **L7 은 행렬 없이 함수를 직접 그린다**(`M.f(x)`) — `Project Admittance Copy.sm` 이 `S1(x)` 를 그렇게 그린다. L5 는 같은 창을 20/8/4/2 px 로 그린다. `build_gainladder.py` · `check_sm.py --allow eval,augment,vectorize,M.f` 로 검사한다 | 확인 대기 |
| **`Smath/L6790A_gainplot.sm`** | **게인 곡선을 SMath 로 그릴 수 있는가 — 독립 확인용 2.6쪽 시트.** 정본에 넣기 전에 답부터 알려고 따로 만들었다. G1 게인식 검산(f_n=1 에서 Q 와 무관하게 1) · G2 네 곡선을 한 번의 for 루프로 N×2 행렬에 채움 · **G3 곡선 1개 · G4 곡선 4개를 프레임 4개** (둘 다 시트 §16.6 이 이미 하는 것이라 확실) · **G5 곡선 4개를 프레임 1개 — 이것이 진짜 질문**이다. `sys(G.a,G.b,G.c,G.d,4,1)` 로 묶으며 `Reference/Smath Bode Plot Example/Project Admittance Copy.sm` 에서 가져온 구문인데, `sys`·`mat` 은 `smsheet.SAFE_FUNCS` 와 `check_sm.ALLOWED` 밖이고 이 프로젝트가 한 번도 내보낸 적이 없다. **G5 가 비거나 붉으면 G4 로 물러서면 되고 그건 오늘도 된다.** `build_gainplot.py` 로 만든다 | 확인 대기 |
| `Smath/L6790A_probe.sm` | 진단용 소형 시트 P1–P20. 큰 시트가 안 열리면 **이것부터** | 완성 |
| `Design Guide/AN_L6790A_Design_Guide_rev2_0.md` + `figures/` | **본체 문서(한국어).** 식 [1]–[148], **예제는 9:1 정본 설계점**(2026-09-11 rev 2.0 — 240 W 잔재와 7.5:1 본문을 전부 9:1 로 교체, 값은 시트·`l6790.py`), 부록 A~D(C 는 C.1–C.31). 새 절: $f_{sw,max}$ 는 λ 의 입력(가이드 §3.6) · 공진 위/아래는 $n$ 이 정한다(가이드 §4.2) · Simpson 5점 검증(가이드 C.18). **개정 이력·프로젝트 사정·내부 파일 참조는 전부 지웠다**(2026-09-11 사용자 지시) — 근거는 이 문서와 `HISTORY.md` 에만 있다. 파일 이름도 `rev2_0` 으로 바꿨다(구 `rev1_1`) | rev 2.0 (2026-09-11) |
| **`Design Guide/AN_L6790A_SingleStage_PFC_LLC_ApplicationNote_v1.2.pdf`** | **배포용 영문 AN 58쪽** + 같은 구조의 한국어판 `Design Guide/AN_L6790A_SingleStage_PFC_LLC_ApplicationNote_KR_v1.2.pdf`. 식 41 · 그림 35 · 표 29. **교차 참조는 이름으로 푼다(2-pass) — 번호를 손으로 적지 말 것.** 안 풀리는 참조는 `check_refs()` 가 **빌드를 실패시킨다**(예전에는 조용히 `Section ?` 를 인쇄했고 실제로 한 건이 PDF 까지 갔다). **설계점은 7.5:1**(`an_pdf.AN_VARIANT` — 정본 시트 9:1 과 무관하게 변형 시트를 읽는다). `an_pdf.py`+`an_body.py` 로 생성하며 **모든 수치가 `l6790.py`·시트에서 나온다**. **2026-09-07 v1.1:** 트랜스포머 절 신설(소절 7개) · 회로도 3장 추가 · 무부하 최소 게인 한계 · f.sw(θ) 닫힌형 · FB 제어 법칙 · K-factor 보상 설계법 · 이득 여유 · 입력 커패시터 신설 · `use_unicode()` 로 그리스 문자 복구 · **응용 특정 서술 전수 제거**(패널·TV·세트 등) — **3분할은 가이드의 뼈대가 아니라 선택지 한 절로 내렸다**. **2026-09-09:** 절 2개 신설 — **`f.sw,max` 는 탱크를 검사하는 것이 아니라 계산한다**(λ 후보 둘이 그것으로 나눈다; 225 kHz 는 1.5×`f.r` 관례이지 한계가 아니고, 실리콘 한계인 `f.Max` 와 종류가 다르다) · **공진 위/아래는 `n` 이 정한다**(라인 조건 7개의 피크 `f.sw` 표). 본문 틀에 하단 5 pt — ReportLab 이 행간으로 맞추다 마지막 줄 디센더가 3.2 px 밖으로 나갔다. **2026-09-08:** 머릿글을 하단 꼬리말로 · **적응형 그림 블록**(`FigBlock`) — 조금 모자랄 때만 그 그림 하나가 양보해 쪽 밑 빈 공간을 없앤다. 전역 축소는 답이 아니다(배율 6종 실측: 0.93 이 최선이어도 여백 1006 pt, 적응형은 921 pt 이고 4장만 최소 83 % 로 줄었다) | v1.2 (2026-09-21) &mdash; **2026-09-21 (9차):** **권선의 턴이 코어 바에 얹혀 있었다** (사용자 지적). 잉크 사이 간격을 **페이지 포인트**로 재니 `an_rac` 1.37 · `an_integrated` 1.88 · `an_flyback_llc` **0.24** · `an_fha_steps` **−1.79**(겹침). **`X.xfmr` 가 `gap` 을 호출자 말대로 쓴 것이 원인** — 턴 반경은 `scale(ax)` 에서 나오므로 같은 `gap` 이 그림마다 다르게 먹는데 호출자는 알 수 없다. 이제 `gap` 은 *요청*이고 실제 값은 `max(요청, core + 2.5·r)` 이며, **호출자는 `x ± gap` 을 가정하지 말고 반환된 단자에서 리드선을 읽어야 한다**. 전 그림 2.1~3.9 pt 로 뜬다. **`figcheck.windings` 신설**(1.5 pt 바닥, 바닥 5.0 으로 올리면 12건 — 검출력 확인) &mdash; **2026-09-21 (8차):** **두 회로의 가로 예산 재배분** (사용자 지적: 플라이백이 너무 크고 LLC 는 글자가 겹친다). 축 범위 하나를 공유하던 탓에 플라이백은 빈 루프가 폭의 3/4 였고 LLC 는 `L.m` 이름이 라이저 위에 얹혔다. **패널이 서로 다른 단에 있으므로 가로 정렬은 필요 없다** — 배율(기호 크기)만 공유하고 배치는 따로. 플라이백 루프를 3.0 폭으로 줄이고, LLC 는 **배터리를 라벨 레일 + 접지로 교체**해 번 폭을 탱크에 줬다. **`figcheck` 가 이 겹침을 못 잡았다** — 회로도 축은 `shield()` 대상이 아니고 겹친 픽셀이 바닥 10 px 밑이었다. **살짝 얹힌 글자는 확대해서 눈으로 볼 것** &mdash; **2026-09-21 (7차):** **그림 글씨체를 본문과 같은 Liberation Sans 로** (사용자 지적: 그림 글씨 가독성). 그림은 matplotlib 기본 DejaVu Sans 였다. `figs.py` 의 `font.family` = `[Liberation Sans, (한글), DejaVu Sans]` — **한글 폰트는 뒤로** 옮겼다(글자마다 폴백하므로 라틴은 앞에서, 한글은 뒤에서 온다). **mathtext 도 같이** (`fontset=custom`, `rm/it/bf/sf/cal` 동일 활자, 폴백 `stixsans`) — 안 바꾸면 `i$_p$` 같은 조각만 DejaVu 로 남아 한 라벨 안에서 활자가 섞인다. **본문 수식은 그대로 `stix`** 다(`an_pdf.eqpng` 가 따로 박는다) &mdash; **2026-09-21 (6차):** 사용자 지적 4건. **LLC 회로에 FET 가 없었다**(게이트 파형만 있고 스위치가 없었다) — 하프브리지 레그 S1·S2 를 그려 넣었다. **자속 행이 net 만 그렸다** — 권선별 자속 점선 2개 + 합 실선 + **그 사이의 띠(= 상쇄량)**로 바꿨다. 같은 띠가 LLC 에서는 *동시 상쇄*, 플라이백에서는 *이어받기* 를 뜻한다. **절을 6장(설계 절차) → 2장(일반 LLC 설명)으로 옮겼다**(데드타임 절 다음). `an_mmf` 는 설계 수치라 7장에 남는다. **그림 3 `an_rac`** 에 `R.ac` 가 무엇을 대체하는지 점선 상자로 표시하고 화살표를 저항까지 닿게 했다 — 상자의 왼쪽 경계가 곧 `n²` 을 넣느냐 빼느냐다. 영문 74쪽 &mdash; **2026-09-21 (5차):** **LLC 와 플라이백 트랜스포머를 가르는 절 신설** (6.9 `Not a flyback, and why that changes the core`, 영문만). 핵심은 **두 권선이 언제 도통하느냐** 하나다 — 플라이백은 같이 안 도통하므로 권선 전류 전체가 자화 전류이고 자속이 *전류*를 따라가지만, LLC 는 같이 도통해 기자력이 상쇄되고 남는 차이만 코어를 자화시키며 자속은 *볼트-초*를, 즉 출력 전압만을 따라간다. **`A.e` 가 필요한 이유는 패러데이 법칙이 저장 여부를 안 묻기 때문**이고 달라지는 것은 계산의 입력뿐이다. **"LLC 는 에너지를 저장하지 않는다" 는 표어이지 사실이 아니다** — 자화 에너지는 반주기마다 저장하고 그것이 ZVS 의 재료다. 그림 2장 신설(`an_flyback_llc` 회로 2 + 파형 4행 × 2열 · `an_mmf` 기자력 상쇄 대 개방시험 + 18.32/15.19/12.41 A 막대). **AN 의 오류 하나를 같이 고쳤다** — 식 [Isat] 의 분모가 `L_open` 이라 인쇄된 산수가 인쇄된 답을 못 만들었다(9.97 대 12.41); 시트는 (7차)에 이미 `L.mu` 였고 AN 만 낡아 있었다. `I.sat = I.eq × V.OVP2/V.o` 도 식으로 세웠다. **도구 결함 둘** — `schem.ind` 가 1 px 미만 리드로 **없는 접점을 만들었고**, `schem.shunt` 가 색을 준 소자의 연결선을 남색으로 그렸다. `schemx.xfmr` 에 `s_dot`(2차 극성점 위치). 그림 41. **한국어판은 미반영** &mdash; **2026-09-21 (4차):** 사용자 지적 11건. 데드타임 전류를 탱크 한 줄 + 레그 네 갈래로 · 2.7절에 커뮤테이션 회로(`an_recovery`)와 드레인 전류 역회복 스파이크 · 그림 19 버스 리플의 **위상 반전과 13배 과장**을 정정(골이 θ=45°, 5 % pk-pk) · 그림 28 흰 글자(`shield` 가 흰 후광을 입히고 있었다) · 그림 13·14 를 8구간 모델로 재작성(게이트·데드타임 포함) · **그림 31 오류 2건**(T-모델 션트는 `L_μ` 이지 `L_m` 이 아니다 · 이상변압기는 `n : 1` 이지 `1 : M_v` 가 아니다) · 권선 턴 수 상한 · **트랜스포머 장 7절 신설**(누설이 코어를 정한다) · **TDK PQ 40/40 채택과 단면도**(`cores.py`·`an_core_section`) · 사슬표 전 행 대입 계산 · **그림 크기 전면 조정**(`figcheck.tiny` 신설, 배율 0.27~0.41 → 0.70, 8구간 시트는 2패널 4장). 그림 41. **한글판은 구조만 따라갔다** &mdash; **2026-09-21 (3차):** 영문판 전수 낭독 검토 완료 — 평이한 영어로 고치고 정정 4건(게인 여유 근호 음수의 뜻 · 3차식 근 부호 · 유도성 영역 · 용량성 경계) · 스위치 이름 Q1~Q4 → **S1~S4** 통일(f15·f16·f17·4장) · f04 를 `f_r` 배수로 · f11 범례 · f19 막대 라벨(`shield` 흰 글자 제외). **한글판은 사용자 컨펌 후** (`HISTORY.md` 2026-09-21 3차) &mdash; **2026-09-21:** 그림 5(두 공진)의 `f.o = 90.4 kHz`·`f.r = 151.7 kHz` 를 `f.o = 0.60 f.r`·`f.r` 로 — 축이 `f/f.r` 인 그림에 아직 소개 안 된 설계의 절대값이 적혀 있었다(사용자 지적). 그림 19(2단 구성)는 블록·파형을 키우고 파형마다 노드로 점선 유도선을 내렸다 — 어느 파형이 어느 블록인지 안 보였다. 정류 파형은 0 선 위로(양극성으로 그려져 있었다), mains 칸은 전압·전류를 같이(1단이 하는 일이 그 전류를 빚는 것) &mdash; **2026-09-17 v1.2:** 명칭을 single-stage **PFC** LLC 로 통일 · 판정 여유(k) 의 정의와 분자/분모를 적은 절 신설 · 계산값을 6장(방법)에서 7장(설계 예제)으로 이동하고 값마다 식 번호와 대입 계산을 붙임(`an_pdf.eqref`) · 표지와 본문의 자기 지시·홍보성 문구 제거 · 영문 문장 단순화 · 한국어판을 같은 판 번호로 맞춤 · **2장(평범한 LLC·평범한 PFC)에서 single-stage 언급을 전부 걷어냈다** — 일반 LLC 를 모르는 독자에게 배경을 설명하면서 아직 설명하지 않은 아키텍처를 들이대고 있었다. 설계 절차를 깨뜨리는 두 단계를 적은 주의 상자는 3장 머리로 옮겼다. 의인화된 표현(lives / 사는 곳 / 일생)을 두 판과 그림 2장에서 제거했다. Q 의 한국어 명칭은 **품질계수로 유지**한다(2026-09-17 사용자 결정) — 한 번 `Q` 로 뺐다가 되돌렸다. 양호도·선택도도 쓰이지만 품질계수가 가장 널리 쓰인다 · **한국어판의 억지 한자어를 영어/외래어로 정리했다**(2026-09-17 사용자 지시) — 용량성/유도성 → `capacitive`/`inductive` · 공진 아래/위 → `below`/`above`(AN 2.6 절에 정의 상자 신설) · 첨두 → 피크 · 승압/강압 → `boost`/`buck` · 이득 → 게인, 이득 여유 → `gain margin` · 왜형 → `distortion`(변위/왜형 인수 → `displacement`/`distortion factor`, 총 고조파 왜형 → THD) · 완충기 → 버퍼 · 응력 → `stress`. **천이·실효·영교차·역회복·기자력은 표준 용어라 두었다.** 영어판은 손대지 않았다 · **그림 9(세 동작 영역)의 색칠이 틀려 고쳤다**(2026-09-17 사용자 지적) — 굵게 그린 곡선은 *전부하*인데 용량성/유도성 경계를 `f.o` 에 긋고 있어서 `f.o`~경계 사이가 초록(유도성·ZVS)으로 칠해져 있었다. 이제 `zvs_edge(Q.pk, λ)` 로 칠하고, 게인 피크는 별표로 따로 표시해 근사라고 적는다. 두 판 본문에도 같은 주의 상자를 넣었다 |
| **`Design Guide/L6790A_Transformer_Design_KR_v1.0.pdf`** | **트랜스포머 설계 정리 9쪽(한국어).** 설계 사양(시트 §1)을 먼저 못박고 "왜 1차 5턴에 2차 2턴인가" 하나를 끝까지 따라간다 — 입력 범위 → λ → 결합도 k → n vs n_T → 5:2 → **보조권선은 λ 보정이 필요 없는 이유**(시트 §8, 부하 전류가 없어 누설 전압강하가 안 생김) → 벤더 사양. `tx_pdf.py`+`tx_body.py` 로 생성하며 **AN 과 같은 레이아웃 엔진**을 쓴다 | v1.0 (2026-09-04 갱신) |
| `Design Guide/generators/` | 도구 체인 — 시트 §9 | 재사용 가능 |
| **`Design Guide/generators/schemx.py`** · `figs_ref.py` | **회로 기호 키트와, 남의 그림을 대체한 도면들.** 8구간 패널에서 사용자가 승인한 기호(인핸스먼트 MOSFET·첨점 없는 권선·센터탭 트랜스포머·배선 아래로 깔리는 전류 강조)를 `schemx.py` 에 모았고 `schem.py` 가 거기서 권선과 스위치를 가져온다. **이동은 패널 3장을 픽셀 단위로 대조해 확인했다.** **2026-09-21:** `figcheck` 에 배선 검사를 심자 키트 자체의 결함 둘이 나왔다 — `schem.cap` 이 단자를 판 위치(±gap)가 아니라 판 *길이*(±s)로 돌려줘 **이 함수로 그린 모든 커패시터가 판에서 s−g 만큼 떨어져 있었고**, `S.diode` 는 한쪽 단자가 바에서 s 만큼 떨어져 있었다(지금은 kit 의 `vdiode` 와 같은 비율로 중심 대칭). 같은 검사가 잡은 것: `an_rac` 다이오드 방향 반대(출력이 뒤집혀 있었다 — 저측 정류로) · `an_integrated` 1차·2차가 리턴 레일 공유(절연 단락) 와 정류기 없는 C_o · `an_fha_steps` 1차 리턴 미연결과 9° 사선 리드 · `an_pfc_boost` 상단 레일이 부하까지 안 감 · `an_architectures` 틀 밖 상자 · 접점 점 누락 다수. **8구간 패널은 레그/레일 T 4곳과 D2 리턴의 접점 점 5개만 늘었고 나머지는 픽셀 단위로 같다**(차이 픽셀을 블롭으로 묶어 위치를 전부 확인했다) | 2026-09-21 |
| **`Calculation Excel Sheet/Uncryped_04092026_LGE_670W_L6790A_spread sheet.xlsx`** | **ST 원본 워크북** (LGE 670W 명의, 2026-09-01 수령). 받았을 때는 **확장자만 xlsx 이고 실제로는 IRM 암호화된 OLE2 컨테이너**였다 — `EncryptedPackage` + `DRMEncryptedTransform`, openpyxl · xlrd · Excel COM 전부 거부(2026-09-04 확인). **2026-09-08 사용자가 암호를 풀어 옆에 `Uncryped_04092026_LGE_670W_L6790A_spread sheet.xlsx` 를 두었다 — 그것을 읽으면 된다.** 평범한 xlsx 이고 openpyxl 로 열린다(시트 10 · 차트 104 · 그림 14). 예전에 쓰던 `worksheets/` XML 추출본은 그래서 없앴다 — 시트 10장만 있던 것보다 수식 그래프·차트·그림이 다 들어 있는 이쪽이 낫다. 원본 `sheetN` ↔ 우리 `sheetN+1`(CHANGELOG 한 장 차이). 시트 보호는 읽기를 막지 않으므로 해제할 필요가 없다 | 암호화 원본 + **디크립트본으로 판독 가능** |
| `Calculation Excel Sheet/L6790_spreadsheet_r1_0_corrected_rev0_6.xlsx` | ST 툴 수정본. `CHANGELOG_rev0_6` 시트에 전 변경 이력 | rev 0.6 (rev 0.7 예정) |
| **`Calculation Excel Sheet/LGE_670W_L6790A_Transformer_Spec_rev1.xlsx`** | **벤더에 보낼 트랜스포머 요구사양서.** 9:1 설계점 · A4 세로 1장 · 빈 칸 없음 · 검사성적서가 아니라 **우리가 보내는 사양서**다. 사용자가 손으로 배치·색을 잡고 결선도 2장을 넣은 판이었고, 핀 배정은 승인원 방식(NS2 3→5, NS3 4→6)을 따른다. **2026-09-08 삭제** — 설계점이 셋이므로 `variants/Transformer_Spec_{9to1,7p5to1,6to1}.xlsx` 가 이 자리를 대신한다. `build_trans_spec.py` 는 **인자로 변형을 요구하고 `variants/` 에만 쓴다** | **2026-09-08 삭제 — `variants/` 의 사양서 3종으로 대체됐다** (사용자 결정) |
| `Calculation Excel Sheet/L6790_Trans_20260901.xlsx` | KH필룩스 **SAMPLE INSPECTION REPORT 양식**에 우리 값을 채워 넣은 것. 위 rev 1 로 대체됐다. 양식 자체는 벤더 것이므로 회신 대조용으로 남겨 둔다 | **2026-09-08 삭제** (폐기 예정이었다) |
| `Training Material/…Design_Training.pptx` | 영문 교육 자료 **61장** (10 × 5.625 in). 25 V 예제 · 모핑 5장 · 출력뱅크 3장 | rev 2 (영문) |
| `Datasheet/20260430 L6790A LLC-PFC IC preliminary.pdf` | ST 원본 DS, 23쪽. **DRAFT** — TBD 다수, 내부 모순 존재 | 원본 |
| `EVB Schematic/*.pdf` | 마더보드 + 도터보드 4종 | 원본 |
| `LGE Material/` | 기존 2단 양산 보드 회로도 + LLC 트랜스포머 사양서. **PFC단은 빠져 있음** | 원본 |
| `Reference/EVLHV101SSR50W Design Guide.sm` | HVLED101 단일단 플라이백 SMath. **본 시트의 레이아웃 원본** | 원본 |
| `Reference/Smath Bode Plot Example/` | `Bode.sm` · `MyBode.sm` · `Project Admittance Copy.sm`. **시트 §16.6 보드선도의 기법 원본** | 원본 |
| **`Reference/Visio-LLC drawing.pdf`** | **본 설계 회로도(벡터).** 3코어 · 1차 직렬 · 2차 병렬 · 센터탭 저측 정류 · 보조 3직렬. **벡터라 배선을 넷리스트로 뽑을 수 있다** — 점프 아크 13개가 배선을 끊으므로 그것을 이어야 한다 | 원본 |
> **▷ AN 의 그림은 전부 우리가 그린 것이다 (2026-09-20).** 예전에는 15장이 ON Semiconductor · Infineon · Toshiba · MPS 의 AN 을 잘라 붙인 것이었다. 전부 `figs_ref.py` 로 다시 그렸고 캡션은 **그림의 출처가 아니라 논지의 출처**를 인용한다. 세 장은 다시 그리면서 **틀린 것을 고쳤다** — ① 용량성/유도성 경계를 게인 피크에 긋던 두 장을 `zvs_edge` 로 옮겼다(시트 §6.1 항목 8b) ② capacitive/inductive 파형 두 칸을 `phase()` 에서 직접 그린다. 처음에는 8구간 패널의 시간영역 모델로 그렸는데 **그 모델은 항상 ZVS 파형을 내므로 유도성 파형 둘에 이름표만 다르게 붙인 꼴**이었다 ③ 세 경우 비교 그림은 공진 위/at 에서 반사인파를 **절단**한다 — 8구간 타임라인은 `f.sw = f.r` 에서 프리휠링 길이가 이미 음수다. `figures/an_ref_*_raw.png` 4장(원본 크롭)만 기록으로 남겼다.

| `Reference/` (그 외) | ECCE Wenbo 1.65 kW 단일단 논문, Infineon AN 2013-03, onsemi AN-4151, **TDK PQ 40/40(B65883A·B65884E)·PQ 32/30(B65879B·B65880E) 데이터시트 2022-10** — 2026-09-21 수령. **치수도가 이 안에 있고 단면도는 거기서 나온다**(`cores.MECH`). TDK CDN 이 기본 UA 를 403 으로 막으므로 브라우저 UA 를 줘야 받아진다. TDK SRX/SRV LLC 트랜스포머 가이드, Infineon ICL5102(LCC) AN, Toshiba PFC AN(AKX00080 — PFC 그림 3장), Infineon AN 2012-09(공진 LLC 동작·설계 — above/below 파형 3열의 출처이고 표기가 우리와 같다), TI SDAA419**(권선비 보정 인수 — 가이드 §3.10 인용, `HISTORY.md` 2026-09-04), **Toshiba `Resonant Circuits and Soft Switching`(2019, 32쪽)** — 그 AN §3.2 가 하프브리지 LLC 반주기를 8개 모드로 쪼갠다. 우리 STEP 1~4 와 데드타임의 **독립 출처 확인**이고, 다만 "freewheeling" 이라는 말은 데드타임의 바디 다이오드에 쓴다 (시트 §6.1 항목 8c) | 원본 |
| `HISTORY.md` | **근거 문서** — 검증 이력, 정오표 C.1–C.17, 하드웨어 분석, 사고 기록, 시트 §8 **사건** 타임라인 | — |

### 2.1 산출물의 성격 (사용자 정의)

1. **Design Guide** — 엔지니어 배포용 기본 설계 가이드
2. **SMath** — 실제 설계 시 수학적 모델링을 정확하게 하기 위함
3. **Training Material** — 이 토폴로지 이해가 부족한 초보자용
4. **Datasheet / Schematic / Reference** — 입력 자료 (수정 대상 아님)

---

## 3. 현재 설계값 — **정본**

<!-- BEGIN GENERATED  ...  snapshot.py 가 씁니다. 손으로 고치지 마십시오 -->

SMath 시트에서 **기계가 읽어 적은 값**이다. 사람이 고치지 말 것 —
`cd "Design Guide/generators" && python snapshot.py` 로 다시 만든다.
시트를 바꾸고 이 블록을 갱신하지 않으면 **Stop hook이 턴을 막는다.**

```
탱크     Cr 100 nF · Lr 11 μH · Lm 24 μH · λ_act 0.458 · fr 151.8 kHz · fo 85.1 kHz
         Q_pk 0.5 · R_ac 20.97 Ω · n 7.453 · n_T 9
주파수   f_sw HB코너 108.5 kHz · f_sw FB코너 209.5 kHz · T_ZC 530 ns · k_ZVS 2.408
전류     I_sec,pk 115.6 A · I_Lr,pk 17.7 A · I_trafo,pk 15.51 A · I_Lm,pk 12.79 A
         I_pri,rms 13.71 A · I_pri,라인 11.04 A · I_diode 30.31 A · I_Cout 33.85 A
출력     Cout 개당 470 μF · 개수 160 · Cout 합계 75.2 mF · 리플 1.184 V · 리플 4.74 %
         홀드업 13.42 ms · k_hold 1.119 · ESR 뱅크 0.1875 mohm · R&N 20.6 mV · 개당 리플전류 0.21 A · 세라믹 10 μF
자기     직렬 개수 3 · Np 3 · Ns 1 · A_L 1296 nH · B_pk 364 mT · A_e 필요 206 · k_Ae 0.549 · 개방시험 전류 15.7 A
IC       R_CS 24 mohm · 개당 120 mohm · 병렬 5 · I_OCP1 22.9 A · k_OCP 1.295
         R_T 11 kohm · C_T 470 pF · f_Min 92.2 kHz · f_Max 319.6 kHz
         R_CFG 30 kohm · V_BO 84.85 V · R_BM 30 kohm · C_in 2200 nF · n_aux/n_sec 3
루프     G_o 130.1 rad/s · EA_o 36.2 rad/s · f_z 6.05 Hz · f_p 47.3 Hz · f_px 999 Hz
         f_cross 17.57 Hz · Φ_M 49.64 ° · 3차 고조파 4.23 %
보상망   R_I 100 kohm · R_o 11 kohm · R_P 1.3 kohm · R_B 6.2 kohm · V_Z 12 V
         C_Fo 82 nF · C_F 560 nF · R_F 47 kohm · C_fx 1.55 nF
1차      V_DS 최소 485.4 V · P_in 684.8 W
```

**판정 지표 — 전부 "> 1" 이어야 한다** (시트 §17):

```
k.675 2.112          k.ADT_hi 1.909       k.ADT_lo 5.500       k.Ae 0.549  <-- 미달
k.BO 1.061           k.CFG_hi 1.567       k.CFG_lo 2.000       k.CT_hi 2.128
k.CT_lo 1.741        k.CTd 1.723          k.Cin 1.071          k.D3rd 1.182
k.GM 3.418           k.NRCS 1.108         k.OCP 1.295          k.OVP1 1.026
k.OVsat 1.226        k.PM 0.993           k.PSR 0.929  <-- 미달 k.Ploss 0.304  <-- 미달
k.RB_hi 1.045        k.RB_lo 1.076        k.RCS 1.022          k.RN 1.455
k.RP 1.106           k.RT_hi 2.727        k.RT_lo 2.200        k.RTd 1.088
k.SU 1.341           k.TT 7.294           k.VCC 0.272  <-- 미달 k.ZVS 2.408
k.auxr 0.279  <-- 미달 k.ceil 1.526         k.floor 1.084        k.hold 1.119
k.lam 0.792  <-- 미달 k.pulse 5.856        k.tau_hi 2.321       k.tau_lo 2.068

통과 34 / 40   미달: k.Ae, k.PSR, k.Ploss, k.VCC, k.auxr, k.lam   ← 아래 설명 참조
k.Ae 0.549 는 **코어가 아직 선정되지 않았다.** EE6405(113 mm²) 를 자리표시자로 둔 결과이며, 필요 A.e 는 206 mm² 다. 2차를 2턴으로 하면(세트 9:1 유지, 유닛 6:2) 103 mm² 가 되어 EE6405 가 들어간다. 가이드 부록 [151]. **계산 오류가 아니라 아직 안 닫은 설계 항목이다**
k.OVP1 1.026 은 V.OVP1_act 28.21 V, 목표 27.5 V, 하한은 V.out+5 % -> 합격
k.PM 0.993 은 Φ.act 49.64°, 목표 50°, 합격선 45° -> 합격
k.PSR 0.929 는 2차 레그당 3.23 W 대 3 W. **9:1 로 오면서 미달로 넘어갔다** — 7.5:1 에서는 1.063 이었다. n.SR 을 3 으로 올리면 1.39 로 회복되고 그것 말고는 방법이 없다(`Power Components` D103 한 칸). **계산 오류가 아니라 아직 안 닫은 설계 항목이다**
k.Ploss 0.304 는 1차 정적 도통 소자(LVG2) 9.62 W 대 잠정 예산 3 W. 어느 기술로도 소자 1개로는 불가능하다(열간 25.3 mohm 필요, 600 V 최선이 17 mohm@25C). **2026-08-24 사용자가 현 구성으로 확정** — 방열판 여부는 시제품 열 실측에서 판정한다. **오류가 아니다**
k.VCC 0.272 는 k.auxr 과 같은 이유로 적용되지 않는다 — 보조권선은 ZCD 전용, VCC 는 외부 12 V. **오류가 아니다**
k.auxr 0.279 는 보조권선이 **ZCD 감지 전용**이라 적용되지 않는다 — VCC 는 외부 12 V 에서 온다. n.aux_max 는 "OVP2 에서 V.aux 가 VCC 정격 25 V 를 넘지 마라"는 뜻이고, 이 권선은 VCC 를 대지 않는다. 워크북과 값을 맞추려고 계산은 남겨 둔다. **오류가 아니다**
k.lam 0.792 는 **무부하** 최소 게인 조건이라 전부하 판정과 무관하다. 무부하 게인은 점근선 1/(1+λ.act) 아래로 내려가지 않으므로 요구 게인이 그보다 낮으면 아예 해가 없다 — 그때는 버스트 모드가 맡는다. 9:1 은 231 kHz 에서 해가 있고 7.5:1 · 6:1 은 해가 없다. 가이드 부록 C.31. **오류가 아니다**
```

> 아래는 **판정이 아니라 그냥 비율**이다. "1 미만"이라고 문제 삼지 말 것:
> 

<!-- END GENERATED -->

### 3.1 왜 이 값들인가 — 놓치기 쉬운 선택 3가지

- **C_r을 계산값(94 nF)보다 크게, L_m을 계산값(22 µH)보다 작게** 잡았다. Q_pk를 낮추고 λ_act를
  지켜 ZVS 여유를 벌기 위해서다. 240 W 예제는 T_ZC 여유가 12 %뿐이었으나 여기는 108 %다.
- **CT(N_rect=1)를 택했다.** 25 V 저전압/대전류에서는 내압 여유보다 도통 손실이 지배한다.
  FB는 도통 경로에 소자가 2개라 **도통 손실이 정확히 2배**(9.6 W vs 4.8 W).
  부수 이득: ST 툴의 C.5 결함을 원천 회피하고 EVL 보드와 검증 경로를 공유한다.
- **권선비는 누설 통합형 N_p/N_s = 15/2 → n_T 7.5 → n 6.04.** LGE 기존 보드의
  3직렬 구조를 그대로 쓴다 (`HISTORY.md` 시트 §5.2). 도면에는 권선비뿐 아니라 **반드시 개방
  인덕턴스와 단락 인덕턴스를 함께 지정**한다 — n과 n_T를 혼동하면 게인이 통째로 어긋난다.

---

## 4. 지금 상태와 다음 할 일

| Phase | 내용 | 상태 |
| :-- | :--- | :--- |
| 0 | 사양 확정 | ✅ 완료 (시트 §1.3) |
| 1 | LGE 기존 보드·트랜스포머 파악 | ✅ 완료 (`HISTORY.md` 시트 §5.2) |
| 2 | SMath 정본 시트 | ✅ 완료 rev 1 |
| 3 | SMath 실행 검증 (F9) | ✅ 완료 — 사용자가 열어 F9까지 돌리고 저장 (revision 8) |
| 4 | 가이드 정오표 반영 | ✅ **완료 2026-08-23** — C.12–C.20 신설, [70a] 센터탭 경고, C.1 8칸 역산표 |
| 4b | 워크북 정오표 반영 | ✅ **완료 2026-08-23** — C.12 f_px 196셀 · C.13 · C.14–C.18 · **C.25–C.27**(CHANGELOG 행 72–74) · 입력 4개 |
| 4c | 교육자료 정오표 반영 | ✅ **완료 2026-08-23** — 정오표 슬라이드 신설(52장), 규칙 9 보강 |
| 5 | 가이드 rev 2.0 — 9:1 정본 예제로 전면 교체 | ✅ **완료 2026-09-11** — `HISTORY.md` 시트 §8 |
| 6 | 스프레드시트 rev 0.7 (문서 정리·CHANGELOG 시트 분리) | Phase 5 후 |
| 7 | 교육자료 — **25 V 예제 · 모핑 5장 · 출력뱅크 3장 ✅ 2026-08-28 (61장)**. 한국어판이 남았다 | 한국어판만 |
| 7b | **배포용 영문 Application Note (PDF)** | ✅ **완료 2026-08-28** v1.0 20쪽 |
| 8 | 시제품 실측 → 시트 §4.3 항목 확정 → 전 산출물 갱신 | 보드 |

### 4.1 Phase 4·5·6 상세

**Phase 4 — ✅ 완료.** 무엇을 했는지는 `HISTORY.md` 시트 §3.4c와 타임라인.
가이드는 C.12–C.20이 부록에 들어갔고, 워크북은 f_px·D73·입력 4개가 고쳐졌고,
교육자료는 정오표 슬라이드가 하나 늘었다. **세 산출물이 같은 정오표를 담고 있다.**

**Phase 5 — 가이드 rev 2.0:**
예제 수치 전면 재계산. **240 W 예제는 완전히 삭제한다 — 2026-08-24 사용자 결정.**
부록으로 옮기지 않는다. 그 예제가 `l6790.py`가 받은 유일한 독립 검증이었으므로 값 29개를
`HISTORY.md` 시트 §1.5에 보존해 두었고 `l6790_validate.py`가 코드 안에 갖고 있다. **문서에서
지워도 검사는 계속 돈다. `l6790_validate.py`를 절대 지우지 말 것.**
CT 선정 근거 · 권선비 정수 해 · 커패시터 트레이드오프를 본문 절로 신설.
본 설계 고유 그림 6종(등가 입력 범위, f_sw(θ), 게인 곡선, 합성 탱크 전류, 루프 보드선도,
블록도)은 새 수치로 재생성. 부록 A(EVL 대조)는 유지 — 탱크가 거의 같으므로 오히려 가치가 크다.

**Phase 6 — 스프레드시트 rev 0.7:** 계산 결함은 **전부 반영 끝났다**(Phase 4b).
**한때 "구조적 한계라 셀 수정으로는 못 고친다"고 적어 둔 두 가지도 2026-08-23에 닫혔다** —
`CC`의 3점 θ 평가와 `Vin_max` 264 Vac 둘 다. 아래 인용 블록에 무엇을 어떻게 했는지 있다.
**그 두 가지를 "남은 한계"로 적으라는 지시가 어딘가에 남아 있으면 그것이 낡은 문장이다**
(`HISTORY.md` 시트 §3.7). rev 0.7에 남은 것은 정리 작업뿐이다 — `CHANGELOG_rev0_6`가 이제
84행이니 `CHANGELOG_rev0_7` 시트로 분리한다. **그때 반드시 채울 것: ST 원본과 대조해 보니 우리가 손댄 설계 셀 105개 중 48개가 CHANGELOG 에 없다**(39개는 `CompensationNet&Check` 123~166행의 이득 여유·`f.px` 작업). 오류가 아니라 기록 누락이다 — `HISTORY.md` 2026-09-08.

> **▷ 워크북에 `I.Lm_pk`(자화전류 피크 12.36 A)가 없다.** 합성 탱크 피크 `I.Lr_pk` 만 있고 자화와 반사 부하를 분리한 성분이 없다. 트랜스포머 사양의 **포화전류 근거가 이 값**이므로, 자속·턴수·A_e 와 함께 **SMath 시트 §12 에만 있는 자산**이다. 워크북은 탱크(L_r·L_m·λ·n_T)까지만 담당한다.

> **구조적 한계라던 2건은 2026-08-23에 닫혔다.** 둘 다 SMath 시트에는 없던 문제이고,
> 이제 워크북에도 없다. 2026-09-02에 워크북 셀을 직접 열어 확인했다.
>
> ① **`CC`의 3점 θ 평가**(C.18의 근본 원인). `RTC`가 이미 θ를 6점 풀고 있었으므로 `CC`에
> 열 **F·G·H**를 더해 6점으로 만들고 **J열 심프슨 적분**(가중치 1,4,2,4,2,4,1 ÷ 18, θ=0
> 끝점 포함)을 넣었다. 설계 셀 `Res.Tank D65·D67`과 `Power D28·D60·D84`가 전부 J열을 읽는다.
> 뱅크 리플전류 21.77 → **30.17 A**.
>
> ② **`Vin_max`가 264 Vac라 FB morphing 코너를 못 보던 것.** 격자 탐색을 Cardano 닫힌형
> 18블록으로 바꾸고 **`RTC!C9 = MAX(D79, D81)`**로 연결했다. 시트가 계산만 해 두고 안 쓰던
> `Design Spec` D81 = 2·235/√2 = **332.34 V**가 이제 실제로 쓰인다. f_sw,max 189.3 →
> **249.6 kHz**, C_T 상한 839 → 699.6 pF. HB 코너는 `Design Spec` D20 = 173.24 V로 원래
> 보고 있었다(C.8).
>
> **`RTC` 격자 범위는 한계가 아니다.** 격자 축은 `f_n` 0.0001 ~ **2.2241**(= 1.5 × `CG26`)
> = 337.5 kHz까지 뻗어 있고, 우리 f_sw,max 249.6 kHz(f_n 1.6448)는 그 **안에** 있다.
> 예전에 이 자리에 "격자 상한 1.3703"이라고 적혀 있었는데, 그것은 격자가 아니라 `fn.max`
> 를 잘못 옮긴 것이었다. 2026-08-23 정정.
>
> **`fn.max` 자체는 이제 해가 없다** (2026-08-24). 무부하 게인은 공진에서 1 로 시작해
> 점근선 1/(1+λ) = 0.6452 까지만 내려간다. 원인 E 가 `Vin_max` 를 FB 코너(등가 332.34 Vac)로
> 옮기면서 요구 게인이 0.6383 이 되어 **점근선 아래**로 내려갔고, `SQRT(λ/(1+λ−1/M))` 의
> 근호 안이 음수가 됐다. RTC 26·27행 164칸이 전부 `#NUM!` 이었다. **읽는 곳이 없어 수치
> 영향은 0** — 격자 축은 `CG25 = 1.5*CG26` 에서 선형보간으로 만들고 답은 Cardano 블록에서
> 나온다. `CG26` 이 이미 갖고 있던 3중 가드(무부하 교차 · 부하 곡선 · 설계 f_sw,max 중 최대)를
> 전 열에 적용해 오류 표시를 없앴다.

### 4.2 아직 미확정 (잠정값으로 진행 중)

| # | 항목 | 잠정값 | 비고 |
| :-- | :--- | :--- | :--- |
| 0 | **1차 MOSFET 후보** | STO60N045DM9 | ST MDmesh DM9 · 600 V · 45 mΩ max · TO-LL. **fast-recovery 바디 다이오드**라 시트 §13.1b 요구에 맞다. `kT.pri` = **1.8** — 2026-08-24 사용자가 데이터시트 normalized R_DS(on) vs T_j 곡선에서 읽어 확정. `kT.sec` = **1.9** 도 2026-08-24 확정 (V_GS = 10 V 곡선, 25 °C 3.2 → 125 °C 6.1 mΩ). **축이 Ta 로 적혀 있어도 "Pulse test" 라 다이가 자기발열하지 않아 Tj = Ta 다** — 펄스가 아닌 곡선이면 이렇게 쓰면 안 된다 |
| 1 | **T_idle** | 250 / 700 ns 병기 | 증거는 250 ns 쪽(`HISTORY.md` 시트 §4). **실측 전까지 확정 아님** |
| 2 | LLC단 효율 η_HB | 0.98 | 낙관적. 0.95로 바꿔도 R_CS·R_ac 3 %만 이동 |
| 3b | **2차 SR — 2 병렬로 확정** | 실측 판정 | **2026-08-24 확정.** `n.SR` = 2 · 3.7 mΩ · kT 1.9. 소자당 14.15 A rms · 49.2 A peak · **1.41 W**, 레그당 2.82 W 로 **`k.PSR` = 1.065** — 3 W 예산을 거의 다 쓰지만 그대로 간다. 3 병렬이면 1.598 로 회복되나 채택하지 않았다. **패키지 전류정격(리드·클립)을 다이보다 먼저 확인할 것** |
| 3 | 출력 커패시터 뱅크 부피/높이 | 미정 | 75.2 mF / 30.2 A rms. 세트 공간이 안 되면 재설계 |
| 4 | 대기전력 목표 | 미정 | 버스트 진입점 `r.BM` = 0.11 가정 |
| 5 | **1차 손실 — 예산 미달을 감수하기로 확정** | 실측 판정 | **2026-08-24 확정: 현 구성 그대로 간다.** `n.par` = **1**, 병렬도 GaN도 쓰지 않는다. HB 모핑에서 정적 도통 소자(LVG2)가 탱크 전류 전체(10.9 A rms)를 연속으로 흘려 **9.62 W**, 요구 Rth(j-a) **10.4 °C/W**. 어느 기술로도 소자 1개로는 3 W 가 불가능하다 — 열간 25.3 mΩ(25 °C 기준 약 14 mΩ)이 필요한데 600 V 급 최선이 17 mΩ 이다. **방열판을 쓸지는 시제품 열 실측에서 결정한다**(시트 §4.3 항목 11). 스위칭 3자리는 4.81 W 로 문제없다. **`k.Ploss` = 0.312 는 이 결정의 결과이지 오류가 아니다. 다시 열지 말 것** |
| 5b | **`R.T` — 7.5:1 을 채택하면 반드시 내려야 한다** | `R.T` 11 kΩ (7.5:1 에 부족) | **공차는 개방·단락 모두 ±10 % 로 확정**(2026-09-07 사용자 결정 — 비대칭은 양산이 못 맞춘다). `f.o ∝ 1/√L.open` 이라 −10 % 는 `f.o` 를 5.4 % 올린다. 버티는 저하는 `1−1/k.floor²` 로 **9:1 14.9 % · 7.5:1 4.1 % · 6:1 58.9 %** — **7.5:1 만 ±10 % 를 못 견디고 하드 스위칭한다**(`f.o` 90.39 → 95.28 kHz > `f.Min` 92.25). **`R.T` 를 11 → 10 kΩ 으로 내리면 `f.Min` 101.0 kHz · `k.floor` 1.117 · 버티는 저하 19.9 % 로 해결**되고 `k.ceil` 도 올라간다. 대가는 `f.Min` 이 9 kHz 높아져 **영교차 클램프가 넓어지는 것**인데 **시트에 그 THD 모델이 없다**(`k.D3rd` 는 루프의 3차 고조파이지 클램프가 아니다) — 실측 항목. **9:1 정본은 그대로 통과하므로 지금 바꿀 필요는 없다** |
| 5c | **2차 SR 병렬 수 `n.SR`** | 2 (미달) | 9:1 로 오면서 2차 소자 rms 가 14.15 → 15.16 A 가 되어 **`k.PSR` 1.063 → 0.929 미달**. **3 으로 올리면 1.39 로 회복**되고 그것 말고는 방법이 없다. `Power Components` D103 한 칸. **2026-09-07 재검토에서도 그대로 열려 있다 — 소자 결정이라 임의로 바꾸지 않았다** |
| 5d | **코어 — 미선정** | EE6405 로는 불가 (`k.Ae` 0.549) | 유닛당 1차가 5 → 3턴이라 자속이 올라 **`A.e ≥ 206 mm²`** 가 필요한데 EE6405 는 113.1 mm² 로 **1.8배 부족**. 시트는 EE6405 를 그대로 두어 `B_pk` 364 mT 로 문제를 드러낸다. **2차 2턴(6:2)으로 가면 세트 9:1 을 유지한 채 `A.e ≥ 103 mm²` 가 되어 EE6405 가 들어간다** — 대신 유닛당 권선이 6 → 11턴이라 창 면적이 관건이고 `n.aux` 가 1.5 로, `R.ZCD_H` 가 330 kΩ 으로 되돌아간다 |
| 5e | **6:1 변형의 `k.NRCS` 0.966** | 5 (미달) | 1차 라인 실효가 12.37 A 로 높아 감지저항 소산이 5.17개분이다. **6개로 올리면 `R.CS` 24 mΩ 를 유지하려고 개당 144 mΩ 이 필요한데 E24 에 없다** — 150 mΩ 이면 `R.CS` 25 mΩ 이 되어 `Pin.max` 가 672 W 로 `P.in` 684.8 W 아래로 떨어진다. 130 mΩ 이면 21.67 mΩ 이 되어 OCP 가 통째로 움직인다. **6:1 은 비교용 설계점이므로 열어 둔다** |
| 6 | 부록 C.9의 근거 불명 상수 | — | [28]의 π²/8, [36]의 지수 5, [128]의 0.744, K_HV |
| 7 | EVL 보드의 n = 3.2가 n인지 n_T인지 | — | 트랜스포머 실측으로만 판정 가능 |
| 8 | LGE 기존 보드의 동작점 | 버스 400 V | **버스는 400 V 로 확정**(1차 소자가 650 V 라 640 V 불가). 2차는 **직렬**이고 3개가 하나의 15:3 으로 동작한다(병렬이면 B_pk 356 mT 로 포화). 다만 그러면 요구 게인 0.536 에 부하 Q > 1 이 되어 **아직 안 닫힌다** — C_r 실측 또는 PFC단 도면 필요. `HISTORY.md` 시트 §5.2a. **우리 설계에는 영향 없다** |
| 9 | **`k.ZVS` 가 한 점만 본다** | 정본은 영향 없음 · 변형 2개는 낙관적 | 시트는 `T.ZC` 를 **저입력 코너 전부하** 한 점에서만 잰다. 그 점은 *게인* 최악점이고 ZVS 최악점이 아니다 — 부하를 덜면 게인을 깎으려고 `f.sw` 가 올라가 자화전류 피크 `V.in/(4 f.sw L.m)` 가 떨어지는데 중점 용량은 그대로다. 부하·등가입력 2차원 스윕: **9:1** 530 ns @ 저코너 전부하(시트 2.408 과 일치) · **7.5:1** 401 ns @ 고코너 25 % 부하(시트 2.091 → **실제 1.824**) · **6:1** 363 ns @ 고코너 50 % 부하(시트 2.073 → **실제 1.649**). **셋 다 여전히 통과하므로 설계 변경 사유는 아니다.** `n` 이 커 이미 공진 깊이 아래에 있는 정본만 전부하 코너가 최악으로 남는다. 시트에 부하 축을 넣는 것은 시트 §8 Cardano 블록을 부하마다 반복해야 해서 큰 작업이라 미뤄 두었다 — AN 은 이미 2차원으로 훑어 보고한다(`an_body._zvs_grid`). `HISTORY.md` 2026-09-08 |

> **1–5는 전부 SMath 시트의 노란 셀이다.** 값이 정해지면 셀만 고치고 F9.

### 4.3 최우선 실측 항목

1. **T_idle 확정** — 라인 반주기에 걸친 f_sw(θ) 실측. **모든 오실레이터 정수가 여기 걸려 있다**
2. **FB morphing 경계(235 V_pk, 전부하)의 f_sw** — 계산 249.6 kHz.
   T_idle = 700 ns면 VCO 상한과 거의 같다
3. **HB morphing 경계(245 V_pk, 전부하)의 ZVS** — 계산 T_ZC 457 ns.
   게이트·중점 파형으로 하드 스위칭 여부 직접 확인
4. **출력 100/120 Hz 리플과 OLED 패널 휘도** — 아키텍처 판정의 근거
5. **C_out 뱅크의 리플 전류와 온도 상승** — 계산 30.2 A rms, 개당 0.19 A
6. 90 Vac 전부하 입력 전류와 THD — 영교차 사각지대 정량화
7. **콜드 스타트업 — 75 mF 출력 뱅크를 120 ms HVSU 창 안에 세울 수 있는가.**
   240 W 예제의 4 mF보다 18배 큰 부하다. **본 프로젝트 고유의 신규 리스크**
8. 경부하에서 FB 리플 대 버스트 임계 마진 ([147] [148], R_BM 조정)
9. EVB 트랜스포머 개방/단락 인덕턴스 + 턴수 → n vs n_T 확정
10. **최악 위상 AC 드롭아웃** — θ = 45°(리플 골)에서 AC를 끊고 12 ms 후 출력이 19 V 이상인지
11. **1차·2차 소자 온도 상승 — 방열판 필요 여부를 여기서 판정한다.** 계산상 LVG2 자리 **9.62 W**(요구 Rth(j-a) 10.4 °C/W) · SR 소자당 **1.41 W**. `k.Ploss` 0.312 · `k.PSR` 1.065 를 감수하기로 한 결정이 성립하는지가 여기서 갈린다. **HB 모핑 상태(라인 피크 > 245 V)에서 잴 것** — FB 모드에서는 그 자리가 스위칭이라 절반밖에 안 뜬다

---

## 5. 동작 원리 — 설계에 직접 쓰이는 형태만

상세는 가이드 §2. 여기서는 설계 판단에 곧바로 쓰이는 것만.

1. **게인은 제어 변수가 아니다.** 양쪽 포트가 전압원(출력은 mF급 C_out, 입력은 벌크 없는
   |v_ac(θ)|)이므로 순시 게인 `M(θ) = n·V_o,eff / V_drive(θ)`가 **경계조건으로 강제**된다.
   주파수가 조절하는 것은 Q, 즉 **전력**이다. → `f_sw(θ)` 변조가 곧 PFC.
2. **두 발산이 상쇄된다.** θ→0에서 요구 게인은 1/sinθ로 발산하지만 부하는 sin²θ로 소멸하고,
   Q→0의 게인 천장은 하측 공진 `f_o`에서 발산한다. **f_o가 주파수 하한**이다.
3. **λ ≈ 0.5 (m = L_p/L_r ≈ 3)가 단일단의 지문.** 일반 LLC의 m = 5–10을 쓰면 저입력에서
   기동조차 못 한다. 순환 전류가 크다는 것이 승압단 제거의 대가다.
4. **FB 핀은 전력 지령이다.** `V_FB = 0.5 V + 0.167 (V/Ω·W)·R_CS·P_in`. 최대전력 제한 ·
   버스트 진입점 · 과부하 검출이 전부 이 하나의 스케일 위에 있다.

**Morphing:** 라인 피크 235 V 이하 → 풀브리지(구동 2배), 245 V 이상 → 하프브리지.
LOUT2를 정적 High로 두어 2번 레그 로우사이드를 계속 도통시키는 방식이며 C_r이 DC를 차단한다.
추가 하드웨어 불필요.

> **▷ 그 결과 탱크가 보는 등가 입력 범위는 90–264 Vac가 아니라 173.2–332.3 Vac (1.92:1)이다.**
> - **HB 경계 245 V_pk → 등가 173.2 Vac** = 게인·**ZVS 최악 코너**
> - **FB 경계 235 V_pk → 등가 332.3 Vac** = **주파수 최악 코너**
>
> 이 두 점을 놓치면 설계가 성립하지 않는다. **ST 원본 툴은 이 둘 다 안 봤다.** 수정본
> rev 0.6은 HB 코너를 `Design Spec` D20(C.8)에서, FB 코너를 `RTC!C9 = MAX(D79, D81)`
> (2026-08-23)에서 본다. 시트 §4.1 인용 블록 참조.
>
> **▷ 히스테리시스 밴드를 넣으면 8 %가 더 넓다 (2026-08-24).** 235–245 V_pk 사이에서는
> **라인 전압만으로 모드가 안 정해진다** — 올라오는 중이면 245 V_pk 까지 FB 를 유지하고,
> 내려가는 중이면 235 V_pk 까지 HB 를 유지한다. 그래서 진짜 최악은 **166.2–346.5 Vac
> (2.08:1)** 이다. 여유가 게인 15.6 → **10.9 %**, ZVS 2.08 → **1.70배**, f_sw,max
> 249.6 → **263.2 kHz** 로 줄지만 **전 항목 통과한다.** 사양을 조일 때 좁은 쪽 숫자를
> 쓰지 말 것. 실사용 계통은 이 밴드(166–173 Vrms)에 앉지 않으므로 **AC 소스로 임계를
> 통과시키는 시험과 라인 딥/서지에서만 만난다** — 즉 시험에서 만난다. **프로그래머블 AC
> 소스이므로 램프가 아니라 스텝으로 볼 것** (175 → 160 V 및 그 반대). 가이드 §3.3.
>
> **▷ 위험 사항 하나 — 전환 순간의 출력 강하.** 구동이 한 라인 사이클 만에 2:1 로 변하는데 루프 교차는 17.6 Hz 다. **홀드업(전력 0, 12 ms)이 더 가혹하고 그건 이미 만족하므로 C_out 이나 루프를 손댈 이유는 없다.** 다만 그 포함관계를 시간영역으로 확인한 적은 없다. 가이드 §3.3 에 위험 사항으로 적어 두었다.

---

## 6. 설계에서 반복해서 틀리는 지점

### 6.1 이 토폴로지에서 반복해서 틀리는 지점

1. 탱크는 **등가 입력 범위의 진짜 양 끝**(173.2 / 332.3 Vac)에서 설계한다
2. λ ≈ 0.5를 예상한다. L_m/L_r = 5–10은 저입력 기동 실패
3. f_Min을 f_o 이상에 둔다. ACP 보호에만 의존하지 말 것
4. 권선비는 항상 **반사 전압 n·V_o,eff**로 검산한다
5. **합성 공진 전류 피크**로 판정할 것이 둘이다 — OCP1 여유와 **1차 소자 전류 정격**. `I.trafo_pk` 는 반사 부하 전류일 뿐이고 자화 전류가 빠져 있다. 가이드 [79]·[115], ST 툴 `D39`·`D65` 가 전부 이 오류였다 (부록 C.14 · C.28)
6. 출력 커패시터 리플 전류는 **2f_l 성분 포함**, 그리고 **CT면 노드 실효값**으로 판정한다
   (`HISTORY.md` 시트 §3.5)
7. L_m은 계산값 **이하**로 택한다. 위로 반올림하면 ZVS 여유가 사라진다
8. ZVS 최종 확인은 [36]–[38] 닫힌형이 아니라 **[38a] 전 영역 스윕**으로 한다.
   **닫힌형의 오차는 부호가 정해져 있지 않다 — 탱크에 따라 양쪽으로 흔들린다** (2026-08-24 실측,
   전부 *선정* 탱크 기준):

   | | 스윕 | 닫힌형 | |
   | :--- | ---: | ---: | :--- |
   | 240 W 예제 (33 nF/34 µH/65 µH) | 246.5 ns | 302.8 ns | **+22.9 % 낙관적** |
   | 본 설계 (100 nF/11 µH/20 µH) | 457.2 ns | 299.9 ns | −34.4 % 보수적 |

   원인 둘: [36]이 적합식이고(지수 5는 근거 없음), 실제 동작 Q가 아니라 **설계값 `Q_ZVS`** 로
   평가한다. 두 오차가 탱크에 따라 다른 비율로 겹쳐 부호가 바뀐다.
   **낙관적일 수 있다는 것이 위험한 쪽이다** — 없는 ZVS 여유를 있다고 보고한다.
   **툴 자신의 `RTC` 격자는 스윕과 5자리까지 일치한다** — 즉 워크북은 정답을 갖고도 ZVS
   검사에만 근사식을 쓴다.

   > **2026-08-23에 이 항목을 "닫힌형은 보수적"으로 고쳤다가 2026-08-24에 되돌렸다.**
   > 그때 `design()`을 선정값 없이 불러 *계산* 탱크로 쟀다 — 예제가 실제로 만드는 탱크가
   > 아니었다. 원래 기록("약 10 % 낙관적")이 방향은 맞았다.
8b. **용량성/유도성 경계는 게인 피크가 아니다** (2026-09-17, 사용자가 AN 그림에서 발견).
    탱크가 용량성인 것은 `arg Z_in < 0` 인 동안이므로 경계는 **위상이 0 이 되는 점**이고,
    그 자리는 게인 곡선의 피크보다 **위**에 있다. λ 0.55·Q 0.766 에서 피크 105.4 kHz 대
    경계 110.1 kHz 이고 **피크 바로 그 자리의 위상이 아직 −8.4°** 다. 피크는 게인 곡선만
    있으면 되니까 쓰는 근사이고 **틀리는 방향이 나쁜 쪽**이다 — 아직 하드 스위칭하는
    구간을 유도성이라고 보고한다. 닫힌형이 있다: `Q²u² + (λ²+λ−Q²)u − λ² = 0`, `u = f_n²`
    — `l6790.zvs_edge(Q, λ)` 이고 `l6790.phase()` 의 수치해와 5자리까지 같다.
    **Q→0 에서 `f_n0` 로 수렴**하므로 무부하에서는 두 정의가 일치한다.
    **본 설계는 영향 없다** — 전 라인 위상 · 부하 4종 · 입력 3종을 훑어도 최악이 여전히
    영교차 근처의 `f.Min`/`f.o` = **1.021**(= `k.floor`)이고, θ > 20° 에서는 1.096 이상이다.
    ZVS 판정 자체는 [38a] `T.ZC` 스윕이라 두 근사 어느 쪽도 안 쓴다
8c. **프리휠링은 전력 전달 *다음* 이되 *같은 반주기 안* 이다** (2026-09-17 사용자 지적).
    시간 순서는 `S1·S4 전력 전달 → S1·S4 그대로 켜진 채 프리휠링 → 데드타임 →
    S2·S3 전력 전달 → S2·S3 프리휠링 → 데드타임` 이다. AN 의 그림 6(전력 전달 두 칸)과
    그림 7(프리휠링 두 칸)은 **시간이 아니라 동작으로 묶여 있어** 위에서 아래로 읽으면
    P1→P2→F1→F2 로 오독된다. 그림 8(Infineon Table 1 파형)의 below 칸이 근거다 —
    `S1,S4` 게이트가 `Ts/2` 내내 높고, 그 안에서 `Tr/2` 까지가 전력 전달, 나머지가
    프리휠링이다. **그리고 ZVS 는 두 회로 어디에도 없다** — 그다음 데드타임에서 일어나고
    그림 8 에서 `V_ds` 계단으로 보인다.
    **2026-09-20 에 네 칸에 `STEP 1~4` 를 박았다** — "1st half / 2nd half" 만으로는 어느 칸이
    먼저인지 여전히 안 보인다는 지적이 나왔다. 그림 6 이 STEP 1·3, 그림 7 이 STEP 2·4 이고,
    그림 8 의 괄호도 같은 번호를 단다. (여기 그림 번호는 그날 빌드의 것이다.)
    **2026-09-21 에 그 두 그림을 통째로 바꿨다** — 사용자 질문 "순서가 1,2,5,6 인데 3,4 는 왜 없고,
    1,2,5,6 은 파형의 어느 범위인가". 답이 그림 안에 있어야 하므로 `an_op_power`·`an_op_free`
    (동작별로 묶은 두 칸짜리)를 지우고 **8구간 시트 두 장(`an_modes_1234`·`an_modes_5678`)과 번호 띠가
    있는 파형(`an_modes_wave`)** 을 넣었다. 이제 칸은 시간 순이고 데드타임 3·4·7·8 이 있으며, 각 칸이
    파형의 어느 띠인지 번호로 대응된다. `figs.py` 의 `an_modes8` 항목 하나가 세 파일을 쓴다.
    **파형 3건도 같은 날 고쳤다** — `an_llc_waves` 의 i_S1 이 8구간(자기 노드가 넘어온 뒤 바디
    다이오드 도통)에서 0 이었다: 주기 시작의 음수 전류와 모순이다. `an_three_cases` 의 above 는
    인덕터 전류 i_Lr 이 수직으로 점프했다: 데드타임 동안 램프로 내려 연속으로. `an_zvs_zcs` 의
    "cut off while still flowing" 점선이 끝까지 그려져 자기 라벨과 반대였다: 반주기 끝에서 자른다.
    **2026-09-21 (2차) — 데드타임 (a) 의 전류 경로가 틀려 있었다** (사용자 지적: "MOSFET 이 꺼졌는데 왜
    전류가 몸통으로 흐르나, 커패시터로 흐른다면 커패시터를 *통과* 해야 한다"). 모드 3·7 이 모드 2·6 의
    프리휠링 경로(`PRI_FWD_LM`·`PRI_REV_LM`)를 그대로 쓰고 있어 **꺼진 채널을 따라 레그를 내려갔다.**
    `cs()`(C_oss 우회, `bd()` 와 같은 모양으로 한 칸 바깥)를 만들고 **루프 두 개**를 그린다 — 위쪽은
    `V_in 레일 → C_oss(S1) → A → 탱크 → B → C_oss(S3) → V_in`, 아래쪽은 `0 레일 → C_oss(S2) → A →
    탱크 → B → C_oss(S4) → 0`. 전류가 넷으로 갈라지는 것이 물리이고, 각 C_oss 의 색(자홍 충전/청록 방전)은
    그대로다. **모드 4·8(바디 다이오드)은 원래부터 `bd()` 로 맞게 우회하고 있었다.**
    **같은 날 접점 점 셋을 지웠다** — 탭 라이저가 + 레일로 꺾이는 모서리, D1·D2 꼭대기(한 배선이 끝나고
    다음이 시작되는 모서리). 점은 "세 가닥 이상이 만난다" 는 뜻이고 모서리에는 할 말이 없다(사용자 규칙).
    `figcheck` 에 `stray-dot`(두 가닥만 만나는 점) 검사를 넣었다 — 한 가닥짜리는 포트 표시라 두고,
    배선에 안 얹힌 점(극성 점)도 둔다. 2차 경로의 마지막 화살촉을 0.45 → 0.15 로 옮겼다(5·6 칸에서
    "centre tap" 글자 위에 앉아 있었다).
    **타임라인 모델(2차 도통은 게이트 온에서 시작, 데드타임 (b) 에는 자화 전류만)은 그대로 둔다** —
    Toshiba AN 의 mode 4 서술("excitation current flows through the body diode of Q2 … transitions
    when Q2 turns on")과 일치한다. 엄밀히는 스윙이 끝난 순간 v_Lm 이 −nV_o 를 넘어 2차가 먼저 켤 수
    있지만 그 폭은 데드타임 (b) 하나이고 독립 출처가 게이트 온으로 적으므로 따른다.
    **그리고 "프리휠링" 이라는 말이 두 구간에 쓰인다는 주의 상자를 넣었다** — 여기서는
    STEP 2·4(2차 꺼짐 · L.m 복귀 · 스위치 쌍 그대로)이고, 위상천이 풀브리지 계열 자료는 같은
    말을 **데드타임**에 쓴다. **데드타임은 주파수와 무관하게 매 주기에 있고 STEP 2·4 는
    below 에서만 있다가 `f.r` 에서 0 이 된다** — 다른 자료와 대조하기 전에 어느 구간인지부터 볼 것.
    **2026-09-20 에 Toshiba `Resonant Circuits and Soft Switching`(2019) 가 도착해 대조했고,
    순서는 독립 출처로 확인됐다.** 그 AN §3.2 의 8개 모드가 우리 STEP 과 하나씩 맞는다 —
    mode 1-1·1-2 = STEP 1(Q1 on · D1 도통) · **mode 2 = STEP 2**(Q1 **그대로 on** · `i.o1` = 0 ·
    2차 꺼짐) · mode 3·4 = 데드타임(Coss 충방전 → 바디 다이오드, **ZVS 는 여기**) ·
    mode 5-1·5-2 = STEP 3 · **mode 6 = STEP 4** · mode 7·8 = 데드타임. Figure 3.7 과 3.12 가
    **서로 거울상이고 둘 다 1차에 점선(자화 전류) 고리만 있고 2차 고리가 없다** — "S2·S3 에는
    프리휠링이 없다"는 의문에 대한 독립 반증이다. **다만 그 문서는 mode 2·6 에 이름을 안 붙이고,
    "freewheeling" 이라는 말을 데드타임의 바디 다이오드에 쓴다**(mode 5-1: "Q2 turns on while
    DQ2 is freewheeling under the ZVS condition"). 즉 **물리는 같고 이름표만 다른 구간에 붙은 것**이며,
    위에서 말한 두 용법 중 둘째가 위상천이 풀브리지만의 것이 아니라 LLC 자료에도 있다는 뜻이다 —
    AN 의 주의 상자를 그 예까지 적도록 고쳤다(양 판 12쪽). **그 AN §3.1 은 비교 대상이 아니다** —
    거기는 `i.Q1`·`i.Q2`·`i.Cr` 만 보는 4단계 개관이라 2차를 아예 안 다루고 프리휠링 구간이 없다
8d. **Infineon AN 2012-09 의 Figure 2.8 은 캡션과 맞지 않는다** (2026-09-17, 위를 확인하다 발견).
    "freewheeling, 2nd half" 라고 돼 있는데 화살표가 **S1·S4 를 거꾸로** 타고 흐른다
    (전류가 + 단자로 되돌아 나간다). S2·S3 가 켜진 프리휠링이라면 Figure 2.6 과 같은 경로에서
    2차 루프만 끊긴 모습이어야 한다. 원문 PDF 를 8배로 확대해 배선을 따라가 확인했고,
    같은 문서의 Figure 2.6·Table 1 과 어긋난다. **그림이 그리고 있는 것은 실은 데드타임**이다 —
    2번째 반주기 끝에서 음의 탱크 전류가 S1·S4 의 바디 다이오드로 흘러 중점을 넘기는 구간.
    결정적 근거 하나 더 — **S2·S3 가 켜져 있는 동안 S1·S4 의 바디 다이오드는 역바이어스**라
    (중점이 각각 0 과 V.in 에 물려 있다) 그 경로로는 전류가 물리적으로 못 흐른다.
    **2026-09-18 사용자 지시로 화살표를 다시 그렸다** — `figedit.fix_freewheel_2nd_half()` 가
    원문의 빨간 선을 지우고 `+ 레일 → S3 → 오른쪽 중점 → L.m → 탱크 → 왼쪽 중점 → S2 → − 레일`
    로 다시 그린다. 좌표는 파일에서 **재서** 쓴다(레일 y 30/291 · 레그 x 818/922 · 탱크 y 125 ·
    귀환 y 207 · L.m x 1091). 캡션에 "다시 그렸다"고 적었다. **`figedit` 의 "그림이 주장하는
    바를 바꾸지 말 것" 규칙에 뚫린 유일한 구멍이고, 사유가 모듈 docstring 과 함수에 남아 있다**
8e. **그림 8 의 네 구간에 괄호를 쳤다** (2026-09-18) — `figedit.mark_modes()`.
    2번째 반주기의 프리휠링이 그림 **오른쪽 끝**에서 끝나 놓치기 쉬워서 두 번 질문이 나왔다.
    픽셀로 재면 두 반주기가 **완전히 같다** — 게이트 on 120 px 중 전력 전달 84 px · 프리휠링 41 px
    (Ts 253 px, 84/120 = 0.70 = f.sw/f.r 로 검산됨). 괄호 위치는 손으로 적지 않는다 —
    **정류기 전류가 자기 0 선을 떠난 구간**을 찾아 긋는다(다른 트레이스의 높이에 영향받지 않는다)
13. **개방시험(포화) 전류의 근거는 OVP2 다. 임의 마진을 다시 넣지 말 것.**
    시험 전류와 1차 피크는 **애초에 같은 종류가 아니다** — 개방시험은 2차 기자력 상쇄가
    없어 같은 전류로도 자속이 훨씬 크므로, 시험 전류는 *동작 자속을 재현하는* 전류이지
    동작 전류가 아니다. **그래서 15 A < 18.3 A 는 모순이 아니다.**
    **근거는 이것이다** (2026-09-01, 사용자가 세 번 되물어 세운 것): 공진 아래에서
    자화 도통 구간이 `T.r/2` 로 고정되므로 자화전류는 **`f.sw` 와 무관하고 출력 전압에만
    비례**한다. 그러면 최악 자속은 컨트롤러가 허용하는 최대 출력 = **OVP2** 에서 나온다.
    따라서 `I.sat_spec = I.sat_eq × V.OVP2_act/V.o_eff` 이고 **곱할 임의의 수가 없다.**
    7.5:1 은 12.41 × 1.2244 = **15.2 A**.
    **2026-09-21 에 이것을 `1.5 × I.sat_eq` 로 되돌렸다가 사용자가 잡아냈다** — 바로 그
    "내가 정한 1.5" 다. `I.sat_spec` 을 시트 §14 의 `V.OVP2_act` 바로 아래에 둔 것은
    그래서다(시트 §12 에 두면 전방 참조이기도 하다). `check_trans_spec.py` 가 OVP2 대조를 한다.
    **같은 날 진짜 오류 하나는 고쳤다** — `I.sat_eq` 의 분모가 `L.open_x` 였는데 개방시험에서
    코어 중앙다리를 도는 것은 `L.mu` 몫뿐이라 `L.mu_x` 여야 한다(`L.mu/L.open` = 0.803 배만큼
    작게 나갔다). 검산: 개방시험에서는 1차 전류 전체가 자화 전류이므로 **`I.sat_eq` 는
    `I.Lm_pk` 와 같아야 하고**, 고친 뒤 네 변형 전부 일치한다. 나간 사양서 값은
    두 오차가 상쇄돼 **결과적으로 맞았다**(7.5:1 15 대 참값 15.2)
9. 커패시터 ESR은 **사용 주파수**의 값을 쓴다 (데이터시트 tanδ는 120 Hz)
10. **소자 1개에 실제로 흐르는 전류로 응력을 판정한다.** 전류표는 스위치 *위치*당(1차) ·
    *레그*당(2차)이고, 병렬 개수(`n.par` · `n.SR`)로 나눠야 소자 값이 된다. 병렬 개수가
    없으면 R_DS(on) 이 "레그당"인지 "소자당"인지도 정해지지 않는다 — 두 문서가 갈라진
    지점이 정확히 여기였다. **센터탭 레그는 2차 전류 전체를 흘린다 — 절반이 아니다**
11. **R_DS(on) 은 25 °C 표제값이 아니라 Tj,max 값으로 손실을 낸다.** 슈퍼정션 약 2배,
    저전압 SR 약 1.5배. 25 °C 손실로 "Tj 125 °C 용 Rth" 를 구하면 **약 2배 후하게** 나온다
12. **C_oss 는 어느 것인지 적는다.** 표제 Coss · Co(er) · Co(tr)(= Qoss)이 3–5배 다르다.
    ZVS 는 전하 문제이므로 **Co(tr)** 이다

### 6.2 핀 규칙 (DS 명시 — 위반 시 즉시 오동작)

| 핀 | 규칙 |
| :--- | :--- |
| ISEN (6) | **필터·직렬저항 금지.** 최대전력 법칙과 OCP 설정점이 이 핀을 공유. AMR ±3 V |
| CFG (11) / BM (10) | **커패시터 금지** (파워업 시 핀스트랩). CFG는 15–100 kΩ 밖이면 IC 래치. BM을 GND에 연결하면 버스트 비활성 |
| DRV_EN (3) / RT (7) | **커패시터 금지** (2 ms마다 15 µs 샘플링 → 오검출 디스에이블) |
| LOUT2 (13) | **풀다운 금지** (morphing 사용 시). 300 µA가 2.5 V를 만들어야 하므로 약 8 kΩ 이하는 고정 HB로 읽힌다 |
| HVSU (1) | 입력 브리지 **앞단(AC 측)**에 HV 다이오드 2개(1000 V급). 브리지 뒤에 달면 X-cap 방전과 브라운아웃 감지가 모두 죽는다 |
| ZCD (12) | 보조 권선 극성은 **LOUT1(및 HOUT2) ON일 때 ZCD가 양(+)**. 반대면 하드 스위칭 |
| HOUTx/LOUTx | **로직 레벨 출력(V_OH 4 V)이며 드라이버가 아니다.** 외부 하프브리지 드라이버 필수 |

## 7. 문서 성격 경고

- 데이터시트는 **DRAFT**다. TBD가 남아 있고 내부 모순이 있다.
  **양산 설계 전 정식 릴리스 DS로 반드시 재검증한다.**
- 설계 가이드는 **비공식 자료**이며 ST 공식 간행물이 아니다.
- 부록 C.5(툴의 권선비 2배)는 제공 자료로부터의 **역추론**이며 ST 문서에 명시된 바가 아니다.
  본 프로젝트는 CT(N_rect=1)를 택하므로 이 문제 자체가 발생하지 않는다.
- 일반 LLC 이론 그림은 onsemi/Fairchild AN-4151, Infineon AN 2013-03 출처다.
  **내부 학습용이며 외부 배포 시 각 사에 확인할 것.**

---

# ── 여기부터는 운영 지시 ──

**아래 네 절은 설계 내용이 아니다.** 이 폴더에서 작업하는 도구 사용자(Claude)가
파일을 깨뜨리지 않고 산출물을 재생성·검증하기 위한 절차와 함정 목록이다.
설계를 읽으려는 사람은 여기서 멈춰도 된다 — 위 시트 §1~시트 §7이 계획서 전부다.

## 8. 새 세션 첫 5분

### 8.1 사람이 해야 할 일 — **먼저 확인**

기계가 대신 못 하는 것만 여기 모은다. 다 지워져 있으면 그냥 아래로 간다.

| 무엇 | 왜 | 언제 생겼나 |
| :--- | :--- | :--- |
| **6:1 워크북을 한 번 열었다 저장** | `Calculation Excel Sheet/variants/L6790_workbook_6to1.xlsx` 에 `calcChain` 이 없다 — `C.T` 를 390 → 330 pF 로 고친 뒤 재계산을 안 거쳤고, `f.Min` 캐시가 **390 pF 로 계산된 110.13 kHz** 다(참값 128.87 kHz). `D24·D27·D28·D29` 와 **`BOM&Schematics E57` 이 390 pF 를 싣는다.** 9:1·7.5:1 은 정상 | 2026-09-08 |
| **워크북을 한 번 열었다 저장** | **이번에는 값이 실제로 바뀌었다.** 반도체 블록의 수식 4개와 신설 셀 9개는 Excel 이 재계산해야 값이 생긴다. 그때까지 `xl_sm_compare.py` 의 신설 8항목이 `MISS` 로 나온다 | 2026-08-24 |
| SMath 시트를 열어 F9 | 시트 §13 이 크게 늘었다(30.4쪽). 붉은 리전이 없는지 확인. **`k.Ploss` = 0.526 은 미달이 맞다** — n.par = 1 의 결과이지 오류가 아니다 (시트 §4.2 항목 5) | 2026-08-24 |
| **프로브 시트 P27·P28·P29 를 열어 어느 것이 그려지는지 볼 것** | **그래프 세로 길이를 코드로 늘릴 수 있는지가 여기서 갈린다.** 같은 그래프 3개다 — P27 손 안 댐(대조군, 반드시 정상) · P28 리전 속성만 2배 · P29 리전과 블롭 rect 둘 다 2배. 둘 중 **틀·제목·축이름·범례가 다 살아 있고 세로만 2배**인 쪽이 답이다. 실패는 오류 메시지가 아니라 **축이 −5~5 로 나오는 기본 그래프**로 나타난다. 둘 다 되면 P28 쪽(블롭을 안 건드림). P27 만 되면 SMath 에서 손으로 끄는 수밖에 없다 | 2026-09-08 |
| 프로브 시트 P16·P18·P19·P20 | 단위 21종·루프·그래프·접기가 이 PC의 SMath 에서 도는지 | 미확인 |

### 8.2 그다음

1. **시트 §3의 현재 설계값을 읽는다.** 이것이 정본이다.
2. **시트 §9의 검증 3종을 돌려 본다.** 셋 다 통과하면 프로젝트가 온전한 상태다.
   ```
   cd "Design Guide/generators"
   python build_l6790_smath.py        # -> mismatches: 0
   python check_sm.py "../../Smath/L6790A_SingleStage_PF_LLC_Design_Guide.sm"   # -> VERDICT OK
   python bom_compare.py              # -> items compared: 29   different: 0
   ```
   그리고 **`python snapshot.py`** — 시트 §3의 설계값 블록을 시트에서 다시 읽어 쓰고,
   **지난번 이후 무엇이 움직였는지 찍어 준다** — 사람이 바꾼 노란 셀부터 보여준다.
3. **다른 PC에서 작업했다면 `HISTORY.md` 시트 §8을 본다** — 무엇을 왜 바꿨는지 한 줄씩 있다.
4. **시트 §4에서 다음에 할 일을 고른다.**
5. 이미 검증된 것을 **다시 검증하지 말 것** — `HISTORY.md` 시트 §1에 목록이 있다.
6. **무엇이 아직 안 봤는지는 `HISTORY.md` 시트 §1.4에 있다.** "다 찾았다"는 주장은 없다 —
   전수 확인이 끝난 부류와 손대지 않은 영역(RTC 10,008셀 등)이 구분돼 있다.

**필요한 것:** Python 3.12 + `openpyxl` · `Pillow` · `numpy`. SMath Solver(파일은 1.5.0.9678로 저장됨).
경로는 전부 스크립트 기준 상대경로이므로 폴더째 옮기면 그대로 돈다.

---

## 9. 도구 체인 — 이것으로 전부 재생성·검증된다

전부 `Design Guide/generators/` 에 있고, 경로는 스크립트 기준 상대경로다.

| 스크립트 | 역할 |
| :--- | :--- |
| **`l6790.py`** | **수치 정본.** 가이드 식 [1]–[106] + θ 스윕의 검증된 참조 구현. **`zvs_edge(Q, λ)` 신설**(2026-09-17) — 용량성/유도성 경계의 닫힌형. `phase()` 의 분자를 0 으로 놓은 `f_n²` 의 이차식이고 수치해와 5자리 일치한다. 게인 피크와 혼동하지 말 것 (시트 §6.1 항목 8b) |
| `l6790_validate.py` | `l6790.py`가 240 W 예제를 재현하는지 한 번에 확인 |
| **`build_l6790_smath.py`** | 정본 시트 본문. 실행하면 **`l6790.py`와 22개 항목을 자동 대조**하고 결과를 찍는다 |
| **`zedpane.py`** | **ZedGraph 판(BinaryFormatter blob)의 바이트 편집기.** 축 범위·눈금 간격·숫자 서식·제목/범례 문자열·선 굵기·로그↔선형. 전부 `Reference/Smath Bode Plot Example/Zedgraph` 의 ZedGraph 5.1 소스에서 나온 것이고 추측이 없다. **판 크기(`set_size`)는 쓰지 말 것** — 함수에 이유가 적혀 있다 |
| **`harvest_pane.py`** | **SMath 가 키운 판을 떼어내 이 프로젝트 것으로 만든다.** SMath 는 판의 곡선 목록을 입력 개수에 맞추고 저장 시 되쓰므로, 4곡선 판에 8개를 먹이고 저장하면 8곡선 판이 파일에 남는다. 그것을 `panes/*.b64` 로 보관하고 곡선마다 이름(`set_curve_labels`)과 굵기를 준다. **새 곡선의 Label 은 빈 문자열을 *참조* 하고 있어** 그 참조를 문자열 레코드로 바꿔야 한다. 첫 Label 은 클래스 정의 레코드라 이름(`ZedGraph.Label`)으로 찾는다 |
| **`zedsheet.py`** | **TI 앱노트 판 카탈로그(트레이스 1~5개)와 `make()`/`emit()`.** 어느 판이 로그 x + 선형 y 이고 어느 판이 전부 로그인지가 표로 있다. `build_gainzed.py` 와 `build_l6790_smath.py` 가 같은 것을 쓴다 |
| **`smsheet.py`** | 레이아웃 빌더. **모든 수식 문자열을 두 번 파싱한다** — 한 번은 SMath RPN, 한 번은 Python SI 값. 따라서 인쇄된 값이 옆의 수식과 어긋날 수 없다 |
| **`check_theta.py`** | **`prog` 블록(벡터 대입)을 검사하는 유일한 도구.** 다른 모든 수식은 `smsheet` 가 RPN 과 Python 으로 **두 번** 파싱하므로 인쇄된 값이 옆 수식과 어긋날 수 없는데, 벡터 대입은 인쇄되는 값이 없어 오타가 어디에도 안 나타난다 — 시트가 틀린 곡선을 그럴듯하게 그릴 뿐이다. 빌더에서 수식 **문자열을 꺼내** Python 으로 평가해 시트 §8 의 닫힌형과 대조한다(θ=π/2 에서 `f.sw.a`·`f.sw.b`, θ→0 에서 `f.o`, 그리고 시트 §7.5 의 `M.Z`·`M.OL`) |
| **`check_sm.py`** | 완성된 `.sm`을 다시 훑는다: XML 적합성 / 리전 겹침 / 우측 여백 초과 / 텍스트 재줄바꿈 / 허용 밖 함수 / 쪽수·그래프·노란 셀 수 |
| **`bom_compare.py`** | **엑셀 ↔ SMath BOM 29항목 대조** — 빠르다. hook이 매번 돌린다 |
| `fix_xl_cardano.py` | **`RTC` 격자 탐색을 Cardano 닫힌형으로** — 18블록(행 220–234). `rtc_check.py` 로 편차 0.0000 % 확인 |
| `fix_xl_fbcorner.py` · `fix_xl_fnmax_shared.py` | `Vin_max` 를 FB 코너(332.34 V)로 + `fn.max` 클램프. **후자는 공유 수식을 깨뜨려 되돌렸다** — `HISTORY.md` 시트 §3.7 |
| `fix_xl_ipri.py` | 1차 실효의 θ=0 끝점(자화 전류) |
| `fix_xl_f.py` | 실현 출력전압을 `Design Spec` 에 표기 |
| `fix_xl_cc.py` | **원인 B(C.18) 수정** — `CC` 에 θ 3열 + Simpson 적분(열 J)을 넣고 설계를 거기에 연결. 적용됨 |
| **`fix_xl_simpson_refs.py`** | **살아 있는 참조 위에 손으로 적은 상수가 얹혀 있었다** — C.18 이 `CC` 에 Simpson J열을 만들고 세 셀만 연결했다. `Power Components` D60·D61·D84·D85 는 θ=π/4 한 점(`CC!C..`)을 읽은 채 F열 손입력으로 덮여 있었다. J열로 연결하고 손입력 4칸 삭제 |
| **`fix_xl_comp.py`** | **kT 를 1 에서 2 / 1.5 로** — 데이터시트 max 는 25 °C 공정산포이지 온도가 아니다. 그리고 **`V.Fo` 1.45 → 1.15 V** — 이 한 입력이 두 문서의 R_B 창을 **겹치지 않게** 벌려 놓고 있었다. R_P 1.3 k · R_B 6.2 k |
| **`fix_xl_trunc.py`** | **[64] 가 [63] 과 다른 파형을 제곱하고 있었다** — 공진 위에서 사인이 잘리는데 실효값은 온전한 반사인파로 계산했다. `CC` 25·31·32행에 절단 인수 [64a]. **저입력에서는 인수가 정확히 1 이라 값은 하나도 안 움직인다** |
| `fix_xl_bodydiode.py` | 1차 스위치 블록에 바디 다이오드 역회복 **조건** 2행 (`Power Components` D74·D75) |
| **`fix_xl_fet.py`** | **FET 선정 결함 4건** — 1차 전류 정격을 합성 탱크 피크로 · 병렬 소자 수와 소자 1개당 전류 4행 · Rdson 온도계수 · 다이오드/SR 선택. `audit_fet.py` 가 찾아낸 것들 |
| `fix_xl_fnmax_num.py` | `RTC` 26·27행 `#NUM!` 288칸. 원인 E 이후 요구 게인이 무부하 점근선 아래로 내려가 `fn.max` 의 해가 사라졌다. `CG26` 의 가드를 전 열에 적용 |
| **`fix_xl_english.py`** | **워크북을 한 언어로.** 한글 57칸(CHANGELOG 50 + 라벨 7)을 영문으로. 수식·참조·수치는 손대지 않는다 |
| **`log_xl_findings.py`** | **셀은 안 건드리고 워크북 CHANGELOG 에만 기록한다.** 두 가지 — ① `CC` J열 Simpson(7점)과 시트의 5점 규칙을 2000구간 격자와 대조했더니 **0.0011 % · 0.0013 %** 낮다(성긴 적분이 정당하다는 증거가 없어 나중에 '개선'되거나 의심받을 자리였다) ② **`Design Spec D43`(fsw.max)은 탱크의 *상류*다** — `Res. Tank Design D13·D14` 가 그것으로 나누므로 **λ 를 계산하고 따라서 `L.m` 을 계산한다.** `Device Setting` 의 오실레이터 천장과 종류가 다르다 |
| `fix_xl_causes.py` · `log_xl_causes.py` | **원인 A·C·D 의 셀 수정과 그 CHANGELOG 기록.** 이미 적용됐다. 다시 돌려도 안전하다 — 적용 여부를 보고 건너뛴다 |
| **`rtc_check.py`** | **`RTC` 격자탐색의 *답*을 독립 solver 둘과 대조** — 18점(θ 6 × 입력 3). 10,008셀을 읽는 대신 결과를 본다. 셋이 코드를 공유하지 않으므로 격자가 깨지면 반드시 어긋난다 |
| **`audit_xl.py`** | **워크북 구조 감사** — 죽은 셀 · 오류값 · 빈 셀 참조 · **선정값이 자기 한계 밖** · 수식에 박힌 상수 · 라벨/수식 방향 불일치 · **선정값을 무시하고 계산값을 읽는 곳** |
| **`audit_sm.py`** | **시트 구조 감사** — 죽은 변수 · 정의 전 사용 · **한계는 있는데 비교가 없는 짝** · 설명 없는 판정값 |
| **`audit_rpn.py`** | **`.sm` 의 RPN 을 되읽어 독립 평가.** 인쇄된 값은 smsheet 의 Python 계산이고 SMath 가 실제로 도는 것은 RPN 이다. 둘이 같은지 보는 유일한 검사 |
| **`audit_dim.py`** | **차원 해석.** 두 평가 경로 모두 단위를 스칼라로 다루므로 `V + A` 를 못 잡는다. 표시 단위가 계산 차원과 맞는지도 본다 |
| **`xl_sm_compare.py`** | **엑셀 ↔ SMath 전량 대조 135항목** — 사양·전력예산·탱크·전류·출력뱅크·IC정수·루프·보상망. 차이마다 `same` / `INTENDED`(사유 인쇄) / `** DIFF` 로 판정. **워크북을 손댔으면 이걸 돌린다** |
| **`xlsx_patch.py`** | **워크북 XML 안전 편집.** `set_cell()`(교체-또는-순서맞춰-삽입) + 쓰기 전 전 시트 구조 검사. 인자 없이 실행하면 현재 워크북을 점검만 한다 |
| **`snapshot.py`** | **CLAUDE.md 시트 §3을 시트에서 다시 만든다.** `--check` 신선도 검사 · `--picks` 노란 셀(사람이 고른 값) 목록. 지난번 대비 무엇이 움직였는지 찍어 준다 |
| **`smresult.py`** | **`<result>` 블록의 값을 제대로 읽는다.** `<result>` 도 RPN 이라 첫 토큰만 읽으면 **단항 마이너스와 지수가 통째로 사라진다.** 도구 4개가 같은 가정을 독립적으로 하고 있었다 (`HISTORY.md` 시트 §6.6) |
| **`audit_fet.py`** | **1차·2차 FET 선정 파라미터 감사** — LLC 가 요구하는 항목별로 워크북 셀과 SMath 변수가 *있는지* 와 *수식에 쓰이는지* 를 따로 판정한다. 있는데 안 쓰이는 쪽이 더 위험하다 — 검토한 것처럼 보인다. 온도 정합성(Rdson 25 °C ↔ Tj 125 °C)도 함께 본다 |
| **`cores.py`** | **코어 후보의 데이터시트 값과, 그것으로 푸는 권선 기하.** `CORES` 는 TDK 데이터시트의 전자기 값(A.e·A.min·A.N·l.N·V.e·A.L), **`MECH` 는 치수도의 기계 치수**다 — 라벨이 붙은 값과 **도면 벡터에서 재서 넣은 값**(외각다리 안쪽면 r = 13.60 mm)이 주석으로 구분돼 있다. 재는 방법은 같은 도면의 라벨 두 개(40.5 · 39.8)로 pt/mm 를 잡는 것이고, 그렇게 재면 중앙다리가 **14.94 mm** 로 나와 라벨 14.9 와 맞는다 — 그것이 방법이 옳다는 증거다. **`winding(V)` 가 설계 전류·전류밀도·표피깊이에서 권선을 배치하고 `report(V)` 가 그 산수를 인쇄한다** — 축을 mm 로 그리는 단면도(`figs_ref.an_core_section`)는 이 결과를 그리기만 한다. **구리가 안 들어가면 `gap` 이 음수로 나오고 그림에서 안 들어가는 것이 보인다.** `J_CU`·`K_U`·`K_LITZ`·`T_FOIL`·`MARGIN` 다섯이 가정이고 한 자리에 모아 뒀다 |
| **`figcheck.py`** | **그림 기하 검사 — `check_sm.py` 의 matplotlib 판.** **기호 크기도 잰다** — 커패시터·저항·권선이 *자기 그림 폭의 몇 퍼밀* 인지를 전부 찍어 편차를 보고한다. 그리고 네 가지를 본다: 글이 **자기 축을 벗어나 남의 패널에 떨어지는가** · 글이 **선 위에 앉았는가**(자기 유도선은 제외 — 상자 안에서 *끝나는* 선은 안 센다) · 글끼리 겹치는가 · **상자 윤곽(Patch)도 선으로 센다**(저항 박스 위에 얹힌 라벨은 Line2D 가 아니라 안 잡혔다). **`figs.save()` 가 저장 직전에 `shield()` 를 불러 선 위에 앉은 글에만 흰 후광을 입힌다** — 60군데에서 후광을 기억하는 대신 실제로 그려진 것을 보고 결정한다. 이 도구가 없던 동안 브리지 다이오드 4개가 거꾸로, 주석 하나가 아래 패널로, 라벨 하나가 배선 위에 실려 나갔다. **2026-09-21 배선 검사 5종 추가 (`topology`·`clipped`)** — 점 없는 T (`no-dot`) · 허공에 끝나는 배선 (`open-end`; 접점을 가진 레일의 여분은 `stub` 으로 보고만 하고 세지 않는다) · 홉도 점도 없는 교차 (`crossing`) · 45° 격자 밖 배선 (`off-grid`) · 자기 축 밖으로 잘린 패치 (`clipped`). 배선은 zorder 2 의 Line2D, 기호는 그 밖의 선과 패치, 접점 점은 마커, `gid='symbol'` 은 소자 자신의 리드(MOSFET 의 벌크 타이와 기생 소자 — 기호 관례라 점을 안 찍는다). **aspect-equal 축만 읽는다** — 파형 축의 곡선도 zorder 2 라 배선으로 읽히면 전부 허공에 끝난다. **주석 상자는 `Text.get_window_extent` 로 잰다** — `Annotation` 의 것은 화살표까지 합친 상자라 화살표가 지나는 라벨을 겹친다고 했다. 8구간 시트는 `figs_modes8.sheet_fig` 로 같은 규칙으로 검사한다. 기호 크기는 이제 *축의 scale 로 나눈 값* 으로 보고한다(cap 0.56–0.64 · res 1.15 · turn 0.26–0.30 · winding 0.54–0.60). 첫 실행에서 95건이 나왔고 전부 실재했다. **2026-09-21 (2차) — 글자·선 겹침 검사를 픽셀로 바꿨다.** 예전 검사는 배선의 *꼭짓점* 만 봤는데 배선은 대부분 점 2개짜리라 **그 사이에 앉은 글자를 전부 통과시켰다** — "0건" 이라고 보고하는 동안 문서는 겹침투성이였다(사용자 지적). 선분 기준으로 고치니 44건, 그런데 그중 태반이 글상자만 스친 것이라(글상자는 글자보다 크다) 상자를 줄여 맞추는 대신 **재기로 했다**: 그림을 두 번 렌더한다 — 그대로 한 번, 글자를 alpha 0 으로 지우고 한 번. 두 렌더의 차이가 글자이고, 둘째 렌더의 흰색 아닌 픽셀이 선이다. 둘이 겹치는 픽셀 수를 보고한다(`ink()`, 바닥 10 px). 글자를 빈 문자열로 지우면 주석 화살표가 글자 자리로 자라나 그것이 글자로 읽혔다 — 그래서 alpha 다. 큰 패치의 면(상자·띠·스위치 후광)과 배선 아래 강조띠(zorder<2)는 둘째 렌더에서 숨긴다 — 상자 안 라벨은 겹침이 아니다. **`shield()` 도 같은 측정을 쓰고 회로도 축은 건드리지 않는다** — 후광이 배선을 끊는다. `--crops DIR` 로 겹친 곳마다 확대 조각을 저장한다(빨간 상자가 글자). 지금 0건 |
| **기호 크기 규칙** (`schemx.scale`) | **기호는 *자기 그림 폭에 비례* 한다 — 데이터 단위 고정도, 가지 길이 비례도 아니다.** `schem.shunt` 가 기호를 가지 길이의 `frac` 로 잡던 탓에 **같은 커패시터가 그림마다 0.73~1.38 로 3.3배** 달랐다(첫 지적 "출력캡 기호가 너무 크다" 가 그것이다). 그리고 판:간격 비가 승인본 2.73 대신 1.67 이었다. 지금은 `REF_SPAN` 22.45(8구간 패널의 x 폭) 대비 비율로 정하고, **권선은 턴 *반경* 을 고정하고 턴 수를 높이에서 뽑는다**(반대로 하면 긴 권선의 턴이 커진다). **2026-09-21 바닥 규칙 추가** — `scale = max(자기 폭 / REF_SPAN, (자기 폭 / 축의 그림 폭 비율) / REF_FIGSPAN 46.0)`. 기호는 종이 위에서 8구간 패널의 것보다 작아지지 않는다는 뜻이다 — 3분할 그림(`an_fha_steps`)은 자기 폭 기준으로 패널의 60 % 였고 배선이 기호를 삼켰다. 바닥은 축의 *명목* 위치를 읽으므로 **`subplots_adjust` 를 그리기 *전에* 부른다** — 뒤에 부르면 있지도 않은 레이아웃의 scale 이 기록된다(`sheet_fig`·`an_llc_stage`·`an_fha_steps` 전부 옮겼다). 8구간 시트는 바닥이 0.994 라 자기 폭 1.0 이 이긴다. `figcheck` 는 그려진 크기를 scale 로 나눠 보고하며 지금 cap 0.56–0.64(C_oss 0.28 · C_r 0.30 · C_o 0.32 는 검토된 차이) · res 1.15 · turn 0.26–0.30 이다. **8구간 패널은 명시 크기를 쓰므로 픽셀 단위로 그대로다** |
| **`audit_guide.py`** | **가이드 수식 태그 감사** — 중복 태그 · 끊어진 참조 · 코드에 대응이 없는 식 |
| **`an_pdf.py`** | **PDF 레이아웃 엔진 (문서 2종 공용)** — 표지·법적고지·점선 목차·머리글·번호 붙은 수식(matplotlib mathtext → PNG)·그림/표 캡션. `T()` 가 **포맷 안 된 `%%`·`%(…)` 를 거부**한다 |
| **`tx_pdf.py`** · **`tx_body.py`** | **트랜스포머 설계 정리(한국어) PDF.** `an_pdf` 를 임포트해 `use_korean()` 으로 맑은고딕을 끼우고 `TITLE`·`COVER`·`HEADSIZE`·`FIGWORD`/`TBLWORD` 만 바꾼다. **레이아웃 코드는 한 벌뿐이다** |
| **`an_body.py`** | **AN 본문.** 레이아웃과 분리돼 있고 **손으로 적은 상수가 없다** — 전부 `an_pdf.V` 보간 |
| **`figs.py`** | 그림 생성 — 교육자료 영문 · 가이드 한국어. 수치는 전부 `l6790.py` 에서 가져온다. `f15`~`f17` 모핑 브리지 3장, `f19` 출력뱅크 판정식. **`--plain` 은 제목을 빼고 `figures/an/` 에 쓴다**(AN 용). **`_morph_levels(T)` 는 두 언어가 같은 그리기 코드를 쓴다** — 복사하지 말 것 |
| **`figtrim.py`** | **참조 PDF 에서 잘라 온 그림 아래에 딸려 온 *원문 캡션 조각*을 찾아 잘라낸다.** 서명이 분명하다 — **맨 아래 가장자리에 닿는 얇은 잉크 띠 + 그 위의 넓은 공백**. 진짜 그림의 패널은 이미지 경계에서 잘리지 않으므로 이 모양은 캡션 조각뿐이다. `--fix` 없이 돌리면 보고만 한다 |
| **`pdffig.py`** | **참조 PDF 에서 그림을 캡션째 잘라낸다.** 캡션을 텍스트로 찾아 앵커로 삼는다. 벡터 도면(onsemi·Infineon)은 위로 훑고, 래스터 그림(MPS)은 이미지 사각형이 곧 정답이다. `list`/`show`/`get` · `--box` 로 수동 지정 |
| **`figedit.py`** | **참조 그림에 가하는 *기록된* 편집.** **`fix_freewheel_2nd_half()`** 가 Infineon Fig 2.8 의 전류 경로를 다시 그리고(시트 §6.1 항목 8d), **`mark_modes()`** 가 그림 8 의 네 구간에 괄호를 친다(항목 8e). **`_font()` 는 Arial 이 없으면 Liberation Sans 로 간다**(2026-09-17) — 예전에는 `ImageFont.load_default()` 로 떨어졌는데 그것은 **크기 인자를 무시하는 비트맵 서체**라, Arial 이 없는 PC(이 컨테이너)에서 머리글이 의도의 1/5 크기로 조용히 그려졌다. 지금은 못 찾으면 예외로 죽는다. 이유는 둘뿐이다 — 잘라낸 상자가 라벨을 못 담았거나, 원문 기호가 이 문서와 반대 뜻이거나. 그림이 *주장하는 바*를 바꾸는 편집은 금지. `trim()`(`figtrim` 으로 캡션 조각 제거)과 **`mirror()`** 를 갖는다. **`mirror()` 는 이 모듈이 직접 쓴 파일만 `figures/an/` 로 민다** — `figures/` 와 `figures/an/` 는 같은 집합이 아니다. 생성 그림은 `figs.py --plain` 이 **제목 없는 판**을 `an/` 에 쓰므로, 전부 복사하면 AN 이 제목 붙은 판을 읽게 된다(2026-09-08 실제로 13장을 그렇게 덮었다) |
| **`deckedit.py`** | python-pptx 기반 PPT 편집기. 레이아웃이 비어 있으므로 기존 장을 복제해 껍데기(로고·제목·꼬리말)를 얻는다 |
| **`fix_deck_overflow.py`** | 글이 상자보다 큰 텍스트 리전을 고친다 — 아래가 비어 있으면 상자를 키우고, 막혀 있으면 글꼴을 0.5 pt 씩 줄인다. **아무것도 움직이지 않는다.** `check_deck.py` 와 같은 판정식을 쓴다 |
| **`check_deck.py`** | **PPT 기하 검사** — 슬라이드 밖 · 겹침 · 종횡비 깨진 그림 · 글 넘침 · 껍데기 누락. `check_sm.py` 의 PPT 판 |
| **`build_trans_spec.py`** | **트랜스포머 요구사양서(xlsx) 생성.** 인자로 변형(`9to1`/`7p5to1`/`8to1`/`6to1`)을 **반드시** 받고 `Calculation Excel Sheet/variants/` 에만 쓴다 — 인자 없이 돌려 정본을 덮어쓴 사고가 두 번 났다. **수치는 전부 변형 시트에서 읽는다**(2026-09-07). 손으로 박은 상수 때문에 6:1 사양서가 `f.Min` 을 90 kHz 로 달고 있었다(참값 110 kHz). openpyxl 로 새로 그린다. **코어 개수·보조 턴수도 이제 시트에서 읽는다**(2026-09-21) — `"Three identical transformers"` 와 보조 `1 T` 가 글로 박혀 있어 `N.x` = 2 인 변형에서 거짓말을 했다. **`openpxxl` 오타로 import 단계에서 죽어 있었다** — Drive 임포트 커밋이 들고 온 것이고, 2026-09-21 에 고쳤다. **빈 칸에 빈 문자열을 쓰지 말 것** — 숫자 서식이 걸린 칸에서 `0.00` 으로 그려진다. 값이 `None` 이면 셀에 아무것도 안 쓴다 |
| **`check_trans_spec.py`** | **완성된 벤더 사양서 xlsx ↔ 변형 시트 대조.** 빌더의 상수가 아니라 **실제로 나가는 파일**을 읽어 턴수·개방/단락 인덕턴스·전류·A.e·주파수·개방 시험 전류 9항목을 본다. 포화 시험 전류는 **1차 피크가 아니라 자속 등가 전류**로 판정한다 — 개방 시험은 2차 기자력 상쇄가 없어 같은 전류로도 자속이 훨씬 크다 |
| **`an_check.py`** | **완성된 AN PDF 의 기하 검사 — `check_sm.py` 의 PDF 판.** 쪽마다 틀 밖으로 나간 것 · 텍스트끼리 겹침 · 그림이 글 위에 얹힘 · 110 dpi 미만 그림 · 원본과 종횡비가 다른 그림. **쪽 아래 여백은 실패가 아니라 보고만 한다** — 큰 그림이 남은 자리에 안 들어가 밀리는 것은 정상이다. 사람이 40쪽을 읽으면 사람이 알아채는 것만 잡힌다 |
| **`audit_md.py`** | **CLAUDE.md·HISTORY.md의 주장을 기계로 대조** — 경로 존재 · § 참조 · 개수·용지 수치 · 표 정합성. 문서를 고쳤으면 이걸 돌린다 |
| **`coldstart.py`** | **정보 없는 새 세션이 이 문서만으로 이어받을 수 있는지 모의 검증** — 답해야 할 질문 21개가 CLAUDE.md 안에서 답이 나오는지, 문서가 시키는 명령이 실제로 도는지 |
| `guard.py` | Stop hook이 부르는 검증기 — 시트 §9.1b |
| `build_probe.py` | 진단용 소형 시트 P1–P20 생성 |
| `smgen.py` · `build_sm.py` · `recrop2.py` · `deck2.js` | 구 산출물(가이드 rev 1.1 시트 §3 시트, PPT)용. 현행 체인에는 안 쓰임 |

### 9.1 표준 절차

```
cd "Design Guide/generators"

python build_l6790_smath.py                                          # 시트 생성 + 자동 대조
python check_sm.py "../../Smath/L6790A_SingleStage_PF_LLC_Design_Guide.sm"   # 기하·함수 검사
python bom_compare.py                                                # BOM 29항목
python rtc_check.py                                                  # RTC 격자 18점
python xl_sm_compare.py                                              # 전량 101항목
python snapshot.py                                                   # CLAUDE.md 시트 §3 갱신
python check_theta.py                                                # 시트 §8.4·시트 §7.5 벡터 ↔ 시트 §8 닫힌형
python check_trans_spec.py                                           # 벤더 사양서 ↔ 변형 시트
python an_check.py                                                   # AN PDF 기하
python audit_md.py                                                   # 문서 주장 대조
python -c "import an_pdf,cores;print(cores.report(an_pdf.V))"          # 권선이 보빈에 들어가는가
```

> **변형을 검사하려면 `SM_OVERRIDE` · `XL_OVERRIDE` 를 준다.** `check_sm.py` 는 경로를 인자로 받고, `audit_sm.py` 는 빌더를 읽으므로 3종 공통이라 스위치가 없다.
>
> ```
> SM_OVERRIDE=../../Smath/variants/L6790A_6to1.sm \
>   XL_OVERRIDE="../../Calculation Excel Sheet/variants/L6790_workbook_6to1.xlsx" \
>   python xl_sm_compare.py
> ```

**기대값:**

```
build      mismatches: 0
check_sm   VERDICT OK
bom        items compared: 29   different: 0
theta      mismatches: 0
rtc        대조 18점 · 최대 편차 0.20 % · VERDICT OK
xl_sm      대조 134항목 — 일치 134 · 의도된 차이 0 · 불일치 0
trans_spec 불일치 0 건
an_check   no geometry problems found
audit_md   no problems found
```

> `xl_sm_compare.py` 는 **이제 차이가 0 이다**(2026-08-23). 원인 A–F 를 전부 닫았고, 무엇을 왜 고쳤는지는 `HISTORY.md` 시트 §3.7 과 워크북의 `CHANGELOG_rev0_6` 76–79행에 있다. 아래 문장은 그 이전 상태를 설명한 것이다 — 남은 — 도구가 A–F 태그와 그룹 요약을 인쇄하고, `HISTORY.md` 시트 §3.7 에 표가 있다. 각 그룹 안의 두 수식은 글자까지 같고 값 하나만 다르다. 새 세션이 같은
> 5건을 다시 조사할 필요가 없다. `** DIFF` 가 하나라도 나오면 그것만 보면 된다.

**빌드 후 `check_sm.py`를 반드시 돌릴 것.** 현재 상태:

```
paper A3 Landscape 1654 x 1169 · regions 1865 · overlaps 0 · right overflow 0
page straddles 0 (한 쪽이 1120 px · 접힘 25개가 23440 px 을 숨긴다)
seam clearance 가장 아슬아슬한 곳 61 px · 여백 띠 위 60 / 아래 60
text re-wrap 0 · functions [abs atan augment cos el eval ln range sin sqrt vectorize] (전부 허용 집합)
height 72250 px, 그중 48810 px 이 인쇄됨 = 44 pages · plots 0 · zedgraph 7 · pictures 3
yellow inputs 113 · VERDICT OK
```

> **"몇 쪽"은 접힘을 뺀 높이로 센다.** 접힌 영역은 종이를 안 먹으므로 파일 높이를
> 쪽 높이로 나누면 64쪽이 나오는데 실제 인쇄물은 44쪽이다. 예전에는 그 64 쪽이 문서에 적혀 있었다.
>
> **쪽 경계는 이제 실측값이다 (2026-09-09).** `Smath/L6790A_ruler.sm` — 20 px 눈금과
> **자기 y 를 적은 글자**밖에 없는 시트다. 수식이 없어 F9 가 무의미하고, 접힘이 없어
> 보이는 y 가 곧 절대 y 이고, 판이 없어 스스로 밀려날 것이 없다. 그래서 인쇄하면
> **SMath 의 경계 말고는 탓할 것이 없다.** 9쪽에서 읽은 첫 눈금이
>
> ```
> 24 · 1124 · 2244 · 3364 · 4484 · 5604 · 6724 · 7844 · 8964
> ```
>
> 이고, 쪽마다 마지막 눈금이 다음 쪽 첫 눈금에서 정확히 끝난다(1104+20 = 1124). 즉
>
> ```
> break(N) = N × 1120 + 4        PAGE_CONTENT = 1120 · PAGE_ORIGIN = 4
> ```
>
> **빌더가 쓰던 1117 · 오프셋 0 은 용지 크기에서 추론한 값이었고 틀렸다** — 쪽당 3 px
> 에 원점 4 px 이라 40쪽이면 124 px, 쪽 높이의 1/9 이다. **위 여백이 쪽마다 다르고
> 아래가 휑하던 것이 전부 이것이다.** 고친 뒤 43쪽 전부 위 여백이 정확히 60 px 이고
> 아래는 62 px 이상이다.
>
> **그리고 인쇄물이 이 모델을 채점했다 (2026-09-09, 13:22 판).** 43쪽 각각의 *첫 행*을
> 파일만 보고 예측해 PDF 와 대조했더니 **42/42 전부 맞았다**(당시 42쪽). 앞선 두 번의 역산이 실패한
> 것은 앵커로 쓴 행이 `fit()` 이 경계에 밀어 놓은 행이라 **모델이 자기 메아리를 채점**하고
> 있었기 때문인데, 이번 예측은 파일에서만 만들고 인쇄물이 독립 관측이라 그 함정이 없다.
> **쪽 경계 문제는 닫혔다. 띠를 키워서 고치려 하지 말 것.**

### 9.1b 자동 안전장치 — Stop hook (`.claude/settings.json`)

**턴이 끝날 때** `guard.py`가 돌아, 추적 파일(시트·워크북·빌더 3종)이 **바뀐 턴에만** 검사한다.
전부 통과하면 아무 말도 안 하고, 실패하면 **exit 2로 턴 종료를 막고 이유를 돌려준다.**

```
1. 시트가 빌더보다 오래됐는가        -> 재빌드 필요
2. check_sm.py                     -> VERDICT OK 인가
3. bom_compare.py                  -> different: 0 인가
4. snapshot.py --check             -> 시트 §3이 아직 시트를 설명하고 있는가
5. audit_md.py                     -> 문서가 주장하는 경로·수치·참조가 실재하는가
```

**4번이 다른 PC 문제의 핵심이다.** 시트 §3은 새 세션이 가장 먼저 읽는 값인데, 예전에는 내가 손으로
옮겨 적어서 조용히 낡았다 — 시트가 71.4 mF / 4.99 %일 때 문서는 80 mF / 4.40 %라고 적혀 있었고
우연히 발견했다. 이제 기계가 쓰고 hook이 지킨다.

**guard는 시트를 절대 쓰지 않는다.** 재빌드는 판단이 필요한 행위다 — 시트에는 사용자가
SMath에서 손으로 잡은 레이아웃이 들어 있을 수 있으므로, 부작용으로 덮어써서는 안 된다.

설정은 **프로젝트 폴더 안**(`.claude/settings.json`)에 있으므로 폴더째 옮기면 다른 PC에서도
그대로 붙는다. 끄려면 `/hooks` 에서 해제하거나 그 파일을 지운다.
새 PC에서 처음 열 때는 `/hooks`를 한 번 열어야 설정이 로드된다.

### 9.2 BOM은 엑셀과 SMath가 항상 같아야 한다

`bom_compare.py`는 **엑셀 BOM 시트를 읽지 않는다.** BOM 셀은 링크이고 캐시값은 Excel이
재계산하기 전까지 낡아 있으므로, **링크가 가리키는 원본 셀**을 읽는다:

```
Res. Tank Design       F9 F43 F44 F45
Power Components       F7 D23 D24 D25 D38
Device Setting         F24 F45 D43 D46 F61 F83 F90 F91 F108 D21
CompensationNet&Check  D14 D36 F37 F41 F43 F93~F96
```

SMath 쪽은 `<result>`와 `<contract>` 표시 단위를 함께 읽어 환산한다.
**`<result>`는 이미 표시 단위 값이다** — SI로 착각해 또 나누면 전부 불일치로 보인다
(`HISTORY.md` 시트 §6.5).

### 9.3 SMath 시트의 구조 (19개 절)

| 절 | 내용 |
| :-- | :--- |
| 1–2 | 사양 · 사전 선택 (**노란 입력 셀 113개** — 시트 전체 기준) |
| 3–4 | 전력 예산 [6]–[17] · morphing 등가 입력 [18]–[20] |
| 5–7 | 권선비·요구 게인 [21]–[25] · 탱크 [26]–[35] · [43]–[45] · 최종 확인 [46]–[54] |
| 8 | **라인사이클 동작점 (닫힌형, `HISTORY.md` 시트 §2)** — HB/FB 양 코너, ZVS [38a] |
| 9 | 최악 코너 전류·응력 [63]–[70] |
| 10 | **라인사이클 실효값 (5점 Simpson)** — C_out 리플, 정류소자, 1차 |
| 11 | 출력 커패시터 [101]–[110] + **최악 위상 홀드업** |
| 12 | 트랜스포머 (LGE 3직렬 구조, A_L·자속). **개방시험 전류는 `L.mu_x` 로 나눈다 — `L.open_x` 가 아니다**. 시험 전류 자체는 시트 §14 에 있다 (시트 §6.1 항목 13) |
| 13 | 반도체 **요구조건** [111]–[121] — 소자를 고르지 않는다 |
| 14 | IC 주변 정수 [72]–[100] + **14b DS 동작 한계 13항 전수 판정** |
| 15 | 전압 루프 [122]–[132] (`K.HV`·`K.M`·`K.FF` 노출) |
| 16 | **보상망·옵토 바이어싱 [133]–[148]** — ST 도면 + 기호↔변수 대조표, 16.4 solver 없는 교차주파수, **16.6 보드선도 — ZedGraph 판 2장**, **16.7 ST 툴과 다른 점** |
| 17 | 설계 요약 (판정 지표 전부 "> 1" 형태) |
| 18 | 가정값 출처와 민감도 표 — [DS]/[TOOL]/[PICK] 구분 |
| 19 | **BOM — 전부 살아 있는 참조.** 각 행이 그 값을 만든 변수를 그대로 에코한다(`S.show`). 위쪽 노란 셀을 바꾸고 F9만 누르면 BOM이 따라온다. 부품번호만 텍스트 |

**시트 §16.6 보드선도** — **ZedGraph 판 2장**. 왼쪽에 |T|·|G_cto|·|EA| 를 **한 프레임에**
(|T| 가 나머지 둘의 합이므로 같이 봐야 한다), 오른쪽에 **루프 위상**. 양쪽에 `f.cross` 세로선이
서 있고 위상 쪽에는 목표 `Φ.M` 가로선이 하나 더 있다 — 여유를 **두 선이 만나는 자리에서 읽는다**.
**x축이 진짜 로그축이라 0.1 · 1 · 10 · 100 · 1000 Hz 로 읽힌다** — 예전에는 SMath 에 로그축이
없어 데이터에 `log10(f/Hz)` 를 넣고 눈금을 글로 설명했다.

> **위상 축 이름을 주의할 것.** 그리는 값은 `180° + arg T` 이고, **이것이 위상 여유가 되는 곳은
> |T| = 0 dB 인 한 점뿐**이다. 다른 주파수에서는 −180° 로부터의 거리일 뿐 여유가 아니다.
> 그래서 축 이름이 "phase margin" 이 아니라 `180 deg + arg T` 다.

그래프가 못 뜨더라도 판단이 가능하도록 **5개 디케이드 지점의 |T|·arg T를 일반 스칼라 행으로
따로 인쇄**한다. 이 스칼라는 루프 블록과 독립이므로, 곡선과 어긋나면 **틀린 쪽은 그래프**다.

**시트 §7.5 게인 곡선** — ZedGraph 판 **2장 · 각 8트레이스**. 계열이 *부하* 가 아니라 **라인 위상 θ** 다
— 단일단에서 동작 중 움직이는 것이 부하가 아니라 위상이기 때문이다. 반주기 동안 `Q ∝ sin²θ` 로 내려가고
요구 게인은 `1/sinθ` 로 올라가며, **그 두 발산이 상쇄되는 것이 이 토폴로지 전부**다.

| | 트레이스 |
| :--- | :--- |
| 두 프레임 공통 | `M.OL`(무부하) · `M(f_n)` at θ = π/2 · π/3 · π/4 |
| 왼쪽 @`V.in_min` | 그 세 위상의 **요구 게인 점선 3개** + **`M.Z`** |
| 오른쪽 @FB 코너 | 그 세 위상의 요구 게인 점선 3개 + **`M.inf`** |

**두 프레임의 차이는 점선 3개뿐이고 곡선은 같다 — 그것이 "입력 전압이 바뀌면 무엇이 달라지나"의 답이다.**
`M(f_n,Q)` 는 `λ.act` 와 `Q(θ) = Q.pk sin²θ` 로 정해지고 `Q.pk` 는 **전부하 라인 피크**의 값이라 입력 전압이
들어가지 않는다. `M.OL`·`M.Z`·`M.inf` 도 마찬가지다. **입력 전압은 `M.req` 로만 들어온다.**

> **그래서 라인 조건마다 프레임을 만들 이유가 없다** (2026-09-09 사용자 질문). 같은 그림을 여섯 번
> 그리고 수평선 3개만 옮기는 꼴이 된다. **그리고 이 두 장은 서로 다른 것의 최악이라 그 둘이어야 한다:**
> 왼쪽(최저 입력)은 요구 게인이 가장 높고 주파수가 `f.o` 에 가장 가까우므로 **게인과 ZVS 의 최악**,
> 오른쪽(최고 입력)은 요구 게인이 가장 낮아 주파수가 치솟으므로 **VCO 천장의 최악**이다.
> 110 V·230 V 같은 중간 조건은 둘 사이라 새로 보이는 것이 없다.
>
> **2026-09-09 에 여섯 조건을 한 장에 얹은 "개관 프레임"을 넣었다가 도로 뺐다.** 만든 이유는
> 110 V·230 V 의 동작점이 그림에 없다는 것이었고, 여섯 교차점이 시트 §8 의 Cardano 해와 정확히 일치해
> 검산이 되기도 했다. 뺀 이유는 둘이다 — **θ=π/2 한 점만 보는데 그 점이 모든 조건에서 가장 여유 있는
> 순간**이라 판정 그림 둘 사이에 놓이면 "곡선이 선 위에 있으니 통과"로 오독되고, 실제로 사용자에게
> 다섯 번 설명해도 전달되지 않았다. **설명이 다섯 번 필요한 그림은 제 몫을 못 하는 그림이다.**
> 그 동작점들은 시트 §8 에 숫자로 있고 시트 §14c 가 위상 축으로 그린다. 다시 만들지 말 것.

- **`M.OL`** = 무부하 게인(Q=0). `f_n0` 에 **극(pole)** 이 있고, 그 극이 `f_n0` 가 하한인 이유다.
- **`M.Z`** = **첨두들의 궤적 = 용량성/유도성 경계.** 닫힌형: `Q² = −2aλ/(f_n³·b·(1+1/f_n²))`,
  `a = 1+λ−λ/f_n²`, `b = f_n−1/f_n`. **`f_n0` 과 공진 사이에서만 존재**하므로(공진 위는 어떤 부하에서도
  유도성) 클램프하지 않고 **자기 x 벡터**로 그 구간만 그린다.
- **`M.inf` = 1/(1+λ.act) = 0.6857** — 무부하 게인의 절대 바닥. **`k.lam` 이 묻는 것이 이것**이다:
  9:1 은 `M.FBmax` 0.7928 > 0.6857 이라 무부하 해가 있고, 7.5:1·6:1 은 아래라 없다(워크북 `#NUM!`).

**시트 §14c 라인 반주기 (신설)** — ZedGraph 판 **3장**.

| 프레임 | 트레이스 |
| :--- | :--- |
| **`f.sw(θ)`** | 라인 조건 6곡선 + **`f.o`·`f.Min`·`f.r`·`f.sw_max_spec` 가로선 4개.** **`f.Max` 는 일부러 안 그린다** — VCO 천장 319.6 kHz 는 최고 트레이스(209.5)보다 반이나 높아 혼자 y축을 350 까지 끌어올리고 곡선 6개를 아래로 눌렀다. `k.ceil` 이 숫자로 판정하고, 그려지는 `f.sw_max_spec` 225 kHz 가 둘 중 낮은 천장이므로 **그것을 통과하면 `f.Max` 는 자동으로 통과**한다. **이 판만 y축이 `yauto`** 라 데이터를 따라간다 |
| `M.req(θ)` | 세 입력의 `M.pk/sinθ` |
| `P(θ)` | `2·P.out·sin²θ` × 100/75/50/25 % |

가운데가 핵심이다 — **`k.floor` 와 `k.ceil` 이 비율이 아니라 거리로 보이는 유일한 자리**이고,
첫 보드에서 스코프로 재는 첫 그림이다(`T.idle` 확정).

> **`f.sw(θ)` 는 §8 의 3차식을 θ 로 훑은 것이 *아니다*.** 그렇게 만들었더니 **SMath 가 계산을 끝내지
> 못했다**(2026-09-08). 문제는 점 개수가 아니라 **참조 사슬의 깊이**였다 — θ → Q → a2,a0 → a1 → p,q → z → f
> 로 7단이고 단마다 여러 번 참조한다. **게인식을 주파수가 아니라 *위상* 에 대해 풀면 `sin²θ` 의 이차식**이
> 되어 닫힌형이 나온다:
> `Q.pk²b²s⁴ − s²/M.pk² + a² = 0`, `a = 1+λ−λ/f_n²`, `b = f_n−1/f_n`, **물리적 근은 마이너스 쪽**.
> `f_n` 을 `f_n0` 에서 §8 이 이미 주는 코너값(`f.n.a`·`f.n.b`)까지 훑고 각 점의 θ 를 읽는다 —
> **`cos` 도 세제곱도 `acos` 항등식도 없고 사슬은 4단**이다. 양 끝이 곧 §8 이므로 검산도 강하다
> (108.47 / 209.47 kHz 대 108.48 / 209.49, θ→0 에서 85.11 대 `f.o` 85.07). `V.in_nom` 곡선은
> 끝점이 시트에 없어 뺐다.

> **두 하한을 혼동하지 말 것.** `f_n0` 는 *무부하* 게인이 발산하는 주파수 = 어떤 부하로도 그 아래는
> 조정 불가. **용량성/유도성 경계는 그게 아니라 각 곡선 자신의 첨두**이고 부하에 따라 움직인다
> (전부하는 f_n 0.612). 둘 다 하한이지만 같은 하한이 아니다. `k.floor` 가 재는 것은 `f_n0` 쪽이다.

숫자를 새로 만들지 않는다; 시트 §17 이 비율로만 말하던 것을 한 프레임에 놓았을 뿐이다.

> **중첩된 분수·근호의 높이를 넉넉히 잡을 것.** `smsheet._rh()` 는 분수 한 단을 0.18줄, 근호를 0.40줄로
> 쳤는데 그건 **한 단에만 맞는 값**이다 — SMath 는 단마다 선과 위아래 여백을 그리고 근호는 내용에 따라
> 자라므로 부족분이 깊이에 따라 쌓인다. 그래서 깊게 중첩된 식만 위아래를 침범했다(2026-09-08 사용자 지적).
> 분수 0.35 · 근호 0.55 + 내용의 10 % · 함수 0.30 · 지수 0.90 · 상자 여백 22 px 로 올렸다.
> **그리고 `S.prog(h=...)` 는 이제 상한이 아니라 *하한* 이다** — 손으로 적은 `h` 는 검사되지 않는 추측이라,
> 추정값이 더 크면 추정값을 쓴다.

> **판 위아래 공간은 손으로 예약한다 — `check_sm` 이 원리적으로 못 보는 유일한 겹침이다.** SMath 는
> 프레임 아래에 **입력 행렬 이름을 한 줄씩 중괄호로** 그리는데 그것은 리전 **밖**이라 파일에 기록이 없다.
> `zedsheet` 의 `LEAD`(프레임 위) · `CAPTION` · `PER_CURVE` · `TRAIL`. **지금은 20 · 0 · 0 · 20 이다** —
> `showInputData="false"` 가 그 이름 목록 자체를 없앴으므로 자리를 예약할 이유가 사라졌다(예전 240 · 60 · 33 · 350 은
> 목록이 그려지던 시절의 값이고, 문서에 그대로 남아 있었다). **한 줄에 두 판을 놓으면 둘째는 `lead=False`.**
>
> **판 높이는 `ZS.fit()` 이 정하는 상한과 `emit()` 이 재는 잔여 공간 중 작은 쪽이다.** `emit()` 은 쪽에 남은
> 자리가 `KEEP` 배 이상이면 판을 줄여서 넣고, 아니면 다음 쪽으로 민다. **`KEEP` 은 2026-09-09 에 0.6 → 0.8
> 로 올렸다** — 0.6 에서는 같은 계열의 판이 637 · 952 · 577 로 제각각 나왔고 사용자가 그것을 손으로 키웠다.
> 0.8 이면 큰 판 다섯이 전부 **952(한 쪽에 들어가는 최대치)** 로 균일해진다. **대가는 두 쪽이다** — 42 → 44쪽이고
> 7쪽·27쪽 아래가 700 px 넘게 빈다. **판이 이 문서의 존재 이유이므로 종이가 지불한다.**
>
> **한 판이 쓸 수 있는 최대는 952 px 이다** — 띠 사이 1000 px 에서 위 20 · 아래 20+8 을 뺀 값. 그보다 크게
> 잡으면(사용자가 한때 1106 으로 했다) **어느 쪽에도 안 들어가 항상 회색 띠로 잘린다.**
> **한 줄에 두 판을 놓으면 둘째는 `lead=False`** — 아니면 여백이 두 번 들어간다.

> **벡터 원소 대입 안에 단위를 넣지 말 것.** 플롯 행렬은 순수한 숫자여야 하고, 단위가 붙은 값이 들어가면
> 그 자리에서 **바로 오류가 난다**(2026-09-08, `el(fs.1,k) := f.r/'kHz/…` 로 실제로 냈다). 시트 §16.6 이 이미
> 같은 규칙을 적어 두고 있었는데 시트 §14c 에서 어긴 것이다. 단위는 **눈에 보이는 스칼라 행에서 한 번씩** 뗀다 —
> `q.fr`·`q.fo`·`q.fmn`·`q.fmx`·`q.fsp`·`q.po`·`q.fc`. 그러면 읽는 사람이 위 행과 대조할 수도 있다.
> **`'kHz` 자체는 죄가 없다** — 시트가 식 안에서 13번 쓰고 프로브 P16 이 검증한 단위다. 자리가 문제였다.

> **`S.prog` 는 두 검사기의 사각지대다.** `smsheet` 는 prog 블록을 Python 으로 평가하지 않고,
> `audit_sm` 은 빌더의 `S.row/const/set` 만 훑었다. 그래서 시트 §14c 가 시트 §8 자리에 있으면서 시트 §14 의 `f.Min` 을
> 참조하던 **전방 참조를 아무도 못 잡았다** — 빌드는 깨끗하고 시트는 깨져 있었다. 이제 `audit_sm` 의
> **검사 6** 이 *완성된 `.sm` 을 리전 좌표 순서대로* 읽어 잡는다(빌더를 훑어서는 못 잡는다 — prog 문의 절반이
> `%s` 로 만들어져 이름이 소스에 없다). 리전을 뒤집으면 336건을 잡으므로 검출력도 확인됐다.

> **상수 선은 2점 벡터로 그린다.** 요구 게인·주파수 한계·목표 위상여유 같은 **가로선**을 스윕 전 구간에서
> 계산하는 것은 순수한 낭비다 — 직선은 두 점이면 정확히 같다. `k.2 := range(1,2)` 와 축 양 끝 벡터
> (`fx.2`·`tx.2`·`bx.2`)를 쓴다. **가로선을 2점으로 만든 것은 영구적이다** — 직선은 두 점이면
> 정확하므로 점을 더 줘도 산수만 는다.
>
> **스윕 점 개수는 2026-09-09 에 되돌렸다.** 2026-09-08 에 "계산이 너무 오래 걸린다"는 지적으로
> 절반으로 줄였다가, 곡선 해상도를 되찾으라는 지시로 **전부 2배**로 올렸다:
> `N.gc` 201→**401** · `N.mz` 120→**240** · `N.th` 121→**241** · `N.fs` 120→**240** ·
> `N.bp` 121→**241**. 원소 대입이 3,494 → **6,988** 이다(가로선 14개는 그대로).
> **`N.fs` 는 반드시 짝수로 유지할 것** — 홀수면 스윕 한가운데에서 거울상의 부호가 0 으로 나눈다.
> 다섯 개 전부 노란 셀이므로 느리면 시트에서 직접 되돌리고 F9 만 누르면 된다(재빌드 불필요).

> **판의 *창* 과 *범례* 도 구워진다 — `[BUILD]` 표시가 그 뜻이다.** `x.gl`·`x.gh`·`x.lo`·`x.hi` 를
> 고치면 **데이터만 넓어지고 프레임은 그대로**라 나머지가 잘리고, `r.g2`~`r.g6`(부하 분율)를 고치면
> 곡선만 움직이고 범례는 **옛 퍼센트를 계속 말한다.** 둘 다 F9 가 아니라 재빌드가 필요하다.
> **F9 가 안 통하는 노란 셀은 시트에서 이 아홉뿐**이라 라벨을 `[PICK]` 이 아니라 `[BUILD]` 로 달았다.
> 범례에 퍼센트를 적을 수 있는 이유가 이것이다 — 값이 아니라 **빌드에 고정된 라벨**이기 때문이다.

> **로그축은 `baseTic` 을 반드시 지정한다.** 자동이면 `ceil(log10(min))` 즉 **min 이상의 첫 온전한
> decade** 에서 눈금을 시작한다. decade 단위 step 이면 맞지만, **0.1 decade 같은 소수 step 이면
> 0.5~2.5 축이 1 부터 눈금을 그려 왼쪽 절반에 숫자가 하나도 안 나온다.** `zedsheet._logfit()` 이
> 모든 로그축에 `log10(min)` 을 박는다. (2026-09-08 사용자가 발견)

> **판 글자에는 계산값을 절대 넣지 않는다 (2026-09-08 사용자 지적).** ZedGraph 판은 **정적
> 바이트 덩어리**다 — 노란 셀을 고치고 F9 를 눌러도 판 안의 글자는 안 바뀌므로, 제목에 적힌
> "crossover 17.57 Hz" 는 곡선이 움직인 뒤에도 그대로 남는다. BOM 에 손으로 숫자를 적지 않는
> 것과 **같은 규칙**이다. 범례는 **값이 아니라 변수 이름**을 말하고(`Q = Q.pk x r.g2`), 값은
> 프레임 바로 위의 행에 있다. `zedsheet._no_values()` 가 소수점 있는 숫자가 들어간 판 글자를
> **빌드 단계에서 거부**한다.

### 9.3b 시트를 PDF 로 뽑을 때의 설정 (사용자 지정, 2026-09-09)

SMath 의 CLI(`-e`·`-p`·`-t`)는 이 PC 에서 라이선스로 막혀 있으므로 GUI 로 인쇄한다.

| 항목 | 값 |
| :--- | :--- |
| 실행 파일 | **`Solver.exe`** — `SMathStudio_Desktop.exe` 는 런처라 바로 종료된다. 프로세스 이름도 `Solver` 다 |
| 프린터 | **ezPDF Builder Supreme** |
| 인쇄 방향 | **가로** — 대화상자에서 직접 고른다 |
| 컬러 모드 | 컬러 |

**F9 를 먼저 누르지 않으면 플롯 행렬이 안 채워져 그래프 틀만 인쇄된다.**
기본 프린터를 바꿨으면 반드시 원복한다.

> **인쇄한 뒤 저장하지 말 것 — SMath 는 저장하면서 리전 좌표를 전부 다시 쓴다.**
> 순서는 **열기 → F9 → 인쇄 → 저장하지 않고 닫기** 다. 저장을 하면 시트를 다시
> 빌드해야 하고 F9 를 다시 눌러야 한다. 근거는 시트 §11.1 D-25.

### 9.4 진단 시트 `L6790A_probe.sm`

큰 시트가 안 열리거나 붉은 리전이 나오면 이것부터 연다.
**P1–P20이 각각 구성요소를 하나씩만 쓰므로, 처음 실패하는 번호가 곧 원인**이다.

| 프로브 | 검사 대상 | 기대값 |
| :-- | :--- | ---: |
| P1–P3 | 평문 수·단위·`<contract>` 표시단위 | 3 / 90 V / 150 kHz |
| P4–P7 | 소문자 `ohm`, 단위 상쇄, `sqrt`, 단위 있는 값의 비 | 0.024 ohm / 700 W / 1.4142 / 0.6 |
| P8–P9 | `atan` | 0.7854 |
| P10–P14 | `min`/`max`·`acos`·`tan` 대체식, `π` | 3·5 / 180 V·264 V / 1.0472 / 1.1918 / 3.1416 |
| **P15** | **시트 §8 Cardano 블록 전체 (k=1 분기)** | **q.x 1.43277, q.fn 0.83543** |
| **P16** | **본 시트가 쓰는 단위 21종 전수 (kohm·mohm 포함)** | 전부 1.0000 |
| P17 | 단위 있는 값의 비 → 비정수 거듭제곱 | 1.5239 / 1.2345 |
| **P18** | **`for`·`range`·`line`·`el` 루프 + 루프 안의 단위 처리** | −0.8000 / 40.0364 |
| **P19** | **`<plot>` 리전 자체** | 기울기 −20 dB/dec 직선, 44.0 → 4.0 dB |
| **P20** | **접기(`AreaRegion`)** | 접힌 상태로 열림 |
| P21–P26 | 벡터 안의 `cos` · 단항 마이너스 · `acos` 항등식 · 스칼라÷벡터 · f.sw 사슬 전체와 그 중간값 전부 | 전부 통과 |
| **P27–P29** | **팬 세로 길이를 무엇이 정하는가** — 같은 그래프를 손 안 댄 것 · 리전만 2배 · 리전과 블롭 rect 둘 다 2배 | **미확인 — §8.1** |

---

## 10. 작업 규약

### 10.1 사용자 작업 방식

- **동작의 정확성 최우선.** 회로도를 주면 회로도와 함께 판단할 것
- **모르는 것은 모른다고 할 것.** 근거 없는 값을 만들지 말 것
- 추측으로 고치지 말고 **진단 수단을 먼저 심을 것** (프로브, 기하 검사기)
- 응답은 **한국어**, 산출물은 지정된 언어. **파일은 ZIP 하나로만** 전달
- 요청하지 않은 workflow / agent / deep-research를 쓰지 말 것

### 10.2 수치 작업의 정본

- **`l6790.py`가 기준 구현이다.** 새 수치는 여기서 내고 스프레드시트/SMath와 **삼중 대조**한다
- **스프레드시트는 정본이 아니다** — 구조적 한계가 있다 (`HISTORY.md` 시트 §3.2)
- 계산값과 **선정값**을 반드시 구분 기록한다. `if(selected, selected, calculated)` 패턴을
  지키지 않으면 최종 확인값이 전부 어긋난다
- **BOM은 손으로 적지 않는다.** 계산이 바뀌면 무엇이 틀렸는지 알 수 없다 — 살아 있는 참조로만

### 10.3 스프레드시트에서 사양을 바꿀 때 함께 바꿔야 하는 셀

**입력 셀만 바꾸면 탱크까지는 정확하게 따라오지만, "선정값(F열)"은 하나도 따라오지 않는다.**

| # | 셀 | 안 고치면 |
| :-- | :--- | :--- |
| 1 | `Power Components` D23 · D24 · D25 | 출력 커패시터가 요구값과 무관하게 남는다. **D31·D32(리플 %)와 D33(홀드업 ms) 두 줄을 반드시 볼 것** |
| 2 | `Device Setting` F45 | D46이 D38을 넘으면 **D48 최대 입력 전력이 잘린다.** OCP1도 같은 저항 |
| 3 | `Device Setting` F83 | D83을 넘으면 OVP2에서 VCC가 25 V를 넘는다. **정수 턴 구현 가능성도 확인** |
| 4 | `Device Setting` F108 | D108을 넘으면 V_BO가 VAC_min 위로 올라가 최저 입력에서 브라운아웃 |
| 5 | `Device Setting` D57 | 절대값이므로 P_in 대비 비율이 통째로 바뀐다 |
| 6 | `CompensationNet&Check` F37 · F41 · F43 · F93~F96 | **1·2를 먼저 고치고 마지막에 다시 계산할 것** |
| 7 | `Design Spec` D46 · D47 · `Device Setting` D13 | ✅ 2026-08-23에 800 pF / 220 ns / T_idle 250 ns로 맞췄다. **사양을 바꾸면 다시 볼 것** |

---

## 11. 도구 함정 — 위반하면 파일이 안 열리거나 조용히 틀린다

발견 경위와 증거는 `HISTORY.md` 시트 §6.

### 11.1 SMath `.sm`

**A. 계산이 조용히 틀리는 것**

1. **표시 괄호는 자동 생성되지 않는다.** 부분식 RPN 뒤에 `<e type="bracket">(</e>`.
   없으면 계산은 맞는데 인쇄물이 다르게 보인다.
2. **저항 단위는 소문자 `ohm`.** 대문자 `Ohm`은 독립 차원이 되어 `W + Ohm*A²`가 error 74.
3. **단위 붙은 값에 `abs()` 금지.** 태그가 남아 나중에 `ln`·비정수 거듭제곱이
   "Operation cannot be performed with units"로 죽는다. **값은 정상 표시되므로 증상이 없다.**
   순수 숫자로 계산 후 단위를 다시 붙일 것.
4. `ln` · `exp` · `atan` · 변수 지수의 인자는 **명시적 무단위화** — `(Vo/'V)/(Vin/'V)`.
5. **역탄젠트는 `atan`.** `arctan`은 없다.
6. 표시 리전에 **`<result action="numeric">`** 이 없으면 값이 안 나온다.

**B. 파일이 아예 안 열리는 것**

7. **리전은 `<regions type="content">` 안에** 넣는다. `<worksheet>` 바로 아래면
   NullReferenceException.
8. **`<dependencies>`에 사용한 리전 타입의 어셈블리를 전부 선언한다** — `SMath Core`,
   `MathRegion`, `TextRegion`, `PictureRegion`, `SpecialFunctions`,
   `PlotRegion`(`c451c2b5-798b-4f08-b9ec-b90963d1ddaa`),
   `AreaRegion`(`4974b228-4974-44cf-8274-bf2936b4a766`).
   GUID는 `Reference/EVLHV101SSR50W Design Guide.sm`에서 복사한다 —
   **다만 버전은 거기서 가져오지 말 것.** 그 파일은 ST 것이고 Solver 1.3.0.9126 /
   어셈블리 1.73.9126.0 인데, 여기 깔린 SMath 는 **Solver 1.5.0.9678 / 1.75.9678.0**
   이다(TI 앱노트 시트, 그리고 SMath 가 다시 저장한 우리 시트에서 읽었다).
   2026-09-08 에 빌더를 설치본에 맞췄다. 옛 버전을 적어도 열리기는 한다
   (`MyBode.sm` 은 1.2.9018 이고 잘 그려진다) — 그래도 안 깔린 버전을 적을 이유는 없다.
   `<metadata>`에는 `<author>`만 둔다.

**C. 쓸 수 있는 함수가 제한적이다**

9. **`sqrt · abs · ln · exp · atan · cos · sin`** 뿐이다.
   `min()` · `max()` · `if()` · `acos()` · `tan()`은 "정의되지 않은 함수입니다"로 거부된다.

   | 없는 것 | 대체식 | 오차 |
   | :--- | :--- | ---: |
   | `min(a,b)` | `(a+b−abs(a−b))/2` | 엄밀 |
   | `max(a,b)` | `(a+b+abs(a−b))/2` | 엄밀 |
   | `acos(z)` | `2·atan(sqrt(1−z²)/(1+z))` | 4.4e-16 |
   | `tan(x)` | `sin(x)/cos(x)` | 1.8e-15 |

   **단위가 붙은 값의 min/max는 단위를 나눠 빼고 다시 곱한다** — `(a+b−abs(a/'V−b/'V)*'V)/2`.
   그래프에는 **`for` · `range` · `line` · `el`** 4개가 추가로 필요하다.

   **가드가 있다.** `smsheet.py`의 `SAFE_FUNCS`/`PROG_FUNCS`와 `check_funcs()`가 파일을 쓰기
   전에 모든 수식을 파싱해 허용 밖 함수가 있으면 예외를 던진다. 이 실수는 다시 나올 수 없다.

10. **Cardano 근 선택에 min/max가 아예 필요 없다.** 물리적 근은 **항상 k = 1 분기**
    `w·cos(ang − 2π/3) − a₂/3` 이다 (6,089건 전수 확인).

**D. 레이아웃 — 겹침의 원인 전부**

11. **입력 셀은 대입식만 쓴다.** `V.AC_min := 90 V` 로 끝내고 `<contract>`·`<result>` 에코를
    붙이지 않는다. 계산 결과 행에는 당연히 `<result>`가 필요하다.
12. **리전 높이를 "슬래시 개수"로 추정하면 안 된다 — 수식 트리로 잰다.**
    `smsheet.py`의 `_rh()`/`_rwid()`/`est_box()`. 기준: `LINE_PX 13.5`, `CHAR_PX 5.2`.
13. **텍스트 줄바꿈을 SMath에 맡기면 안 된다.** TextRegion은 폭에 안 맞는 `<p>`를 말없이 다시
    줄바꿈하고, 늘어난 줄이 다음 리전 위에 찍힌다. `_wrap()`이 직접 나눈다.
    글자폭 상수 **`CH_PER_PX = 0.67`** (PDF 실측 0.58에 15 % 여유).
14. **텍스트 리전 높이는 실측표를 쓴다** — 첫 줄 ≈ 1.4·size + 8.7, 이후 한 줄당 ≈ size + 19.
    `Sheet.text_h()`.
15. **SMath는 저장할 때 텍스트 리전을 내용 크기로 다시 맞춘다.** 그래서 **좌측 라벨은 무조건
    한 줄**이어야 한다. `_label(nowrap=True)`가 두 줄이 되려 하면 **빌드를 실패시킨다.**
16. **그래프에는 로그 축도 자동 스케일도 없다.** x축에 `log10(f/Hz)`를 넣고 눈금은 글로 적는다.
    `20·log10(x)`는 `20·ln(x)/ln(10)`으로 쓴다. `scale_*`가 창을 고정하고 **창 밖 데이터는 빈
    그래프로 보인다.** `Sheet.plot_view(w,h,x0,x1,y0,y1)`가 계산해 준다
    (격자 한 칸 20 px, `scale_*` = 칸당 단위수, `transpose_*` = −(창 중심 × px/단위)).
17. **그래프 리전은 프레임 아래에 변수 이름을 한 줄 더 그린다** — `PLOT_CAPTION = 24 px`.
18. **접기(`AreaRegion`)로 기계 부분을 숨긴다.** 여는 리전이 내용과 종료 리전을 **품는다.**
    현재 8곳. **중첩 금지** — 시트 §10은 안쪽 Cardano 접기를 `collapse=False`로 꺼서 depth 2 유지.
    접힌 리전도 절대 좌표를 그대로 가지므로 `check_sm.py`는 펼친 상태로 검사한다.
19. **`range(a,b,c)`의 셋째 인자는 증분이 아니라 *두 번째 값*이다** (Mathcad 방식).
    `line(s1,…,sN, N, 1)`은 `args = N+2`, `for(변수, range(…), 본문)`은 `args=3`.
26. **접힌 블록은 가로줄을 하나 그리고, 그 줄도 자리를 차지한다.** `area_end()` 가
    6 px 만 띄우던 탓에 **그 줄이 바로 아래 행을 관통**했다 — 13:22 인쇄물에서 열 군데,
    그중 20쪽은 노란 입력 `R.T := 11 kohm` 을 그어 놓아 **지운 숫자처럼 읽혔다.** 노란 셀은
    사람이 고치는 자리이므로 가장 나쁜 자리다. 24 px 으로 올렸다(쪽수 42 → 43).
    **SMath 는 접힘·종료 리전에 `height` 를 안 적으므로(전부 `None`) 파일에서는 잴 수 없다** —
    인쇄물의 부족분이 6 px 이었다는 것만 근거이므로 다음 인쇄에서 확인할 것.
25. **SMath 로 저장하면 레이아웃이 무너진다 — 빌더가 잡은 여백은 저장 한 번을 못 견딘다.**
    2026-09-09 에 사용자가 열어 저장한 파일과 같은 빌드를 리전 1908개 전수 대조했다.
    **내용은 한 글자도 다르지 않은데** 좌표는 거의 전부 달라져 있었다 — top 1837개 ·
    left 1851개 · height 1849개. 크기 변화는 대부분 무해하지만(SMath 가 라벨을
    실제 한 줄 높이로 줄인다: 138 → 23 px) **±1~4 px 짜리 위치 흔들림이 아래로 누적**되고,
    여러 줄 주석의 높이 재측정이 ±100~350 px 계단을 만든다. 결과: 재빌드 직후
    **겹침 0 · 걸침 0** 이던 시트가 저장 한 번에 **겹침 3 · 걸침 14 · seam −30 px** 이 됐다.
    이건 사용자가 무엇을 잘못해서가 아니라 **저장 자체의 효과**다 — 손으로 끌어 옮긴
    것(그때는 접힘 헤더 4개와 판 2장)을 전부 제자리로 돌려 놓고 다시 재도 18건이 남았다.
    **그러므로 `.sm` 의 정본은 언제나 빌더이고, 인쇄는 저장 없이 한다**(시트 §9.3b).
    저장된 파일이 생기면 그것을 고쳐 쓰려 하지 말고 **무엇을 바꿨는지 확인해 빌더에
    역이식한 뒤 재빌드**한다.
20. **용지 설정은 `smsheet.py`의 `PAGE_W/PAGE_H/PAPER_ID`에 있다.** 사용자가 SMath에서 바꾼
    용지를 빌더에 반영해 두었으므로(A3 1654×1169, id 8) 재빌드해도 되돌아가지 않는다.
    그림 위치도 마찬가지로 빌더에 반영되어 있다 — **사용자가 SMath에서 손으로 옮긴 것은
    반드시 빌더에 역이식할 것.** 안 그러면 다음 재빌드에서 사라진다.

**E. `.sm`을 손으로 읽거나 쓸 때**

21. 단위는 `<e type="operand" style="unit">V</e>` 를 값과 `*` 로 결합한다.
    표시 단위는 `<input>`과 `<result>` 사이의 `<contract>` 블록이다.
22. **파싱 전에 base64 이미지를 지운다** — 안 그러면 XML 파서가 몇 MB를 문자열로 물고 늘어진다:
    `re.sub(r'<raw format="[^"]+" encoding="base64">.*?</raw>', '<raw/>', s, flags=re.S)`
23. 수식은 **RPN**이다. 중위로 덤프할 때 연산자 우선순위 괄호를 넣지 않으면 오독한다
    (`smgen.py`의 역변환이 참고가 된다).
24. **`<result>`는 표시 단위(contract) 값이다.** SI가 아니다.

### 11.2 xlsx를 XML로 직접 고칠 때

이 워크북은 **차트 30 · 이미지 11 · 코멘트 7**을 갖고 있고 **openpyxl은 차트를 라운드트립하지
못한다.** 그래서 zip 안의 XML을 직접 고친다. **openpyxl로 저장하지 말 것.**

1. **한 `<row>` 안에 같은 `r=` 셀이 두 번 있으면 안 된다.**
2. **`<row>` 안의 셀은 열 번호 오름차순이어야 한다.**
3. **수식을 추가/삭제하면 `xl/calcChain.xml`을 삭제한다** — `[Content_Types].xml`의 Override와
   `xl/_rels/workbook.xml.rels`의 Relationship도 함께. Excel이 다시 만든다.
4. `workbook.xml`의 `<calcPr>`에 `fullCalcOnLoad="1"`.
5. **쓰기 전에 전 시트를 훑어 "행 안의 셀이 중복 없이 오름차순인가"를 검사한다.**
   통과 못 하면 설치하지 않는다. → **`xlsx_patch.py`가 1–5를 전부 대신한다.**
   손으로 zip을 뜯지 말고 이것을 쓸 것: `set_cell()` · `cell_t/f/n()` · `style_of()` ·
   `open_book()` / `write_book()`(검사 실패 시 쓰기 거부) · `backup()`.
6. **`BOM&Schematics`의 G열은 비어 있지 않다** — 모든 행이 `=IF(E.., "", "<출처 시트>")`
   힌트를 갖고 있다. **H·I열은 그림이 앵커되어 있어 그림 밑에 깔린다.**
7. **수정 전 백업을 남긴다** — `xlsx_patch.backup()`. 작업이 검증되면 지운다 (중간 상태 백업 4개는 2026-08-23에 정리했다).

---
