# HISTORY — L6790A 단일단 PF LLC 프로젝트의 근거 문서

**이 문서의 역할.** `CLAUDE.md`는 "무엇을 하면 되는가"만 담는다. 이 문서는 **"왜 그렇게 정했는가"**다.
근거·검증 이력·발견한 오류 전부·사고 기록이 여기 있다.

**언제 읽나:** 어떤 값이나 판단의 근거가 필요할 때, 또는 "이거 이미 확인한 건가?"가 궁금할 때.
평소에는 읽을 필요 없다.

---

## 1. 검증 이력 — **다시 하지 말 것**

### 1.1 식 전수 대조

**설계 가이드 식 [1]–[148] 전체를 6회 독립 재구현으로 대조했다. 불일치 0건.**

- 기준 구현: `Design Guide/generators/l6790.py` (식 [1]–[106] + θ 스윕)
- 대조 스크립트: `l6790_validate.py` — 240 W 예제를 재현하는지 한 번에 찍는다
- 6회차 대조 결과: 스칼라 28개 전부 일치 · `f_sw(θ)` 표 25칸 전부 0.1 kHz 이내 ·
  `[38a]` T_ZC 스윕 20칸 전부 일치 · 최악 코너 전류 6종 소수 3자리까지 일치

### 1.2 개별 검증 항목

| 항목 | 결과 |
| :--- | :--- |
| FHA 실제 동작점 | 수치해와 일치 |
| 위상식 [37] | 탱크 입력 임피던스 위상과 10⁻¹⁶ 수준 동일 |
| 파형 수치적분 | [63] 엄밀 · [64]의 √d 엄밀 · 합성 탱크 피크 일치 |
| 제어 루프 (240 W 예제) | f_cross 19.76 Hz · Φ_M 49.09° · D_3rd 4.54 % |
| EVL6790_670W 역산 | 보드 주석까지 재현 — λ_act 0.5000 · f_o 88.05 kHz · f_r 152.5 kHz · R_CS,max1 24.07 mΩ(실장 23.4 mΩ) · OCP1 여유 23 % |
| Cardano k=1 분기 | λ 0.35–0.80 × Q_pk 0.2–1.4 × 등가입력 6종 × θ 2°~90°, **6,089건 전수 · 예외 0 · 오차 4.8e-14** |
| 닫힌형 vs bracketed solver | 5개 코너 × θ 2°~90° 전 구간, **최대 오차 2.9e-15 · 실패 0** |
| SMath 시트 ↔ `l6790.py` | 22개 항목 자동 대조, 불일치 0 (`build_l6790_smath.py` 실행 시 매번) |
| 엑셀 ↔ SMath 전량 | **101개 값 대조, 차이 0** (의도된 차이 4건은 시트 §3.5) |
| 엑셀 ↔ SMath BOM | **29개 항목, 차이 0** (`bom_compare.py`) |

### 1.3 SMath 실행 검증

**2026-08-21 사용자가 SMath에서 직접 열고 F9까지 돌린 뒤 저장했다** (파일 revision 8).
저장된 `<result>` 값이 `l6790.py`와 일치하므로 **SMath 엔진이 시트를 우리 의도대로 계산한다는
독립 확인**이 끝났다. 붉은 리전 없음, 겹침 0.

---

### 1.4 검사 범위 — 무엇이 닫혔고 무엇이 안 닫혔나

**"다 찾았다"고 말할 수 없다.** 닫힌 것과 안 본 것을 구분해 둔다. 2026-08-23 기준.

#### 전수 확인이 끝난 것 (그 부류 안에서는 더 나올 것이 없다)

| 무엇 | 규모 | 결과 |
| :--- | ---: | :--- |
| SMath RPN 왕복 (`audit_rpn.py`) | 485식 | 불일치 0 — **SMath 가 인쇄된 값 그대로 계산한다** |
| SMath 차원 해석 (`audit_dim.py`) | 485식 | 충돌 0 · 표시 단위 불일치 0 |
| SMath 구조 (`audit_sm.py`) | 변수 328개 | 죽은 변수·정의 전 사용·검사 없는 한계 전부 해소 |
| 판정 지표가 시트 §17 요약에 나오는가 | 33개 | 33/33 |
| 엑셀 ↔ SMath 값 대조 (`xl_sm_compare.py`) | **135항목** | 일치 101 · 의도된 차이 31 · 나머지 3은 캐시 지연뿐 |
| BOM 대조 (`bom_compare.py`) | 29항목 | 차이 0 |
| 워크북 구조 (`audit_xl.py`) | 설계 시트 수식 781개 | 7개 부류 전수 |
| 워크북 캐시 오류값 | 전 시트 | 54개, **전부** RTC 행 29 의 의도적 `NA()` 차트 마스킹 |
| 보상망 이분법 수렴 | 27회 반복 | 독립 수치해와 0.013 % 이내 (17.564 vs 17.562 Hz) |
| `RTC` 격자가 선정 권선비를 쓰는가 | 2,803셀 | 가드 없는 셀 **0개** — C.5 수정이 완전하다 |
| **`RTC` 격자탐색의 답** (`rtc_check.py`) | **18점** (θ 6 × 입력 3) | Cardano·이분법과 **최대 0.197 %** — 격자 한 칸(2.199 kHz)의 14 %. 요구 게인 `M_req`는 18점 전부 0.000 % |

#### 아직 안 본 것 — 여기서 더 나온다면 이 중 하나다

| 무엇 | 규모 | 왜 안 봤나 |
| :--- | ---: | :--- |
| `RTC` 시트의 나머지 셀 | 수식 **10,008개** 중 설계에 안 쓰이는 부분 | **우선순위가 내려갔다(2026-08-23).** RTC가 설계에 영향을 주는 경로는 **셀 9개뿐**이고(`CC` 9수식 + `Device Setting` D2, 서로 겹침) 그 9개가 전부 `rtc_check.py` 18점 안에 들어 있다. 나머지는 차트 23장의 데이터다. 골격 750종의 전수 확인은 여전히 안 했다 |
| **`OutCap_Sel_DB`** | 수식 **3,172개** | 설계가 참조하지 않는다(시트 §3.2). 그래서 틀려도 지금은 영향이 없지만, 누가 쓰면 그때 드러난다 |
| **워크북의 차원 해석** | 수식 781개 | SMath 쪽만 했다. 엑셀은 수식 파서가 필요해 아직 없다 — `10^-6` 하나가 빠져도 잡을 방법이 없다 |
| 수식에 박아 넣은 상수 | 207건 | `audit_xl.py` 가 세기만 하고 검토하지 않았다 |
| 부록 C.9 의 근거 불명 상수 | 4개 | \[28\] 의 π²/8, \[36\] 의 지수 5, \[128\] 의 0.744, `K_HV`. **ST 문서에 근거가 없다** |
| 240 W 예제 전반 | 가이드 전체 | `R_P` 만 정정했다. 다른 반올림 방향 오류가 더 있을 수 있다 |
| `T_idle` | 1개 | 실측 전까지 확정 불가 (시트 §4) |

#### 그리고 이것이 원리적 한계다

**여기 있는 검사는 전부 *일관성* 검사다. *정확성* 검사가 아니다.**

`l6790.py` · SMath 시트 · 스프레드시트 셋이 같은 답을 낸다는 것은, 셋이 같은 식을 구현했다는
뜻이지 그 식이 물리적으로 옳다는 뜻이 아니다. **C.18 과 \[70a\] CT 결함은 일관성 검사로
찾은 것이 아니다** — 물리와 어긋나서 찾았다. θ=π/4 를 라인사이클 실효값이라 부르는 것은
세 구현이 모두 그렇게 했더라도 여전히 틀렸을 것이다.

> 그러므로 남은 위험의 대부분은 **셀이 아니라 모델에** 있다. 실측(`CLAUDE.md` 시트 §4.3)이 그것을 판정하는
> 유일한 수단이다.

### 1.5 240 W 예제 — 삭제 전 보존한 검증 기준

**2026-08-24.** 가이드 rev 2.0 에서 240 W / 60 V 예제를 **완전히 삭제**한다(사용자 결정).
그 예제는 `l6790.py` 가 지금까지 받은 **유일한 독립 검증**이었다 — ST 가 낸 수치를 우리
구현이 재현하는지 보는 것. `l6790_validate.py` 는 아래 29개 값을 코드 안에 갖고 있으므로
가이드에서 지워도 **검사는 계속 돈다.** 지워지는 것은 그 값들의 **출처**다. 그래서 여기 남긴다.

```
설계 조건   Vout 60 V · Pout 240 W · Nrect 2 (FB) · Cr 33 nF · Lr 34 uH · Lm 65 uH · n 2.5

Pin_LLC 244.90 W   Iin_max 2.721 A    Pd_BR 0.984 W    Pd_EMI 1.111 W   Pin 246.99 W
Vin_min 173.24 V   n_calc 2.519       MVmin 1.2245     MVmax 0.8035
l1 0.2445          l2 0.4401          lTD 0.5413       lam 0.5413       Rac 37.24 ohm
Qz1 0.9736         Qz2 4.073          Qz 0.8466        Z0 31.52 ohm     Cr_c 33.66 nF
lam_a 0.5231       nT 3.085           Z0s 32.10 ohm    Z0p 54.77 ohm
fr 150.25 kHz      fo 88.05 kHz       Lmu 80.22 uH     LL1 18.78 uH     LL2 1.973 uH
Qpk 0.862

라인사이클 (최악 코너 173.24 Vac 등가, 전부하)
  Isec 6.185 A   Idio 4.374 A   Ipri 3.760 A   ICout 4.72 A
  Isec_pk 15.42 A   Itr 6.169 A   ILm 3.840 A   comp 6.660 A   Tzc 246 ns
```

> **▷ 이 값들이 왜 중요한가.** 우리 25 V 설계에는 대조할 상대가 없다 — ST 가 25 V 예제를
> 낸 적이 없다. `l6790.py` 가 맞다는 근거는 **오직 이 240 W 예제를 재현한다는 것뿐**이고,
> 그 위에 SMath·워크북 삼중 대조가 얹혀 있다. 그러므로 `l6790_validate.py` 를 **절대
> 지우지 말 것.** 예제가 문서에서 사라져도 검사는 남아야 한다.

## 2. 닫힌형 해 — 이 프로젝트의 방법론적 발견

가이드 §4.2/부록 B.2는 `M(f_n,Q) = M_req`를 **수치적으로** 풀되 근이 두 개(용량성/유도성)이므로
반드시 큰 쪽을 잡으라고 경고한다. **이 방정식은 애초에 수치로 풀 필요가 없다.**

`M_req = M_pk/sinθ`, `Q = Q_pk sin²θ`를 [2]에 넣고 `x = 1/f_n²`로 정리하면 **x에 대한 3차식**:

```
λ² x³ + (q − 2λ(1+λ)) x² + ((1+λ)² − 2q − u/M_pk²) x + q = 0 ,   u = sin²θ , q = Q_pk² u²
```

- 근의 곱 = −q/λ² < 0, 근의 합 > 0 → **세 근은 항상 실근이고 정확히 하나만 음수**
- 따라서 물리적(유도성) 근은 **언제나 "가운데 근"** — 분기 판정 자체가 필요 없다
- Cardano 삼각함수형의 **k = 1 분기** `w·cos(ang − 2π/3) − a₂/3` 가 항상 그 근이다
  (6,089건 전수 확인, 시트 §1.2)

**의의:** 근 탐색·격자 해상도·초기값 문제가 **전부 사라진다.** ST 툴은 `RTC`에서 83칸 격자를
훑고 인접 두 칸 사이를 선형보간해 답을 얻는다 — 닫힌형에는 그 잔차(현재 최대 0.197 %)조차
없다.
역방향(주어진 f_n에 대한 sin²θ)은 2차식이며 역시 기계정밀도.

> **미반영:** 가이드 §4.2·부록 B.2가 아직 수치해 방식으로 서술되어 있다. rev 2.0에서 역이식할 것.

### 2.1 합성 탱크 피크의 닫힌형

`i_Lr(x) = I_Lm,pk(2x−1) + I_trafo,pk·sin πx` 의 최대는 `cos πx = −z`, `z = 2·I_Lm,pk/(π·I_trafo,pk)`
에서 발생 →

```
I_Lr,pk = I_Lm,pk·(2·acos(−z)/π − 1) + I_trafo,pk·√(1−z²)
```

격자 최대값과 **7e-6 A 이내 일치**. 스프레드시트 `Device Setting` D32가 이 식을 쓴다.

---

## 3. 발견한 오류 전부

### 3.1 데이터시트 (DRAFT, 2026-04-30)

| # | 위치 | 내용 |
| :-- | :--- | :--- |
| C.1 | T_idle | **3중 모순.** 시트 §5.3.2 `C_T,max` 식의 700 ns = 2·T_idle → 350 ns / 같은 절 `f_sw_min/max/SU` 식의 700 ns = T_idle 그 자체 → 700 ns / **Table 5 실측표 8칸 역산 → 250 ns** (시트 §4) |
| C.2 | V_BO | min/max 표기 뒤바뀜 |
| C.3 | 전류 규격 | peak/rms 혼용 |
| C.6 | 시트 §5.3.2 | 상수 **541.34 µA/V** = `(400 + 412.0)/1.5` — Table 5의 `I_OSC_SU = 412.5 µA`와 어긋난다(541.67이어야 함). 툴 전용 오차로 알려졌던 412 µA가 **DS 자신의 식에도 있다** |
| C.7 | 시트 §5.3.4 | `R_ZCD,L` 차원 오류 |
| — | 시트 §5.3.1 | 본문은 "a strong pull down on **LOUT1**"이라 쓰지만 Table 6의 열 제목과 실제 핀스트랩 동작은 **LOUT2**다. EVL 보드도 LOUT2 풀다운(R7 100 k)을 미실장하여 morphing을 쓴다 → **본문 오타** |

### 3.2 ST 스프레드시트 r1.0 — rev 0.6에서 수정 완료

| # | 위치 | 내용 |
| :-- | :--- | :--- |
| C.5 | `Res.Tank!D9` + **RTC 2,803셀** | N_rect=2에서 권선비 2배. 두 곳을 함께 고쳐야 한다 |
| C.6 | `D35` `D60` `CC!D26/E26` | 평균전력/rms/참조 오류 |
| C.8 | 14건 | `Design Spec!D20/D79` 등가입력 · `Device Setting!D83` /N_rect · `CC!C25` √d 누락 · `CC!C31` 2f_l 누락 · `Comp!D15/D43` CTR 이중 |
| C.10 | 3건 | `Device Setting!D3` 누락된 max · `Preliminary!D17` η 오라벨 · `Comp!D42` I_min |

**셀 수정으로는 못 고친다고 적었던 구조적 한계 — 셋은 철회됐고 둘이 남았다:**

- ~~`RTC`의 f_n 격자 상한 1.3703 — f_sw,max가 격자 밖~~ → **틀린 기록. 2026-08-23 철회.**
  1.3703은 격자가 아니라 `fn.max`(RTC 행 26 = √(λ/(1+λ−1/M)), 현재 **1.34178**)였다.
  격자 축은 행 25이고 `CG25 = 1.5·CG26` = **2.0127** = 305.4 kHz까지 간다. 우리 f_sw,max
  249.6 kHz는 f_n 1.6448이므로 **격자 안**이다. 격자의 실제 오차원은 상한이 아니라
  **해상도**이며, 칸 간격 f_r/69 = 2.199 kHz에 선형보간이 붙어 잔차는 0.2 % 미만이다
- ~~`CC`의 3점 θ 평가~~ → **틀린 기록. 2026-08-23 철회** (시트 §3.7 원인 B). `RTC`가 이미 θ를
  6점 풀고 있었으므로 `CC`에 열 F·G·H와 J열 심프슨 적분을 넣어 닫았다. 뱅크 리플전류
  21.77 → **30.17 A**. "스프레드시트로는 적분을 못 한다"는 판단이 틀렸던 것이다
- `OutCap_Sel_DB` 미참조 · `D48` 죽은 셀 · `D77`의 c_HB 미피드백 · `B113` 빈 절
- ~~**`Device Setting` D2의 주파수 상한은 `RTC!BD212`, 즉 V_in,max = 264 Vac에서의 값이다.**~~
  → **틀린 기록. 2026-08-23 철회** (시트 §3.7 원인 E). 격자 탐색을 Cardano 닫힌형 18블록으로
  바꾸고 `RTC!C9`를 `MAX(D79, D81)`로 연결해, 시트가 계산만 해 두고 안 쓰던 `Design Spec`
  D81 = 332.34 V가 실제로 쓰이게 했다. f_sw,max 189.3 → **249.6 kHz**.
  670 W 실험의 툴 189.1 kHz vs 실제 249.8 kHz는 수정 전 상태를 말한 것이다
- **`Res. Tank Design` D30의 ZVS는 V_in,min·전부하 한 점만 본다.** 진짜 최악은 대개
  FB 코너의 경부하다 (670 W 실험: 툴 309.6 ns vs 전 영역 스윕 최악 375 ns @ 332.3 Vac·25 %)

> **그래서 스프레드시트는 정본이 아니다.** 정본은 `l6790.py`와 SMath 시트다.

### 3.3 ST 스프레드시트 — 보상망 시트 (C.12 · C.13, 2026-08-21 신규)

`CompensationNet&Check` [133]–[148] **26개 식을 셀 단위로 전부 대조했다. 수식 자체는 26개 모두
가이드와 일치**한다 (`D37 D38 D41 D42 D43 D64 D65 D66 D67 D72 D73 D74 D78 D83 D84 D85 D86
D93 D94 D95 D96 D101 D102 D103 D104 D128 D129`). 회로 토폴로지도 동일 — 시트에 박혀 있는
도면(H14, 894×531 px)을 추출해 확인했고 SMath 시트 §16 첫머리에 실려 있다
(`Design Guide/figures/comp_network_st.png`).

**그러나 루프 검증부에 결함이 2건 있다.**

| # | 위치 | 내용 | 영향 |
| :-- | :--- | :--- | :--- |
| **C.12** | `CN!H132/H134/H139`, 이분법 `H141:N167`, `D124` | **고주파 극점 `f_px`가 통째로 빠져 있다.** 이득·위상·교차주파수 모두 `f_z`·`f_p`만으로 계산 | 교차 근방에서 위상 약 **0.8–1.1° 낙관** |
| **C.13** | `CN!D73` | 라벨은 *"Required E/A Gain @ 2fLmin"* 인데 분모가 **`D65`(공칭 f_l의 리플)**다. `D68`이 바로 아래에서 f_l,min 리플을 계산해 놓고도 **어디에서도 참조되지 않는다.** `D125`도 `AV122 = 2·D110`(공칭 f_l)에서 읽는다 | 50/47 = **1.064배 낙관.** 3차 고조파 예산이 그만큼 헐거워진다 |

두 건 모두 **SMath 시트는 보수적인 쪽으로 이미 고쳐져 있다**(시트 §16.4, `f.line_min` 전면 적용).
차이는 시트 §16.7 표에 명시. 부수 확인: `D64`의 계수 `2·K_HV/(K_M·K_FF)` = 2×0.00334/(0.2×0.2)
= **0.167 V/(Ω·W)** 로 [122]와 정확히 일치.

### 3.4 ST 스프레드시트 — ISEN 블록 (C.14~C.17, 2026-08-21 신규)

**부록 C.8·C.10에도, `CHANGELOG_rev0_6`에도 없던 항목이다.** rev 0.6에서 Device Setting은
D3와 D83만 고쳤고 ISEN 블록(D38~D48)은 손대지 않았다.

- **C.14 — `D39 R.CS.max2`의 분모가 `I_trafo,pk`다.** R_CS는 브리지 리턴에 있으므로
  자화 성분을 포함한 **합성 탱크 전류 피크**를 본다. 670 W 실험: 16.90 대신 **18.62 A** →
  32.54가 아니라 **29.54 mΩ**. **1.10배 낙관.** 최대전력 조건이 지배하는 설계에서는 결과가
  안 바뀌지만, λ가 크거나 반사 전압이 높아 D39가 지배하면 그대로 위험하다.
- **C.15 — `D45`는 라벨이 "Select…"인데 수식 셀이다.** `(3/220+1/440)^-1` = 62.857 mΩ는
  **EVL 보드의 220‖220‖220‖440 실장망을 박아 넣은 상수**이고, 거기에 D43을 곱해 뒤의
  `D46 = D45/D43`이 같은 값을 다시 뱉게 해 놓았다. **계산이 아니라 상수 왕복이므로 사양을
  무엇으로 바꿔도 R_CS는 영원히 62.857 mΩ이다.** 게다가 `D46 = D45/D43`은 "동일 저항 D43개
  병렬"을 전제하는데 실제 망은 220 3개 + 440 1개다. 결정적으로 **D45(188.57)가 바로 위
  D44(72.20)를 2.6배 위반해도 경고가 없다** — 240 W 원본에서는 D44 = 204.05 > 188.57이라
  우연히 통과했을 뿐이다.
- **C.16 — `D33`의 전류 기준이 틀렸다.** `MAX(CC!C29, Prelim!D11)`인데 `CC!C29`는
  **θ = π/4 한 점**의 스위칭주기 실효값이다. 670 W 실험에서 10.239 A. 라인사이클 실효는
  10.880 A(손실의 물리적 정답), 가이드가 쓰는 최악 스위칭주기(θ=π/2)는 13.932 A.
  **툴 값은 둘 중 어느 것도 아니다.** R_CS = 24 mΩ에서 2.52 / 2.84 / 4.66 W →
  1 W 부품 **3개가 아니라 5개**가 맞다.
- **C.17 — OCP가 아예 없다.** 워크북 전체에 `0.75`(V_OCP2)도 "OCP"라는 문자열도 어떤 수식·
  라벨에도 없다. R_CS를 고른 뒤 **OCP가 몇 A에서 트립하는지 시트가 알려주지 않는다.**
  670 W 실험에서 62.86 mΩ이 조용히 살아남은 이유가 이것이다 — I_OCP1 = 8.75 A인데 실제
  합성 피크는 18.62 A로 상시 트립인데도 시트 어디에도 그 사실이 안 나온다.

**▷ 스프레드시트에 반영 완료 (2026-08-21).** `Device Setting` 9셀 수정 + 11셀 신설:

| 셀 | 전 | 후 |
| :--- | :--- | :--- |
| D32 | `=CC!E27` (I_trafo,pk) | 합성 피크 **닫힌형** (시트 §2.1) |
| D33 | `MAX(CC!C29,…)` θ=π/4 | `MAX(CC!E29,…)` θ=π/2 |
| D44 | (그대로) | `=MIN(D38,D39)*D43` — 단일 저항의 **상한** |
| D45 | `=D43*(3/220+1/440)^-1` | `=LOOKUP(D44/10^INT(LOG10(D44)),{E24 24개})*10^INT(LOG10(D44))` — 상한 이하 최대 **E24 실물값**. 실제 선택은 F45(노란 셀) |
| F45 / D43 / D46 | 텍스트 / 3 / `=D45/D43` | **120** / 5 / `=IF(ISNUMBER(F45),F45,D45)/D43` |
| F61 | 78 kΩ | **30 kΩ** — R_BM ∝ R_CS이므로 D61 상한이 78.7 → 30.1 kΩ. 안 고치면 버스트 진입이 75 → 195 W |
| D49·G49·D50 (신설) | 없음 | `I.OCP1 = 0.55/R.CS` · `I.OCP2 = 0.75/R.CS` · `k.OCP1 = I.OCP1/D32` |

### 3.4b ST 스프레드시트 — θ=π/4 를 "라인사이클"이라 부른다 (C.18, 2026-08-23 신규)

> **정정.** 이전 판의 시트 §3.6 표는 `Power Components` D28(I_Cout)의 차이를 **[70a] CT 결함**
> 탓으로 적었다. **틀렸다.** `CC!C31 = √(0.5·d·I_sec,pk² − I_o,inst² + I_out²/2)` 의
> `0.5·d·I_sec,pk²` 는 이미 **노드** 평균제곱이다(권선당 값의 2배). 워크북은 CT 함정에
> 빠지지 않는다. 실제 원인은 아래 C.18 — 그 열 전체가 θ=π/4 한 점이라는 것이다.
> **[70a] CT 결함은 가이드 본문의 문제이고(시트 §3.5), 워크북의 문제가 아니다.**

`CC` 시트는 θ 를 **세 점(π/4, π/3, π/2)** 에서만 평가한다. 그 중 **θ=π/4 열(C열)** 이
여러 곳에서 *"(mains cycle)"* 라벨을 달고 라인사이클 실효값 행세를 한다:

| 셀 | 참조 | 라벨 | 실제 내용 |
| :--- | :--- | :--- | :--- |
| `Res. Tank Design` D65 | `=CC!C29` | Ipri.rms **(mains cycle)** | θ=π/4 한 점의 스위칭주기 실효값 |
| `Res. Tank Design` D67 | `=CC!C25` | Isec.rms **(mains cycle)** | 같음 |
| `Power Components` D60·D61 | `=CC!C29`·`=CC!C30` | Ipri.rms / per mos | 같음 → **D78 손실식으로 들어간다** |
| `Power Components` D84·D85 | `=CC!C25`·`=CC!C26` | Isec.rms / Idiode.rms | 같음 → **D100 손실식으로 들어간다** |

**왜 θ=π/4 인가.** 포락선이 정확히 `sin θ` 라면 그 점의 값이 라인사이클 실효값과 같아진다
(`sin(π/4) = 1/√2`). 근거 없는 선택이 아니라 **의도된 근사**다.

**그런데 실제 포락선은 `sin²θ` 다.** `I_out,inst = 2·I_out·sin²θ` 이므로 근사가 성립하지 않는다.
`l6790.py` 의 5점 Simpson 적분과 대조하면:

```
                θ=π/4 한 점      라인사이클 실효      차이
  I_pri          10.302 A         10.912 A        +5.9 %
  I_diode(권선당) 24.146 A         28.401 A       +17.6 %
```

**손실은 전류의 제곱이므로 영향이 커진다:**

```
  D100 Pdiss_d   7.93 W  ->  10.98 W   (1.38 배)
  D101 필요 Rth  12.60   ->   9.11 °C/W
  D78  Pdiss.s   2.65 W  ->   2.98 W   (1.12 배)
```

**▷ 스프레드시트에 반영 완료 (2026-08-23).** `CC` 시트는 θ 열이 3개뿐이라 적분이 불가능하다.
그래서 워크북 자신의 패턴을 따랐다 — 계산 열은 근사를 유지하되 **라벨을 `@θ=π/4 (approx.)`
로 정정**하고, F열에 SMath 시트의 라인사이클 실효값을 받는 **노란 입력 셀을 신설**했다
(F60 10.90 / F61 7.707 / F84·F85 28.31 A). 손실식 D78·D100 은
`IF(ISNUMBER(F..), F.., D..)` 로 선정값을 우선한다.

> **SMath 시트는 처음부터 5점 Simpson 라인사이클 실효값을 쓴다** (시트 §10). 영향 없음.

### 3.4c 2026-08-23 — 남아 있던 워크북 오류 전부 반영

`xl_sm_compare.py` 로 101항목을 대조하며 "아직 안 고친 것"을 뒤졌다. 넷이 남아 있었다.

| # | 무엇 | 어떻게 고쳤나 |
| :-- | :--- | :--- |
| **C.12** | 고주파 극점 `f_px` 가 `D104` 에 계산만 되고 **어디에서도 참조되지 않는다** | 루프 검증식 **196셀**에 f_px 항 삽입 — 이득 120개에 `/(1+(f/f_px)²)`, 위상 76개에 `−atan(f/f_px)`. 공유 수식이라 **마스터 7개**만 고치면 전체가 따라온다 |
| **C.13** | `D73` 이 `D65`(공칭 f_l 리플)로 나눈다. `D68`(f_l,min)이 계산되고도 미참조 | 분모를 `D68` 로 |
| **입력** | `Design Spec` D46·D47 이 ST 툴 기본값 500 pF / 270 ns | **800 pF / 220 ns** — 본 설계의 실제 선정 소자. SMath 는 처음부터 이 값이었다 |
| **입력** | `Device Setting` D13 T_idle = 350 ns, SMath 는 250 ns | **250 ns 로 통일.** 두 문서가 같은 값을 써야 대조가 의미를 갖는다. **실측 전까지 확정 아님** (시트 §4) |

> **⚠ Excel 을 한 번 열어야 한다.** 수식을 바꿨으므로 캐시값이 낡아 있다. 파일에
> `fullCalcOnLoad="1"` 이 설정되어 있으므로 **열기만 하면** 재계산된다. 그 전까지
> `xl_sm_compare.py` 는 옛 값을 비교한다.
>
> 재계산 후 기대: C.12·C.13 계열 **9행**과 T_idle 계열 **5행**이 `INTENDED` 에서
> `same` 으로 바뀌어야 한다. 안 바뀌면 조사할 것.

### 3.4d 구조 감사 — 2026-08-23, C.21 ~ C.24

C.12(계산만 하고 아무도 안 읽는 극점)와 C.15(자기 상한을 2.6배 위반하는 선정값)는 **읽어서
찾은 것이 아니라 구조가 이상해서 찾았다.** 그 두 가지 *모양*을 전 시트에서 기계로 훑도록
`audit_xl.py`(워크북)와 `audit_sm.py`(SMath)를 만들었다. 넷이 더 나왔다.

**`audit_xl.py` 가 보는 것**
① 계산만 하고 아무도 안 읽는 셀 ② 캐시에 남은 오류값 ③ 빈 셀을 읽는 수식
④ **선정값이 자기 한계를 벗어남** ⑤ 셀로도 존재하는 값을 수식에 박아 넣음
⑥ 라벨은 max 인데 수식은 MIN(그 반대도)

**`audit_sm.py` 가 보는 것**
① 계산만 하고 아무도 안 쓰는 변수 ② 정의보다 먼저 쓰임 ③ **한계는 있는데 비교가 없음**
④ 설명 없는 판정값 ⑤ 가이드에는 있는데 시트에 안 보이는 식

#### C.21 `Res. Tank Design` D20 `QZVS.3` — 계산하고 버린다

`D26 QZVS = 1/(1+m)·MIN(D18:D19)` 는 **가이드 [34] 와 정확히 일치**한다 — [34]도
`min(Q_ZVS,1, Q_ZVS,2)` 두 개뿐이다. 그런데 툴은 D20 에 세 번째 값을 계산해 놓고 쓰지 않는다.

역산하면 `Q_ZVS,3 = √(λ(1+λ)) / M_Vmin,pk` = **0.7460**. 우리 설계의 `Q_pk = 0.7717` 은
그보다 3.4 % 크다. 만약 이것이 실제 한계라면 위반이다.

**한계가 아니다 — 두 가지로 직접 확인했다.**

```
FHA 게인 천장 @ Q_pk=0.7717, λ=0.550 : M_peak = 1.4161
요구 게인 M_Vmin                      : M      = 1.2245   -> 여유 15.6 %
[38a] 전 영역 ZVS 스윕                : T_ZC 457 ns vs t_D 220 ns -> 여유 108 %
```

즉 탱크는 요구 게인을 여유 있게 내고 ZVS 도 넉넉하다. `Q_ZVS,3` 는 **근거를 확인할 수 없는
과보수 식**이며(부록 C.9 계열), 그대로 적용하면 Q 를 불필요하게 3 % 낮추게 된다.
**가이드와 SMath 는 [34] 를 따르므로 영향 없다.**

#### C.22 `Device Setting` D24 `RT.min` — 라벨이 정반대다

`f_Min = 1/(2·(C_T·R_T + T_idle))` 은 **R_T 가 커질수록 내려간다.** D24 가 계산하는 값은
`f_Min` 이 `f_o` 와 정확히 같아지는 R_T 이므로, R_T 는 그 **아래**에 있어야 한다.
"min" 이라고 적혀 있으면 R_T 를 올림하게 되고, 그러면 VCO 클램프가 `f_o` 아래로 내려가
용량성 영역 진입을 ACP 보호에만 의존하게 된다 — 설계 규칙이 명시적으로 금지하는 상황이다.

**▷ 정정 완료.** 워크북 `C24` 를 `RT.ceil (NOT a minimum)` 으로, SMath 변수를
`R.T_min` → **`R.T_ceil`** 로 바꾸고 양쪽에 비교 검사를 넣었다
(`k.RT.ceil` / `k.RTd`, 현재 1.022). 선정 11 kΩ 은 상한 11.02 k 아래라 **방향은 원래 맞았다.**

#### C.23 `R_P` 선정값이 자기 상한을 4.3 % 위반 — 가이드 예제도 같다

`R_{P,max} = V_F/I_min` [144] = 1.15/0.8 mA = **1.4375 kΩ**. 그런데 워크북과 SMath 와
**가이드의 240 W 예제까지 전부 1.5 kΩ** 을 쓴다 — 상한을 올림한 것이다.

1.5 kΩ 이면 션트 레귤레이터 캐소드 전류가 `1.15/1.5 = 0.767 mA` 로 최소 0.8 mA 아래로
떨어져 FB 범위 하단에서 조정 능력을 잃는다. **상한은 내림한다.**

**▷ 1.4 kΩ (E96) 로 변경.** 부수 효과로 `R_B` 창이 16.02–18.22 → **15.39–17.41 kΩ** 로
이동하지만 선정 16.5 kΩ 은 여전히 안쪽이고 오히려 여유가 균형 잡혔다
(`k.RB_lo` 1.030 → **1.072**, `k.RB_hi` 1.104 → **1.055**).

#### C.24 `C_in` 선정값이 하한 미달 · 그리고 비교 검사 자체가 없던 곳 3군데

`C_in` 선정 2000 nF, 하한(3 nF/W) 2054 nF — **2.6 % 미달**. → **2200 nF (E6)** 로 변경.

그리고 **한계를 계산해 놓고 비교하지 않는 곳**이 양쪽 문서에 있었다:

| 한계 | 선정값 | 검사가 없었다 | 새 검사 |
| :--- | :--- | :--- | :--- |
| `C.T_max` / `C.T_min` | C_T 470 pF | 없음 | `k.CTd` 1.489 / 워크북 `k.CT` |
| `R.T_ceil` | R_T 11 kΩ | 없음 | `k.RTd` 1.022 / 워크북 `k.RT.ceil` |
| `n.aux_max` | n_aux/n_sec 0.8 | 없음 | `k.auxr` 1.045 / 워크북 `k.naux` |
| `R.P_max` | R_P | 없음 | `k.RP` 1.027 |
| `C.in` (하한) | C_in | 없음 | `k.Cin` 1.071 |

> **한계를 계산만 하고 비교하지 않으면 위반이 조용히 지나간다.** C.15 가 정확히 그 구조였다.
> SMath 판정 지표는 이제 **29개, 전부 통과**.

### 3.4e Phase margin 과 Gain margin

**위상 여유는 원래 양쪽에 다 있었다** — 워크북 `CN!D124 = 180 + AT139`, SMath `Φ.act`.
독립 수치해로 검산하면 **49.275°** 이고 두 문서가 인쇄하는 값과 일치한다(49.28).
다만 워크북 쪽은 `f_px` 극점이 빠져 있었고(C.12), 그것은 오늘 고쳤다.

**이득 여유(gain margin)는 어느 쪽에도 없었다.** 위상 여유만으로는 안정성 논증이 닫히지
않는다 — 이 보상망은 고주파에서 영점 1개 대 극점 2개라 위상이 −270° 로 가고, 따라서
`arg T` 가 **유한 주파수에서 −180° 를 지난다.**

**이분법이 필요 없다.** `atan(f/f_z) = atan(f/f_p) + atan(f/f_px)` 의 양변에 탄젠트를 취하고
`f` 로 나누면 제곱근 하나가 남는다:

```
f_180 = √( f_p·f_px − f_z·(f_p + f_px) )
      = √(49172.6 − 6730.4) = 206.02 Hz          (수치 근 206.013 과 일치)
|T(f_180)| = 0.01935   ->   GM = 34.26 dB
```

근호 안이 음수가 되면 위상이 −180° 에 닿지 않고 이득 여유는 무한대다 — 식 자체가 그 경우를
말해 준다.

**▷ 양쪽에 반영 완료.** SMath 시트 **시트 §16.4b** (`f.180` `T.180` `GM` `GM.t` `k.GM`),
워크북 `CompensationNet&Check` **C130:E134**. 현재 `k.GM` = **3.426** (목표 10 dB).

### 3.4f 반복 구조 감사 — 10라운드, C.25 ~ C.27

한 번 훑고 끝내는 대신 **매 라운드 다른 종류의 결함을 찾도록 검사를 바꿔 가며** 돌렸다.
음성 결과도 결과다 — 무엇을 이미 확인했는지가 남는다.

| 라운드 | 무엇을 찾았나 | 결과 |
| :-- | :--- | :--- |
| 1 | 독립적으로 중복된 입력 (하나만 고치면 조용히 어긋나는 것) | `Config.` 이름 충돌 1건. **배선 오류는 없음** |
| 2 | bulk 시트에서 θ 열들의 구조 불일치 | **C.25** — `CC` 4셀이 잘못된 셀 참조 |
| 3 | `.sm` 의 RPN 을 되읽어 독립 평가 | **475식 전수, 불일치 0** |
| 4 | 차원 해석 (V + A 같은 것, ln 인자의 단위) | **475식 전수, 충돌 0 · 표시 단위 불일치 0** |
| 5 | 아무도 읽지 않는 노란 입력 | `N.mos` · `R.ZCD_H_sel` 2건이 진짜 |
| 6 | 열 골격 일관성을 전 시트로 확대 | C.25 외 새 예외 없음 |
| 7 | 선정값(F)이 있는데 계산값(D)을 읽는 곳 | **C.27** 1건. `RTC` 격자 n 은 **0건**(C.5 수정 완전) |
| 8 | SMath 시트 §19 BOM 이 선정값을 보는지 | 깨끗함. 뒤바뀐 라벨은 워크북 쪽만 |
| 9 | 판정값이 시트 §17 요약에 나오는지 | **31개 중 26개가 안 나왔다** |
| 10 | 가이드 152식을 두 구현과 대조 | **네 식이 SMath 에 없었다** |

#### C.25 `CC` D39·E39·D52·E52 — C.6 수정이 절반만 적용돼 있었다

`IF('Design Spec'!$D$108=1, ...)` 이 정류 구성(`D107`) 대신 **`Vf.BR`(0.08 V)** 를 본다.
0.08 은 1 이 아니므로 조건이 **항상 거짓**이 되어, CT 설계인데도 FB 분기(`/√2`)가 돈다.

`CHANGELOG_rev0_6` 이 이 오류를 C.6 으로 기록하고 **행 26(100 % 부하 블록)만** 고쳤다.
75 % / 50 % 블록의 같은 셀 넷은 남았다. **지금 행 34–54 를 읽는 셀이 하나도 없어 수치 영향이
없다 — 그래서 살아남았다.** 그 블록을 쓰는 순간 권선당 실효값이 29 % 낮게 나온다.

#### C.26 BOM 의 ZCD 저항 설명이 서로 바뀌어 있다

`BOM&Schematics` 행 55 는 `R.ZCD.L`(19 kΩ)을 **"Upper ZCD resistor"**, 행 56 은
`R.ZCD.H`(162 kΩ)를 **"Lower ZCD resistor"** 라고 적는다. 값과 링크는 맞고 **말만 뒤바뀌었다.**

**이 BOM 대로 실장하면 분압비가 뒤집힌다.** OVP1 임계가 27.4 V 가 아니라
`2.3/0.8 × (19/162 + 1) = 3.2 V` 가 되어 **기동 즉시 OVP 로 걸린다.**
SMath 시트 §19 는 정상이다.

#### C.27 BOM 이 선정값 대신 계산값을 읽는 곳

`BOM&Schematics` E37 "Single Rsense value" 가 `Device Setting!D45`(계산된 E24 값)를 읽는다.
워크북의 관용구는 `IF(F,F,D)` 이고 다른 BOM 행은 전부 그것을 쓴다. **현재 D45 와 F45 가 우연히
둘 다 120 mΩ 이라 보이지 않는다.**

> **▷ C.25 · C.26 · C.27 은 셋 다 워크북에 반영됐다** — `CHANGELOG_rev0_6` **행 72 · 73 · 74**.
> 2026-09-02 에 셀을 직접 열어 확인한 현재 상태:
>
> - C.25 → `CC` D39·E39·D52·E52 가 전부 `'Design Spec'!$D$107` 을 본다
> - C.26 → BOM 행 55 = "Lower ZCD resistor (to GND)" · 행 56 = "Upper ZCD resistor
>   (to aux winding)". **이 BOM 대로 실장해도 기동 즉시 OVP 로 걸리지 않는다**
> - C.27 → BOM E37 = `IF('Device Setting'!F45, F45, D45)`
>
> 이 세 건이 시트 §3.4c 의 Phase 4b 목록과 `CLAUDE.md` 시트 §4 표에 빠져 있어서 "반영 안 된 것" 처럼
> 보였다. **C.26 은 라벨 오류라 "계산 결함" 이라는 말에 안 걸리므로 특히 눈에 안 띈다.**

#### 라운드 5 · 9 · 10 에서 SMath 에 채운 것

| 무엇 | 왜 필요했나 |
| :--- | :--- |
| `V.OVP1_act` `V.OVP2_act` `k.OVP1` `k.VCC` — 식 [97][98] | 선정한 ZCD 분압기가 **실제로** 만드는 OVP 임계를 되계산하지 않고 있었다. `R.ZCD_H_sel` 은 BOM 에만 쓰이는 죽은 입력이었다. 워크북은 D94~D99 에 갖고 있다 |
| `N.mos` 를 `P.pri_tot` 에 연결 | `2·R·I²` 의 "2" 가 실은 `N.mos/2` 였다. 입력이 아무 일도 안 하고 있었다 |
| **시트 §17.1 전 판정값 표** | 31개 중 **26개가 시트의 설계 요약에 없었다.** 시트 §14b 깊은 곳에서 실패하면 요약만 읽는 사람에게는 보이지 않는다 |
| `I.R1_pk` `I.R0` `T.T` `k.TT` — 식 [40][41][42] | **ZVS 조건이 둘인데 하나만 검사하고 있었다.** 공진 전류가 아직 흐를 것(`k.ZVS`)과 **천이가 데드타임 안에 끝날 것**(`k.TT`). 두 번째가 통째로 없었다. 현재 T_T = 29.7 ns, k.TT = 7.41 |
| `C.T_min` — 식 [73] | 설계 하한이 없었다. DS 의 270–1000 pF 창과는 다른 조건이다. `k.CTd` 가 이제 양쪽을 본다 (1.489 → 1.446) |
| `N.RCS_req` `k.NRCS` — 식 [82] | 저항 개수가 근거 없는 자유 선택이었다. 최악 스위칭주기 손실 기준으로 4.54 → 5개 필요, 현재 5개 |
| `V.out_SUend` — 식 [99] | 기동이 넘어가는 출력 전압. 75 mF 뱅크의 콜드 스타트 판정에 직접 쓰인다 |

> **판정 지표 23 → 33개.** 전부 통과. 새 검사기 `audit_xl.py` · `audit_sm.py` ·
> `audit_rpn.py` · `audit_dim.py` 가 이 열 라운드를 매번 재현한다.

### 3.7 엑셀 ↔ SMath 의 차이 — 행 18개가 아니라 **원인 6개**다

2026-08-23. 두 수식을 나란히 놓고 전수 대조한 결과다. **각 그룹 안에서 두 식은 글자까지
같고 들어가는 값 하나만 다르며, 뒤의 행들은 첫 행의 하류다.** 행을 세면 문제가 실제보다
많아 보인다. `xl_sm_compare.py` 가 이 태그를 매번 인쇄한다.

| 원인 | 무엇 | 영향 행 | 수정 |
| :-- | :--- | :--- | :--- |
| **A** | **ZVS 를 선정 후 재검산하지 않는다.** `D28` 은 근사식 [36](f_n 0.82111), `D29` 는 선정 Q(0.8466). 실제 동작점은 f_n 0.84006 · Q 0.7717 이고 **툴 자신의 `RTC!BB149` · `RTC!C41` 에 있다** | D30·D35·D36·D37 | 셀 2개 |
| **B** | **C.18 — `CC` 가 θ 를 3점만 본다.** `D65`·`D67`·`D28` 은 각각 `CC!C29`·`C25`·`C31` **한 칸을 그대로 읽는다** | D65·D67·D28·D40 | `CC` 확장 |
| **C** | **검증 블록의 입력 `D110 = 50 Hz`.** 설계 블록(`D7`·`D68`·`D73`·`D83`)은 47 Hz 다 — 47 로 설계하고 50 으로 검증한다 | D125·D126·D128·D129 | **입력 1개** |
| **D** | **홀드업 시작 전압이 공칭 `D14` = 25 V.** 실제 출력은 리플로 24.41–25.59 V 를 오간다 | D22·D33 | 수식 2개 |
| **E** | **`RTC!C9` 가 264 V(AC 최대)를 가리킨다.** 탱크가 보는 최대 등가입력은 `Design Spec!D81` = 332.34 V 이고 **그 셀은 아무도 읽지 않는다**(읽는 곳 0) | D19 | 링크 1개 |
| **F** | **정의가 다르고 양쪽 다 맞다.** 목표 25 V vs 분압기 실현 25.18 V · `CEILING` 적용 여부 · `R_CFG` 는 90 V 절대상한 vs 85 V 설계값 | D23·D42·D108 | 없음 |

> **B 는 "구조적으로 불가능" 이 아니다** — 앞서 그렇게 기록했으나 틀렸다. `RTC` 는 이미 θ 를
> **6점**(15°·30°·45°·60°·75°·90°) 푼다. `CC` 에 3열을 더하고 θ=0 끝점(전류가 0)을 넣어
> 사다리꼴로 적분하면 181점 스윕 대비 **오차 0.35 %** 다. 단순평균은 θ≈0 구간을 빼먹어
> +9.7 % 가 되므로 반드시 끝점을 넣어야 한다.

#### 이 표가 필요했던 이유 — 내가 반복해서 틀린 방식

이 세션에서 나는 차이를 **숫자만 보고** 진단했고 그래서 네 번 틀렸다:

1. `RTC` 를 "설계가 안 쓰는 참고용" 이라고 했다 → 실제로는 `CC` 9수식 + `Device Setting` D2 가 읽는다
2. 개당 리플전류 0.19 A 를 **정격**이라고 했다 → `I.Cout_rms/n.C` 로 계산된 **스트레스**다
3. B 와 E 를 "셀 수정으로는 불가능" 이라고 했다 → 둘 다 가능하다 (위 참조)
4. `ΔV.loop` 를 `CC!D65`("@2fL" 참고 행)에 매핑해 **없는 결함을 만들어냈다** → 설계가 쓰는 것은
   `D68`("@2fLmin") 이고 그 값 1.18430 은 시트와 정확히 같다

**교훈: 두 수식을 나란히 놓고 같은 양인지 먼저 확인할 것.** 숫자 차이는 진단이 아니다.

#### 2026-08-23 워크북에 적용 — A · C · D (`fix_xl_causes.py`)

셀 6개. 전부 **워크북이 이미 갖고 있는 값으로 링크를 바꾼 것**이고 새로 계산한 값은 없다.
순환 참조가 없음을 의존 그래프로 먼저 확인했다.

| 원인 | 셀 | 바꾼 것 | 예상 (재계산 후) |
| :-- | :--- | :--- | :--- |
| A | `Res.Tank D28` | 근사식 [36] → `RTC!$BB$149` | f_n 0.82111 → **0.84006** |
| A | `Res.Tank D29` | Q 두 곳을 `D26`(0.8466) → `RTC!$C$41`(0.7717) | 위상 0.23476 → **0.36621 rad** |
| A | `Res.Tank D30` | 분모를 `Design Spec!D102`(목표 150 kHz) → `D55`(실제 151.748) | T_ZC 303 → **457.2 ns** |
| C | `CC D110` | 50 → **47 Hz** | 3차 고조파 3.86 → **4.31 %** 외 4행 |
| D | `Power D22` | `D14^2` → `(D14−D31/2)^2` | 요구 용량 59.77 → **67.22 mF** |
| D | `Power D33` | 같음 | 홀드업 15.10 → **13.42 ms** |

> **예상값은 SMath 와 소수점까지 같다.** 다만 오늘 "재계산하면 일치한다" 는 예측이 4건 틀린 적이
> 있으므로, **사용자가 Excel 에서 열었다 저장한 뒤 `xl_sm_compare.py` 로 확인해야 확정이다.**

`CHANGELOG_rev0_6` 시트에 3행(76–78)으로 기록했다 — 워크북만 받은 사람도 이유를 알 수 있다.

#### 2026-08-23 워크북에 적용 — B, C.18 을 닫았다 (`fix_xl_cc.py`)

**"스프레드시트로는 적분을 못 한다" 는 판단이 틀렸다.** `RTC` 는 이미 θ 를 **6점**
(π/12 · π/6 · π/4 · π/3 · 5π/12 · π/2, 15° 간격) 푼다. `CC` 가 그중 3점만 읽고 있었을 뿐이다.

| 무엇 | 어디 |
| :--- | :--- |
| θ 열 3개 추가 | `CC` 열 **F**(5π/12) · **G**(π/6) · **H**(π/12), f_sw 를 `RTC!BC150 · BC153 · BC154` 에서 |
| 노드 전류의 제곱 | `CC` **행 32** — 적분에 필요한 양 |
| Simpson 적분 | `CC` **열 J** — 가중치 1,4,2,4,2,4,1 ÷ 18. **θ=0 표본은 정확히 0**(부하가 sin²θ) |
| 설계 연결 | `Res.Tank D65 → CC!J29` · `D67 → CC!J25` · `Power D28 → CC!J31` |

**결과 (721점 스윕 대비):**

```
2차 실효 (권선당)   24.14 -> 28.299 A   오차 −28 %  ->  −0.04 %
뱅크 리플전류       21.77 -> 30.167 A   오차 −28 %  ->  −0.08 %     <- 커패시터 160개를 정하는 값
1차 실효           10.30 -> 10.701 A   오차 −11 %  ->  −1.83 %
뱅크 손실          0.0839 -> 0.1610 W  (SMath 0.161)
```

**1차 전류만 잔차가 남는다.** 자화 전류는 θ=0 에서도 사라지지 않으므로 그 끝점이 0 이 아니다.
그런데 끝점을 f_o 기준으로 외삽하면 **+4.7 % 로 오히려 나빠진다** — θ→0 은 실제로 버스트
영역이라 도달하지 않기 때문이다. 0 이 최선이다. 사다리꼴과 Simpson 을 둘 다 재보고
Simpson 을 택했다 (1차 −1.83 % vs −2.70 %, 나머지는 동일).

> **`CC!C31` 의 `C15^2/2` 항은 그대로 두었다.** `C15 = C14/C5 = 0` 이라 값에 영향이 없지만,
> 그 항이 무엇을 뜻하는지 알아내지 못했다. 새 열에는 `$C$15` 로 고정해 열 C 와 같게 했다.

#### 2026-08-23 워크북 정리 완료 — 대조 134/134, 차이 0

원인 A–F 를 전부 닫았다. 마지막 상태:

```
xl_sm_compare   134항목 — 일치 134 · 의도된 차이 0 · 불일치 0
rtc_check       18점   — 최대 편차 0.0000 %
bom_compare     29항목 — 차이 0
```

| 원인 | 무엇을 했나 | 결과 |
| :-- | :--- | :--- |
| A | `D28`→`RTC!$BB$149`, `D29` 의 Q→`RTC!$C$41`·λ→`D50`, `D30` 분모→`D55` | T_ZC 303 → **457.2 ns** |
| B | `CC` 에 θ 3열(F·G·H) + 행 32 + Simpson 적분(열 J), 설계를 `CC!J..` 로 | 뱅크 리플 21.77 → **30.17 A** |
| B 잔차 | `I28`·`I29` 에 θ=0 끝점(자화 전류), `J29` 에 가중치 1 항 | 1차 실효 −1.83 → **−0.02 %** |
| C | `CC!D110` 50 → **47 Hz** | 3차 고조파 3.86 → 4.31 % |
| D | `Power D22`·`D33` 의 `D14^2` → `(D14−D31/2)^2` | 홀드업 15.10 → **13.42 ms** |
| E | **Cardano 닫힌형 18블록**(RTC 220–234) + `BB149..BB202` 연결, `fn.max` 클램프, `RTC` 행 9 → `MAX(D79,D81)` | f_sw,max 189.3 → **249.6 kHz** · C_T 상한 839 → **699.6 pF** |
| F | 대조 짝 3건이 잘못돼 있던 것 — 워크북은 이미 `D38`(25.177 V)·`D109`(84.85 V)·`D42`(5)를 맞게 갖고 있었다 | 전부 일치 |

**`E` 는 "고칠 수 없다" 고 두 번 기록했으나 둘 다 틀렸다.** 격자 탐색을 닫힌형으로 바꾸니
범위 문제가 사라졌고, 보간 잔차 0.2 % 까지 없어졌다.

#### 남은 흠 — `RTC` 행 26·27 의 `#NUM!` 164칸

`Vin_max` 가 FB 코너로 옮겨가면서 `fn.max = √(λ/(1+λ−1/M))` 이 82칸에서 음수의 제곱근이
됐다. `CG26` 만 3항 MAX 로 클램프해 두었고 나머지는 옛 수식 그대로다.

**기능적 영향은 없다** (2026-08-23 확인):

```
행 26·27 을 그리는 차트        없음
f_n 축(행 25)의 오류           0개   <- 14개 차트가 쓰는 행, 멀쩡
행 26·27 을 읽는 곳            CG25 하나 + 행 27 자기 자신뿐
```

격자 열마다 같은 값을 82번 반복 계산하는 칸들이고, 실제로 쓰이는 것은 `CG26` 하나다.

**공유 수식 마스터를 고치려다 파일이 깨졌다.** `<f t="shared" ref=... si=...>` 의 텍스트만
바꾸는 것은 XML 문법으로는 맞지만 Excel 이 거부한다 — 열 때 "내용에 문제가 있습니다" 가
뜨고 `sheet9.xml` 의 수식과 공유 수식을 통째로 제거하려 한다. `xlsx_patch.validate()` 도,
급히 만든 괄호·셀타입 검사도 이것을 잡지 못했다. **`BACKUP_5` 로 되돌려 복구했다.**

> **교훈: 공유 수식 마스터는 텍스트만 바꾸지 말 것.** 고쳐야 하면 공유를 풀어 각 칸을
> 독립 수식으로 쓰거나, 손대지 말 것. C.12 가 196셀에 성공한 것은 **항을 삽입**한 것이지
> 수식 구조를 바꾼 것이 아니었다.

#### E 는 적용하지 않았다 (철회 — 아래 참조) — 링크를 바꾸면 워크북이 깨진다

`RTC!C9` 를 `Design Spec!D81`(332.34 V)로 돌리면:

```
M_req = 2·n·V_o / (√2 · 332.34) = 0.6383
탱크의 무부하 게인 하한 = 1/(1+λ) = 0.6452        ->  M_req 가 그보다 낮다
RTC 행 26  fn.max = √(λ/(1+λ−1/M)) = √(0.55/−0.0167)  ->  #NUM!
RTC CG25  = 1.5·CG26                              ->  f_n 격자 전체가 죽는다
                                                  ->  RTC · 차트 23장 · CC · 설계
```

**에러가 아니라 물리다.** FB 코너에서 경부하면 어떤 주파수로도 게인을 그 아래로 못 낮추므로
버스트로 가야 하고, 격자는 그런 점을 표현할 수 없다. 고치려면 격자 범위를 `fn.max` 에서
떼어내는 설계 변경이 필요하다. **rev 0.7 의 과제로 남긴다.**

### 3.5 설계 가이드 자체의 오류

- **[70a]는 풀브리지 전용이다 — CT 설계에서 2.8배 과소평가.**
  [70a]의 첫 형태 `√(⟨I_sec,rms²⟩ − I_out²)`에 쓰는 `I_sec,rms`는 **출력 노드로 들어가는
  전류의 실효값**이어야 한다. 그런데 [64]는 N_rect=1(CT)에서 `I_sec,pk√(d/4)`, 즉 **권선
  하나의 실효값**을 준다. CT에서는 두 권선이 번갈아 도통하므로 노드 전류 실효값은
  `I_sec,pk√(d/2)`다.

  | 무엇을 넣느냐 | I_Cout,rms |
  | :--- | ---: |
  | [64]의 CT 값 28.3 A (권선당) | 10.8 A ← **틀림** |
  | 노드 실효값 40.0 A | **30.2 A** ← 맞음 |
  | [70]+2f_l 교차 검증 | 30.2 A ✓ |

  **2.79배.** 240 W 예제는 N_rect=2라 이 결함이 드러나지 않았다. **우리는 CT다.**
  [70]과 [70a]의 두 번째 형태는 원래 맞으므로 그쪽을 정본으로 삼는다.
- [125] [126] [130] [137]의 `G_o`·`EA_o`는 Hz가 아니라 **rad/s**
- [134] [135]는 **선정값** 기준

### 3.6 엑셀과 SMath의 **의도된** 차이 4건

같은 사양을 넣어도 다르게 나오는 곳이다. 전부 SMath가 보수적이다.

| 항목 | 엑셀 | SMath | 사유 |
| :--- | ---: | ---: | :--- |
| 홀드업 시작 전압 | V_out에서 시작 | **V_out − ΔV_pp/2** | 리플 골(θ=45°)에서 AC가 끊기는 것이 최악. 툴이 **13.2 % 낙관** |
| I_Cout,rms | 21.8 A | **30.2 A** | **C.18** — `CC!C31`의 수식은 옳다(노드 실효값을 제곱한다). 그 열이 θ=π/4 한 점인 것이 원인 |
| f_sw 최대 | 189.3 kHz | **249.6 kHz** | 툴은 264 Vac를, SMath는 FB morphing 경계(등가 332.3 Vac)를 본다 |
| ZVS T_ZC | 303 ns 한 점 | **457 ns 전 영역 최악** | 툴은 V_in,min·전부하 한 점, SMath는 [38a] 전 영역 스윕 |

추가로 C.12(f_px 누락, Φ_M 약 1° 낙관), C.13(f_l vs f_l,min), C.18(θ=π/4 근사)도 SMath 쪽이 맞다.

**전체 대조는 `xl_sm_compare.py` 가 자동으로 한다** — 사양·전력예산·탱크·전류·출력뱅크·
IC 정수·루프·보상망에 걸쳐 **101 항목**. 2026-08-23 기준 **일치 79 · 의도된 차이 22 ·
불일치 0**. 의도된 차이는 전부 행마다 사유가 붙어 있으므로, 새 세션이 같은 22건을 다시
조사할 필요가 없다.

---

## 4. T_idle — 증거는 250 ns 쪽이지만 **확정 아님**

DS Table 5의 `fosc` 행 **8개 값 전부**(RT/CT 4조합 × min/max 2열)를 역산했다.

| RT / CT | min 열에서 역산 | **max 열에서 역산** |
| :--- | ---: | ---: |
| 10 k / 330 p | 251.1 ns | **250.0 ns** |
| 20 k / 330 p | 249.3 ns | **249.9 ns** |
| 10 k / 560 p | 248.0 ns | **250.2 ns** |
| 20 k / 560 p | 241.6 ns | **250.1 ns** |

max 열은 `I_EA,max` 경로가 추가로 들어가는 **다른 식**인데도 4개가 **250.0 ns에 0.2 ns 이내로
일치**한다. 우연일 수 없다.

**판단:** 실리콘 값은 250 ns일 가능성이 높고, 시트 §5.3.2의 700 ns는 한 상수를 두 자리에 잘못 쓴
것으로 보인다. **확정은 실측으로만 한다.** 그때까지 마진 확인은 700 ns(가장 보수적)로,
성능 예측은 250 ns로 병기한다.

**왜 중요한가** — f_o 90.4 kHz, f_sw,max,op 249.6 kHz, C_T = 470 pF 기준:

| T_idle | R_T = 13 kΩ → f_Min / f_Max | R_T = 11 kΩ → f_Min / f_Max | f_Max 여유 |
| :--- | :--- | :--- | ---: |
| 250 ns | 78.6 k / 309.0 k | 92.3 k / **319.6 k** | **+28 %** |
| 350 ns | 77.4 k / 291.1 k | 90.6 k / 300.4 k | +20 % |
| **700 ns** | 73.4 k / 241.8 k | 85.2 k / **248.2 k** | **−0.6 %** |

- **T_idle = 700 ns라면 FB morphing 경계 전부하 운전이 VCO 상한에 걸린다.**
  회피책: f_r을 150 → 130 kHz 부근으로 내려 주파수 범위 전체를 스케일 다운
- R_T = 13 kΩ에서는 f_Min이 f_o보다 **낮아** 용량성 영역 진입을 클램프로 못 막고 ACP 보호에만
  의존하게 된다. **R_T = 11 kΩ**이면 f_Min/f_o = 1.02–1.04로 클램프가 f_o 위에 놓인다

---

## 5. 하드웨어 분석 기록

### 5.1 ST 평가보드 EVL6790_670W

**ST 자신도 OLED 레일을 단일단 LLC로 직접 만들지 않는다.** 마더보드 회로도의 부하 주석:
48 V 중간 버스를 만든 뒤 후단 벅 3개 —

- BUCK1 **24 V / 21 A = 504 W → OLED CNTR**
- BUCK2 20 V / 3 A = 60 W → SOUND CNTR
- BUCK3 12 V / 8 A = 96 W → T-con CNTR

우리 사양(25 V 직출력)은 그 후단 벅을 없애는 구성이며, 그 대가가 75 mF 출력 뱅크다.
**이것이 이 프로젝트의 가장 큰 아키텍처 판단이다.**

**도터보드 실장값** (`DAUGHTER BOARD_L6790_REV1`):
R4=13 k(RT) · C4=470 pF 1 %(CT) · R5=24 k(BM) · R6=31 k(CFG) · R2=150 k / R3=23 k(ZCD) ·
**R7=100 k N.M.**(LOUT2 풀다운 미실장 → morphing) · C2=100 nF(VCC).
주석 BMi=55 W · VBO=85 V · OVP1=52 V · OVP2=57 V. 가이드 부록 A와 전부 일치.

2차: TEA2096 + ISC079N15NM6(150 V, 7.9 mΩ) × 4 = **2채널 × 2병렬 센터탭 SR**.
입력: TEA2209T + ST8L65N044M9(44 mΩ) × 4 동기 브리지.
1차: STDRIVEG600 + ISG6123TA(GaN, TOLL) × 2.

> **미확정:** EVL 보드의 n = 3.2가 n인지 n_T인지. 마더보드 BOM에 별도 공진 인덕터가 보이지
> 않아 **누설 통합형(→ 3.2가 n_T)일 가능성**이 있으나, 그 경우 반사 전압이 125 V로 낮아져
> 설계 등가 입력이 177 Vac가 되어 가이드 부록 A의 217 Vac 해석과 충돌한다.
> **자료만으로 확정 불가 — 트랜스포머 실측(개방/단락 인덕턴스 + 턴수)으로 판정할 것.**

### 5.2 LGE 기존 2단 양산 보드

회로도의 벡터 경로에서 배선을 직접 추적하여 확정한 것.

| 항목 | 내용 |
| :--- | :--- |
| 구성 | UCC256403 하프브리지 LLC (Q151/Q152 = TK190U65Z 650 V) + TEA2095 센터탭 SR (TPH3R70APL1 × 4) |
| 출력 | **25 V (25VD)** — 우리 사양과 동일 |
| 트랜스포머 | **26OP-LM83W × 3개, 1차 직렬** (핀2→핀1 배선). **2차 결선은 직렬이다** — 시트 §5.2a |
| 권선 | N_p = 5 T (리츠 0.04 × 360), N_s = 1 + 1 T (**동판 0.5 × 4.5 mm**) → 합계 N_p = 15 |
| 인덕턴스 | 개방 53 µH · 단락 14 µH (개당) → 직렬 합계 159 / 42 µH → **λ = 0.359 (m = 3.79)** |
| 코어 | EE6405 (64 × 33.5 × 8 mm 편평형), **A_e = 113.1 mm²**, l_e = 57.13 mm, V_e = 6461 mm³, GP98 |
| 공진 커패시터 | 0.01 µF/630 V 2직렬 × 5병렬(1열 미실장) = **25 nF** |
| 제조 | KH필룩스, P/N EBJ30067801 |

**이 프로젝트에 직접 쓰이는 결론 3가지:**

1. **자기부품 구조를 그대로 가져올 수 있다.** 25 V/26 A에서 2차를 1턴 동판으로 감고 여러 개를
   병렬하는 것이 검증된 해법이다. 1차 직렬이므로 전류 분배가 자동으로 맞는다.
2. **기존 설계의 λ = 0.359는 이미 단일단 영역에 가깝다.** 통상 2단 LLC는 λ = 0.1–0.2인데 이
   보드는 m = 3.79다. **같은 코어·보빈 플랫폼에서 갭만 키우면 단일단이 요구하는 λ ≈ 0.55에
   도달**한다.
3. **N_s만 1 → 2턴으로 바꾸면 권선비가 정확히 맞는다.** N_p = 5 × 3직렬 = 15, N_s = 2 →
   n_T = 7.5 → n = 6.04 (필요값 6.046).

> **위 3가지는 그대로 유효하다.** 아래 시트 §5.2a 의 정정은 *기존 보드의 동작점*에 대한 것이고,
> 우리가 물려받는 것은 *기계 플랫폼*이므로 영향받지 않는다.

### 5.2a 2026-08-28 정정 — 버스는 400 V 이고, 2차는 병렬이 아니다

**사용자가 "버스가 400 V 아니냐"고 물어서 사양서 원본으로 되돌아갔고, 두 가지가 틀려 있었다.**

**① 버스 전압.** 나는 "보드가 M ≈ 1 에서 돈다"고 가정하고 권선비에서 640 V 를 역산했다.
**틀린 것은 버스가 아니라 그 가정이다** — 1차 스위치가 **TK190U65Z(650 V)** 이므로 640 V
버스는 성립할 수 없다. 유니버설 입력 부스트 PFC 의 출력은 **400 V** 다. 이 근거(소자 내압)는
처음부터 내 손에 있었고 쓰지 않았다.

**② 2차 결선.** "핀4+5 CT 공통 버스" 를 3개 병렬의 증거로 읽었는데, 승인원 6쪽 권선표는

| No. | 단자 | 전선 | 턴수 |
| :-- | :--- | :--- | :--- |
| NP1 | 1 → 2 | MIW-F(LZ) 0.04Φ × 360 (리츠) | 5 Ts |
| NS2 | 3 → 5 | Cu plate 0.5T × 4.5W | 1 Ts |
| NS3 | 4 → 6 | Cu plate 0.5T × 4.5W | 1 Ts |

로, **한 유닛 안에 1턴 2차가 두 개** 있다. 핀 4·5 를 잇는 것은 그 유닛 자신의 센터탭일
뿐이고 유닛 간 병렬의 증거가 아니다.

**포화가 결선을 정한다.** 1차 직렬이므로 유닛 하나가 받는 전압은 `n_T·V_o,eff/3` 이고,
공진 아래에서 도통 구간은 `T_r/2` 로 고정된다:

| 2차 결선 | n_T | n | **B_pk** | 400 V 에서 요구 M |
| :--- | ---: | ---: | ---: | ---: |
| 병렬 (N_s,eff = 1) | 15.0 | 12.87 | **356 mT** | 1.608 |
| **직렬 (N_s,eff = 3)** | 5.0 | 4.29 | **119 mT** | 0.536 |

페라이트는 100 °C 에서 약 390 mT 에 포화하므로 356 mT 는 여유가 9 % 뿐이고, 더 결정적으로 **코어손이 대략 B^2.5 로 가므로 356 대 119 mT 는 손실이 약 16배**다. 평면 TV 트랜스포머에서 성립할 수 없다. 따라서 2차는
직렬이고, 3개는 사실상 **하나의 15 : 3 트랜스포머**로 동작한다 — 평면 TV 높이 때문에 코어를
셋으로 쪼갠 것이다.

**아직 안 닫힌다.** n_T = 5 로 보면 400 V 에서 요구 게인이 **0.536** 인데, 이 탱크의
Q→0 최저 게인이 `1/(1+λ)` = 0.736 이라 상당한 Q 와 f_sw ≫ f_r 이 필요하다. 그런데
Z_0 = 41 Ω(C_r = 25 nF 가정) 에 25 V 출력이면 **부하 Q 가 1 을 넘는다**(600 W 에서 2.6).
정상적인 LLC 설계 영역이 아니다. **내가 회로도에서 읽은 C_r = 25 nF 가 틀렸거나, 그 레일의
전력이 내 짐작보다 훨씬 작거나 둘 중 하나다.** 더 추측하지 않는다 —
**PFC단 도면 또는 공진 커패시터 실측이 있으면 닫힌다.**

> **▷ 이것이 우리 설계를 막지 않는다.** 우리는 기존 보드의 동작점을 물려받지 않는다.
> 물려받는 것은 코어(EE6405, A_e 113.1 mm²) · 보빈 · 3분할 구조 · 리츠 1차 · 동판 2차 ·
> 벤더 · 절연 시스템이고, 우리 권선비는 우리 요구에서 나온다.

### 5.3 참고 논문이 가이드 §2.2를 독립 증명한다

ECCE (Wenbo Liu et al., 1.65 kW 단일단 LLC OBC) 식 (14): `sin²θ ≤ sinθ`이므로
**G_req(θ) = G_req,pk/sinθ ≤ G_req,pk/sin²θ = M_fr2(θ)**. 즉 라인 피크·전부하에서 게인을
만족시키면 **모든 위상에서 자동으로 만족**한다. 가이드 §2.2 "두 발산의 상쇄"와 같은 결론.

논문의 λ = L_r/L_m = 8.5/21 = **0.405**로, "λ ≈ 0.5가 단일단의 지문"을 외부 설계가 독립적으로
뒷받침한다. Infineon AN 2013-03도 "m > 5는 게인 부족"이라 적는다.

---

## 6. 사고 기록 — 같은 실수를 반복하지 않기 위해

### 6.1 xlsx가 열리면서 "복구했습니다" 대화상자를 띄웠다 (2026-08-21)

Excel이 *"제거된 레코드: /xl/worksheets/sheet8.xml 부분의 셀 정보"* 로 복구했다.
**셀을 "추가"하다가 깨뜨렸다. 원인 3가지 — 전부 규칙 위반이다:**

1. **한 `<row>` 안에 같은 `r=` 셀이 두 번 있으면 안 된다.** `G18`이 이미
   `=IF(E18,"","Power Components")`로 존재하는데 뒤에 `G18`을 또 붙였다.
2. **`<row>` 안의 셀은 열 번호 오름차순이어야 한다.** 새 `H` 셀을 행 끝(= `I`, `W` 뒤)에 붙였다.
3. **수식을 추가/삭제하면 `xl/calcChain.xml`이 낡는다.**

**그래서 만든 것: `Design Guide/generators/xlsx_patch.py`의 `set_cell()`** — 있으면 교체, 없으면 올바른 위치에 삽입하고 `spans`를 다시
계산한다. 그리고 **쓰기 전에 전 시트를 훑어 "행 안의 셀이 중복 없이 오름차순인가"를 검사**한다.
이 검사를 통과 못 하면 설치하지 않는다.

**부수 교훈:** `BOM&Schematics`의 **G열은 비어 있지 않다.** 모든 행이
`=IF(E.., "", "<출처 시트>")` 힌트를 갖고 있으므로 남의 열을 뺏지 말 것.
그리고 **H·I열은 "Power Part Schematics" 그림이 I열에 앵커되어 있어 그림 밑에 깔린다.**
결국 커패시터 뱅크는 표 안쪽(C~F)에 **행으로** 넣었다 — 빈 행 22를 써서 행 삽입 없이.

### 6.2 PDF에서 글자가 겹치는데 검사기는 "겹침 0"이라고 했다

검사기가 **선언된 크기**만 봤기 때문이다. 세 단계로 고쳤다.

1. **리전 높이를 "슬래시 개수"로 추정하고 있었다** (`28 + 20*src.count('/')`). SMath는 수식을
   2차원 트리로 배치하므로 분수는 쌓이고 근호는 막대 한 줄, 지수는 위첨자 한 줄이 붙는다.
   → `smsheet.py`의 `_rh()`/`_rwid()`가 트리를 재귀로 재고 `est_box()`가 픽셀을 돌려준다.
2. **텍스트 줄바꿈을 SMath에 맡기고 있었다.** TextRegion은 폭에 안 맞는 `<p>`를 **말없이 다시
   줄바꿈**하고, 늘어난 줄이 선언 높이 밖으로 나가 다음 리전 위에 찍힌다.
   → `_wrap()`이 미리 줄을 나눈다. 관건은 글자폭 상수: PDF 실측 **폭 1100 px · fontSize 9에
   약 208자** → 0.58 × 폭/글꼴크기. `CH_PER_PX = 0.67`로 잡아 15 % 여유를 뒀다.
   `check_sm.py`가 0.60 기준으로 모든 `<p>`를 다시 재어 "재줄바꿈이 일어날 것"을 미리 잡는다.
   **회귀 시험으로 확인함** — 0.52로 되돌려 빌드하면 정확히 겹쳤던 문단들을 집어낸다.
3. **텍스트 리전 높이를 추정하고 있었다.** SMath가 직접 저장한 워크시트에서 실측:

   | fontSize | 1줄 | 2줄 | 3줄 | 4줄 |
   | ---: | ---: | ---: | ---: | ---: |
   | 8 | 20 | 47 | | |
   | 9 | 21 | 49 | 77 | 105 |
   | 10 | 23 | | | |
   | 12 | 26 | | | |
   | 15 | 30 | | | |

   → **첫 줄 ≈ 1.4·size + 8.7, 이후 한 줄당 ≈ size + 19.** 종전 `n·(size+4)+6`은
   fontSize 9·3줄에서 **45 px vs 실제 77 px** — 시트 §18 표가 겹친 원인이 정확히 이것이다.

**결정적 사실:** SMath는 저장할 때 텍스트 리전을 **내용 크기로 다시 맞춘다.** 그래서 두 줄짜리
라벨은 선언 높이보다 커지고 그대로 다음 행 위에 얹힌다. → **좌측 라벨은 무조건 한 줄**로 만들고,
두 줄이 되려 하면 **빌드를 실패시킨다**(`_label(nowrap=True)`). 추정으로 버티지 말고 실패 모드를
없애는 쪽이 맞다.

### 6.3 보드선도의 위상 곡선이 통째로 안 보였다

**그래프에는 로그 축이 없을 뿐 아니라 자동 스케일도 없다.** `<plot>`의 `scale_*`/`transpose_*`가
창을 고정하는데, **그 창 밖의 데이터는 빈 그래프로 보인다.** 위상 곡선(−130 ~ −222°)이
−64부터 시작하는 창 밖에 있었다. 캘리브레이션 결과:

- **격자 한 칸 = 20 px, `scale_*` = 칸당 단위수** → `px/단위 = 20/scale`
- **`transpose_*` = −(창 중심 × px/단위)** [px]
- `smsheet.py`의 `Sheet.plot_view(w, h, x0, x1, y0, y1)`가 이 계산을 해 준다

부수 조치: 위상 대신 **위상 여유(180 + arg T)**를 그린다. 0–50° 안에 들어와 바로 읽힌다.

### 6.4 SMath 파일이 아예 안 열렸다 (NullReferenceException)

*"개체 참조가 개체의 인스턴스로 설정되지 않았습니다"* — 원인 2가지:

1. **리전은 `<regions type="content">` 안에 넣어야 한다.** `<worksheet>` 바로 아래에 두면 실패.
2. **`<dependencies>`에 사용한 리전 타입의 어셈블리를 전부 선언해야 한다** —
   `SMath Core` 외에 `MathRegion`, `TextRegion`, `PictureRegion`, `SpecialFunctions`,
   그래프를 쓰면 `PlotRegion`, 접기를 쓰면 `AreaRegion`.

정확한 GUID는 `Reference/EVLHV101SSR50W Design Guide.sm`에서 복사한다.

### 6.5 BOM 대조 스크립트가 24항목 불일치를 냈다 (2026-08-21)

**틀린 쪽은 스크립트였다.** SMath의 `<result>`는 이미 **표시 단위(contract)** 값인데 SI로
착각해 한 번 더 나눴다. 설계값은 처음부터 같았다.
→ `bom_compare.py`가 `<contract>`를 읽어 환산하도록 고쳤다. 교훈: **대조 도구 자신도 검증
대상이다.** 전부 다르게 나오면 도구를 먼저 의심할 것.

---

### 6.6 `<result>` 도 RPN 이다 — 첫 토큰만 읽으면 부호와 지수가 사라진다

**2026-08-24.** `snapshot.py` · `xl_sm_compare.py` · `bom_compare.py` · `audit_rpn.py`
넷이 전부 `float(list(res)[0].text)` 로 결과를 읽고 있었다. **넷이 같은 가정을 독립적으로
했으므로 아무도 서로를 잡아주지 못했다.**

`<result>` 는 값 하나가 아니라 **RPN 식**이다:

```
<e>22.26</e><e args="1">-</e>                        -> -22.26   (22.26 로 읽힘)
<e>3.87</e><e>10</e><e>15</e><e args="1">-</e>
   <e args="2">^</e><e args="2">*</e><e args="1">-</e>  -> -3.87e-15  (3.87 로 읽힘)
<e style="unit">nF</e><e>92.21</e>                   -> 92.21 (단위가 먼저, 곱셈 생략)
```

**발견 경위.** `n.SR` 을 1 로 바꾸고 사용자가 엑셀을 재계산한 뒤 `xl_sm_compare` 가
`Power Components!D37`(필요 세라믹 용량)에서 **엑셀 0 · SMath 3.87 µF** 불일치를 냈다.
파고 보니 **양쪽 다 맞았다** — `C.cer_min = MAX(C.cer_raw, 0)` 이고 `C.cer_raw` 는
**−64.14 µF**(ESR 만으로 이미 30 mV 예산을 지키므로 세라믹이 불필요)라 시트는
−3.87e-15 µF, 즉 0 으로 클램프하고 있었다. **틀린 것은 내 리더였다.**

전체 결과 525개 중 **토큰이 2개 이상인 것이 53개**다 — 부호가 날아가던 것 16개
(Cardano 중간값 `a2.*` `p.*` `r.b`, 보드선도 스칼라 `Ta.4` `Ta.5` `Ma.5`, `C.cer_raw`),
지수까지 날아가던 것 1개(`C.cer_min`), 나머지는 단위 토큰이라 수치에는 무해했다.

`smresult.py` 로 평가기를 하나만 두고 넷 다 거기에 연결했다.

> **▷ 고치다 두 번 더 틀렸다.** ① `audit_rpn` 에서 `val` 을 재사용해 **독립 평가값을
> 덮어썼다** — 인쇄값끼리 비교하게 되어 81건이 거짓 불일치로 떴다. ② 단위가 앞에 오고
> 곱셈이 생략된 형태를 스택 잔여 2개로 보고 `None` 을 냈다. **둘 다 도구가 즉시 소리를 내서
> 잡혔다** — 검사기가 있다는 것의 값어치가 이것이다.
>
> **▷ 비교기에 절대 허용치도 넣었다.** 정답이 0 일 때 상대오차는 발산한다.
> `1e-15` SI 미만이면 같은 값으로 본다 — 두 문서에서 가장 작은 실제 물리량이
> 100 pF 기생용량(1e-10 F)이므로 안전하다.

## 7. 구 산출물과 그 제작 규약

### 7.1 rev 1.1 시대의 산출물

| 파일 | 상태 |
| :--- | :--- |
| `AN_L6790A_Design_Guide_rev2_0.md` + `figures/` | **현행 — rev 2.0 (2026-09-11).** 예제는 9:1 정본 설계점. 240 W 는 부록 C.5·C.30·C.31 에만 남는다 |
| `Smath/L6790A_Section3_PowerStage_Tank_rev1_1.sm` | **2026-08-23 현재 폴더에 없다.** 구 시트(240 W 예제, 시트 §3.1–3.13). 진단 프로브는 현행 `L6790A_probe.sm`이 대체한다. `build_sm.py`가 있으나 옛 `fig/` 그림 세트가 없어 그대로는 재생성되지 않는다 |
| `L6790A_Section3_Calculation_Sheet_1page.sm` | **프로젝트 폴더에 없음.** A1 세로 1페이지 4단 판이었다 |
| `Training Material/…Training.pptx` | 영문 **61장**, 25 V 예제로 교체 완료. 한국어판이 남았다 |

### 7.2 PPT 제작 규약 (Phase 7에서 필요)

- 테마: navy `03234B` / yellow `FFD200` / magenta `E6007E` / cyan `3CB4E6`, 폰트 Arial
- ST 로고: `ST_Template__16-9_.potx` → `ppt/media/image1.png`
- 수식: matplotlib mathtext → PNG(26 pt, dpi 260), 슬라이드에는 **15 pt 크기로 자연 배치**
  (`inches = px × (15/26)/260`). 박스 폭에 늘려 채우면 30 pt가 되어 너무 커진다
- `\dfrac` · `\left\{` · `\begin{array}` 미지원. `\frac`과 단순 나열로
- 그림: pymupdf 200 dpi 래스터화 → 넉넉히 크롭 → **잉크 경계로 자동 트림**(`recrop2.py`).
  좌표로 직접 자르면 캡션이 잘린다
- pptxgenjs: `LAYOUT_16x9` = **10 × 5.625 in**. 색상에 `#` 금지
- **기하 검사기 필수**: python-pptx로 전 도형 좌표를 훑어 `y+h > 5.26 in` 위반을 자동 검출.
  눈으로 51장을 보는 것보다 훨씬 빠르고 정확하다
- 변수명: 유니코드 아래첨자가 있으면 진짜 아래첨자(Lᵣ Cᵣ fₒ Z₀ Vₒᵤₜ), 없으면 언더바 제거

### 7.3 구 워크북 수정 방식 (지금은 쓰지 않음)

rev 0.6까지는 `shutil.copy` → openpyxl 수정 + `Comment` → `recalc.py <file> 600` 방식이었다.
**차트 30개 때문에 지금은 XML 직접 편집만 쓴다** (`CLAUDE.md` 시트 §6.2).
정상 상태의 잔여 오류는 **54건**이며 전부 RTC 행 29의 의도적 `NA()` 차트 마스킹이다
(rev 1.1 기록의 "53건"은 하나 적게 센 것 — 2026-08-23 전수 확인).
CHANGELOG의 텍스트 셀은 `cell.data_type='s'`를 명시해야 `=`로 시작하는 문자열이 수식이 되지 않는다.

---

## 8. 타임라인 — 수정 이력

**설계값이 바뀌면 여기에 한 줄 적는다.** 무엇을 왜 바꿨고 그래서 무엇이 나빠지거나
좋아졌는지. 발견·사고·결정도 같이 담는다 — 이 프로젝트에서 시간순으로 남는 기록은
이 표 하나뿐이다.

`snapshot.py`를 돌리면 지난번 대비 움직인 값을 전부 찍어 주므로, 그것을 보고 여기에
요약해 적으면 된다. **스크립트가 이 파일에 직접 쓰지는 않는다** — 근거 문서를 기계가
건드리게 두지 않기 위해서다.

| 날짜 | 한 일 |
| :--- | :--- |
| 2026-09-22 | **(21차) 설계 예제를 단일 트랜스포머로 — ETD 49/25/16DG 채택** (사용자 확정 "진행해줘"). **시트 변형 `7p5to1_x1`** (`build_l6790_smath.VARIANTS`: `N.x` 1 · `N.p` 15 · `N.s` 2 · `n.aux` 1.5 → `N.aux` 3 · **`Ae` 211**, 이제 `A.e_mm` 은 변형 파라미터고 기본은 113.1) — `mismatches: 0`. `an_pdf.AN_VARIANT` 기본을 이것으로. **코어 판정은 `cores.py` 한 곳에서**: `CHOSEN` = ETD 49/25/16DG, `BOBBINS`(ETD 49 20핀 · PQ 40/40 12핀 기하와 배정), `winding()` 이 **포일을 `W_FOIL_MAX` 12 mm 초과 시 병렬 스트립으로 나누고 1차 층수를 반경 창 안에서 고른다**(전에는 단일 코어 입력에서 퇴화했다). 네 후보 결과 — PQ 40/40 분리 간격 −8.1 · PQ 50/50DG −19.7 · **ETD 49 +6.35(1차 3층 5+5+5, 반경 7.11/8.40)** · ETD 54 +3.34(2층). **핀 배정(20핀, 두 줄 × 10):** NP1 1+2 – 4+5 · NAUX 8 – 10(앞줄) · NS2 11+12 – 14+15 · NS3 16+17 – 19+20(뒷줄) · **탭 = 14·15·16·17 PCB 결합** · 빈 핀 3·6·7·9·13·18. **2차 단자는 핀 2개씩** — 권선 하나가 28 A rms 를 흘리는데 □0.8 핀 하나로는 부족하다(벤더가 핀 정격 확인 또는 러그). **ETD 49 도면에는 핀 번호도 핀 1 표시도 없다** — 한 줄 따라 세고 반대 줄로 돌아오는 관례를 가정으로 적었다(PQ 50/50DG 도면은 그렇게 찍혀 있다). **AN:** 7.4 `n.T` 줄 "15 : 2" 단독 · 7.8 도입(한 부품, 3유닛은 6장의 대안으로만) · `trafo-built` 표 3열 · 핀 문단·그림·표(`pins`)·**극성 문단**(3유닛 결선 문단 삭제) · **7.9 후보 4종 표**(A.e·B.pk·A.N·권선폭·반경·1차 층/축/반경·분리 간격·l.N·질량·판정) + 노트 2개(**창 깊이가 결정**, **누설은 벤더가 배치로 맞춘다** — N² 라 3배) · `legend42`·`winding`·사양표(코어 행 신설) 단일화. **그림:** `an_core_section` 이 `cores.CHOSEN` 을 그린다(ETD 49 단면 48.5 × 49.8, 3층 1차, 포일 12층, 판 크기·배율 S2 7.7·S3 2.4 재조정, 포일 패널은 NS2 6층만) · `an_xfmr_pins` 오른쪽 패널을 `BOBBINS[CHOSEN]['plan']` 으로 일반화(ETD 는 두 줄, PQ 는 두 열) · `an_xfmr_read` 라벨 "one unit/per unit" 을 `nser` 로 분기. `figcheck` 3장 tiny 만. **사양서:** `build_trans_spec` 이 `N.x` 로 보빈을 고른다(1 → `CHOSEN`, 아니면 PQ 40/40) — **한 번 이 분기 없이 돌려 3코어 사양서 셋에 ETD 핀이 들어갔다가 바로 잡았다**. 5종 재생성, `check_trans_spec` 불일치 0. **손실·온도는 실측 항목** — 러프 추정 동손 3.4 + 철손 3.7 ≈ 7 W(철손은 ETD 54 데이터시트의 N87 값을 환산, ±50 %). 영문 82쪽 · 식 49 · 그림 42 · 표 29, `an_check` 통과. 한국어판 미반영. |
| 2026-09-22 | **(22차) 7.8·7.9 를 설계만 남기고 정리** (사용자: "table 11 은 필요없는 내용 … 단면도만 봐서는 2차측을 어떻게 권선하라는건지 모르겠어 … 2, 3 strips 가 무슨 말이야 … enlarged 7x 는 뭔데 … 이유를 덧붙이고 덧붙이고 너무 복잡해"). **뺀 것:** 7.9 의 후보 코어 4종 비교표(구 Table 11)와 설명 상자 2개(창 깊이 · 누설 N² 배율) — 후보 비교의 근거는 이 표(21차 행)에만 남는다. 7.8 도입·핀 문단·극성 문단·자속/자화/포화 문단·`an_mmf` 캡션을 사실만 남기고 절반 이하로 줄였다. 절 이름 `Choosing the core, and building the winding on it` → **`The core and the winding`**. **그림 40 (`an_core_section`) 다시 그림:** 권선 색을 핀 그림(Figure 38)과 같게 — NP1 자홍 · NS2 초록 · NS3 하늘 — 단면·전개 패널에서 12층 포일이 아래 6층 NS2 · 위 6층 NS3 로 갈린다(전에는 전부 빨강 한 덩어리). 셋째 패널을 **"the secondary, layer by layer (not to scale)"** 로 교체 — 보빈 위로 NS2 turn 1·2, NS3 turn 1·2 를 층마다 그리고 권선별 괄호에 핀(11+12 – 14+15 / 16+17 – 19+20), "one turn = 3 foils 0.20 × 10.5 mm, wound together, connected in parallel", 절연 0.05 mm, 반경 3.0 mm. **배율 표기("enlarged 2.2×/7×")와 "strips" 라는 말을 전부 없앴다** — 포일은 "3 foils in parallel". 범례 번호 1~7 로 재배열(1 NP1 · 2 NS2 · 3 NS3 · 4 분리 · 5 마진 테이프 · 6 중앙다리 갭 · 7 창). `figcheck` tiny 만(쪽 배율 0.70, 기존). 영문 **81쪽 · 표 28**, `an_check` 통과. 한국어판 미반영. |
| 2026-09-22 | **(23차) 보상망을 수식과 함께 · 그림 40 확대 · 전수 낭독 검토** (사용자 3건: 그림 40 15 % 확대 · 전 문장/파형/그래프/회로도 재검토와 비원어민 영어 · Feedback compensation 이 계산 과정 없이 덩그러니 나온다, onsemi TND381 수준의 설명). **6장:** 6.26 을 절 8개로 — 루프 블록도 신설(`an_loop_blocks`, 보상기/플랜트 괄호) · 플랜트가 적분기인 이유와 `G.o` 식 · **목표와 어긋났을 때 표**(`loop-aims`: 교차·위상 여유·이득 여유·`G.EA(2f.l)` 각각 너무 낮으면/높으면 — TND381 의 76°/45°/Q 대응, CTR 2:1 산포) · 2f.l 리플이 3차 고조파가 되는 식(`D.3 = G.EA ΔV/(4 V.FB)`, 4 의 출처: 진폭 절반 × 변조 m/2) · TL431·옵토·FB 핀 회로의 동작 서술과 전달함수(`EA.o`·f.z·f.p·f.px), fast lane 없음, `R.B` 창의 뜻, CTR 두 값 · K-factor 배치와 부품 식 · **solver 없는 검사**(|T|·arg T 닫힌형, 교차 고정점 반복, 위상 여유) · 이득 여유 · 버스트 리플(`ΔR.BM` 식). 식 49 → 64. **7.13 을 13단계 대입 산수로**(분압 → 플랜트 → 2f.l 허용 이득 → K.v → EA.o → f.MB/f.p/f.z → 바이어스 R.P·R.B 창 → 부품 4개 → 실제 구현값 → 교차·위상 여유(atan 세 항) → 이득 여유 → 실제 3차 고조파 → 버스트 리플·R.BM). 시트 상수(V.Fo·R.FB·I.FB·CTR.m·C.opto·f.pHF·Φ.M·α.v·D.3rd·I.min·GM.t)는 **`an_pdf._builder_const()` 가 빌더의 `S.const` 줄에서 읽는다** — 손으로 다시 적지 않는다. `an_loop_bode` 에 f.180/이득 여유 표시. **그림 40:** figsize 를 1/1.15 로, mm/inch 배율을 1.15 로 — 같은 쪽 폭에서 코어·권선·글자가 15 % 커진다. `fig(shrink=False)` 신설(MINFIT 1.0) — 쪽에 안 맞으면 줄이지 않고 다음 쪽으로. **낭독 검토(89쪽 전부):** 수치·수식 오류 0 · 몽타주 15장 겹침 0 · 문장 6곳 평이하게(`Why resonance buys anything` → `What resonance is for` 등) · **표지 '그림 일부는 타사 AN 에서' 문장이 낡아 있었다**(9-20 이후 전부 자작) → 고침 · 'PF LLC' 명칭 1곳 · 3.3/2.16 의 절 참조를 리플 절로 · 기호표에 루프 기호 10줄 · 참고문헌에 TND381. mathtext 에 `\le`·`\text` 가 없어 `\leq`·`\mathrm` 으로. 영문 **90쪽 · 식 64 · 그림 43 · 표 28**, `an_check` 통과. 한국어판 미반영. |
| 2026-09-22 | **(24차) v1.3 — OP 앰프 등가 회로 · 예제 Bode · 한국어판 전면 이식 · 참고문헌 제목 복구** (사용자: TL431 보상 회로를 OP 앰프로 등가한 회로도, 위상 여유·gain margin·교차 주파수를 표기한 Phase/Gain 그래프 예시, 전문 재검토, 한글판 수정, 두 판 1.3). **6.29:** `an_comp_opamp` — TL431 을 비반전 입력이 V.R 에 묶인 OP 앰프로 놓고 R.I 입력 저항 + Z.f = C.Fo ∥ (R.F + C.F) 궤환 임피던스의 반전 증폭기, 캐소드 전류 = LED 전류(V.Z 에서 R.B 로 공급되므로 fast lane 없음), 옵토의 전류 게인 CTR, FB 핀 풀업 R.FB 와 C.opto + C.fx 의 극점 — 세 인수의 곱이 식 42 의 `G.EA(s)` 이고 부호는 음(부궤환). 그림 라벨은 `v.FB = -i.C · R.FB ∥ C` 로 부호를 적었다. **6.31:** `an_loop_example` — 자기 교차 f.c 단위의 2적분기 + Type II 루프(영점 f.c/K, 극점 K f.c, f.px)에 교차(|T| = 0 dB) · 위상 여유(교차에서 -180° 까지의 거리) · gain margin(f.180 에서 |T|) · 2f.l 의 점을 표시. 설계 수치가 없으므로 6장에 둘 수 있고, 7.13 이 같은 그림 위에 이 설계의 숫자를 얹는다(`an_loop_bode`). **참고문헌:** 19차(fa19176)에 `h1('References')` 가 지워져 참고문헌 10건이 기호표 아래 제목 없이 붙어 있었다 — 복구, 정오표가 10장. **한국어판:** `an_kr_body.py` 를 처음부터 다시 썼다(2725 → 3529행). 영문 `an_body.py` 를 절 단위로 따라가며 그림·표·수식 호출과 키를 같은 순서로 두고 글만 옮겼다. 새로 옮긴 절: 2.8 다이오드 역회복 · 2.12 플라이백 비교 · 6.13~6.21 트랜스포머(코어 두 면적·권선 배치·갭·창·표피 깊이·절연·손실) · 6.26~6.33 보상망 8절 · 7.4 9단계 · 7.8 핀 배정 · 7.9 코어와 권선 · 7.13 13단계 · 8장 기호표 · TND381. 2~6장의 설계 수치도 영문처럼 걷어냈다. **NanumGothic 에는 그리스 문자가 없다** — v1.2 한국어판은 맑은고딕이 있는 PC 에서 만들어 몰랐고, 이 컨테이너에서 첫 빌드에 λ·θ·π·Ω 가 전부 빠졌다(pymupdf 로 본문에 λ 가 있는지 확인해 잡았다). `an_pdf._wrap_missing()` — `T()` 가 본문 서체의 `charToGlyph` 에 없는 글자(이름 엔티티 포함)를 `<font name="AN">` 으로 감싸고, 제목과 표 머리는 `_boldalt()` 가 `AN-B` 로 바꾼다. `use_korean()` 이 Liberation Sans 를 `AN` 으로 같이 등록한다(맑은고딕 PC 에서는 등록만 되고 안 쓰인다). `EQWORD`/`EQAGAIN` 로 수식 표제어를 판마다 정한다(`식 N` · `식 N, 다시`). **검사:** 두 판 92쪽 · 식 64 · 그림 45 · 표 28, `an_check` 둘 다 `no geometry problems found`. 영문 6장 새 두 절과 그림 두 장을 다시 읽어 라벨 2건 고침(`Equation D3 needs` → `the D3 check needs`, v.FB 부호). v1.2 PDF 둘은 삭제. |
| 2026-09-22 | **(25차) 그림 35 를 회로 기호로** (사용자: 회색 상자로는 무엇을 하라는 건지 알 수 없다, V.R 이 뭔가, 제대로 된 등가 회로를). `an_comp_opamp` 를 다시 그렸다 &mdash; TL431 을 점선 상자 안의 **OP 앰프 + 전압원 V.R = 2.495 V(내부 기준)** 로, 애노드 = 접지 · R.O 도 그려 넣고 "신호 전류가 안 흐른다" 고 적음 · 옵토커플러를 **LED(빛 화살표) + 포토트랜지스터(NPN 기호)** 로, R.B 는 V.Z 레일에서, R.P 는 LED 에 병렬(ST 도면 그대로) · FB 핀은 IC 내부 풀업 R.FB 와 C.opto + C.fx. 회색 블록 두 개(V.R · i.LED/i.C 식)를 전부 없앴다. 세 인수 식은 그림 아래 한 덩어리로. 본문과 캡션(두 판)에 V.R 이 무엇인지와 R.O 가 게인에 안 들어가는 이유를 적었다. `figcheck` 겹침 0(`tiny` 는 쪽 배율 보고). |
| 2026-09-22 | **(26차) 그림 35 세 번째** (사용자: 그림 안 수식도 본문 수식과 같은 글씨체로 · 포토커플러 기호를 그대로 쓰지 말고 회로 이론에 맞는 등가 기호로 · OP 앰프 출력의 궤환 접점에 점이 없고 출력 핀에 너무 붙어 있다). 수식 라벨을 **STIX mathtext** 로(`math_fontfamily='stix'`, `\dfrac`, 12.5 pt — 본문 수식 PNG 와 같은 활자) · 옵토커플러 트랜지스터 쪽을 **전류 제어 전류원(CCCS, 마름모) `CTR·i.LED`** 로, LED 가지에 제어 전류 `i.LED` 화살표 — TND381 의 소신호 모델 그대로 · 출력 노드를 삼각형 꼭짓점에서 0.8 떨어뜨리고 접점 점을 찍었다. 캡션·본문(두 판)을 맞췄다. |
| 2026-09-22 | **(27차) 그림 35 네 번째 — TL431 을 기능 블록도대로** (사용자: R.O 는 TL431 의 일부가 아닌데 왜 점선 상자 안에 있나, 지금 등가가 맞긴 하나). 상자를 부품이 실제로 담는 것으로 줄였다 — 증폭기(REF 가 **비반전**, 내부 기준 V.R 2.495 V 가 **반전** 입력) + 출력 NPN(컬렉터 = K, 이미터 = A). 트랜지스터가 반전하므로 REF → 캐소드가 반전이고 캐소드 → REF 궤환이 부궤환이 된다. 이전 판은 그 두 반전을 합쳐 입력을 맞바꾼 OP 앰프 하나로 그렸는데 단자 거동은 같지만 부품 안의 구조가 아니었다. R.O · R.I · Z.f 는 상자 밖. 핀 이름 REF · K · A 를 상자 경계에 적었다. 본문·캡션(두 판)을 그 서술로 고쳤다. |
| 2026-09-22 | **(28차) 그림 35 비례 재조정** (사용자: 미리보기 회로가 엉망이다 — 피드백 루프가 너무 크고, OP 앰프가 너무 크고, V.R 쪽 선이 너무 짧고, BJT 가 너무 납작하다). 레이아웃을 다시 잡았다 — 궤환 루프를 REF 노드 5.6 → 8.1 로(전 5.6 → 8.6, 증폭기가 3.5 아래에 있어 루프 높이가 5.7 이었다) 줄이고, 삼각형을 2.2 × 2.2 → 1.7 × 2.0 으로, BJT 베이스 바를 0.8 → 1.3 에 컬렉터·이미터 사선을 0.62 × 0.62 로(전 0.35) 세우고, V.R 리드를 반전 입력에서 0.65 + 소스 아래 0.85 로 늘렸다. **그리고 기준 전압을 접지가 아니라 애노드로 되돌렸다** — 데이터시트대로이고, 이제 점선 상자를 지나는 배선이 REF · K · A 셋뿐이다. 핀 이름은 상자 밖에. **그리고 같은 날 두 번째 지적으로 접지를 갈랐다** — 보상망은 2차측이고 FB 핀은 1차측이며 그 사이가 옵토커플러, 즉 절연 장벽이다. 양쪽을 같은 접지 기호로 그리면 이어져 있다는 뜻이 되므로, 1차측은 채운 삼각형(`figs_ref._pgnd`)으로 따로 그리고 장벽을 점선으로 세워 `secondary side` / `primary side` 를 적었다. CTR 전류원과 C.opto + C.fx 의 접지까지 내려오는 길이도 2.25 · 2.40 → 1.05 · 1.35 로 줄였고, 삼각형(1.7 × 2.0 → 1.45 × 1.7)과 BJT(베이스 바 1.3 → 1.05)도 더 줄였다. |
| 2026-09-22 | **단일 트랜스포머 검토 — PQ 40/40 도 PQ 50/50DG 도 안 들어간다** (사용자: "이 가이드는 트랜스포머 1개를 썼을 때를 기준으로 해야 해 … PQ4040 이 턱없이 부족하면 미리 말해줘"). 한 코어에 15 : 2 를 감으면 **자속은 문제가 아니다**(B.pk 는 N.s·A.e 만의 함수 — PQ 40/40 109 mT · PQ 50/50DG 62 mT). 막히는 것은 **권선 형상과 누설**이다. `cores.winding` 으로 재면 1차 15 T 리츠(ø2.37)는 2층이면 축방향 19.0 mm · 반경 4.74 mm, 3층이면 11.9 · 7.11 이고 2차 권선 하나가 28.3 A 라 6.3 mm² → 포일 0.2 × 15.7 두 장 병렬. **PQ 40/40**: 권선폭 21.4(마진 제외)·반경 4.85 → 분리 간격 −12 mm. **PQ 50/50DG**(B65981Q·B65982E, 사용자가 PDF 로 전달 — TDK CDN 이 이 컨테이너를 403 으로 막는다): A.e 332(필요 103 의 3배) 인데 창은 오히려 **얕다** — 외각다리 안쪽면 31.5 min → 반경 15.75, 관 ø23.2 → **반경 4.15 mm**, 권선폭 30.4. 분리 간격 −7.5 mm. `cores.CORES`·`MECH` 에 등록했다. **누설은 코어 크기와 별개의 문제**: L.short ∝ N² 라 같은 형상에서 15 T 한 코어는 5 T 세 코어 합의 3배. 러프 모델(3코어 형상 대입 5.4 µH 대 시트 3.67 — 1.5배 과대)로 나란히 감기 59~79 µH(간격 0), 동심 3.2 µH, 목표 11 은 그 사이 — 1차 분할(8 | S | 7)이나 스페이서로 벤더가 맞춰야 한다. **필요한 창**: 나란히 배치면 권선폭 ≥ 약 40 mm 와 반경 ≥ 5 mm, 3층 1차면 권선폭 ≥ 약 30 mm 와 반경 ≥ 약 8 mm. PQ 계열은 전부 얕아 부적합 — ETD 49/54 급(깊은 창)을 후보로 제시했고 **그 치수는 데이터시트가 없어 확인 대기**다. 단일 코어 A.L 138 nH(PQ 50/50DG 재고 100/250 사이, 주문 사양). **PQ 50/50DG 코일포머 도면에는 핀 번호가 찍혀 있다**(1 왼쪽 아래 → 6 위, 7 오른쪽 위 → 12) — 같은 TDK 계열이라 PQ 40/40 에 가정한 반시계 번호와 일치하지만, B65884E 자체의 확인은 아니다. **AN 은 아직 3코어 그대로** — 코어가 정해지면 `N.x` = 1 변형 → 7.8~7.9 재작성 → 사양서. |
| 2026-09-21 | **(20차) 설계 예제 7.8 에 트랜스포머 기호 + PQ 40/40 보빈 핀 배정** (사용자: "트랜스포머 심볼 만들어서 어느 권선과 어느 핀이 무엇인지 표기해야지. 핀 배열은 PQ4040 보빈 핀 배열 참고하고"). **근거는 TDK B65884E 도면 3쪽(FPK0430)** — 단자 12개, 한쪽 6개가 3개씩 두 무리, 무리 안 피치 5.08, 두 무리 안쪽 핀 사이 15.24, 두 열 사이 38.1, **핀 1 표시는 평면도 왼쪽 아래 모서리**이고 그 밖의 핀 번호는 도면에 없다. 벡터 좌표로 재서 확인했다(핀 표시 2.1 pt 정사각형이 y 345.2·356.2·367.1·398.8·409.7·420.6 · x 138/219 → 피치 5.10 mm, 열 간격 37.8 mm — 라벨 5.08/38.1 과 일치). **번호 방향은 가정이다** — 핀 1 에서 반시계(표시 쪽을 따라 1→6, 반대쪽 7→12, 12 가 1 의 맞은편). 데이터시트가 말하지 않으므로 벤더 보빈 도면으로 확인할 것이라고 AN 캡션·사양서 주석·`cores.py` 에 같이 적었다. **배정은 방향과 무관**하다(어느 무리에 어느 권선인지는 도면이 정한다). **배정:** NP1 1–3 · NAUX 4–6(표시 쪽 = 1차 기준 권선 둘) · NS2 7–9 · NS3 10–12(반대쪽) · **센터탭 = 9 와 10 을 PCB 에서 결합**(15.24 간격을 사이에 둔 이웃 핀) · 각 무리 가운데 핀(2·5·8·11)은 비운다(리츠 다발과 포일 끝이 핀 하나씩, 남는 핀이 두 단자를 띄운다). 극성점 = 시작 = 짝의 첫 핀; NS2 끝과 NS3 시작이 탭이라 두 권선은 같은 방향. **한 곳에서 읽는다** — `cores.PIN_XY`·`PINMAP`·`CENTRE_TAP`·`pins()` 를 그림(`figs_ref.an_xfmr_pins`)·AN 표(`pins`, `legend42`)·**벤더 사양서(`build_trans_spec.py`)** 가 같이 읽는다. **그래서 사양서 4종의 단자가 바뀌었다** — 예전 `1 – 2 / 3 – 5 / 4 – 6 / a – b` 는 LGE 양산품(26OP-LM83W) 승인원 방식이었고 PQ 40/40 12핀 보빈과 맞지 않았다(1차·2차가 같은 쪽). `check_trans_spec` 불일치 0. **그림 `an_xfmr_pins`:** 왼쪽 한 유닛의 기호(NP1·NAUX·NS2·NS3, 단자마다 핀 번호, 시작/끝, 탭 결합), 오른쪽 보빈 평면도(치수 3개를 도면 그대로 표기, 핀마다 권선 색 고리, 핀 1 표시). `figcheck` 0건. AN 7.8 에 배정표 `pins` 와 **3유닛 결선 문단**(1차 직렬 3→1 · 보조 직렬 6→4 · 2차 병렬 7끼리/12끼리/9+10 끼리, 직렬은 dot–non-dot · 병렬은 dot–dot). 영문 80쪽 **같은 날 간격 완화**(사용자: "설계 예제에서 갑자기 줄간·항목 간 간격이 좁아졌어") — 수식 라벨 위 5 → 12 pt · 수식과 대입 산수 아래 0 → 10 pt · 표 위 6 → 10 pt · 표 캡션 아래 11 → 13 · 문단 뒤 7 → 8. 77 → 80쪽 **그리고 수식 크기 통일**(사용자: "작은 것은 너무 작다, 일관성이 없어 … 분수 안에 있더라도 분수가 아닌 것과 사이즈가 같아야지") — 원인 둘: ① `eq()` 가 열 폭을 넘는 수식을 *축소* 해 넣고 있었다(긴 수식만 작아졌다). 이제 축소하지 않고 **열 폭을 넘으면 빌드가 실패**하며(`an_pdf.WIDE`, `check_refs()` 에서 보고) 저자가 줄을 나눈다 — `eq()`/`calc()` 가 tex 리스트를 받아 여러 줄로 쌓는다. 넘던 것은 `B.pk`·`A.e` 대입 한 줄뿐이었다(498 pt). ② mathtext 의 `\frac` 은 분자·분모를 text style 로 70 % 축소한다 — `eqpng()` 가 `\frac` 을 전부 `\dfrac`(display style, 축소 없음)으로 바꿔 그린다. 81쪽 · 식 49 · 그림 42 · 표 29. **한국어판 미반영.** |
| 2026-09-21 | **(19차) 설계 예제를 AN4932 방식으로 — 문장 → "Equation N" → 수식 → 대입 산수, 그리고 기호표** (사용자가 ST AN4932(HVLED001A 플라이백 AN)를 예시로 첨부: "너가 만든 설계 예제에서는 무슨 수식을 써서 도출했는지 도대체 알 방법이 없어 … 순서대로 잘 돼있지. 글 간 간격도 깔끔하고 사용한 수식도 뭔지 깔끔하고. 용어정리도 마지막에 해놓고 … 계산 수식이나 공식 폰트 사이즈도 적절하고. 우리껀 너무 크거나 너무 줄간 간격도 좁고 … 항목 하나하나를 있는 그대로 수정하라는 뜻이 아니야. 글을 어떻게 쓰는지 수식은 어떻게 소개하는지 보라는 거야"). **AN4932 에서 배운 것** (`Reference/AN4932 HVLED001A enhanced QR high PF flyback controller.pdf`, 설계 장 32~53쪽, 부록 B 약어표 57~59쪽): ① 수식마다 **한 문장 도입 → 굵은 `Equation N` 줄 → 본문 크기의 가운데 수식 → 그것을 쓰는 다음 문장**의 네 박자 · ② 설계 장에서는 **앞에서 세운 식을 번호로 다시 부르고 그 아래에 숫자를 대입한 산수를 한 줄 더** · ③ 문서 끝의 기호·약어표. **`an_pdf.py`:** `EQSIZE` 12 pt(본문 9.3 pt 의 1.3배 — 예전 판은 그보다 컸다) · `eq()` 가 위에 `Equation N` 라벨 줄을 붙이고 이미지를 가운데 정렬(`KeepTogether`) · **`eqagain(key)`** 가 같은 식을 `Equation N, repeated` 로 다시 그리고(**`EQTEX` 에 tex 를 보관**하므로 식이 두 곳에서 어긋날 수 없다) · **`calc(tex)`** 가 번호 없는 대입 산수 줄 · 행간 12.2 → 13.2, 문단 뒤 6 → 7, 노트·표 셀도 같이 · `tbl(split=True)` 로 긴 표가 쪽을 넘고(머리 반복) 표 위 6 pt. **`an_body.py`:** 2.4 에 `f.n`·`Z.0`·`Q`·`λ` 정의식 신설(`Qdef`) — 7장이 부를 식이 없었다 · **7.4 를 Step 1~9 로** (굵은 도입 문장 → `eqagain` → `calc` 굵은 결과) + 결과표 한 장(Step·Quantity·Eq.·Result) — 예전 사슬표는 "Computed as" 열에 산수를 글로 눌러 담아 무슨 식인지 안 보였다 · 7.8 의 `B.pk`·`A.e`·`I.eq`·`I.sat` 와 7.10 의 리플·홀드업·스크리닝도 같은 박자 · **8장 `List of symbols`** 신설(약 50행, References 앞). **AN4932 를 그대로 베끼지 않은 것:** 표 캡션은 아래 그대로(ST 는 위), 절 끝 수식 요약표는 넣지 않았다(결과표가 그 역할). 영문 77쪽 · 식 49 · 그림 41 · 표 28. `an_check` 통과. **한국어판 미반영.** |
| 2026-09-21 | **(18차) 설계 수치는 설계 예제(7장)에만 — 2~6장은 기호로, 7장은 표 하나에 산수까지** (사용자: "설계 예제 이전에 있는 전류값이나 전압값이나 에너지값 같은 것들은 갑자기 왜 튀어나오는거야? … 여전히 여기저기 그래프나 파형이나 회로나 테이블에 많잖아 … 설계 예제도 완전히 지금 개판이야. 글만 엄청 많고 … 테이블도 너무 많고"). **옮긴 그림 7장:** `f12_two_divergences`·`an_above_below`(7.6) · `an_tank_current`(7.7) · `an_xfmr_read`(7.8, 표시 1~7 그대로) · `f19_cout_criterion`(7.10) · `an_loop_bode`(7.13). **`f14_fsw_theta` 는 AN 에서 뺐다** — `an_above_below` 가 같은 곡선을 `f.r` 기준선과 함께 그린다. **2~6장에서 걷어낸 숫자:** 2.9 경계 노트의 105.4/110.1 kHz·−8.4° → 7.5 · 2.16 의 685 W → 7.1 노트 · 3.2 홀드업 7.9 J·274 µF·245× → 7.10(그림 캡션) · 3.3 의 1.18 V · 4.6 λ 0.55 · 4.7 의 λ 후보값과 f.Max 319.6 · 4.8 무부하 해(M∞ 0.645 대 0.641, 231 kHz) → 7.4 노트 · 5.2 정적 소자 10.9 A/9.65 W · 5.4 전환 노트 127→249 kHz · 6.1 47 Hz · 6.11 35 % · 6.15 δ 0.19 mm · 6.23 스크리닝 결과 · 6.24 kT 1.8/1.9. 6.8 읽기 표에서 "Worked design" 열을 뺐다. **그림 자체의 숫자:** `f11` 을 p/P 로 정규화(658 W·2.23 J·47 Hz 삭제) · `f02`/`f04` 범례 "full load Q = 0.77" → "full load", f04 콜아웃의 0.73/0.69 f.r 삭제 · `an_loadshift` 범례의 Q 값 삭제 · `f18` 의 "123 → 250 kHz" 삭제. IC 상수에서 오는 173.2/332.3/166.2/346.5 Vac 와 235/245 V.pk 는 남겼다(설계가 아니라 데이터시트). **7장 재구성:** 사양표 4개 → 1개(given/chosen/assumed 열) · 판정 여유 정의 4문단 → 1문단, 노트 2 → 1 · 사슬표에 "Computed as" 열을 넣고 불릿 15개를 지웠다(같은 산수) · "Tank: calculated against selected" 표 삭제(사슬 12~16행과 같다) · 트랜스포머 표에 산수 열, 불릿 3개 삭제, 양자화 노트 삭제 · 출력 뱅크 표에 산수 열, 불릿 삭제 · 손실 노트 절반 · "왜 2 + 2" 문단 삭제. **"2 + 2" 표기 폐기** (사용자: "센터탭이면 2턴 그리고 2턴으로 따로 넣어야지 2+2면 4턴이라는 거야?") — 모든 표에서 **NS2 2 T, NS3 2 T** 로 따로 적고, 범례 표에 NS2·NS3 행을 따로 뒀다. **Figure 31(`f17`)** 의 "held LOW/HIGH" 상자가 자기 파형을 가리고 있었다 — 상자를 없애고 선 옆(위/아래)에 글자만. **한국어판 미반영.** |
| 2026-09-21 | **(17차) AN 전체 중복·장황 정리 — 그림마다 글 하나, 본문의 되풀이 삭제** (사용자: "PDF 한줄 씩 그리고 파형그래프회로도 등 전체적으로 살펴보고 설명이 중복되고 길고 이런거는 삭제하거나 간단화 … 무작정 줄이라는게 아니야"). **가장 큰 중복은 그림 아래 회색 꼬리말(`foot`)과 PDF 캡션이 같은 말을 두 번 하던 것** — 41장 전부. `figs.finish()` 가 **`PLAIN`(AN) 모드에서는 꼬리말을 안 그린다**(교육자료·가이드 PNG 는 그대로). 꼬리말에만 있던 사실은 캡션으로 옮겼다: an_recovery 출처(AN-4151) · f04 영역 구분 출처(ROHM) · f05 ZVS 두 조건 · f14 최고 곡선은 264 V 가 아니라 FB 경계 332 V · an_two_stage 리플 과장 · f02 음영 경계는 무부하 위치 · f11 두 면적이 같다 · an_loop_bode 축이 180°+arg T. `figcheck.run` 은 `figs.PLAIN=True` 로 검사한다(저장되는 그림과 같게). **본문 삭제·축약 15곳:** 모드 그림 뒤 노트 둘을 하나로 · "freewheeling 두 용법" 노트를 절반으로(Toshiba 인용은 한 줄) · 세 경우 불릿에서 정의 되풀이 삭제 · `f.o` 문단과 "두 주파수는 한 범위의 양끝" 노트를 합침 · an_flyback_llc 캡션 200→110 단어 · "게인은 경계조건" 둘째 문단의 반복 삭제 · 설계 절차 "사양" 문단 축약(예제 장이 되풀이한다) · 반도체 절의 정적 소자 노트를 4장 노트로의 포인터로 · 컨트롤러망의 보조권선 문단을 ZCD 절 노트로의 포인터로 · **"Ten things that go wrong" 절 삭제**(System design rules 가 열 항목을 전부 담고 있었다) · 판정 여유 정의 4문단→2 · `R.ac` 노트 축약과 an_fha_steps 캡션의 같은 문장 삭제 · ZVS 검증 3문단→2(같은 절 참조가 두 번) · 권선비 절의 n/n.T 문단을 6.9 절 포인터로. **그림 결함 1건:** f16 반브리지의 두 패널 제목이 겹쳐 있었다("(S4 standing)" 와 "second") — 제목을 짧게. 트랜스포머 장의 "Only the secondary current divides" 노트도 새 표의 첫 행과 같아 뺐다. **그림 42 는 도면 세 장만 남겼다** — 범례 6줄과 권선 표를 그림 안에 두면 적응형 그림 블록이 쪽에 맞추려고 71 % 로 줄여 9 pt 글자가 5 pt 가 됐다. 범례·권선 목록은 본문 표(`legend42`)로, 그림은 9.3 × 4.85 in(패널 인치 크기는 그대로라 축척 불변). 최종 텍스트 스캔: 깨진 참조 0 · 반복 문장 0 · 그림 42/표 33 번호 연속 · 겹친 낱말 1("then then", 사슬표 12행) 수정. **한국어판 미반영.** |
| 2026-09-21 | **(16차) 트랜스포머 장을 파형 중심으로 — 사양 항목마다 "파형의 어디를 읽는가" 를 그림 한 장과 표 하나로** (사용자: "6.8에서 LLC의 전류 파형과 함께 어떤 스펙을 정할 때는 전류 파형에서 무엇을 봐야하고 … 피크값은 이거고 RMS값은 어디서부터 어디의 값으로 구하는 거니까 권선 두께는 이래야 하고 DC OVERLAP 전류는 이러이러한 상황에서의 스펙이니까 … 파형과 함께. 6.8부터 6.21에 다 해당 … 지금은 수식만 남발"). **`an_xfmr_read` 신설** — 최악 사이클(라인 피크 · 173 Vac 등가 · 전부하, `f.sw/f.r` 0.84)의 1차 권선 전류 · 2차 권선 2개 전류 · 2차 권선 전압 세 줄에 표시 1~6, 아래 왼쪽에 라인 반주기의 실효 포락선(`l6790.sweep`, 라인사이클 실효 10.9/9.45 A 가 동선을 정한다), 아래 오른쪽에 DC-overlap 벤치 조건(표시 7: 2차 개방 · 1차 DC · 15.2 A 까지 L ≥ 90 %, 12.4 A × V.OVP2/V.o 에서 온다, 18.3 A 가 아니다). 파형은 8구간 모델(`_cycle`)이고 라벨 값은 시트(`an_pdf.V`)다 — 다른 파형 그림과 같은 규칙. **읽기 표(`xfmr-read`)** 가 표시마다 "무엇을 · 어느 구간에서 · 이 설계에서는 · 무엇을 정한다" 를 적고, AN 의 자속 절 · 창 절 · 포화 시험 절 셋이 각자 표시 번호로 시작한다. 설계 예제 사양서 표의 조건 열에도 표시 번호를 달았다. **Figure 41(단면도)도 같은 지적** ("너무 번잡 · 글이 너무 많고 · 2차측은 2+2라는데 뭔 소린지") — 범례 6줄을 한 줄씩으로 줄이고(②: "NS2·NS3 2턴씩 두 권선을 센터탭에 · 포일 4겹"), 권선 표에 "언제 도통하나" 열(첫/둘째 반주기)을 넣고 권선 방법 열과 인덕턴스 문장을 뺐다(본문 표에 있다). 꼬리말은 절반으로. 본문에는 **"왜 2 + 2 인가"** 문단과 **"전류에서 동선까지" 표**를 넣었다 — 10.93 A ÷ 4.5 = 2.43 mm² → 리츠 310 × ø0.10 → 번들 ø2.37 → 5턴 2층(3 + 2) → 7.11 mm, 9.45 A ÷ 4.5 = 2.10 mm² → 포일 0.20 × 10.5 → 4겹 1.00 mm, 25.4 − 4.0 − 7.11 − 10.5 = **3.79 mm** 가 분리 간격. 예전 권선 표(`winding`)는 이 표가 대체한다(키 유지). **모델과 설계식 사이의 한 가지를 그림에 그대로 두었다** — 프리휠링 동안 `L.m` 이 작은 전압을 계속 보므로 모델의 `i.Lm` 은 스위칭 순간까지 조금 더 오른다(그림 셋째 줄 "rectifier off: L.m still sees a little"). 설계식 `B.pk = V.o/(4 f.r N.s A.e)` 는 도통 구간의 볼트-초만 세고, 정본 시트도 같다. 이 차이를 수치로 주장하지 않았다 — 모델의 데드타임 폭은 그리기 값이다. figcheck: 새 그림 0건(처음엔 9.9 in 높이에 26건 `tiny` → 8.9 in · 글자 9.4~9.6 pt). **한국어판 미반영.** |
| 2026-09-21 | **(15차) 극성점 위치 재정의 · 다이오드 2배 · Figure 1/3/4/28 지적 6건** (사용자: “권선기호는 빨간점처럼. 빨간 박스안에 회로단 정의가 서로 겹쳐져서 더 이상해졌어. figure 3도 센터탭 접점 표시 지워주고. figure 28 은 2 x Vin 글자가 겹친 것, 오른쪽 그래프 글자박스가 곡선을 가림. figure 4 의 가장 왼쪽 회로는 완전히 엉망. 바디다이오드를 제외한 모든 다이오드 2배”). **극성점은 세 번째 자리에서 정착했다** — 리드선 *바깥쪽*(턴이 부푸는 반대편), **첫 턴 높이**. 사용자가 빨간 점으로 그려 준 자리다. 처음 자리(리드선 바깥 위, 단자 높이)는 회로선 위였고, 두 번째(코일과 코어 사이)는 코어 바에 붙었다. 턴은 반대쪽으로 부풀므로 리드선 바깥 첫 턴 옆은 빈 공간이다 — 키트 `DOT = 0.75 × r`, 시트 0.28. `i.p`·`i.s`·`N.s` 라벨을 그만큼 밀어냈다. **Figure 1 그룹 띠** — 변압기가 넓어지자 `transformer` 띠가 양옆 띠와 겹쳐 한 줄로 읽혔다. 띠 경계를 상수가 아니라 **그룹 사이 중점 ∓ 0.18** 로 뒀다. **Figure 4 왼쪽 패널** — 턴이 커지며 변압기 리드선이 코어 양옆 0.9 로 나갔는데 자리는 그대로여서 **1차 리드선이 `L.m` 가지 위에 정확히 얹혔고** 블록이 2차를 덮었다. 왼쪽(소스·`C.r`·`L.r`·노드)을 당기고 변압기를 4.75 로, 블록은 2차 리드선에서 틀 끝까지 채운다. **다이오드** `vdiode` 0.26 → **0.52**, `schem.diode` 0.28 → **0.56**, `_diode_along`(브리지) 도 같이 — **`mosfet()` 의 바디 다이오드는 0.26 그대로**(기생 소자라 작게, 사용자 지시). 8구간 시트의 `D1`·`D2` 이름은 0.38 → 0.60 (삼각형 반폭이 0.40 이 됐다). **Figure 3 센터탭** 접점점 제거 — `xfmr(tap_dot=False)` 가 `nodot()` 을 스스로 선언한다. **Figure 28** — `2 x V.in` 은 x 축을 2.62 → 3.05 로 늘려 화살표 오른쪽에 자리를 줬고, 오른쪽 그래프의 메모 4개는 **곡선이 안 가는 네 모서리**로 옮겨 최대·최소 점에 화살표로 잇는다 (가지 옆에 두면 그 가지를 덮어 어디가 FB 이고 어디가 HB 인지 안 보였다) |
| 2026-09-21 | **(14차) 권선 기호를 반원 하나 단위로 50 % 키우고, 코어·간격·극성점을 그에 맞춰 다시 잡았다** (사용자 지적 6건: “Figure 1에서 트랜스포머 코어 길이가 너무 기니까 맞춰주고. 권선과 코어간 간격 지금보다 3배로. 센터탭에 접점표시 제거. 인덕터와 트랜스포머에서 반원 1개의 사이즈를 50 % 키워달라. figure 6,7,8,9도 마찬가지. figure 18은 권선 표시가 회로선과 너무 가깝고 권선과는 너무 멀어. LLC 그림에서 GND 심볼이 회로선과 겹치고 Flyback에는 GND가 없다”). **턴 반경** `TURN_R`·`WIND_R` 0.14·0.28 → **0.21·0.42**. 반경이 커지면 같은 높이에 들어가는 턴 수가 줄어드는 것이 의도한 맞바꿈이다 — 8구간 시트는 1차 6 → **4**턴(1.68/8 = 0.21) · 2차 각 반 3 → **2**턴(0.78/4 = 0.195), `L.r` 은 폭 0.90 → **1.26**(3턴에 0.21), `L.m` 은 5 → **3**턴(1.32/6 = 0.22). 이제 한 시트 안의 모든 권선이 같은 크기 반원으로 그려진다(예전에는 `L.m` 0.132 대 1차 0.14 대 `L.r` 0.15 였다). **권선↔코어 간격** 8구간 시트 `XP/XS` = `XTR ∓ 0.34` → **∓0.62**. 실여유가 0.07 → **0.28** 로 요청한 3배(0.21)에 0.07 을 더 준 것인데, 그 0.07 이 **극성점이 들어갈 자리**다. **코어 길이** 시트는 `YB−0.38 … YT+0.38` → **`YB−0.10 … YT+0.10`**, 키트는 상수 0.30 → **0.45 × 턴 반경** — 상수로 두면 짧은 권선이 자기 길이의 절반만큼 긴 코어를 달고 “바 두 개 옆에 코일 하나” 로 읽힌다. **극성점은 자기 권선 옆으로** — 예전에는 리드선 바깥 위쪽에 있어 *회로선* 위에 앉았고 어느 코일 것인지 말하지 않았다. 지금은 턴이 부푸는 쪽, 즉 코일과 코어 사이 틈에 놓는다(시트는 리드선에서 0.30 · 단자에서 0.10 아래 — 그 높이에서 바깥 고리가 0.12, 코어 바가 0.49 이므로 양쪽을 자기 반지름만큼 비운다). **센터탭 접점점 제거** — 세 가닥이 만나므로 관례는 점을 요구한다. 사용자 지시로 뺐고, 검사기를 느슨하게 하는 대신 **`schemx.nodot()`** 으로 “여기는 일부러 안 찍었다” 를 도면이 선언하게 했다. `figcheck` 는 그것을 `nodot` 으로 **보고하되 세지 않는다**(레일 `stub` 과 같은 취급) — 그래야 다음번 진짜 누락이 같은 침묵에 묻히지 않는다. **접지** `schem.gnd` 에 `lead` 를 넣었다. `(x, y)` 는 *연결점*이지 첫 바가 아니어서, 레일 높이로 부르면 가장 넓은 바가 배선 위에 그대로 그려져 덩어리로 읽혔다. 리드는 기호와 같은 zorder 로 그려 배선 검사가 “허공에 끝나는 선” 으로 읽지 않는다. 크기도 이제 `X.scale(ax)` 를 타므로 넓은 패널에서 세 개의 짧은 눈금이 되지 않는다. **플라이백에 접지를 넣었다** — 옆의 LLC 에는 있고 여기에는 없어서 두 패널이 리턴 레일을 서로 다르게 말하고 있었다. **키트 결함 하나가 여기서 드러났다** — `schem.ind` 의 “리드가 너무 짧으면 흡수한다” 판정이 `s` 의 비율(4 %)이었는데, **픽셀은 `s` 를 따라 커지지 않는다.** 턴을 키우자 1.35 짜리 인덕터에 0.042 리드가 남았고(비율로는 기준 위, 종이 위에서는 1 px) 양 끝이 한 점에 떨어져 **없는 3갈래 접점**이 두 개 생겼다. 판정을 **턴 반경의 절반**으로 바꿨다. **그리고 한글 글꼴 가드가 배치 작업을 죽이던 것을 고쳤다** — `figs.py --plain` 이 한국어 전용 그림에서 멈췄다. 지금은 그 장만 건너뛰고 끝에 **어느 장을 어디서 다시 그려야 하는지 찍고 exit 2** 로 끝난다(5장: `fsw_theta`·`gain_regions`·`loop_bode`·`morphing_levels`·`tank_current`). 영문 그림 11장 재생성 · `figcheck` 해당 6장 **0건** |
| 2026-09-21 | **(13차) 검사 결과를 PDF 위에 되돌려 놓는 도구를 만들었다 — `an_locate.py`** (사용자: “그렇게 말하면 뭐가 문제인지 내가 PDF에서 확인할 방법이 없는데?”). 맞는 지적이었다. `figcheck` 의 보고는 **`an_core_section` 에 6.0 pt 라벨이 78개** 같은 문장인데, **PDF 에는 그림 이름이 없다** — 번호와 쪽뿐이다. 즉 생성기 소스를 연 사람만 검증할 수 있는 보고였고, PDF 를 든 사람에게는 그냥 믿으라는 말이었다. **이름 → 쪽은 픽셀로 푼다**: PDF 안의 이미지는 `figures/an/*.png` 의 재인코딩이라 바이트는 다르지만 화소는 같으므로 48×48 회색 서명으로 맞춘다 — 그림 41장 전부 매칭되고 수식 PNG 48개는 하나도 안 걸린다(오차가 0.x 대 수십 이라 조정할 중간지대가 없다). 번호는 **그 이미지 바로 아래 캡션**에서 읽으므로 독자가 보는 번호 그대로다. 출력 셋 — 표 · `--mark`(테두리 + 그림당 메모 하나가 든 PDF 사본; 뷰어 주석 목록이 곧 할 일 목록) · `--crops`(인쇄 크기로 렌더한 뒤 확대). **결과: 영문 AN 에 걸린 것은 `tiny` 5장뿐이다** — Figure 11 p16 · 12 p17 · 32 p38 · 36 p48 · 41 p62. 나머지 4장(`an_architectures`·`an_pfc_idea`·`f03_gain_walk`·`morphing_levels`)은 **이 PDF 에 실리지도 않는다** — 한국어판·교육자료 소속이고, 그 구분을 도구가 스스로 말한다. **첫 테두리는 라벨보다 한참 아래에 그려졌다** — `savefig(bbox_inches='tight')` 는 캔버스가 아니라 잉크의 타이트 상자를 잘라내는데 좌표를 캔버스 기준으로 재고 있었다. 고치면서 `figs.save` 를 `finish()`(후광 + 꼬리말 캡션)와 `saved_box()`(savefig 이 실제로 자를 상자)로 쪼갰고, **`figcheck` 는 이제 `finish()` 를 부른 뒤 검사한다** — 즉 독자가 받는 그림 그대로를 본다. 꼬리말은 캔버스 밖에 걸치는 것이 설계이므로 `_is_foot` 로 빼지 않으면 캡션마다 `off-page` 가 뜬다. **같이 드러난 사고 하나** — 이 컨테이너에는 한글 글꼴이 없어서 `figs.py loop_bode` 한 번이 **한국어 그림을 통째로 네모로 덮어썼다**(`git status` 가 잡아 되돌렸다). `figs._hangul_guard` 신설: 쓰기 직전에 한글을 세어 글꼴이 없으면 예외로 죽는다. **그리고 열려 있던 두 번째 건을 닫았다** — `an_loop_bode` 의 y 축이 `phase margin` 이었는데 그려지는 값은 `180° + arg T` 이고 **그것이 여유가 되는 곳은 |T| = 0 dB 한 점뿐**이다(`CLAUDE.md` §9.3 이 시트 판에 대해 이미 적어 둔 규칙이고 AN 만 예외였다). 축 이름을 바꾸고 주석도 `Φ_M = 49.6° at 0 dB` 로 고쳤다. 영문 AN 74쪽 재빌드 · `an_check` 기하 문제 0 |
| 2026-09-21 | **(12차) 권선의 턴이 자기 코어 바 위에 얹혀 있었다 — 키트 결함이고 그림 5장이 걸려 있었다** (사용자: "권선 기호도 또 권선이랑 겹쳐있어"). **재서 확인했다** — 코어 바와 턴의 잉크 사이 간격을 *페이지 포인트*로 환산하니 `an_rac` 1.37 pt · `an_integrated` 1.88 pt · **`an_flyback_llc` 0.24 pt** · **`an_fha_steps` −1.79 pt**(실제로 겹쳐 있었다). 데이터 단위로는 다 양수라 눈으로만 보면 "안 겹쳤다" 고 넘어간다 — **A4 가 그림을 줄인 뒤의 값으로 봐야 한다.** **원인: `X.xfmr` 가 `gap` 을 호출자 말대로 썼다.** 턴 반경은 `scale(ax)` 에서 나오므로 **같은 `gap` 0.52 가 어떤 그림에서는 넉넉하고 어떤 그림에서는 바에 닿는데, 호출자는 그걸 알 방법이 없다.** `an_fha_steps` 는 `gap` 0.30 이라 아예 파고들었다. **고침: `gap` 은 이제 *요청*이고, 실제로 쓰는 값은 `max(요청, core + 2.5·r)` 이다** — 리드선 위치가 움직이므로 **호출자는 `x ± gap` 을 가정하지 말고 반환된 단자에서 읽어야 한다**(`an_flyback_llc` 를 그렇게 고쳤다). 결과: 전 그림이 **2.1~3.9 pt** 로 뜬다. **`figcheck.windings` 신설** — `xfmr` 가 실제로 쓴 여유를 축에 기록하고(`ax._xfmr_clear`), 검사기가 페이지 포인트로 환산해 선 굵기 절반씩을 빼고 1.5 pt 미만이면 잡는다. **검출력 확인**: 바닥을 5.0 pt 로 올리면 12건이 뜬다. **이 결함은 PNG 를 화면 크기로 봐서는 안 보인다** — 4배로 확대해서야 눈에 띄었고, 그 그림은 이미 검사를 통과해 나간 뒤였다 |
| 2026-09-21 | **(11차) 두 회로의 가로 예산을 다시 나눴다** (사용자: "플라이백 회로만 너무 크고 llc회로는 글자랑 겹쳐있어"). 둘이 **같은 축 범위 하나를 공유**하고 있었는데, 그래서 플라이백은 폭의 3/4 가 **빈 루프**였고 LLC 는 탱크·자화 가지·변압기를 같은 폭에 욱여넣느라 **`L.m` 이름이 옆 라이저 위에 얹혔다**(확대해서 확인). **두 패널은 서로 다른 단(column)에 있으므로 가로로 맞출 이유가 없다** — 축 범위는 공유해 *배율*(따라서 기호 크기)은 같게 두고, **회로마다 필요한 폭으로 따로 배치**했다. 플라이백: 변압기를 `XT` 7.0 → **4.3** 으로 당겨 루프를 3.0 폭으로 줄이고 정류단도 당겼다. LLC: **배터리를 없애고 라벨 붙은 레일 + 접지**로 바꿨다 — 하프브리지를 그리는 통상 방식이고, 왼쪽에서 번 2 단위가 오른쪽 탱크가 필요로 하던 폭이다. 레그 −0.6 · `C.r` 1.3 · `L.r` 2.8 · 라이저 4.0 · `L.m` 5.1 · 변압기 7.3 · 블록 11.2. `L.m` 이름은 **코일 옆, 코일 자기 높이**로 내렸다(위에 두면 라이저 모서리와 `i.p` 에 닿는다). 레일 왼쪽 끝에는 **포트 점**을 찍었다 — 안 찍으면 `figcheck` 가 `open-end` 로 잡는다(한 가닥짜리 점은 포트 표시라 통과). **그림 안의 회색 캡션도 줄였다** — 문서 캡션이 읽는 법을 이미 말하는데 같은 것을 두 번 적어 쪽에 진한 회색 문단이 둘 겹쳐 있었다. **`figcheck` 가 이 겹침을 놓쳤다** — 회로도 축은 `shield()` 가 손대지 않고(후광이 배선을 끊으므로), 겹친 픽셀이 바닥 10 px 밑이었다. **글자가 선에 *살짝* 얹히는 것은 아직 기계가 못 잡는다 — 확대해서 눈으로 볼 것** |
| 2026-09-21 | **(10차) 그림의 글씨체를 본문과 같은 것으로 바꿨다** (사용자: "Figure 안에 쓴 글씨들이 너무 가독성이 안좋아. 본문 글씨랑 같은 글씨체로 사용해줘"). AN 본문은 `an_pdf.use_unicode()` 가 끼우는 **Liberation Sans** 인데 그림은 matplotlib 기본 **DejaVu Sans** 였다 — 자폭이 눈에 띄게 넓은 다른 활자라, 문단 바로 위의 그림이 **다른 데서 가져온 스크린샷처럼** 읽혔다(이 문서가 몇 주에 걸쳐 벗어나려 한 바로 그것이다). `figs.py` 의 `font.family` 를 `[Liberation Sans, (한글 폰트), DejaVu Sans]` 로 놓았다 — **한글 폰트를 뒤에 둔다**: matplotlib 은 글자마다 폴백하므로 라틴은 앞에서, 한글은 뒤에서 온다(예전에는 한글 폰트가 앞이라 라틴까지 한글 폰트로 그려졌다). **아래첨자·그리스 문자도 같이 옮겼다** — `i$_p$`·`$\Phi$`·`L$_m$` 같은 mathtext 조각이 그림마다 수십 개라 이것을 안 바꾸면 한 라벨 안에서 두 활자가 섞인다. `mathtext.fontset` 을 `custom` 으로 놓고 `rm/it/bf/sf/cal` 을 전부 같은 활자로, 없는 기호만 `stixsans` 로 폴백한다. Liberation Sans 는 λ·μ·Φ·π·Δ·×·≥·±·° 를 전부 갖고 있어 실제로 폴백이 도는 자리는 거의 없다(√·분수·∫ 도 확인했다). **`mathtext.cal` 을 같이 지정한 이유**는 기본값이 `cursive` 이고 custom fontset 이 그것까지 해석하려 들어 그림마다 findfont 경고가 찍혔기 때문이다. **본문 수식은 안 건드렸다** — `an_pdf.eqpng()` 가 번호 붙은 수식마다 `stix` 를 따로 박으므로 그쪽은 그대로 세리프 수식이다. 두 세트(`figures/` 와 `figures/an/`) 46장씩 전부 다시 그렸다 |
| 2026-09-21 | **(9차) 사용자 지적 4건 — 플라이백 비교 절을 고치고 옮겼다.** ① **LLC 회로에 게이트 파형은 있는데 FET 가 없었다** — `v.sq` 사각파 소스만 그려 놓고 아래에 게이트 2줄을 보이고 있었다. **하프브리지 레그(S1·S2)를 그려 넣었다** — 중점 → `C.r` → `L.r` → 1차, `L.m` 은 1차에 병렬, 1차 귀환은 0 레일. 일반 LLC 장에 들어가는 그림이므로 하프브리지가 맞고, 풀브리지는 진폭만 바꾼다고 캡션에 적었다. 자리를 만드느라 변압기를 `XT` 6.2 → 7.0 으로 밀고 `L.m` 라벨을 코일 *위*로 올렸다(옆에 두면 왼쪽 라이저나 오른쪽 1차 권선에 얹힌다). ② **자속 행이 net 만 그리고 있었다** — 이제 **권선별 자속 2개(점선)와 합(굵은 실선), 그리고 그 사이의 띠**를 그린다. 띠가 곧 상쇄량이고, **같은 띠가 두 가지를 뜻하는 것이 이 그림의 요점**이다 — LLC 에서는 2차가 *동시에* 깎아내는 양, 플라이백에서는 1차가 멈춘 뒤 2차가 *이어받는* 양. 처음에는 한 순간을 겨냥한 양방향 화살표로 그렸는데 **한 점은 상쇄가 아니라서** 면적으로 바꿨다. 합 아래 보라 채움은 **지웠다** — 플라이백 off 구간에서 `Φ.p` = 0 이라 띠와 정확히 겹쳐 회색으로 섞였고 제3의 양으로 읽혔다. ③ **절을 6장(설계 절차)에서 2장(일반 LLC 설명)으로 옮겼다** — 데드타임 절 다음, `How an LLC is normally designed` 앞. `A.L`·`L.μ` 는 2장에서 아직 정의되지 않았으므로 `L.m` 으로 고쳐 썼고, 6장 `The transformer` 머리에 역참조를 달았다. **`an_mmf` 는 7장에 그대로 둔다** — 막대가 이 설계의 수치라 방법 장에 있으면 안 된다. ④ **그림 3(`an_rac`)에서 무엇이 `R.ac` 로 바뀌는지 안 보였다** — 변압기·정류기·`C.o`·부하를 점선 상자로 묶고 이름을 붙였으며, 화살표가 저항 *앞*에서 끊기던 것을 저항까지 닿게 했다. **상자의 왼쪽 경계가 판정이다** — 변압기가 안에 있느냐 밖에 있느냐가 `R.ac` 의 `n²` 을 넣느냐 빼느냐와 같은 말이다. `figcheck` 신규 2장 0건 · 전체 45장 회귀 0 · `an_check` 74쪽 0건 |
| 2026-09-21 | **(8차) AN 영문판에 LLC 와 플라이백 트랜스포머를 가르는 절을 신설했다** (사용자 요청: "LLC는 어차피 자화에너지를 저장하는게 아닌데 왜 Ae값을 고려하는지 … DC overlap은 자화 전류만 보는 거고 부하 전류는 보지 않는다라는 개념을 Flyback의 동작과 비교해서 회로와 파형과 자속으로서 설명해줘"). 6.9절 `Not a flyback, and why that changes the core` 와 그림 2장이다. **핵심은 두 권선이 언제 도통하느냐 하나다** — 플라이백은 절대 같이 안 도통하므로 매 순간 권선 전류 전체가 자화 전류이고 자속이 *전류*를 따라간다. LLC 는 같이 도통하므로 기자력이 상쇄되고 남는 차이만 코어를 자화시키며, 자속은 *볼트-초*를 따라간다. **`A.e` 가 여전히 필요한 이유는 패러데이 법칙이 저장 여부를 묻지 않기 때문**이고, 달라지는 것은 계산의 *입력*뿐이다. **"LLC 트랜스포머는 에너지를 저장하지 않는다" 는 표어이지 사실이 아니다** — 자화 에너지는 반주기마다 저장하고, 그것이 데드타임에 브리지 노드를 스윙시키는 ZVS 의 재료다. 저장하지 않는 것은 *부하로 가는* 에너지다. 그림 `an_flyback_llc`(회로 2 + 파형 4행 × 2열: 게이트 · i_p,i_s · i_μ · Φ)와 `an_mmf`(사용 중 대 개방 시험의 기자력, 그리고 18.32 / 15.19 / 12.41 A 를 실제 비율로 그린 막대). **같은 작업에서 AN 의 진짜 오류 하나를 고쳤다** — 식 [Isat] 의 분모가 `L_open` 이라 인쇄된 산수가 인쇄된 답을 못 만들고 있었다(9.97 대 12.41). 시트는 2026-09-21 (7차)에 이미 `L.mu` 로 고쳐져 있었고 AN 만 낡아 있었다. `I.sat = I.eq × V.OVP2/V.o` 도 식으로 세웠다. **도구 결함 둘도 나왔다** — `schem.ind` 가 리드가 1 px 미만일 때 양 끝이 같은 점에 떨어져 **없는 접점을 3갈래로 만들었고**, `schem.shunt` 가 색을 준 소자의 연결선을 기본 남색으로 그려 한 네트가 두 네트로 읽혔다. `schemx.xfmr` 에 `s_dot` 을 넣었다 — 2차 극성점을 아래에 찍을 수 있어야 플라이백을 그린다. **한국어판은 아직이다**(사용자 지시: 영문판만 먼저) |
| 2026-09-21 | **(7차) 포화 시험 전류 — 내가 근거를 지우고 1.5 를 다시 넣었다가 사용자가 잡아냈다.** 사용자 질문 "DC overlap 전류가 15 A 하는 게 맞아 7.5:1 에서? 1차측 전류 피크가 18 A 가 넘는데?" 에 **두 값이 같은 종류가 아니다**(개방시험은 2차 기자력 상쇄가 없다)고 답한 것까지는 맞았다. 그런데 계산을 훑다가 `I.sat_eq` 의 분모가 `L.open_x` 인 것을 찾아 `L.mu_x` 로 고쳤고 — **이건 진짜 오류가 맞다** — 그 참에 `I.sat_spec = 1.5 × I.sat_eq` 를 그대로 두고 값을 19 A 로 올려 사양서 4종을 다시 뽑고 "벤더에 정오 통지를 보내라" 고 권했다. **틀렸다.** 사용자가 "이전에 15 A 로 한 건 포화 전에 OVP2 에 걸리게 하려고 그런 거 아니었어?" 라고 물어 기록을 찾아보니 **2026-09-01 에 바로 그 1.5 를 버리고 OVP2 로 옮긴 항목이 있었다** — "1.5 는 내가 정한 수였다. 사용자가 세 번 되물어서 다시 봤다." 즉 **내가 2026-09-07 에 시트로 옮기면서 근거를 임의 마진으로 되돌려 놓았고**, 오늘 그것을 고치는 대신 강화했다. 근거는 이것이다: 공진 아래에서 자화 도통 구간이 `T.r/2` 로 고정되므로 자화전류는 `f.sw` 와 무관하고 **출력 전압에만 비례**하며, 컨트롤러가 허용하는 최대 출력은 **OVP2** 다. 따라서 `I.sat_spec = I.sat_eq × V.OVP2_act/V.o_eff` 이고 곱할 임의의 수가 없다. **되돌렸다** — `k.OVsat` 와 `I.sat_spec` 을 시트 §14 의 `V.OVP2_act` 아래로 옮겼다(시트 §12 에 두면 전방 참조다). **`L.mu` 수정은 유지한다** — 개방시험에서 코어를 도는 것은 `L.mu` 몫뿐이고, 검산이 있다: 개방시험에서는 1차 전류 전체가 자화 전류이므로 `I.sat_eq` 는 `I.Lm_pk` 와 같아야 하며 네 변형 전부 일치한다(12.79 · 12.41 · 13.23 · 14.79). **결과적으로 벤더에 나간 값은 맞았다** — 7.5:1 참값 15.19 A 대 나간 값 15 A(1.2 % 차이, 올림 차이). **제작 중단도 정오 통지도 필요 없다.** 새 값은 올림으로 9:1 16 · 7.5:1 16 · 8:1 17 · 6:1 19 A. `check_trans_spec` 의 대조도 "탱크 피크를 넘는가"(전제가 틀렸다)에서 **"OVP2 자화전류를 덮는가"** 로 바꿨다. **교훈: 값을 옮길 때 근거 문장을 같이 옮기지 않으면 다음 사람이 그 자리에 임의의 수를 넣는다 — 그 다음 사람이 나였다** |
| 2026-09-21 | **(6차) 트랜스포머 단면도를 실제 도면으로 다시 그렸다 — 사용자 지적 "와이어가 타원이고, 실제 코어·권선 크기를 축척해서 그려야 하는데 맞는지도 모르겠고, 설명도 못 알아보겠다. 실제 사양서처럼 그려라".** 셋 다 맞는 지적이었다. 예전 판은 **자기 docstring 에 "proportions are schematic" 이라고 적어 두고 있었고** 그 이유도 적어 뒀었다 — 데이터시트에서 A.e·A.min·A.N·l.N 만 갖고 있었지 **창 치수를 몰랐다.** 그래서 먼저 근거부터 구했다: **TDK PQ 40/40·32/30 데이터시트를 받아 `Reference/` 에 넣었다**(CDN 이 기본 UA 를 403 으로 막아 브라우저 UA 가 필요했다). 치수도에서 나온 것 — 코어 40.5 × 39.8 · 창 높이 29.5 · 중앙다리 ø14.9, 보빈 **관 ø17.5 · 보어 ø15.5 · 권선 폭 25.4 · 플랜지간 29 · 핀 12개**. **라벨이 없는 외각다리 안쪽면은 PDF 의 벡터 좌표를 직접 읽어 r = 13.60 mm 로 쟀다** — 같은 뷰의 라벨 둘(40.5·39.8)이 2.1455 pt/mm 를 0.03 % 안에서 서로 확인해 주고, 그 척도로 중앙다리를 재면 **14.94 mm** 가 나와 라벨 14.9 와 맞는다. **축척은 주장이 아니라 좌표계로 만들었다** — `_mm_ax()` 가 패널의 *지면 크기*에서 mm/inch 를 계산해 xlim·ylim 을 잡고 aspect 를 equal 로 둔다. 즉 데이터 단위가 곧 mm 라 틀리려면 코어 모양 자체가 틀려야 한다. 그림은 **`cores.winding()` 이 내는 배치를 그리기만 한다**: 설계 전류 10.93 A 와 J = 4.5 에서 1차 2.43 mm² → 리츠 310 × ø0.10, **번들 ø2.37 mm** 를 2층 3+2 로 7.11 mm, 2차 2.10 mm² → **포일 0.20 × 10.5 mm**, 마진 2 mm × 2, **남는 분리 간격 3.79 mm**. `report()` 가 그 산수를 인쇄하고 안 들어가면 `gap` 이 음수로 나온다. 패널 셋 — 단면(1:1) · 보빈 전개(2.7배) · **포일 적층(8배, 1:1 에서는 4층 1 mm 가 한 줄로 뭉친다)**, 각 패널이 자기 배율을 적는다. 설명은 **번호 풍선 + 옆의 범례**로 바꿨다 — 긴 문장에 지시선을 달아 40 mm 짜리 코어를 가로지르던 것이 "못 알아보겠다" 의 정체였다. 권선 스케줄 표와 개방/단락 인덕턴스도 사양서처럼 넣었다. **중앙다리는 갭을 그려 넣었다** — 풍선 5가 통짜 페라이트를 가리키고 있었다. `figcheck` 이 그림에서 **0건**, `an_check` 기하 문제 0, 영문 70쪽 |
| 2026-09-21 | **(5차) 설계점 4번째 — 8 : 1 (2 × 8:2) 시트와 벤더 사양서.** 사용자 질문 "3 × 5:2 대신 2 × 8:2 면 어떤 부작용·이점·단점이 있나" 에 수치로 답한 뒤, 사양서를 요청받아 만들었다. **막고 있던 것은 `N.x` 가 빌더에 `3` 으로 박혀 있던 것** — 변형 파라미터로 뺐고 기존 셋은 `Nx=3` 이라 **값이 하나도 안 움직인다**(9:1·7.5:1·6:1 재검사로 확인). 8:1 은 탱크를 7.5:1 과 똑같이 두고(`C.r` 100 nF · `L.r` 11 µH · `L.m` 20 µH · λ 0.55) `n.T` 만 8 로 올린 점이라 **`n` 이 6.024 → 6.426(+6.7 %)** 이고 그것 하나가 전부다: `k.ZVS` 2.091 → **2.364**(최악점이 FB 코너 25 % 부하에서 저코너 전부하로 되돌아온다) · `k.ceil` 1.283 → **1.348** · `k.lam` 0.443 → **0.537** · `Q.pk` 0.766 → 0.673 · `k.PSR` 1.063 → 1.033 · `k.Ploss` 0.311 → 0.296. **`B.pk` 는 182.1 mT 로 안 움직인다** — `B.pk = V.o_eff/(4 f.r N.s A.e)` 라 `N.p` 도 코어 개수도 안 들어가므로 `N.s` = 2 를 지키는 한 `k.Ae` 1.098 그대로다. **자기 쪽이 대가를 치른다**: `A.L` 413.3 → **242.2 nH**(코어당 `L.open` 10.33 → 15.50 µH) · 코어당 2차 실효 9.45 → **14.37 A**(+52 %) · 피크 32.9 → **51.2 A**(+56 %) · 나권 동선 +59 %. `n.aux` 1.5 → **1**(`N.aux` = `n.aux`·`N.s`/`N.x` 로 코어당 1턴 유지), `R.ZCD_H` 330 → **220 kΩ**, OVP1 28.93 V. **`k.floor` 는 1.021 로 7.5:1 과 같다** — `f.o` 가 같은 탱크에서 나오기 때문이고, 따라서 시트 §4.2 항목 5b 의 `R.T` 11 → 10 kΩ 경고가 8:1 에도 그대로 적용된다. **사양서가 개방 인덕턴스에 ±10 % 를 거는데 이 `k.floor` 가 버티는 저하는 4.0 % 뿐이므로, 8:1 을 채택하면 사양서를 내기 전에 `R.T` 를 먼저 내려야 한다.** **같이 드러난 결함 3건** — ① **`Smath/variants/L6790A_9to1.sm` 이 7.5:1 빌드와 바이트 단위로 같았다.** `cmp` 로 확인했고 `Transformer_Spec_9to1.xlsx`(Np 3 · Ns 1 · `L.open` 11.67 µH · `A.e` 206) 쪽이 맞다 — 9:1 시트만 낡은 것이라 다시 빌드했다(`mismatches: 0` · `VERDICT OK`). ② **`check_trans_spec.py` 의 루프가 누적 `bad` 를 보고 `continue`** 해서, 9:1 하나가 어긋난 순간 나머지 셋을 통째로 건너뛰고 있었다 — 없는 파일 판정을 그 변형에만 걸도록 고쳤다. 그러지 않았으면 8:1 사양서가 **검사되지 않은 채** 나갈 뻔했다. ③ **`build_trans_spec.py` 가 `openpxxl` 오타로 import 단계에서 죽어 있었다**(Drive 임포트 커밋). 그리고 같은 파일이 코어 개수("Three identical transformers")와 보조 턴수(`1 T`)를 **글로 박고 있어** `N.x` = 2 에서 거짓말을 했다 — 둘 다 시트에서 읽도록 고쳤다. `check_trans_spec` 4종 36항목 **불일치 0 건** · `check_sm` VERDICT OK · `audit_rpn` 0 · `check_theta` 0 |
| 2026-09-21 | **(4차) 사용자 지적 11건 — 데드타임·2.7절·리플·흰 글자·탱크 전류·그림 크기, 그리고 트랜스포머 설계 전면 보강.** ① **그림 6·7 데드타임 (a)** — 두 C_oss 루프를 탱크 위에 겹쳐 그려 "전류가 겹쳐 이해가 안 간다". 탱크에 **전류 한 줄**, 레그에서 네 갈래로 쪼갰다. "왜 브릿지끼리 반대인가" 는 맞는 관찰이고 물리다 — KCL 로 `dv/dt = ∓i/(2 C_oss)`, A 로는 모여들어 내려가고 B 에서는 나가며 올라간다. 그 문장을 칸에 적었다. ② **2.7절** — 회로 그림 `an_recovery` 신설(용량성: S1 이 도통 중인 D_S2 에 닫혀 레일 단락 / 유도성: 자기 바디 다이오드에 0 V 로 닫힘)과 `an_cap_ind` 에 **닫히는 스위치의 드레인 전류** 행 추가 — 용량성에 역회복 스파이크, 유도성에 바디 다이오드 음전류(onsemi AN-4151 Fig 12 형식, 두 열 같은 축척). 본문에 절 신설. ③ **그림 19 리플** — 2f_l 은 맞았으나 **위상이 뒤집혀 있었고**(+sin → 골이 θ=45° 가 아니라 마루) **진폭이 13배 과장**(±32 %)이었다. `−sin` 으로 고치고 274 µF 에서 계산한 **5 % pk-pk** 를 제목에, 평균선을 점선으로, 과장했다고 꼬리말에 명기. ④ **그림 28 흰 글자** — 원인은 `shield()` 가 흰 글자에 흰 후광을 입힌 것(막대 밑 격자선). `_white()` 로 제외. `ink()` 보고에서도 제외(어두운 면 위 흰 글자는 읽힌다). ⑤ **그림 13·14 탱크 전류** — 완전 정현파에 게이트도 데드타임도 없었다. `figs_modes8.series()` 로 8구간 모델을 내보내 **그림 13 은 게이트·중점·전류 3단**, **그림 14 는 천이 확대**로 다시 그렸다. 스위칭 순간의 전류가 `i_Lm` 피크(12.41 A)이고 합성 피크가 18.32 A 로 설계값과 일치한다. `T_ZC` 는 **턴오프 기준**으로 그린다(끝에서 재면 정확히 t_D 만큼 짧다). ⑥ **그림 31** — 실제 오류 2건: 상단 T-모델 션트가 `L_m` 으로 적혀 있었으나 **`L_μ = √(L_m(L_m+L_r))`** 이다(20 vs 24.9 µH). 하단 이상변압기가 **`1 : M_v`** 로 뒤집혀 있었다 → `n : 1`, `n = n_T/M_v`. 직렬 소자도 식과 같은 `L_L1·L_L2` 로. ⑦ **권선 턴 수** — `xfmr` 가 코어 높이를 고정 반경으로 채워 10턴 넘게 그렸다. 최대 5/4 턴으로 묶고 반경은 유지, 나머지는 리드로. 코어 막대도 권선 범위만. `schem.ind` 도 최대 4턴. ⑧ **트랜스포머 장 보강** — 절 7개 신설: 코어 선정(A_e·A_min·창 A_N, 그리고 **단일단에서는 누설이 결정한다** — `L_short/L_open = λ/(1+λ) = 35.5 %`) · 권선 배치가 곧 누설(인터리브 금지, 나란히) · 갭과 A_L(도면에 갭을 적지 말 것) · 창 예산(J·k_u) · 표피 깊이(**0.194 mm @ 151.7 kHz, 100 °C**, 계산값) · 절연 마진 · 손실과 온도. ⑨ **설계 예제** — 코어 선정 단계 신설: **TDK PQ 32/30 · PQ 40/40 실 데이터**(`cores.py` 에 데이터시트 값 그대로) 비교표, 둘 다 자속은 통과하고 **창이 갈라 PQ 40/40 채택**. 단면도 `an_core_section`(코어 단면 + 보빈 전개, 나란히 배치와 분리 간격). 사슬표 19행을 **전부** 대입 계산으로 펼쳤다(1·2·3·4·10·14·15·16·17·19 추가). ⑩ **그림 크기** — `figcheck` 에 **`tiny` 검사** 신설: `an_body` 에서 배치 폭을 읽어 지면 배율과 글자 크기를 계산한다(핵심은 `글자 pt / 그림 in`, dpi 는 상쇄된다). 지적된 10장이 정확히 **0.27~0.41** 이었다. 모든 그림을 **전폭 배치**(`width=CW*x` 21곳 삭제)하고 figsize 를 9.35 in 이하로 줄여 **0.70** 으로. 8구간 시트는 2×2 를 **2패널씩 4장**으로 쪼개 패널 폭이 3.3 → 6.5 in 이 됐고, 쪽 높이 상한을 430 → 560 pt 로 올렸다. 그 대가로 생긴 겹침 21건(브릿지 다이오드 이름·소스 이름·블록 라벨·패널 제목 등)을 전부 정리했다. `figcheck` 43장 16건 → 실제 겹침 0. **영문 70쪽 · 그림 39 · 식 45 · 표 31.** 한글판은 **구조만** 따라갔다(모드 그림 4장 이름·캡션, 전폭 배치) — 언어 검토는 사용자 컨펌 대기 |
| 2026-09-21 | **(3차) 영문 AN 전수 낭독 검토 — 사용자 지시("비원어민이 이해하기 어려운 문장과 단어를 알기 쉽게 · 동작원리/파형/회로도/그래프 오류 재검토 · 컨펌 후 한글판").** `an_body.py` 1~9장을 한 줄씩 읽어 관용구·비유·긴 문장을 평이한 영어로 바꿨다(1~2장 약 40곳, 3~9장 69곳). **정정 4건** — ① 게인 여유 절: "근호 안이 음수면 위상이 −180° 에 닿지 않아 GM 이 무한" 은 틀렸다. 위상 = −180° + atan(f/f_z) − atan(f/f_p) − atan(f/f_px) 이라 f_p·f_px < f_z(f_p+f_px) 이면 위상이 **모든 주파수에서 −180° 아래**라 어느 교차점에서도 위상 여유가 음수(불안정)다 — 시트 §16.4b 의 `f.180` 식과 같은 조건. ② 3차식 근 서술: "x 가 커지면 −∞" 는 최고차 계수 λ² > 0 이라 틀렸다 — x → −∞ 에서 −∞ 로 정정. ③ "위쪽 공진 위에서 운전" → "유도성 영역", 용량성 경계는 피크가 아니라 부하에 따라 움직인다(1~2장). ④ "실현 f_sw,max 가 f_r 에 가까워지면 발산" 을 가정법으로. **표기 통일** — 브리지 그림 f15·f16·f17 과 4장 본문의 Q1~Q4 를 8구간 시트·Infineon 그림과 같은 **S1~S4** 로(같은 위치·같은 쌍). **그림 4건** — `f04` 절대 kHz → `f_r` 배수(그림 5 와 같은 규칙) · `f11` 범례가 45° 주석을 가려 우측 상단으로 · `f19` 막대 안 흰 글자가 `shield()` 흰 후광에 지워져 있었다 → `_white()` 제외, 막대 중심 격자선 제거(막대를 지운 두번째 렌더에서 잉크로 읽혔다), 막대 0.56→0.78 · 눈금 라벨 3줄 · `an_above_below` ABOVE 상자를 범례 아래로. `figcheck` 0 건 · `an_check` 0 · 58쪽. **한글판(`an_kr_body.py`·KR PDF)은 손대지 않았다 — 사용자 컨펌 대기.** 컨펌 후 같은 69+40곳과 Q→S 를 한글판에 옮긴다 |
| 2026-09-21 | **(2차) 데드타임 전류 경로 · 접점 규칙 · 픽셀 겹침 검사 · 그림 5/19.** ① 사용자 질문 "MOSFET 이 꺼졌는데 왜 전류가 몸통으로 흐르나" — 맞는 지적이었다. 모드 3·7 이 프리휠링 경로를 재사용해 꺼진 채널을 지나고 있었다. `cs()` C_oss 우회와 **레일마다 하나씩 두 루프**(위: S1→탱크→S3, 아래: S2→탱크→S4)로 다시 그렸다; 모드 4·8 은 원래 맞았다. ② 사용자 규칙 "세 가닥 이상 아니면 점은 의미 없다" — 스켈레톤의 모서리 점 3개(탭 라이저 꼭대기·D1·D2 꼭대기) 삭제, `an_rac` 의 단자 점 2개도 삭제(저항만 남김), `figcheck` 에 `stray-dot`. ③ "여전히 겹침이 많다" — 검사기가 꼭짓점만 봐서 2점 배선 위의 글자를 전부 놓치고 있었다(0건 보고). 선분 검사 → 44건, 그중 대부분 글상자 스침. **렌더 두 번(글자 alpha 0)으로 잉크를 직접 잰다.** 큰 패치 면·강조띠는 배경으로. 실재 겹침: `an_rac` i_ac 가 배선 위, 5·6 칸 화살촉이 'centre tap' 위, 모드 3·7 의 새 주석이 칸 밖 — 전부 이동. `shield()` 도 픽셀 기준, 회로도 제외. ④ 그림 5 의 90.4/151.7 kHz → `0.60 f.r`/`f.r`(정규화 축에 절대값). ⑤ 그림 19 재구성 — 블록 2.2 높이, 파형 0.25 fig, 노드로 점선 유도선, 0 선, 정류 파형 단극, mains 에 전류 추가. `figcheck` 0 건 · `an_check` 두 판 0 · **남은 일: 영문판 전수 낭독 검토(사용자 지시, 컨펌 후 한글판)** |
| 2026-09-21 | **AN 회로도·파형 전수 재검증 — 사용자 지시(모델 교체 후 "잘못 그려진 노드·기호 크기·글자 겹침·틀린 파형·그림 6/7 의 3,4 누락").** 고치기 전에 진단기를 심었다: `figcheck.py` 에 배선 검사 5종(`no-dot`·`open-end`/`stub`·`crossing`·`off-grid`·`clipped`). 첫 실행 95건, 전부 실재. **키트 결함 둘** — `schem.cap` 단자가 ±s(판 길이)라 모든 커패시터가 판에서 떨어져 있었고, `S.diode` 한쪽 단자가 바에서 s 만큼 떨어져 있었다. **그림 결함** — `an_rac` 다이오드 반대(출력 뒤집힘) · `an_integrated` 1차·2차 리턴 공유(절연 단락)·정류기 없는 C_o·기본파에 구형파 기호·떠 있는 V_RO^F · `an_fha_steps` 1차 리턴 미연결·9° 사선·기호 60 % · `an_pfc_boost` 상단 레일이 부하까지 안 감·V_bus 가 − 레일 아래 · `an_pfc_cap` −v_C 미표기 · `an_two_stage` 상자 밑을 지나는 배선 · `an_architectures` 잘린 load 상자 · 브리지 다이아몬드 1.55×1.35 라 팔이 4° 틀어짐 · 접점 점 누락(C 상하·L_m 하단·레그/레일 T·D2 리턴). **파형 3건** — `an_llc_waves` i_S1 이 8구간에서 0(주기 시작과 모순) · `an_three_cases` above 의 i_Lr 수직 점프(인덕터 전류) · `an_zvs_zcs` 'cut off' 점선이 안 잘림. **기호 크기 바닥 규칙**(`REF_FIGSPAN` 46) — 종이 위에서 패널보다 작아지지 않는다; `subplots_adjust` 는 그리기 전에. **그림 6/7 교체** — `an_op_power`/`an_op_free`(1,2,5,6) → `an_modes_1234`·`an_modes_5678`·`an_modes_wave`(번호 띠). 영·한 캡션과 주의 상자 재작성. 타임라인 모델은 Toshiba mode 4 와 일치해 유지. **승인된 8구간 패널은 접점 점 5개 외 픽셀 동일**(블롭 대조). `figcheck` 0 건 · `an_check` 양 판 기하 문제 0 · 그림 35장 |
| 2026-09-11 | **가이드를 깔끔한 설계 가이드로 정리 — 사용자 지시("불필요한 코멘트·이력 다 지우자").** 2432 → 2313행. 지운 것: 머리의 개정 이력 6블록(Rev 0.2–2.0) · 본문의 "(rev 1.1)"·"rev 0.x 까지는" 식 이력 주석 9곳 · `HISTORY.md`/`CLAUDE.md`/스크립트/워크북 셀 참조 · 프로젝트 사정(본사·LGE·미결·변형·정본) · 부록 B 의 낡은 변수 표 · C.11 의 프로브/1쪽 인쇄본 주석 · **C.32–C.34**(시트 `k.*` 읽는 법 · 사양서 손입력 사고 · 6:1 `C.T` 사고 — 가이드가 아니라 프로젝트 사고 기록). **정리하다 잡은 구 예제 잔재 12곳**: §3.1 사양표(홀드업 10 ms/50 V → 12 ms/19 V · 리플 10 → 5 % · $N_{rect}$ 2 → 1 · $c_{HB}$ 500 → 800 pF) · §5.1 오실레이터 주석 2개($R_T$ 13 k/77.4/88.05 kHz → 11 k/92.25/85.07, $f_{Max}$ 240.9/241.8 → 209.5/248.2) · §5.2 [78] 주석(246.99/244.90 W → 684.8/670.9) · 보드선도 캡션(19.8 Hz/49.1° → 17.6/49.6) · [134][135] 선정값 주석(195.71/1154/180/20.68 → 77.88/525.5/82/45.72) · 이득 여유 블록(49.20/6.418/206.0 Hz/34.3 dB → 47.34/6.047/202.4/34.2, 시트 `f.180`·`GM`) · SR 패키지 피크 49.2 → 57.8 A · [115] 12 → 14 % · C.1 표($R_T$ 13 k 기준 → 11 k) · C.23($R_P$ 1.4 k/E96, $R_B$ 16.5 k → 1.3 k/6.2 k) · D.3 실측 항목 7·8(246 ns/241 kHz → 530 ns/209.5 kHz). 캡션 끝의 `.**` 오타 6곳. 새로 적은 것: $T_{idle}$ 700 ns 이면 $f_{Min}$ 85.2 kHz 로 $f_o$ 와 사실상 같다(§5.1). `audit_guide.py` 구조 문제 0건. 백업 `%TEMP%/guide_before_clean.md` |
| 2026-09-11 | **가이드 rev 2.0 — 예제를 9:1 정본으로 전면 교체.** 본문에 세 시대의 숫자가 섞여 있었다 — 240 W/60 V 잔재(C.18 표 · 소자 손실), 7.5:1 설계점의 본문 §3–§6 · 설계 요약 · C.21/C.28/C.30(457 ns · 249.6 kHz · 18.32 A · $Q_{pk}$ 0.772 · $R_{ZCD,H}$ 330 k · OVP1 27.4 V), 9:1 의 트랜스포머 절. §3 19블록 · §4 14 · §5–6 15 · 요약/부록 7 을 일회용 치환 스크립트(세션 스크래치, 보관 안 함)로 바꿨고 값은 전부 9:1 시트와 `l6790.py` 에서 읽었다(예: $Q_{pk}$ 0.5001 · $T_{ZC}$ 529.8 ns · 108.5/209.5 kHz · $I_{Lr,pk}$ 17.70 A · OCP1 22.92 A 29 % · $I_{diode}$ 30.31 A · SR 레그 3.23 W). **새로 적은 것 세 가지** — ① $f_{sw,max}$ 는 탱크를 검사하는 값이 아니라 λ 를 계산하는 *입력*(가이드 §3.6) ② 공진 위/아래는 $n$ 이 정한다 — 9:1 은 실사용 라인에서 반주기 내내 아래, 7.5:1 은 110 V 부터 위(가이드 §4.2 표) ③ 5점/7점 Simpson 이 2000구간 격자 대비 0.0013/0.0011 % (C.18). ZCD 절은 VCC 외부 12 V · 보조권선 ZCD 전용 · [93] 비적용 · 19 k/680 k · 28.21 V 로 다시 썼다. C.21 은 $Q_{ZVS,3}$ 가 9:1(0.628 > $Q_{pk}$)에는 안 걸리고 7.5:1 에만 걸린다고 고쳤다 — 워크북 `Res. Tank Design D20` 에서 읽었다. 그림 6종은 `AN_VARIANT=9to1 python figs.py` 로 재생성(`figures/an/` 은 손대지 않았다 — AN 은 7.5:1). `figs.py` 의 tank_current 꼬리말이 "12 %" 를 손으로 갖고 있어 계산값으로 바꿨다. **남긴 것:** 부록 C.5 검증(240 W, $N_{rect}=2$ 에서만 나타나는 결함) · C.30/C.31 의 240 W·7.5:1 비교 행 · 옛 개정 이력. 파일 이름도 `rev2_0` 으로 바꾸고 참조 5곳(`CLAUDE.md`·`HISTORY.md`·`audit_guide.py`·`audit_sm.py`·`build_l6790_smath.py`)을 같이 고쳤다(사용자 지시). `audit_guide.py` 구조 문제 0건 |
| 2026-09-09 | **시트의 목적은 가르치는 것이 아니라 설계 뼈대를 만드는 것 — 사용자 결정.** 코멘트는 **행에서 얻을 수 없는 것**만 담는다 — 노란 셀이 들어갈 숫자나 범위, 무시하면 부품이 깨지는 핀 규칙, 검사가 실패했을 때 바꿀 셀, 거꾸로 알기 쉬운 단위·반올림 방향. 그 밖은 전부 삭제 — 왜 그 식인지, 바디 다이오드가 무엇인지, 데이터시트 읽는 법, ST 툴이 뭘 틀렸는지, 그래프 보는 법. 그건 설계 가이드 문서가 할 일이다. **164 → 54개 · 36,476 → 3,944자(−89 %) · 가장 긴 것 143자 · 시트 55.0 → 50.0쪽** |
| 2026-09-09 | **검사기가 못 보는 곳에서는 검사기가 성공을 보고한다.** 이번 세션의 모든 스캐너가 `ast.literal_eval` 로 코멘트를 읽고 실패하면 건너뛰었는데, **`%` 서식으로 만든 코멘트 7개는 리터럴이 아니다.** 하필 그것들이 계산값을 산문에 박아 넣은 것들이었고, 그래서 **바로 그것을 잡으라고 만든 `audit_notes` 가 0을 보고했다** — 여섯 개가 값을 인용하고 있는 동안. 사용자가 보조권선 코멘트를 짚어 드러났다. 일곱을 고치고, `audit_notes` 는 이제 서식 코멘트의 문자열 조각을 이어 붙여 읽는다 |
| 2026-09-09 | **시트는 이 프로젝트의 문서가 아니라 L6790A 의 설계 도구다 — 사용자 결정.** 누군가 열어 노란 셀을 바꾸고 답을 읽는 물건이다. 그런데 코멘트에 **우리가 정한 것들이 박혀 있었다** — LG 보드의 평형 트랜스 3직렬, 스위치 위치당 1개, 자리표시자 EE6405, 시제품에서 정할 값. 이것들은 시트를 여는 사람에게 **앞에 있는 것과 다른 설계를 설명하므로** 쓸모가 없는 정도가 아니라 해롭다. 4건 삭제 · 9건을 일반론으로 · 시트 제목에서 "25 V / 26.3 A OLED TV SMPS" 제거. 남기는 기준은 **어느 L6790A 설계에나 성립하는 것** — 데이터시트 창, 핀 규칙, 검사가 진짜 묻는 것, 그리고 부품과 ST 스프레드시트의 함정 |
| 2026-09-09 | **줄 이음매에서 띄어쓰기가 122곳 사라졌던 것도 인쇄물이 잡았다.** 코멘트를 줄이면서 `textwrap` 결과를 붙일 때 끝 공백을 안 남겨 "Eachcurve" "thepeak locus" 처럼 나왔다. **수식도 같은 방식으로 여러 줄에 쪼개져 있어** 일괄 치환은 식을 깨뜨린다 — AST 로 코멘트 인자 안의 이음매만 골라 고쳤다 |
| 2026-09-09 | **빈 용지의 진짜 이유 — 접힌 블록은 세로 공간을 차지하지 않는다.** SMath 는 접힌을 회색 한 줄로 그리고 아래 내용을 끌어올려 인쇄한다. 이 시트는 20곳에 17,012 px 를 접어 두었으므로 **절대 y 기준 페이지 계산은 SMath 와 수천 px 씩 어긋난다.** 게다가 한 쪽이 담는 것은 용지 1169 px 가 아니라 여백과 꼬리말을 밀어낸 **1117 px** 다 — 시트와 인쇄 PDF 에서 유일한 문자열 451개를 짝짓어 재었다. `Sheet.vy()` 와 `PAGE_CONTENT` 을 넣고 `check_sm` 도 같은 좌표로 바꿨다. 문서 중간의 빈 용지 2장이 0 이 됐고, 마지막 꼬리 한 장은 남았다(모델 오차 약 2 %) |
| 2026-09-09 | **코멘트를 44 % 줄였다 — 36,476 → 20,321자, 중앙값 185 → 129, 260자 넘는 것 0.** 사용자 지적: "이렇게 길게 쓰면 어차피 읽지도 않는다". 기준은 하나다 — **지시와 함정만 남기고 유도·정당화·이력은 버린다.** 쉽은 말로, 한 문장으로 |
| 2026-09-09 | **`ZS.fit()` 이 끝의 gap 을 빼먹어 빈 용지 5장을 만들었다.** 블록이 정확히 한 쪽 높이가 되고 그 뒤에 `S.gap` 8 px 이 더해져 다음 쪽 8 px 지점에 떨어졌고, 그러면 다음 리전이 또 한 쪽을 내려가면서 사이 쪽이 통째로 비었다. **예산에서 한 항이라도 빠지면 빈 용지가 된다** |
| 2026-09-09 | **쪽 경계를 예측하려던 시도가 실패했고, 실패한 것이 맞다.** 12:18 인쇄물로 (쪽높이 · 원점 오프셋 · 접힘당 보정) 세 미지수를 다 열어 놓고 풀었는데 **해가 없다.** 앵커 18개는 피치 ≈ 1118 을 요구하고 갈라진 행 하나는 ≥ 1121.6 을 요구하는데, 둘을 동시에 만족하는 조합이 존재하지 않는다. **그리고 그 앵커 18개는 애초에 측정값이 아니었다** — 값이 14551 · 15668 · 16785 · 17902 로 **정확히 1117 간격**인데, 그건 `fit()` 이 그 행들을 경계+SAFE_TOP 자리에 밀어 놓은 결과다. 모델이 자기 메아리를 채점하고 있었다. **독립적인 관측은 갈라진 행 하나뿐이고 그것 하나로는 피치가 안 정해진다** |
| 2026-09-09 | **그래서 예측을 안 쓰는 해법으로 갔다 — 라벨과 값의 높이를 같게 했다.** `row()` 는 둘을 **같은 y** 에 쓰면서 **높이만 다르게**(라벨 25 px · 수식 62 px) 준다. SMath 는 경계를 걸치는 리전을 **자르지 않고 다음 장으로 옮기므로**, 경계가 그 두 높이 사이에 떨어지면 수식만 가고 라벨이 남는다 — 인쇄물의 `I.mos_rms` 가 정확히 그것이다. `_label(pair=hgt)` 로 라벨이 값과 같은 높이를 예약하면 **경계가 어디든 둘이 같이 가거나 같이 남는다.** 쪽 높이를 몰라도 성립하는 것이 요점이다 |
| 2026-09-09 | **`Smath/L6790A_ruler.sm` 신설 — 경계를 풀지 말고 읽는다.** 20 px 간격 · 20 px 높이 눈금만 8쪽. 수식이 없어 F9 가 무관하고, 접힘이 없어 절대 y = 보이는 y 이고, 판이 없어 다른 이유로 밀릴 것이 없다. 쪽마다 첫 눈금을 읽으면 경계가 20 px 안에서 정해지고, 8쪽이면 독립 관측이 7개다. `build_ruler.py`. **`check_sm` 이 이 시트에는 11건을 보고하는데 정상이다** — 눈금을 일부러 경계에 걸쳐 놓았다 |
| 2026-09-09 | **여백 띠를 위 60 / 아래 60 으로 올렸다** (사용자가 30 도 부족하다고 두 번 지적). 시트 40 → 42쪽. 이음매까지 가장 가까운 거리 41 → 64 px |
| 2026-09-09 | **인쇄물의 그래프 7장이 전부 비어 있었던 것은 F9 미실행이다.** 값은 빌더가 써 넣은 `<result>` 라 그대로 인쇄되지만 플롯 행렬은 prog 블록이라 재계산 전에는 비어 있다. CLAUDE.md 시트 §9.3b 에 이미 적혀 있던 함정이다 |
| 2026-09-09 | **여백 띠를 위 30 / 아래 30 으로 나눴다** (사용자 지시). 처음에 아래 40 · 위 20 으로 둔 것은 "넘치는 쪽은 아래 이음매"라는 이유였는데, 아래를 크게 잡으면 **다음 장이 그만큼 없이 시작**한다 — 밀려 내려간 행이 이음매에서 20 px 자리에 앉아 딱 붙어 인쇄된다. 합은 같고 양 끝에 반씩. 그리고 **`check_sm` 이 이제 "이음매까지 가장 가까운 거리"를 매번 인쇄한다** — 걸침 0 은 안전과 같은 말이 아니다. 리전 높이는 전부 `est_box` 의 *추정*이라, 이음매 4 px 위에서 끝나는 리전은 추정이 한 번만 짧으면 넘어가고, 넘어가면 **SMath 가 그것만 다음 장으로 옮기고 라벨은 두고 간다** — 행과 값이 갈라지는 것이 그것이다. 지금 가장 아슬아슬한 곳이 41 px 다 |
| 2026-09-09 | **용지 걸침의 원인은 페이지 높이가 아니라 접힘을 두 번 뺀 것이었다.** 사용자가 뽑아 준 40쪽 PDF 에서 라벨과 값이 갈라진 행이 셋(12→13 `I.mos_rms` · 32→33 `R.BM_rec` · 36→37 `L.short`), 쪽 한가운데 빈 띠가 셋(35·37·38) 나왔다. **시트 §16.6 에 `S.area_end()` 가 하나 빠져 접힘이 중첩**돼 있었고, `area_end` 는 자기 span 을 매번 `_hidden` 에 더하므로 안쪽 814 px 이 두 번 빠졌다. 그 아래로는 모델이 시트를 3/4쪽 짧게 알고 있었고 `fit()` 이 그만큼 이른 자리에서 쪽을 넘겼다. **`PAGE_CONTENT` 1117 은 처음부터 맞았다** — PDF 에서 읽은 쪽 첫 행 22개로 검증했고, 중첩을 한 번만 빼면 **22개 중 21개와 쪽수 40 이 정확히 맞는다**(남은 하나는 SMath 자신이 다음 장으로 민 행이다). 고친 곳 셋: `area_end` 는 바깥 접힘이 열려 있으면 더하지 않는다 · `check_sm` 은 구간을 병합해서 뺀다 · **`area_begin` 은 접힘 안의 접힘을 빌드 오류로 거부한다** |
| 2026-09-09 | **쪽 경계를 재서 확정했다 — 1117 이 아니라 1120 이고 원점이 4 다.** 앞의 두 줄이 보여 주듯 인쇄물에서 역산하는 시도는 두 번 다 실패했다. 실패한 이유가 분명했다 — 앵커로 쓴 행들이 `fit()` 이 경계에 밀어 놓은 행이라 **모델이 자기 메아리를 채점**하고 있었고, 독립 관측은 갈라진 행 하나뿐이었다. 그래서 **역산을 그만두고 자를 만들었다**: `build_ruler.py` → `Smath/L6790A_ruler.sm` 은 20 px 간격 눈금과 자기 y 를 적은 글자뿐이고 **수식·접힘·판이 하나도 없다** — 그래서 인쇄하면 SMath 의 경계 말고 탓할 것이 없다. 사용자가 9쪽을 뽑아 주었고 첫 눈금이 `24 · 1124 · 2244 · 3364 · 4484 · 5604 · 6724 · 7844 · 8964`, 각 쪽 마지막 눈금이 다음 쪽 첫 눈금에서 정확히 끝난다(1104+20 = 1124). 즉 **`break(N) = N × 1120 + 4`**. 1117·오프셋 0 은 용지 크기에서 추론한 값이었고, 쪽당 3 px + 원점 4 px 이라 40쪽에서 124 px — 쪽 높이의 1/9 이 어긋난다. **위 여백이 쪽마다 다르고 아래만 휑하던 증상이 전부 이것이었다.** `smsheet.PAGE_CONTENT/PAGE_ORIGIN` 과 `fit()`·`zedsheet.fit()`·`check_sm` 을 같은 식으로 맞췄다. 결과: **42쪽 전부 위 여백 정확히 60 px** (예외 0), 아래 61–193 px, straddle 0, seam clearance 61 px. 34쪽 아래만 481 px 인데 다음이 648 px 짜리 보드선도 판 2장이라 밀린 것이고 정상이다. **교훈: 추론한 상수를 실측으로 착각하지 말 것. `build_ruler.py` 는 지우지 말고, 용지나 SMath 버전이 바뀌면 다시 인쇄해서 읽는다** |
| 2026-09-09 | **SMath 저장이 레이아웃을 무너뜨린다 — 리전 1908개 전수 대조로 확인.** 사용자가 "일부 수정했더니 엉망이 됐다"며 다듬어 달라고 했다. 재빌드하기 전에 그 파일을 떠 놓고 같은 빌드와 리전 단위로 맞춰 봤다. **내용은 완전히 동일**(1908/1908, 한쪽에만 있는 것 0) 한데 좌표는 top 1837 · left 1851 · height 1849 개가 달랐다. 사용자가 실제로 한 것은 둘뿐이다 — **접힘 헤더 4개를 끌어 내려 y≈23895 에 겹쳐 쌓았고**(원래 20813·21331·22158·22954, `check_sm` 의 겹침 7건이 정확히 그 자리), **판 2장의 세로를 늘렸다**(618→813 · 949→1000; 같은 949 인 `Tp` 와 648 인 보드선도 판은 그대로라 SMath 의 자동 최소높이가 아니라 손으로 끈 것이다). 그런데 **그 여섯 군데를 전부 제자리로 돌려 놓고 다시 재도 18건이 남았다** — 겹침 3 · 걸침 14 · seam −30 px. 즉 나머지는 **저장 자체의 효과**다: SMath 는 저장하면서 라벨을 실제 높이로 줄이고(138 → 23 px, `pair` 예약을 지운다) 위치를 ±1~4 px 씩 흔드는데 그것이 아래로 누적된다. **재빌드한 시트는 겹침 0 · 걸침 0 · seam 61 px 로 깨끗하다.** 그래서 결론은 하나다 — `.sm` 의 정본은 빌더이고, 인쇄는 **열기 → F9 → 인쇄 → 저장 없이 닫기**. 저장본이 생기면 고쳐 쓰지 말고 무엇이 달라졌는지 재서 빌더에 역이식한다. CLAUDE.md 시트 §11.1 D-25 · 시트 §9.3b |
| 2026-09-09 | **인쇄물이 쪽 모델을 채점했고 42/42 로 통과했다 — 그리고 진짜 결함 하나가 드러났다.** `PAGE_CONTENT` 1120 을 넣고 F9 까지 눌러 뽑은 42쪽 판을 받았다. 먼저 **각 쪽의 첫 행을 파일만 보고 예측해 PDF 와 대조**했다 — 42쪽 전부 일치. 앞선 두 번의 역산이 실패한 이유(앵커가 `fit()` 이 경계에 밀어 놓은 행이라 모델이 자기 메아리를 채점)가 여기엔 없다. 예측은 파일에서만 만들고 인쇄물은 독립 관측이다. **쪽 경계 문제는 이것으로 닫혔다.** 그다음 눈으로 42쪽을 훑어 결함을 찾았다: **접힌 블록이 그리는 가로줄이 바로 아래 행을 관통**한다. `area_end()` 가 6 px 만 띄우는데 그 줄은 한 텍스트 줄만큼 두껍다. 열 군데에서 났고 **20쪽은 노란 입력 `R.T := 11 kohm` 을 그어 놓아 지운 숫자처럼 읽힌다** — 노란 셀은 사람이 고치는 자리라 가장 나쁜 자리다. 24 px 으로 올렸고 그 대가로 42 → 43쪽이 됐다. **SMath 는 접힘·종료 리전에 `height` 를 안 적으므로(전부 `None`) 저장본에서도 못 잰다** — 인쇄물의 부족분 6 px 만 근거이므로 다음 인쇄에서 확인해야 한다. 함께 고친 것: 시트 §10 의 **라벨 없는 네 식**(`I.lm.0`·`P.0`·`S.a`·`P.a`)에 설명 한 줄을 붙였다 — 사용자가 이전 인쇄물에서 동그라미 친 것과 같은 부류인데 cardano 수정이 닿지 않은 자리였다. 이 넷은 Simpson 합의 **양 끝점**이다 |
| 2026-09-09 | **13:44 인쇄물 전수 검토 — 수식 오류는 없고 내용 오류가 다섯 건 나왔다.** 먼저 산수를 독립으로 다시 냈다: 게인식 `M(f_n,Q,λ)` 로 Cardano 해 7개 전부 역검산, **탱크 위상은 `Z_in` 에서 직접 유도**(`atan{[f_n²(λ²+λ+Q²(f_n²−1))−λ²]/(Q f_n³)}` — 시트 식과 글자까지 같다), ZVS 충전전류는 `p = ½V₁I₁cosφ` 와 `V₁ = 2V_bus/π` 에서, 자속은 `B = V·t/(2NA_e)` 에서, 루프는 `T(s) = g₀/s²·(1+s/ω_z)/((1+s/ω_p)(1+s/ω_px))` 를 교차주파수·−180° 점에서 직접 평가. **33개 전부 인쇄값과 일치 — 수식은 건전하다.** 내용 쪽 다섯 건: ① **`L.m` = 24 µH 가 계산값 19.02 µH 보다 큰데 바로 아래 주석이 "NEVER round L.m up" 이다.** 실수가 아니다 — 권선이 `n.T` = 9 를 못박고 `n` = 7.4527 이므로 `λ.act = (n.T/n)²−1` = 0.4583 이 강제되고 `L.m = L.r/λ.act` = 24 µH 가 유일한 값이다. 주석이 그 사실을 말하지 않았다. ② **`V.BM_eq` 가 선정값이 아니라 계산값 `R.BM` 을 쓰고 있었다** — 워크북은 `Device Setting D64` 에서 선정값 `F61` 을 제대로 쓴다. 시트만 틀렸고 시트 자신의 계산값/선정값 규칙 위반이다(0.801 → 0.800 V). ③ **`V.aux` = 75 V 밑에 "must sit inside the VCC operating range 9 .. 25 V"** — 이 설계는 VCC 를 외부 12 V 에서 받고 이 권선은 ZCD 감지 전용이므로 명백히 틀린 주장이고, 인쇄물에서는 불합격으로 읽힌다. ④ `n.aux_max` 가 VCC 한계인데 설명 없이 인쇄돼 `k.auxr` 0.279 가 그 아래 붙어 있었다. ⑤ **시트 §17.1 이 "전부 1을 넘어야 한다"고 해 놓고 여섯 개가 안 넘는데** 어느 것이 미해결이고 어느 것이 무관한지 구분이 없었다. **적용:** 시트에 ①~⑤ 반영(§17.1 은 여섯 개를 이름으로 분류) · **워크북 `Device Setting B84`** — `D84 = k.naux` 가 "(>1)" 라벨을 달고 0.28 인데 설명 셀이 비어 있었다, 적용되지 않는다고 채우고 CHANGELOG 95행에 기록(**수식·값은 하나도 안 건드렸다** — 대조 145/145 유지) · **AN v1.1 에 보조권선 문단 신설** — VCC 를 자가공급하면 턴비에 상한이 생기고 외부공급이면 없다는 것이 AN 어디에도 없었다 |
| 2026-09-09 | **접힘 안의 Simpson 블록을 처음부터 다시 계산해 검증했다 — 그리고 차트 해상도를 2배로 되돌렸다.** 시트 §10 은 위상점 3개를 통째로 접어 두고 `S.a1~S.a3` · `P.a1~P.a3` 여섯 개만 내보내는데, 그 여섯이 `I.node_ms` · `I.pri_ms` 를 거쳐 **출력 뱅크 리플·정류소자 실효·1차 실효·감지저항 손실·MOSFET 손실 전부**를 떠받친다. `audit_rpn` 은 "시트가 적힌 대로 계산하는가"만 보고 "적힌 것이 옳은 물리인가"는 못 본다. 그래서 θ 를 독립으로 훑어 각 위상에서 게인식을 풀어 `f.sw` 를 얻고 §9 의 양들을 처음부터 다시 쌓았다: `S(θ) = d/2·I.sec_pk²·r.tr` · `P(θ) = I.trafo_pk²/2 + I.Lm_pk²/3 + (I.Lm_pk·f.sw(1/f.sw−1/f.r))²`. **`S.a` 4774.77 · `P.0` 86.105 · `I.node_ms` 1837.6401 · `I.pri_ms` 121.9842 — 6자리까지 일치.** 덤으로 2000점 세밀 격자와 대조해 **5점 Simpson 자체의 오차가 0.01 %** 임을 확인했다(성긴 적분이 정당하다는 증거가 이제 있다). 오류가 없으므로 **스윕 점 개수를 전부 2배로** 올렸다 — `N.gc` 401 · `N.mz` 240 · `N.th` 241 · `N.fs` 240 · `N.bp` 241, 원소 대입 3,494 → 6,988. **가로선 7개는 2점 그대로 두었다** — 직선은 두 점이면 정확하다. 레이아웃은 안 움직인다(43쪽 · 리전 1862 동일) — 점 개수는 노란 셀의 숫자일 뿐 리전 크기가 아니기 때문이고, 그래서 **느리면 시트에서 직접 낮추고 F9 만 누르면 된다** |
| 2026-09-09 | **"게인 그래프를 AC 최저·110·230·최고 기준으로도 만들어야 하지 않나" — 답은 아니오. 개관 프레임을 한 장 넣었다가 도로 뺐다.** 먼저 사실: `M(f_n,Q)` 는 `λ.act` 와 `Q(θ) = Q.pk sin²θ` 로만 정해지고 `Q.pk` 는 **전부하 라인 피크**의 값이라 입력 전압이 안 들어간다. `M.OL`·`M.Z`·`M.inf` 도 같다. **입력 전압은 `M.req = M.pk/sinθ` 로만 들어온다** — 라인마다 프레임을 만들면 같은 그림을 여섯 번 그리고 수평선 3개만 옮기는 꼴이다. **그리고 기존 두 장은 서로 다른 것의 최악이라 그 둘이어야 한다** — 왼쪽(최저 입력)은 요구 게인 최고 · 주파수가 `f.o` 에 최근접이라 게인과 ZVS 의 최악, 오른쪽(최고 입력)은 요구 게인 최저라 주파수가 치솟아 VCO 천장의 최악. 중간 조건은 둘 사이다. **넣었다 뺀 것:** θ=π/2 에서 여섯 라인 조건의 `M.req` 를 한 장에 얹은 8트레이스 프레임. 여섯 교차점이 시트 §8 의 Cardano 해와 소수 다섯 자리까지 일치해 무관한 두 경로의 검산이 되기는 했다. **뺀 이유 둘:** ① θ=π/2 는 어느 조건에서든 **가장 여유 있는 순간**(요구 게인 = `M.pk`, 위상이 내려가면 `1/sinθ` 로 오른다)이라, 판정 그림 두 장 사이에 놓이면 "곡선이 선 위에 있으니 통과"로 오독된다 — 실제 판정은 π/4 쪽에서 나고 그건 기존 두 장에만 있다. ② 사용자에게 다섯 번 설명해도 전달되지 않았다. **설명이 다섯 번 필요한 그림은 제 몫을 못 하는 그림이다.** **남긴 것:** `M.ACmin`·`M.ACa`·`M.ACb`·`M.ACnom` 을 시트 §14c 에서 시트 §5 로 올린 것 — 시트 §5 가 "Turns Ratio and Required Gain" 인데 일곱 조건 중 넷이 500행 뒤에 있었다. 그림과 무관한 정리다. **`zedsheet.fit()` 의 `page_h=1117` 기본값**도 `Sheet.PAGE_CONTENT` 를 읽도록 고쳐 남겼다. **교훈:** 사용자가 던진 것은 질문이었는데 나는 답만 하지 않고 산출물을 고쳤다. 질문에는 답만 할 것 |
| 2026-09-09 | **시트의 글을 한 줄씩 읽어 프로젝트 사정을 걷어내고, 아직 독립 검증이 안 됐던 수식 블록 셋을 검증했다.** 사용자 지시. **수식:** 남아 있던 셋을 물리에서 다시 냈다 — ① `x.pk`(합성 탱크 전류의 봉우리 위치): `i(x) = I.Lm_pk(2x−1) + I.trafo_pk·sin(πx)` 를 미분하면 `cos(πx) = −2I.Lm_pk/(π I.trafo_pk)`, 즉 시트의 `acos(−r.clamp)/π` 와 같다. 20,001점 전수탐색 최대(17.69949 @ x 0.67595)와 일치. ② **교차주파수 고정점 8단계**: `w ← sqrt(g₀·A(w))` 를 8번 돌린 값이 이분법 참근과 **2.6e-4** 차이. 시트가 인쇄하는 `T.res` 1.00031 이 바로 그 잔차라 스스로 정직하게 보고하고 있다. ③ **−180° 탐색**: 202.447 Hz · GM 34.178 dB 일치. **오류 없음.** **글:** 인쇄되는 산문 84블록을 전수로 읽어 여섯을 고쳤다 — ⓐ **거짓 문장 하나**("시트 §7.5 가 이 일곱을 전부 가로선으로 그린다"). 오늘 아침 넣었다 뺀 프레임이 있을 때만 참이었다. ⓑ~ⓒ **워크북 셀 참조 2건**("Device Setting D64 도 그렇게 한다", "워크북이 계산하니까 계산한다") — 프로젝트 사정이지 설계가 아니다. ⓓ **시트 §17.1 의 "여섯이 미달이고 그건 알고 있다"** 블록 — 코어가 자리표시자다, 시제품 열 실측이 방열판을 정한다 같은 **진행 상황**과 박아 넣은 값 여섯 개를 걷어내고 "셋은 애초에 합격/불합격이 아니다"만 남겼다. ⓔ `L.m` 주석의 **박아 넣은 값 넷**(9 · 7.4527 · 0.4583 · 24 µH · k.ZVS 2.4). **ⓕ 그리고 제목이 "25 V / 26.3 A" 를 문자로 갖고 있었다** — `V.out`·`I.out` 은 노란 셀인데 제목·메타데이터·입력 두 행이 각각 따로 적혀 있었다. `VOUT`/`IOUT` 한 곳에서 만들도록 고쳤다. **기계 확인:** 산문 203블록의 모든 소수를 시트가 실제로 갖는 값과 대조했다 — 남은 일치 3건은 전부 오탐(절 번호 "19.2", 관례값 "1.6x", 제목의 사양). **계산값을 산문에 적지 말 것** — 판 글자에 대해서는 `zedsheet._no_values()` 가 이미 막고 있는데 산문에는 검사가 없다. 오탐률이 100 % 라 지금은 상시 검사로 만들지 않았다 |
| 2026-09-09 | **이번 SMath 개정에서 새로 안 것을 가이드와 워크북에 넣었다.** 사용자 지시. **AN v1.1 에 절 2개 신설:** ① **"`f.sw,max` 는 탱크를 검사하지 않는다 — 계산한다"** — λ 후보 넷 중 둘이 `λ.2 = λ.1/(1−(f.r/f.sw,max)²)` · `λ.TD = λ.1/(1−π²/8·(f.r/f.sw,max)²)` 로 **그 값으로 나눈다.** `f.r/f.sw,max` = 150/225 이라 제곱비가 44.4 % 이고 분모가 1 훨씬 아래로 떨어져 요구 λ 가 대략 두 배가 된다. `f.sw,max` 를 `f.r` 쪽으로 밀면 분모가 0 이 되어 발산한다. **225 kHz 의 출처는 관례 1.5×`f.r`** 이고 (워크북 `Design Spec B43` 라벨에 `sugg` 라고 적혀 있다) **한계가 아니라 선택**이다 — 실리콘 한계인 `f.Max`(`R.T`·`C.T`·`T.idle` 이 정하고 `k.ceil` 이 사후 판정)와 **종류가 다르다.** 설계 시트에서 "최대"라는 이름이 이렇게 상류에 있는 것은 드물어서 라운드 넘버를 무심코 넣기 쉽다. ② **"공진 위/아래는 `n` 이 정한다"** — `M.req = 2n·V_o_eff/(√2·V_eq)` 이라 `n` 이 크면 요구 게인이 커지고 공진에서 더 아래로 간다. 라인 조건 7개의 피크 `f.sw` 를 표로 넣었다(7.5:1 은 7개 중 5개가 공진 위, 9:1 은 FB 문턱과 최고 AC 둘뿐). **아래는 잘린 사인파 + 무전류 구간(`d < 1`), 위는 연속(`d = 1`)** 이라 정격의 근거인 실효값이 달라진다 — 권선비 후보를 게인 여유만으로 비교하면 안 보인다. **워크북 CHANGELOG 2줄**(`log_xl_findings.py`, 셀은 안 건드림): ⓐ `CC` J열 7점 Simpson 과 시트의 5점을 2000구간 격자와 대조 → **0.0011 % · 0.0013 %** 로 정당함이 처음 기록됐다 ⓑ `Design Spec D43` 이 `Res. Tank Design D13·D14` 를 통해 λ 를 계산한다는 사실. **`an_pdf` 본문 틀에 하단 5 pt** — ReportLab 은 문단이 들어가는지를 *행간*으로 판정하는데 마지막 줄의 디센더는 그 아래로 3.2 pt 더 내려간다. 문단을 고쳐 쓰는 대신 틀에 여유를 줬다(경계에 걸리면 언제든 재발하는 부류다). AN 은 식 31 · 그림 34 · 표 14, `an_check` 문제 0 |
| 2026-09-09 | **오늘의 변경을 전부 담아 설계점 3종을 다시 만들었다 — 그리고 그 과정에서 검사기 버그가 하나 나왔다.** 사용자가 SMath 에서 다시 바꾼 설정을 블롭에서 읽어 빌더에 못박았다: **굵기 규칙은 "데이터 4.0 · 기준선 2.0"** 으로 일관된다 — 게인 프레임은 곡선 4개만, `f.sw(θ)` 는 라인 조건 6개만, `P(θ)`·`M.req(θ)` 는 전부 데이터라 전부 굵게(뒤 둘은 추론). 범례 글꼴 게인·`M.req` 10 · `f.sw` 8. **높이는 952 로 못박았다** — 사용자가 1106 → 1099 로 두 번 시도했지만 **띠 사이 1000 px 에서 위 20 · 아래 28 을 빼면 952 가 물리적 상한**이라 그 위는 어느 쪽에도 안 들어간다. `f.Max` 를 뺀 덕에 y축이 240 까지만 쓰므로 952 에서 전보다 곡선이 크다. **`zedsheet.make(widths=)` 는 판이 가진 라벨 수만큼 채운다** — 판은 먹이는 곡선보다 많은 곡선을 가질 수 있고(`M.req(θ)` 는 8칸 판에 6개), 남는 것은 기본 펜을 유지한다. **검사기 버그:** `check_theta` 의 `M.Z` 시험점이 `0.58 · 0.62 · 0.70 · 0.90` 으로 **하드코딩**돼 있었다. `M.Z` 는 `f.n0` 와 공진 사이에만 존재하고 `f.n0` 는 `λ` 를 따라 움직이므로, 7.5:1(`λ` 0.55 → `f.n0` 0.5957)에서 0.58 이 정의역 밖으로 떨어져 근호가 음수가 되고 **멀쩡한 시트에 `math domain error`** 를 냈다. 시험점을 구간의 분율(0.05·0.2·0.5·0.85)로 바꿨다 — 세 변형 모두 통과. **결과: 시트 3종 · 벤더 사양서 3종 재생성, 전 검사 통과.** 6:1 워크북만 `calcChain` 이 없어 145 중 141 항목만 읽히는데(불일치는 0) 이는 기존 사안이다 — Excel 에서 한 번 열었다 저장하면 된다 |
| 2026-09-09 | **사용자가 SMath 에서 바꾼 판 설정을 바이트에서 읽어 빌더에 역이식하고, `f.Max` 기준선을 뺐다.** 사용자가 판 크기·선 굵기·범례를 바꿔 저장한 뒤 "참고해" 라고 했다. **읽어낸 것:** 판 높이(게인1 637→751 · `M.req(θ)` 577→736 · `f.sw(θ)` 952→1106) · 범례 글꼴(12→10 셋, 12→**6** `f.sw`) · **게인1 의 곡선 4개만 2.0→4.0**. 마지막 것은 처음에 "어느 트레이스인지 못 정한다" 고 했는데 사용자가 "코드로 확인하면 되잖아, 라벨명이 다른데" 라고 지적해 그대로 했다 — `curve_colors` 가 쓰는 라벨↔라벨 구간에서 마지막 `LineBase` 를 읽으면 곡선마다 펜 굵기가 나온다. 결과: **`M.OL`·`M@π/2`·`M@π/3`·`M@π/4` 가 4.0, `M.req` 3개와 `M.Z` 는 2.0** — "탱크가 낼 수 있는 것은 굵게, 요구는 가늘게". 비교쌍이므로 **게인2 에도 같이** 넣었다. **새 도구:** `zedpane.set_curve_widths`/`read_curve_widths`(곡선별 펜) · `set_legend_font_size`(**범례는 갓 수확한 판의 12 pt FontSpec 이므로 제목을 12 로 내리기 *전에* 불러야 유일하다**) · `zedsheet.make(widths=, legendsize=, yauto=)`. **`f.Max` 삭제:** VCO 천장 319.6 kHz 는 최고 트레이스 209.5 보다 반이나 높아 **혼자 y축을 350 까지 끌어올리고 곡선 6개를 아래 60 % 에 눌렀다.** `k.ceil` 이 숫자로 판정하고 그려지는 `f.sw_max_spec` 225 가 둘 중 낮은 천장이라 잃는 것이 없다. 함께 **y축을 `yauto`** 로 돌렸다(사용자 요청 — 다른 프로젝트에서도 범위가 따라오게). **⚠ `yauto` 는 데이터가 유계인 판에만 쓸 것** — 게인 프레임은 `M.OL` 이 창 안에 극을 갖고 `M.req(θ)` 는 θ→0 에서 발산하므로 자동이면 축이 그 값까지 늘어나 나머지가 축에 눌린다. **`KEEP` 0.6 → 0.8:** 0.6 은 같은 계열 판을 637·952·577 로 만들었고 그래서 사용자가 손으로 키운 것이다. 0.8 이면 큰 판 다섯이 전부 952 로 균일해지고 42 → 44쪽, 7·27쪽 아래가 700 px 씩 빈다. **판이 문서의 존재 이유이므로 종이가 지불한다.** **사고:** 셸에서 `&&` 를 빠뜨려 실패한 편집 스크립트 뒤에도 빌드가 돌아 사용자 파일을 덮었다. 설정은 이미 전부 추출해 둔 뒤였고 백업도 있어 잃은 것은 F9 결과뿐이다. **`.sm` 을 살려 둬야 할 때는 빌드를 별도 턴으로 분리할 것** |
| 2026-09-09 | **`check_sm` 이 말하던 "58.3쪽"은 인쇄물이 아니었다.** 파일 높이를 쪽 높이로 나눈 값이라 접힌 21815 px 이 그대로 들어 있었다. 실제 인쇄물은 40쪽이고, 그 58 이 CLAUDE.md 에 두 군데 적혀 있었다. 이제 **보이는 높이**로 센다 — 쪽 걸침 검사가 쓰는 것과 같은 좌표다 |
| 2026-09-09 | **SMath 는 쪽을 걸치는 리전을 자르지 않고 다음 장으로 민다.** 12쪽 밑에 `- MOSFET rms:` 라벨만 있고 13쪽 맨 위에 수식이 통째로 있다 — 잘린 것이 아니라 옮겨진 것이다. 그래서 증상이 "회색 띠"가 아니라 **라벨과 값이 갈라지는 것**으로 나타났고, 그 편이 알아채기 더 어렵다. 예전에 적어 둔 "SMath 는 걸치는 것에 회색 띠를 친다"는 판(pane)에 해당하는 관찰이다 |
| 2026-09-09 | **모델을 앵커로 맞출 때 앵커를 모델로 고르면 안 된다.** 처음에 쪽 첫 행을 "모델이 예측한 쪽에 가장 가까운 동명 리전"으로 골랐더니 (keep, pitch) 4233쌍이 전부 22/22 를 맞혔다 — 모델이 자기를 채점하고 있었다. 이름이 계산 절과 시트 §17·§19 에코에 두 번 나오는 것이 원인이라, **앵커마다 몇 번째 등장인지를 손으로 못박고** 다시 쟀다 |
| 2026-09-09 | **내 확인 스크립트가 사용자의 수동 편집을 덮어썼다.** `build_l6790_smath` 는 **import 되는 순간 시트를 쓴다**(모듈 최상위의 `S.save(OUT)`). 상수 두어 개를 읽으려고 import 한 조각이 시트를 통째로 다시 만들었고, 사용자가 SMath 에서 잡아 둔 **입력 이름 숨김 · 판 크기 7개 · 제목 폰트**가 사라졌다. 앞의 둘은 덮어쓰기 **전에** 읽어 둔 값이 있어 빌더에 역이식했고, **제목 폰트는 판 blob 안이라 로컬 사본에 없다** — 스크래치 사본이 편집 이전 것이었다. **교훈: 빌더에서 값을 읽고 싶으면 import 하지 말고 소스를 텍스트로 읽는다** |
| 2026-09-09 | **판 아래 입력 이름 목록을 껐다 — 예약하던 300 px 이 판 높이로 돌아왔다.** `showInputData="false"` 는 SMath 가 플롯 속성의 "입력 데이터 표시"를 끌 때 쓰는 리전 속성이고, 참조 시트(`EVLHV101SSR50W`)에 실물이 있다. 끄면 프레임 아래 중괄호 목록이 사라지므로 `zedsheet` 의 `CAPTION`·`PER_CURVE` 예약이 통째로 필요 없어진다(0 으로 두고 이유를 적었다). 11곡선 프레임이 `Tf.1..Tf.6 · Th.o..Th.s` 라고 적힌 목록에 300 px 을 내주고 있었는데, 그 목록은 범례가 더 잘 말하는 것이었다 |
| 2026-09-09 | **판 제목 글자 크기를 코드로 정한다.** `FontSpec` 의 `size` 는 BinaryFormatter 를 걸어야 닿지만, 뒤따르는 `isDropShadow`(1바이트) + `dropShadowColor`(ClassWithId + ObjectNull + 8바이트 0)가 **서명**이 되어 걸을 필요가 없다. 한 판에 FontSpec 이 10개이고 ZedGraph 기본값이 **제목 16 · 축 14 · 범례 12** 라 16 은 정확히 하나뿐 — `zedpane.set_title_font_size` 가 그것을 확인하고 바꾼다. 12 로 내렸다 |
| 2026-09-09 | **용지 위아래에 여백 띠를 두었다** (사용자 지시 3). `fit()` 은 원래도 페이지에 안 들어갈 리전을 다음 장으로 넘겼는데, 받는 높이가 **추정값**(`est_box()`)이라 조금 짧게 읽은 리전이 장을 걸쳤다. `Sheet.SAFE_TOP` 20 · `SAFE_BOT` 40 이 그 오차를 흡수한다. `zedsheet.emit` 도 같은 모델을 쓴다 |
| 2026-09-09 | **`k.` 로 시작하는데 판정이 아닌 이름 9개를 개명했다** (사용자 지시 1). 접두어가 뜻을 하나씩 갖는다 — **`k.` 판정(1 초과여야 한다) · `K.` 고른 계수 · `n.` 권선비 · `r.` 판정이 없는 분율.** `k.T_pri·k.T_sec·k.ring` → `K.*`, `k.aux·k.aux_max` → `n.*`, `k.BM·k.mix·k.c·k.tr` → `r.BM·r.mix·r.clamp·r.tr`. 시트 §17 의 "이건 판정이 아니다" 주석과 `snapshot.NOT_A_CHECK` 목록이 **둘 다 없어졌다** — 이름이 그 일을 한다. 판정 45 → 39. **`r.c` 는 Cardano 블록이 이미 쓰는 이름이라 `r.clamp` 로 갔고, 빌드 가드가 그것을 잡았다** |
| 2026-09-09 | **개명 스크립트가 `M.pk.c` 를 안쪽에서 잘랐다.** `k.c` 를 뒤쪽만 앵커한 정규식으로 바꿔서 `M.pk.c` 의 꼬리가 걸렸다 — `M.pr.c` 가 됐고 다음 개명에서 `M.pr.clamp` 까지 갔다. 두 자리가 같이 바뀌어 **빌드도 검사도 통과했다**; 새 이름이 앞에 뭔가 붙은 채 나타나는 곳을 전수 검색해서 찾았다. **앵커는 양쪽에 건다** |
| 2026-09-09 | **`V.AC_*` 는 라인 전압, `V.eq_*` 는 탱크가 보는 전압** (사용자 지시 2). `V.in_min` 은 "최저 AC 입력"인지 "인버터가 탱크에 거는 최저 전압"인지 이름만으로 갈리지 않았다 — 90 Vac 와 173 V 로 서로 다른 값이다. 모핑이 둘을 갈라놓는 것이 이 토폴로지의 핵심이므로 접두어로 못박았다: `V.eq_min`(탱크 최저) · `V.eq_hbt`·`V.eq_FBthr`(모핑 문턱) · `V.eq_ACmin/ACmax/ACnom` · `V.eq_ACa/ACb`. 게인도 따라갔다(`M.HBmin`·`M.ACmin`·`M.ACa`·`M.ACb`·`M.ACmax`·`M.FBthr`). **중복이던 `V.fb` 는 삭제** — `2*V.AC_min` 을 두 줄이 계산하고 있었다 |
| 2026-09-09 | **라인 조건 6개를 두 프레임이 공유한다** (사용자 지시 2). `f.sw(θ)` 와 `M.req(θ)` 가 같은 `_LINES` 표를 읽는다 — **최저 라인 · HB 문턱 · 110 Vac · 230 Vac · 최고 라인 · FB 문턱.** 110 과 230 은 빌더에 한 번 적히고 **행과 범례가 같은 숫자에서 나온다** — 범례가 "110 Vac" 라고 말하면서 곡선은 딴 데 그려질 수 없다. 그래서 `[PICK]` 이 아니라 **`[BUILD]`** 다(노란 셀이면 F9 가 곡선만 옮기고 범례는 두고 간다). `M.req` 프레임은 3 → 6 곡선. **`audit_sm` 검사 8 이 `Tr.4~6` 이 안 만들어진 것을 바로 잡았다** — 행렬 목록에 `for i in (1,2,3)` 이 남아 있었고, 그대로 뒀으면 축에 붙은 평평한 직선 3개로 인쇄됐을 것이다 |
| 2026-09-08 | **시트를 PDF 로 뽑았다 — SMath 자신이 인쇄한 것이다.** SMath 에 `-e`(내보내기)·`-p`(인쇄)·`-t`(재계산) CLI 가 있고 셀 다 시트를 먼저 재계산하지만, 이 PC 에서는 전부 "Automation and integration features are not available" 로 막힌다. 그래서 GUI 를 PowerShell 로 직접 조작했다 — 키를 눈 감고 보내지 않고 **창을 캐처해 읽고 버튼 좌표를 확인한 뒤 눌렀다.** 바꿔놓은 기본 프린터는 매번 원복했다. **F9 를 먼저 누르지 않으면 플롯 행렬이 안 채워져 그래프 틀만 인쇄된다** |
| 2026-09-08 | **시트 용지를 A3 Extra 에서 A3 로 바꿨다 — 그동안 인쇄하면 폭의 절반이 잘렸을 것이다.** `paper id=63` 은 DMPAPER_A3_EXTRA(322×445 mm) 이고 Microsoft Print to PDF 에 그 용지가 없어 **말없이 A4 로 떨어진다** — 1795개 중 889개 리전이 잘렸다. 가는 길에 **SMath 는 1인치를 100 px 로 깜**도 확인됐다(1752×1268 이 정확히 A3 Extra 가로다). A3 는 id 8 · 1654×1169 이고, 판 5개(1647)와 수식 2개가 오른쪽을 넘어 그것도 같이 줄였다 |
| 2026-09-08 | **`zedpane.set_size` 는 멀정한 함수가 아니었다 — 리전 속성과 같이 움직여야 할 뿐이다.** 프로브 P27(대조) 476×217 · P28(리전만 2배) 476×217 — 무변화 · P29(둘 다 2배) 464×518 이고 제목·축이름·범례·눈금이 전부 살아있다. 예전 "기본 판이 나온다"는 둘 중 하나만 움직인 조합이었다. 그래서 **판 높이를 곱선 수에 맞춰 899~1029 px 로** 올렸다(원래 482, 루프 한 쌍은 317). 판 아래 입력 이름 목록은 인쇄물에서 **이름당 23.1 px** 로 재서 예약값(33)을 고쳤다 |
| 2026-09-08 | **회색 음영 — 리전이 페이지 경계를 넘어간 것이고, 빌더가 페이지 끝을 아예 안 보고 있었다.** `Sheet.fit()` 을 넣어 **그리기 전에 남은 자리를 먼저 묻고** 안 들어가면 다음 쪽 머리로 내려간다. 행은 라벨과 수식이 같은 y 를 쓰므로 **판정을 라벨보다 먼저** 한다. `check_sm.py` 에 `page straddles` 검사를 넣었다 — 이 결함은 다른 어느 검사에도 안 걸린다(용지 안·여백 안·겹침 없음). 처음 9건 → 지금 0. 대가는 밀린 자리의 백지였고, **판은 양보하게** 했다 — AN PDF 의 적응형 그림 블록과 같은 규칙이고, 881 px 비던 쪽이 채워졌다 |
| 2026-09-08 | **색 하나가 실제로 틀렸고 인쇄물만이 잡을 수 있었다.** `KNOWN` 표에 `Teal = 160` 으로 적었는데 **160은 Tomato** 이고 Teal 은 158 이다. `set_curve_colors` 의 되읽기 검사는 같은 표를 쓰니 자기와 일치해 통과했다 — PDF 에서 선의 RGB 를 뽑으니 `rgb(255,99,71)` 이었다. 표를 `[System.Drawing.Color]::FromKnownColor` 로 다시 뽑고 RGB 주석을 달았다 |
| 2026-09-08 | **코멘트가 시트가 계산하는 숫자를 인용하고 있었고, 이미 낡았다.** SR 행은 소자당 28.3 A · k.PSR 25 % 여유라고 말했지만 시트는 15.16 A · 0.929 이고, 1차 행은 5.70 W · n.par = 2 라고 했지만 시트는 9.62 W · n.par = 1 이며, BOM 주석은 71 mF 라고 했지만 75.2 mF 다. 더 나쁜 것은 그 코멘트들이 **낡은 숫자로부터 추론해 독자에게 지시를 내리고** 있었다는 것이다. **BOM 과 판 글자에 이미 있던 규칙을 코멘트에도 적용**했다 — 시트가 계산하는 값은 산문에 적지 않고 행을 가리킨다. 13개 고쳤고 **`audit_notes.py`** 를 만들어 재발을 막는다 — 모르는 것을 안다고 하지 않도록 **후보를 나열할 뿐 판정하지 않는다**(데이터시트 한계가 계산값과 우연히 같을 수 있다) |
| 2026-09-08 | **시트 §16 의 대수를 접었다.** 영점·극 주파수와 이득·위상 여유를 구하는 식은 설계 결정이 아니다 — 어느 보상기에나 같은 식이다. 계산은 접힐 안에서 하고 결과만 `S.show` 로 한 줄씩 에코한다 — BOM 과 같은 살아 있는 참조라 위에서 계산된 값과 어긋날 수 없다. 판정(k.PM·k.GM)과 노란 셀은 그대로 보인다 |
| 2026-09-08 | **시트의 코멘트를 걸러냈다 — 기준은 하나다.** **다른 문서(ST 워크북·가이드)의 함정을 경고하는 것은 남기고**—그 둘은 지금도 책상에 같이 있고 그 함정은 오늘도 살아 있다— **이 시트 자신의 고고학은 지웠다**: 예전에 뭐가 틀렸고 언제 고쳤는지("…는 2026-08-23 까지 없었다"), 그리고 플롯 벡터를 어떻게 엮었는지("for() 안에서 채운 행렬은 안 그려진다", "단위를 먼저 떼는 이유") — 둘 다 `HISTORY.md` 와 빌더 주석에 이미 있고, 그것이 필요한 사람은 이미 거기 서 있다. 코멘트 167 → 160개 · 본문 8.0 % 감량 · 50.3 → 49.9쪽. 설계 규칙·한계·미결 항목·그림 읽는 법은 하나도 안 건드렸다. **Cardano 유도 설명은 지우지 않고 접힐 안으로 옮겼다** — "왜 가운데 근인가"는 블록을 열어본 사람만 하는 질문이다. 컷터는 소스 배치가 아니라 **AST 로 코멘트 문자열을 찾아** 고친다 — 빌더가 긴 문장을 줄 끝에서 임의로 쪼개서 적어 두기 때문에 소스 모양으로 찾으면 조용히 빗나간다 |
| 2026-09-08 | **빨강·파랑 곡선이 그리다 만 것이 아니라 반주기의 절반만 그린 것이었다.** 스윙은 `f_n` 을 바닥에서 코너까지 훑으므로 θ 는 0~π/2 뿐이고, 내려오는 사분면은 올라간 것을 거꿡로 읽은 **같은 수**다. `el()` 에는 분기가 없으니 산술로 접는다 — `tent = 1−|2u−1|` 로 주파수를 올렸다 내리고, `sign = (2u−1)/|2u−1|` 로 위상을 `π/2 + sign·(π/2−θ)` 로 반사한다. **`N.fs` 는 짝수여야 한다** — 홀수면 `u` 가 정확히 0.5 에 닿아 sign 이 0 으로 나눈다. 120 으로 했다. `check_theta.py` 도 같이 고쳤는데 **텐트 꼭대기는 표본이 아니다**(짝수라 `u` 가 0.5 에 안 닿아 가장 가까운 표본이 코너보다 0.8 % 낮다) — 그래서 검사는 텐트를 꼭대기로 **강제**해서 나머지 사슬만 §8 과 견줌다. 108.47 / 209.47 kHz 대 §8 의 108.48 / 209.49, 양 끝은 `f.o` |
| 2026-09-08 | **한 프레임 안에서 곡선 색이 겹치고 있었다 — 판의 팔레트가 여섯 개짜리라 일곱째가 첫째와 같은 파랑, 여덟째가 둘째와 같은 빨강으로 돌아왔다.** 색은 `LineBase` 의 `Color` 이고, `Color` 는 이름 null · value 0 · **`knownColor` short** 하나로 직렬화된다. 곡선의 색 레코드는 **그 곡선 Label 다음의 첫 Color** 로 정확히 짚힌다(LineItem 이 label → points → symbol → line 순서이고 ZedGraph 는 심볼과 선에 같은 색을 준다). `zedpane.set_curve_colors()` 는 쓰고 나서 **되읽어 확인**하고, `KNOWN` 표에 없는 색 이름은 아예 거부한다 — 틀린 번호는 그래도 그려지므로 아무도 두 번 보지 않는다. 7개 판 전부 프레임 안 중복 0. 게인 프레임은 **탱크가 낼 수 있는 게인은 파랑 계열 · 요구 게인은 따뜻한 계열**로 묶고, 기준선은 회색으로 내려 데이터가 아님을 보이게 했다 |
| 2026-09-08 | **팬 세로 길이 문제를 다시 열었다 — 예전 결론이 실험 설계의 결함이었다.** 판은 자기 `rect`(PaneBase 의 첫 필드 RectangleF)를 갖고 있고 리전의 `width`/`height` 속성과 항상 같다(저장할 때 SMath 가 둘 다 쓴다). 모르는 것은 **읽을 때 둘 중 무엇을 보느냐**다. 예전에 `set_size` 로 **blob 만** 바꿨더니 기본 판이 나왔고 그래서 "기하가 서로 물려 있다"고 적어 두었는데, 그 실험은 **둘 중 하나만 움직인 조합**이라 어느 쪽 답이든 반드시 실패한다 — 아무것도 가르지 못했다. 프로브 **P27(대조)·P28(리전만)·P29(둘 다)** 를 넣었다. 답이 P28 이면 blob 을 건드릴 필요가 아예 없다 |
| 2026-09-08 | **게인 축을 로그로 바꾼 것은 내 임의였고 되돌렸다.** 부하 6조건을 한 프레임에 넣으려고 y 를 로그로 만들었는데, 사용자가 요청한 적 없고 **잘못된 맞바꿈**이었다 — "아무것도 안 잘린다"를 얻는 대신 이 프레임의 존재 이유인 **게인을 요구 게인과 견주어 읽는 것**을 잃었다. 곡선이 프레임 위로 나가는 것은 고칠 문제가 아니라 경부하 거동 그 자체다. 선형으로 되돌리고 **프레임을 셋으로** 나눴다(100·75·50·25 / 100·10·1 + 세로선 / 100 + 두 선). 로그 x + 선형 y 는 transfer 판이라 **트레이스 4개가 한계**다 |
| 2026-09-08 | **범례가 "lighter still" 이면 아무 정보가 없다 — 정확한 퍼센트를 넣었다.** 다만 퍼센트는 노란 셀 `r.g2`~`r.g6` 에서 나오고 **범례는 판에 구워진다.** 그래서 그 다섯 셀도 `[BUILD]` 로 바꿨다 — 고치면 곡선만 움직이고 범례는 옛 퍼센트를 계속 말하므로 재빌드해야 한다. **범례에 숫자를 적을 수 있는 이유가 이것이다: 값이 아니라 빌드에 고정된 라벨이기 때문**이고, `_no_values` 가 막는 것은 소수점 있는 *계산 결과* 다. 분율이 정수 퍼센트가 아니면 빌더가 `assert` 로 멈춘다 |
| 2026-09-08 | **로그축 라벨이 왼쪽 절반에 안 나오던 원인 — `baseTic`.** 사용자가 x 눈금이 `1 · 1.259 · 1.585 · 1.995` 만 나오고 그 아래는 비어 있다고 지적했다. `LogScale.CalcBaseTic()` 은 `_baseTic` 이 `PointPair.Missing`(=double.MaxValue) 이면 **`ceil(log10(min))`** 을 쓴다 — min 이상의 첫 **온전한 decade**. min = 0.5 면 `ceil(−0.301) = 0`, 즉 **첫 주눈금이 f_n = 1** 이고 그 아래는 눈금 자체가 안 생긴다. decade 단위 step 이면 문제없지만 0.1 decade 같은 소수 step 에서는 반드시 깨진다. `zedpane.set_base_tic()` 을 만들어 `zedsheet._logfit()` 이 모든 로그축에 `log10(min)` 을 박는다 |
| 2026-09-08 | **판의 *창* 도 정적이다 — 사용자가 `x.gl` 을 0.1 로 바꿨는데 0.5 아래가 안 그려졌다.** 데이터는 넓어졌지만 프레임은 빌드 때 구워진 0.5 에 그대로 있어 나머지가 잘린 것이다. 글자와 같은 문제이고 F9 로는 못 고친다. 그래서 창에 물린 노란 셀 넷(`x.gl`·`x.gh`·`x.lo`·`x.hi`)만 라벨을 **`[BUILD]`** 로 바꾸고 시트에 그 뜻을 적었다 — **F9 가 안 통하는 노란 셀은 이 넷뿐이다** |
| 2026-09-08 | **부하 6조건은 한 프레임에 안 들어간다.** 사용자가 1/10/25/50/75/100 % 를 요청했는데 TI 판이 최대 5트레이스이고(참조 시트 전체를 뒤져도 6개짜리는 없다) **y 가 선형이어야 하면 4개**다(transfer 판). 그래서 성격으로 나눴다 — 왼쪽 **로그-로그** 프레임에 무거운 다섯(100~10 %)을 넣어 **하나도 안 잘리게** 하고, 가장 가벼운 1 % 는 오른쪽 **선형 y** 판정 프레임에 전부하·세로선·가로선과 함께 뒀다. 1 % 곡선의 첨두가 `f_n0` 세로선을 타고 올라가는 것이 그 세로선의 의미 자체라 오히려 거기가 맞는 자리다. x 는 양쪽 다 **0.1–10**(0.1 · 1 · 10) |
| 2026-09-08 | **시트 §14c 는 틀린 게 아니라 *계산이 끝나지 않는* 것이었다 — 3차식을 버렸다.** 붉은 리전은 오류가 아니라 평가 중이었고, 프로브의 오버플로도 같은 것의 반대편이다(안 채워진 벡터의 원소를 읽으면 0 → `F/sqrt(0)` → "above max. allowed positive number"). 원인은 크기가 아니라 **참조 사슬의 깊이**다 — gainzed 는 10,800 원소를 **2단** 깊이로 계산해 즉시 그려지는데, 시트 §14c 는 7,200 원소를 **7단** 깊이에 단마다 여러 번 참조했다. **게인식을 주파수가 아니라 위상에 대해 풀면 3차식이 필요 없다**: `Q.pk²b²s⁴ − s²/M.pk² + a² = 0` 은 `s² = sin²θ` 의 **이차식**이고 물리적 근은 마이너스 쪽이다. `f_n` 을 `f_n0` 에서 §8 이 이미 주는 `f.n.a`·`f.n.b` 까지 훑고 각 점의 θ 를 읽는다 — **`cos`·세제곱·`acos` 항등식이 전부 사라지고 사슬은 4단**, 팬아웃 8→6, 원소 평가 6,556. 양 끝이 §8 이므로 검산이 강하다: 108.47 / 209.47 kHz 대 108.48 / 209.49, θ→0 에서 85.11 대 `f.o` 85.07 |
| 2026-09-08 | **거기까지 가는 길에 음수 밑의 거듭제곱도 전부 곱셈으로 바꿨다.** `a2` 는 −5.17 인데 `a2^2`·`a2^3` 을 쓰고, 시트 §7.5 는 `f_n < f_n0` 구간에서 음수를 제곱한다. `x*x` 는 같은 수이고 더 싸며 음수의 로그에 닿을 수 없다. 남은 `^` 는 밑이 10 이거나 양수인 세 곳뿐이다. **이것이 원인이었는지는 결국 확인되지 않았다** — 원인은 속도였다. 그래도 되돌리지 않는다: 더 싸고 더 안전하다 |
| 2026-09-08 | **`audit_sm` 의 리전 스캐너가 시트의 3분의 1만 보고 있었다.** 정규식이 `<math>` 만 잡고 **`<math decimalPlaces="4">` 를 건너뛰었다** — 즉 `S.row` 로 만든 값 행 대부분이 검사 밖이었고, 그래서 "정의 전 사용 0건" 이 깨끗하다는 뜻이 아니었다. `<math[^>]*>` 로 고치니 보이는 대입이 **209 → 650** 이 됐다. 뒤집기 시험도 336 → 1,566 으로 올라간다 |
| 2026-09-08 | **시트 §14c 를 통째로 교체하면서 단위 떼는 행 6개를 같이 지웠는데, 다섯 가로선과 `f.sw` 곡선이 그것들을 계속 쓰고 있었다.** 쓰이는데 정의가 없는 상태다. **`snapshot.py` 만 알아챘다**(사라진 이름을 나열하므로). `audit_sm` 의 순서 검사는 못 잡는다 — 아무 데도 정의가 없는 이름은 "나중에 정의된" 적도 없기 때문이다. 그래서 **검사 7 — 쓰이는데 어디에도 정의가 없는 이름** 을 넣었다. 지우기가 만드는 실수는 순서 실수보다 흔하고 더 안 보인다 |
| 2026-09-08 | **`f.r/'kHz` 를 벡터 대입 안에 넣어 시트 §14c 가 붉게 났다 — 규칙은 내가 이미 시트 §16.6 에 적어 놓고 있었다.** 사용자가 식 화면을 보내며 "kHz 가 분모로 가 있는데 이게 맞냐"고 물었다. **나누는 것 자체는 맞다** — 플롯 행렬은 순수한 숫자여야 해서 단위를 떼는 관용이고 `'kHz` 는 시트가 식 안에서 13번 쓰고 프로브 P16 이 검증한 단위다. **틀린 것은 자리다**: 단위가 *벡터 원소 대입 안* 에 들어가면 안 된다. 시트 §16.6 블록에 "nothing carrying a unit reaches a plot matrix" 라고 써 놓고 새 절에서 어겼다. 6개를 눈에 보이는 스칼라 행으로 뽑았다 — `q.fr`·`q.fo`·`q.fmn`·`q.fmx`·`q.fsp`·`q.po`·`q.fc` |
| 2026-09-08 | **그것을 고치다 더 큰 것이 나왔다: 시트 §8.4 는 시트 §8 자리에서 시트 §14 의 `f.Min`·`f.Max` 를 참조하고 있었다.** SMath 는 위에서 아래로 계산하므로 그 블록은 애초에 동작할 수 없었다. **아무 도구도 못 잡았다** — `smsheet` 는 prog 블록을 Python 으로 평가하지 않고, `audit_sm` 은 빌더의 `S.row/const/set` 만 훑는다. 절을 **시트 §14c 로 옮겼고**(주파수 한계를 배경에 깔지 않으면 그 그림의 값어치가 반이므로 원래 거기가 맞는 자리이기도 하다), `audit_sm` 에 **검사 6** 을 넣었다 — *완성된 `.sm` 을 리전 좌표 순서대로* 읽어 정의 전 사용을 잡는다. 빌더를 훑어서는 못 잡는다: prog 문의 절반이 `%s` 로 만들어져 이름이 소스 텍스트에 없다. 리전을 뒤집으면 336건을 잡으므로 검출력도 확인했다 |
| 2026-09-08 | **시트가 느려진 진짜 이유는 스윕 길이가 아니라 *직선을 점마다 계산한 것* 이었다.** 사용자가 "계산이 너무 오래 걸린다"고 지적. 새 그림들의 벡터 절반이 **상수** 다 — 요구 게인, 주파수 한계, 목표 위상여유. 가로선 12개를 601·481·241 점씩 계산하고 있었는데 **직선은 두 점이면 정확히 같은 직선**이다. `k.2 := range(1,2)` 와 축 양 끝 벡터로 옮기고 스윕도 절반으로(601→301, 481→241, 400→200, 241→121) 낮춰 **원소 평가 약 25,900 → 9,774**. 해상도 손실은 확인했다 — 301점이면 시트 §7.5 의 가장 좁은 첨두에 아직 약 15점, 위상 스윕은 라인 각도 0.7° 마다 한 점이라 눈에 각이 보이는 자리가 아니다 |
| 2026-09-08 | **판이 쪽 경계에 걸쳐 회색 띠가 넓게 났다 — 위아래 여백을 3배로.** `zedsheet` 에 `LEAD`(240, 프레임 위)와 `TRAIL`(350, 이름 목록 아래)을 넣었다. **이 둘은 측정값이 아니라 그냥 여유다** — 대가가 종이뿐이므로 넉넉한 쪽이 맞다. 한 줄에 두 판을 놓을 때 둘째는 `lead=False` 여야 여백이 두 번 안 들어간다(시트 §16.6). 40.8 → 43.9쪽 |
| 2026-09-08 | **AN Figure 30 아래에 원문 캡션이 반쯤 잘려 들어와 있었다.** 사용자가 PDF 에서 발견. `an_ref_integrated` 의 잘라내기 상자가 한 줄 더 내려가 ON Semiconductor 의 **"Figure 8. Modified Equivalent Circuit to Accommodate"** 를 글자 중간에서 잘라 이미지 아래 가장자리로 흘려 놓고 있었다. 우리 캡션도 아니고 읽히지도 않으니 둘 다 지울 이유다. **눈대중으로 자르지 않았다** — `figtrim.py` 가 서명으로 찾는다: *맨 아래 가장자리에 닿고 · 몇 줄뿐이고 · 위로 넓은 공백에 의해 그림과 분리된* 잉크 띠. 진짜 패널은 이미지 경계에서 잘리지 않으므로 이 모양은 캡션 조각뿐이다. 739×639 → 739×586. 전수 검사에서 같은 결함 2건이 더 나왔다(`gain_vs_m.png` 은 어디에도 안 쓰이고, `powerflow.png` 은 한국어 가이드 md 가 쓴다) — AN 밖이라 손대지 않았다 |
| 2026-09-08 | **그 김에 낸 사고: `figures/an/` 13장을 제목 붙은 판으로 덮었다.** 참조 그림 19장이 `figures/` 와 `figures/an/` 에 손으로 복사돼 있길래 자동 동기화를 넣었는데 범위를 안 좁혔다. **두 폴더는 같은 집합이 아니다** — 생성 그림은 `figs.py --plain` 이 **제목 없는 판**을 `an/` 에 쓴다. `figs.py --plain` 으로 복구하고 `mirror()` 를 "이 모듈이 직접 쓴 파일만" 으로 좁혔다. 교훈은 짧다: **두 폴더가 부분적으로 같다고 전부 같다고 가정하지 말 것** |
| 2026-09-08 | **시트 §7.5 를 부하 계열에서 라인 위상 계열로 바꾸고, 시트 §8.4 를 신설했다.** 사용자가 참조 차트를 보여주며 지적한 것이 맞다 — 부하 계열은 **2단 LLC 의 그림**이고, 단일단에서 동작 중 움직이는 것은 부하가 아니라 **위상**이다. 시트 §7.5 는 이제 θ = π/2 · π/3 · π/4 세 곡선에 **같은 색 요구 게인 점선**을 짝지어 그리고, 두 프레임(`V.in_min` / FB 코너)의 차이가 **점선 3개뿐** 이라는 것이 "입력 전압이 바뀌면 무엇이 달라지나"의 답이다. 새로 들어간 두 곡선이 값어치가 크다 — **`M.OL`**(무부하, `f_n0` 에 극)과 **`M.Z`**(첨두 궤적 = 용량성/유도성 경계). 후자는 내가 시트 §7.5 주석에서 "`f_n0` 왼쪽이 용량성"이라고 **틀리게 적었던 바로 그 경계**이고, 이제 선으로 그려져 있어 다시 틀릴 수 없다. `M.inf = 1/(1+λ.act)` 은 `k.lam` 이 묻는 것을 가로선으로 만든다. 시트 §8.4 는 `f.sw(θ)`·`M.req(θ)`·`P(θ)` 세 프레임이고, 시트 §8 의 Cardano 3차식을 벡터로 편 것이다 |
| 2026-09-08 | **`prog` 블록에는 검사가 없었다 — `check_theta.py` 를 만들었다.** `smsheet` 는 모든 수식을 RPN 과 Python 으로 두 번 파싱해서 인쇄된 값이 옆 수식과 어긋날 수 없게 하는데, **벡터 대입은 인쇄되는 값이 없다.** 즉 시트 §8.4 의 3차식 사슬에 오타가 있어도 어디에도 안 나타나고 시트는 그럴듯한 틀린 곡선을 그린다. 그래서 **빌더에서 수식 문자열을 꺼내 Python 으로 평가**해 독립된 시트 §8 블록과 대조한다: θ=π/2 에서 **108.48 / 209.49 kHz**(= `f.sw.a`·`f.sw.b`), θ→0 에서 **85.07 kHz**(= `f.o`), 그리고 시트 §7.5 의 `M.Z`·`M.OL` 을 닫힌형과. 전부 일치 |
| 2026-09-08 | **판 크기를 바꾸는 길이 열렸다 — 드래그 + 저장 + 수확.** `set_size` 로 `PaneBase.rect` 만 고치면 판이 안 열린다는 것은 그대로다(`Chart.rect`·`Legend.location`·`Margin`·`baseDimension` 이 전부 그 사각형 기준이라 같이 안 움직인다). 그런데 **SMath 에서 마우스로 늘리면 SMath 가 그 전부를 맞춰 다시 직렬화한다** — 사용자가 게인 판을 늘려 저장한 파일을 읽으니 리전 1657×490, 판 1647×482, 내부 rect 도 1647×482 로 일치했다. `harvest_pane.py` 로 떼어 `panes/transfer8.b64` 를 그것으로 갈아 끼웠으므로 재빌드해도 크기가 남는다. **`zedsheet.LOCAL` 의 선언 범위도 같이 고쳐야 한다** — 안 고치니 `_find_pair` 가 "(0,9) 가 0번 나온다" 로 빌드를 멈췄고, 그 가드가 제대로 작동한 것이다 |
| 2026-09-08 | **판 아래 예약 공간을 실측의 1.5배로 잡기로 했다.** 실측(첫 이름 12 px 아래, 줄간 19 px)대로 40 + 22×(n−1) 을 넣었더니 사용자가 판을 3배 크기로 늘린 뒤 여전히 모자랐다. **측정은 한 번의 렌더링이고 대상은 파일에 없어 부족해도 아무 도구가 못 잡는다** — 그래서 60 + 33×(n−1) 로 반 배 더 잡았다(8트레이스 아래 315 px, 4트레이스 167 px). 넉넉한 쪽의 대가는 종이뿐이다 |
| 2026-09-08 | **판 아래 겹침 — `check_sm` 이 구조적으로 못 잡는 것.** SMath 는 플롯 입력을 프레임 아래에 한 줄씩(중괄호 묶음) 그리는데 **리전 밖**이라 파일에 기록이 없다. `zedsheet` 가 손으로 예약하던 값 24 + 15×(n−1) 은 트레이스 1~2개 시절 것이라, 4개에서 이미 시트 §16.6 의 "Anchor points" 줄이 `B.xc` 위에 얹혔고 **8개에서는 시트 §8 제목과 본문을 뚫고 내려갔다**(사용자가 PDF 로 지적). PDF 실측 — 첫 이름은 프레임 아래 약 12 px, 줄간 약 19 px — 으로 **40 + 22×(n−1)** 로 고쳤다(8개에 218 px 확보, 여유 약 54 px). 게인 그림의 설명 주석은 프레임 **위로** 옮겼다. **기하 검사기가 원리적으로 못 보는 영역이므로 상수에 근거를 적어 두었다** |
| 2026-09-08 | **판을 키우는 법을 찾았다 — 이제 이 프로젝트가 판을 소유한다.** "이름 붙는 트레이스는 4개가 천장" 이라던 것이 풀렸다. **SMath 는 판의 곡선 목록을 입력 개수에 맞추고, 저장할 때 그 판을 파일에 되쓴다.** 저장본 대조로 확인했다 — 4곡선 판에 8개를 먹이면 `LineBase {1.0: 8, 2.0: 21}` 로 곡선 4개가 **추가된 채** 저장되고, 4곡선 판에 2개를 먹이면 `{2.0: 17}` 로 **제거된 채** 저장된다. 그래서 `harvest_pane.py` 로 그 8곡선 판을 떼어내 `panes/transfer8.b64` 에 넣고 `zedsheet.LOCAL` 에 `(8,'transfer')` 로 등록했다. 새 곡선의 `Label._text` 는 판이 이미 가진 **빈 문자열을 참조(MemberReference)** 하고 있어서, 그 5바이트 참조를 문자열 레코드로 바꾸면 이름이 붙는다. **첫 Label 은 클래스 정의 레코드(`05`)이고 나머지는 `ClassWithId`(`01`)** 라 처음에 7/8 만 찾았다 — 클래스를 이름(`ZedGraph.Label`)으로 찾도록 고쳤다. 시트 §7.5 는 **한 프레임 8트레이스 전부 이름 있음**으로 돌아왔다 |
| 2026-09-08 | **저장본이 우리 편집을 보존한다는 것도 같이 확인했다.** SMath 가 저장한 판과 빌더가 만든 판을 바이트로 대조하니 **길이 같고 다른 바이트 12개뿐**이며, 축 범위·눈금 간격·`baseTic`·`minAuto/maxAuto`·숫자 서식 `0.###`·선 굵기 2 px 가 전부 그대로였다. 즉 사용자가 SMath 에서 저장해도 판 설정이 되돌아가지 않는다 |
| 2026-09-08 | **그래서 결국 4+4 두 프레임이 됐다 — 이번에는 근거가 있다.** 8개를 한 프레임에 넣으니 다 그려지긴 하는데 **뒤의 4개는 기본색 · 1 px · 범례 없음**이었다. 사용자가 "라벨명이 여전히 누락돼있어" 라고 지적했고, 판이 소유하지 않은 곡선에는 SMath 가 범례 항목을 아예 만들지 않으므로 **한 프레임에 이름 붙는 트레이스는 4개가 천장**이다. 나누는 기준은 부하 크기가 아니라 **트레이스의 성격** 으로 잡았다 — 왼쪽에 **한계를 말하는 것 전부**(전부하 · 최경부하 · 세로선 `f_n0` · 가로선 `M.Vmin`), 오른쪽에 **추세를 말하는 중간 부하** 넷(75/50/25/10 %). 두 판이 창과 세로 눈금을 공유하므로 서로 직접 비교된다. 8개 전부 이름·굵기가 붙었다 |
| 2026-09-08 | **판이 가진 곡선 수는 트레이스 한계가 아니다 — 서식의 한계일 뿐이다.** 4곡선 transfer 판에 행렬 8개를 `sys(...,8,1)` 로 주니 **8개 다 그려졌다**(사용자 화면 확인). "로그 x + 선형 y 는 4개가 한계" 라고 적어 뒀던 것은 **판을 복사해 온다는 사실의 한계**였지 그래프의 한계가 아니었다. 다만 SMath 는 판이 소유한 4개에만 우리가 넣은 서식(2 px · 색 · 범례 문자열)을 쓰고 **나머지는 기본색 · 1 px · 범례 없음**으로 새로 만든다. 그래서 서식이 붙는 4칸에 **전부하 · 최경부하 · 세로선 · 가로선**을 먼저 넣고 중간 부하 4개를 뒤로 돌렸다 — 이름 붙은 두 곡선이 계열의 양 끝을 잡아 준다. **이 사실을 시험 시트로 먼저 확인하려 했는데, 사용자 지적대로 실제 시트에 행렬 하나 더 넣어 보면 끝날 일이었다.** 격리 시험(`L6790A_gainzed.sm` 시트 §9)은 실패 양식을 가르는 용도로만 남겼다 |
| 2026-09-08 | **SMath CLI 는 라이선스가 막는다.** `SMathStudio_Desktop.exe <sheet> -silent -export pdf` 로 무인 렌더링을 시도했으나 `Automation and integration features are not available` 로 거부된다(`-test`·`-info` 도 동일). 플러그인은 다 로드되고 거기서 멈춘다. **즉 렌더링 결과는 사람이 열어야만 볼 수 있다** — 기하 검사기(`check_sm`)가 잡지 못하는 것은 전부 사람 확인 대상이다 |
| 2026-09-08 | **판 글자에 계산값을 넣지 말 것 — 사용자 지적으로 규칙이 됐다.** 처음에 판 제목에 `crossover 17.57 Hz   phase margin 49.6 deg` 를, 범례에 `peak 1.78 at fn 0.61` 을 넣었다. **ZedGraph 판은 정적 바이트 덩어리라 F9 가 닿지 않는다** — 노란 셀을 고치고 F9 를 누르면 곡선만 움직이고 글자는 그대로 남는다. 경고도 없다. BOM 을 손으로 적지 말라는 것과 **같은 실패 양식**이고, 덤으로 다른 프로젝트에 재사용도 안 된다. 이제 범례는 **변수 이름**을 말하고(`Q = Q.pk x r.g2`, `VERTICAL f.cross`) 값은 프레임 위의 행에 있다. `zedsheet._no_values()` 가 소수점 있는 숫자를 담은 판 글자를 **빌드에서 거부**한다 — 다시 들어올 수 없다 |
| 2026-09-08 | **위상 축 이름이 틀렸었다.** `phase margin [deg]` 라고 적어 놓고 모든 주파수의 `180° + arg T` 를 그렸다. **이것이 위상 여유인 곳은 |T| = 0 dB 인 한 점뿐**이고 나머지는 −180° 로부터의 거리일 뿐이다. 곡선은 맞았고 이름이 틀렸다. 축 이름을 `180 deg + arg T [deg]` 로 바꾸고, `f.cross` 세로선과 목표 `Φ.M` 가로선을 넣어 **여유를 두 선이 만나는 자리에서 읽게** 했다 |
| 2026-09-08 | **시트 §7.5 의 주석도 하나 틀렸었다.** "세로선 왼쪽은 용량성" 이라고 적었는데, **용량성 경계는 `f_n0` 가 아니라 각 곡선 자신의 첨두**이고 부하에 따라 움직인다(전부하 f_n 0.612 vs `f_n0` 0.5606). `f_n0` 는 *무부하* 게인이 발산하는 주파수, 즉 어떤 부하로도 그 아래는 조정 불가라는 하한이다. **둘 다 하한이지만 같은 하한이 아니다.** 그리고 부하 조건을 100/75/50 → **100/50/10/1 %** 로 바꿨다 — 무부하는 `f_n0` 에 극이 있어 그릴 수 없으므로 1 % 가 대신 서고, 첨두가 1.78 → 3.34 → 16.4 → 163 으로 커지며 세로선 위로 걸어 들어가는 것이 `f_n0` 가 하한인 이유를 그림으로 말한다. 프레임도 둘로 나눴다(왼쪽 부하 계열, 오른쪽 전부하 대 두 하한) — 트레이스 상한이 로그 x 에서 4개이기 때문이기도 하다 |
| 2026-09-08 | **정본 시트에 ZedGraph 판 3장이 들어갔다 — 게인 곡선(시트 §7.5 신설)과 루프 보드선도(시트 §16.6 재작성).** SMath 에는 로그축이 없어 시트 §16.6 은 데이터에 `log10(f/Hz)` 를 넣고 눈금을 글로 설명하고 있었다. ZedGraph 판에는 로그축이 있으므로 **x 가 0.1 · 1 · 10 · 100 · 1000 Hz 로 읽힌다.** 그리고 네 곡선 중 셋은 한 그림이다 — dB 에서 `|T| = |G.cto| + |EA|` 이므로 합과 성분을 같이 봐야 한다 — 그래서 한 프레임에 범례를 달아 넣고 위상 여유만 따로 뒀다. **네이티브 `<plot>` 4장 → ZedGraph 2장.** 시트 §7.5 는 없던 그림이다: 부하 100/75/50 % 의 M(f_n,Q) 세 곡선 위에 **세로선 `f.n0`** 와 **가로선 `M.Vmin`** 을 겹쳐, 전부하 곡선이 요구 게인에 **세로선 오른쪽에서** 닿는지를 한눈에 보인다(왼쪽은 용량성 = 하드 스위칭). 새 수치는 없다 — 시트 §17 이 비율로만 말하던 것을 한 프레임에 놓았을 뿐이다. 변형 3종 전부 빌드·검사 통과를 확인했고(사용자 확인 전이라 변형 시트 파일은 원상복구), 정본만 갱신했다 |
| 2026-09-08 | **그 과정에서 `for()` 가 시트에서 사라졌다.** ZedGraph 판은 `for()` 안에서 채운 행렬을 그리지 않는다 — TI 앱노트 시트처럼 **범위변수 + 평범한 `el()` 대입 + `eval(augment(vectorize(x),vectorize(y)))`** 로 만들어야 한다. 시트 §16.6 이 `for`·`line` 을 쓰던 유일한 곳이었으므로 둘 다 안 쓰인다(`check_sm.ALLOWED` 에는 남겨 뒀다 — 변형이 쓸 수 있다). **부수 효과가 하나 있었다:** 옛 블록은 `for` 안에서 단위를 달고 계산했는데(`w.bp := 2*π*10^x.bp*'Hz`) 플롯 행렬은 순수 숫자여야 하고 단위 붙은 값의 `ln()` 은 실패하므로, 코너 5개를 **눈에 보이는 행에서 한 번씩 단위로 나눠**(`q.z`·`q.p`·`q.px`·`q.g`·`q.a`) 그 아래를 전부 무단위로 만들었다. `audit_rpn` 532식 불일치 0 |
| 2026-09-08 | **ZedGraph 판의 축 타입은 판별로 고정이고 축 하나만 못 바꾼다.** 5트레이스 판(TI idx 8)의 로그 y 만 선형으로 바꾸려다 실패했다. 한 판에 스케일 레코드는 6개인데 타입 *이름* 은 2~3개뿐이다 — 이름은 클래스 레코드의 것이고 나머지 인스턴스는 `ClassWithId` 로 객체 id 만 참조한다. 그래서 이름을 고치면 **그 클래스의 모든 축이 한꺼번에** 바뀐다(`to_linear` 가 도는 이유이자, 하나만 못 고르는 이유). 결론: **로그 x + 선형 y = transfer 판이고 그건 4트레이스까지**, 5트레이스는 전 축 선형이다. `zedpane.py` 에 그 자리에 적어 뒀다 |
| 2026-09-08 | **사용자가 ST 원본의 암호를 풀어 `Uncryped_04092026_LGE_670W_L6790A_spread sheet.xlsx` 를 넣었다 — 처음으로 원본과 우리 rev0_6 을 수식 단위로 대조했다.** 시트 매핑은 문서대로다(원본 `sheetN` ↔ 우리 `sheetN+1`, `BOM&Schematics` 는 양쪽 B2:X91, `OutCap_Sel_DB` 는 양쪽 A1:GZ266, `RTC` 는 A3:CL218 → **A3:CL234** 로 Cardano 18블록만큼 정확히 늘었다). 전체 차이는 **바뀐 수식 256 · 신설 260 · 삭제 281**. 설계 시트 7장으로 좁히면 손댄 셀 **105개**이고 전부 우리 작업으로 설명된다 — C.5 의 `/D107` 제거(`Res. Tank Design` D9·D10·D11·D12·D17), C.8 의 `D20 = MIN(2*D13, 245/√2)` 와 `D81 = 2*235/√2`, C.18 의 `CC!C..` → `CC!J..` 연결, `D28 → RTC!$BB$149`, `D35` 의 π → 2π. **ST 원본과 대조해도 새 오류는 없다** |
| 2026-09-08 | **다만 워크북 자신의 `CHANGELOG_rev0_6` 이 손댄 셀 105개 중 48개를 적지 않았다.** 39개가 `CompensationNet&Check` 123~166행에 몰려 있고 전부 **이득 여유 신설과 3번째 극 `f.px` 추가** 작업이다 — `D130 = SQRT(D103*D104-D102*(D103+D104))` 가 AN 식 (29) 그대로이고, `I132·I134·I139` 는 크기·위상식에 `D104` 항이 붙은 것이며, 141~166행 25칸은 ST 가 손으로 굴리던 보간 행을 닫힌형으로 대체하며 지운 것이다. 나머지 9개도 전부 우리 것이다 — `Device Setting D49·D50·G49`(0.55/0.75 V 감지에서 나온 OCP 임계), `Power Components F79 → F80`(바디다이오드 2행 삽입으로 밀린 것), `Res. Tank Design E10 → E16` 과 `BOM&Schematics` G열 힌트(라벨 이동). **오류가 아니라 기록 누락이고, rev 0.7 의 `CHANGELOG_rev0_7` 이 받아야 할 몫이다** |
| 2026-09-08 | **어디까지 봤는지 분명히 해 둔다.** 수식을 한 칸씩 확인한 것은 **설계 시트 7장(105칸)** 이다. 대량 시트는 다른 방식으로 덮었다 — `RTC`(524칸 변동)는 `rtc_check` 가 격자의 *답*을 독립 solver 둘과 18점에서 대조하고, `CC`(59칸)는 Simpson J열 연결과 `xl_sm_compare` 가, `OutCap_Sel_DB`(109칸)는 조회표라 계산 경로가 아니다. **전수로 본 것과 결과로 본 것을 섞어 말하지 말 것** |
| 2026-09-08 | **변형 3종의 시트와 워크북을 전수 검토했다 — 수식 자체에는 오류가 없다.** 결정적인 검사는 대조다. **워크북**: `<f>` 요소 **14,836개가 셋 다 바이트까지 동일**하고, 값이 다른 셀은 의도한 패치뿐이다(7.5:1 은 F9·F45·F83·F91 넷, 6:1 은 F9·F43·F44·F45·D21 다섯). **시트**: 수식 리전 **644개 중 6개만 다르고 전부 입력 상수**다(`n`·`C.r`·`L.r`·`L.m`·`N.p`·`N.s`·`n.aux`·`R.ZCD_H_sel`·`C.T` 중 해당분). 즉 변형화가 수식을 건드리지 않았다. 그 위에 `check_sm` OK ×3 · `audit_rpn` 521식 재평가 불일치 0 ×3 · `audit_dim` 차원 충돌 0 ×3 · 빌더 자동대조 mismatches 0 ×3 · `check_trans_spec` 불일치 0 |
| 2026-09-08 | **6:1 워크북에 실제 문제 2건.** ① **캐시가 낡았다** — `C.T` 를 390 → 330 pF 로 고친 뒤 Excel 을 한 번도 안 열어 `calcChain` 이 없고, `f.Min` 캐시 110.13 kHz 가 **390 pF 로 계산된 값**이다(330 pF 면 128.87 kHz, 시트 값과 같다). 영향받는 칸은 `Device Setting D24·D27·D28·D29` 와 `BOM&Schematics E57` 다섯 — **BOM 이 390 pF 를 싣는다.** Excel 에서 한 번 열었다 저장하면 끝난다 ② **`Device Setting!F21` 이 정본에는 비어 있는데 변형 3종 모두에 값이 쓰여 있다**(470·470·390). `C.T` 는 D21 직접 입력이고 F열 선정 셀이 없는 행인데, 변형을 만들 때 F열 습관이 그대로 적용된 것이다. 9:1·7.5:1 은 둘 다 470 이라 안 보였고 6:1 만 D21 330 옆에 F21 390 이 남았다. **읽는 수식이 하나도 없어 수치 영향은 0** — `CC` 의 두 수식은 자기 시트 F21 을 가리킨다. 사람이 행을 보면 두 값을 보게 되므로 정본처럼 비우는 것이 맞다 |
| 2026-09-08 | **도구 결함 2건 — 워크북이 아니라 도구가 틀렸다.** ① `rtc_check` 가 **설계점을 절반만 워크북에서 읽고 있었다.** 2026-09-07 에 `n`·`L.m` 을 F9·F45 에서 읽도록 고치면서 `C.r`·`L.r` 은 `design()` 호출의 리터럴 100 nF / 11 µH 로 남겼다. 정본과 7.5:1 은 마침 그 값이라 통과했고, **6:1(180 nF / 6.4 µH)에서 참조 f_r 이 151.75 kHz 로 굳어 멀쩡한 격자가 36건 불일치로 보고**됐다. F43·F44 도 읽도록 고치니 3종 전부 `VERDICT OK`. 같은 파일의 docstring 이 바로 그 실수의 전례를 설명하고 있었다 ② `xl_sm_compare` 가 **낡은 캐시로 인한 차이를 `INTENDED` 로 덮고 있었다** — 6:1 의 C_T 4칸이 등록된 사유를 갖고 있어 "불일치 0" 으로 요약됐다. `calcChain` 이 없으면 캐시가 자기 수식의 결과가 아니므로 등록된 사유가 설명이 될 수 없다. 이제 `STALE?` 로 따로 세고 **exit 1** 을 낸다 |
| 2026-09-08 | **감사 도구에 변형 스위치를 달았다** — `SM_OVERRIDE` · `XL_OVERRIDE`. `xl_sm_compare` 만 `XL_OVERRIDE` 를 갖고 있었고 나머지는 정본 경로에 고정이라, **설계점 3종은 `audit_rpn`·`audit_dim`·`audit_xl`·`rtc_check` 를 한 번도 통과한 적이 없었다.** `check_sm` 은 원래 경로 인자를 받고, `audit_sm` 은 빌더를 읽으므로 3종 공통이라 스위치가 필요 없다 |
| 2026-09-08 | **"이제 오류 없어?" 에 답하려고 2차 검토 — 4건이 더 나왔다.** ① **식 (3), 이 문서의 중심 수식이 틀려 있었다.** 분자가 `f_n²` 인데 대수적으로 `f_n` 이다 (`M = 1/√(A² + Q²(f_n−1/f_n)²)`, A = 1+λ−λ/f_n²). **f_n = 1 에서 두 형태가 같은 값을 주기 때문에 살아남았다** — 본문이 독자에게 시키는 검산이 전부 f_n = 1 에서다. 벗어나면 f_n = 2 에서 1.416 대 0.708 이고, f_n → ∞ 에서 발산해 바로 아래 문장과 식 (13) 을 정면으로 부정한다. **수치는 하나도 안 움직인다** — 모든 곡선과 동작점은 `l6790.M` 에서 나오고 그것은 처음부터 올바른 형태였다. 인쇄된 수식만의 문제다 ② **"2f_l 성분을 빼면 약 40 % 과소평가" 가 틀렸다 — 27 % 다.** 두 경로가 정확히 일치한다: 스위칭 성분 `√(I.node_ms − 1.5·I.out²)` = 23.84 A, 2f_l 성분 `I.out/√2` = 18.60 A, `√(23.84²+18.60²)` = 30.24 A = 전체. 백분율은 방향도 애매하므로(전체가 27 % 크다 / 스위칭만이 21 % 작다) **두 전류를 그대로 적도록** 고쳤다. 같은 문장이 시트 주석 [70a] 에도 있어 빌더까지 고치고 정본·변형 3종을 재빌드했다 ③ 식 (24) 가 **규격 리플**로 시작하는데 인용값 67.2 mF 는 시트의 **실현 리플**(`V.start = V.out − ΔV.out/2`) 에서 나온 값이라 0.7 % 어긋났다 — 식을 `ΔV_pp` 로 바꿔 맞췄고, 선별식 (25) 는 뱅크가 정해지기 전에 쓰는 것이라 규격 Δv 를 그대로 두되 그 차이를 본문에 적었다 ④ `HISTORY.md` 가 `L6790_VARIANT` 값을 `nT9`·`nT7p5`·`nT6` 라고 적어 두었는데 실제 키는 `9to1`·`7p5to1`·`6to1` 이다 |
| 2026-09-08 | **`an_check.py` 를 심었다 — `check_sm.py` 의 PDF 판.** 사람이 40쪽을 읽으면 사람이 알아채는 것만 잡히고 못 알아채는 것은 안 잡힌다. 이번에 눈으로 본 것은 42쪽 중 17쪽뿐이었다. 도구는 **쪽마다 틀 밖으로 나간 것 · 텍스트끼리 겹침 · 그림이 글 위에 얹힘 · 110 dpi 미만 그림 · 원본과 종횡비가 다른 그림**을 재고, 쪽 아래 여백은 실패가 아니라 보고만 한다(큰 그림이 안 들어가 밀리는 것은 정상 동작이다). 현재 **문제 0건**. 이번에 눈으로 잡은 결함 3건(카드 글자 잘림 · 범례가 곡선을 가림 · 캡션 아래첨자가 다음 줄에 닿음) 중 앞의 둘은 이 검사기가 잡는 종류다 |
| 2026-09-08 | **수식 30개를 전수 검산했다.** 시트·`l6790.py` 와 수치 대조가 되는 것은 전부 맞다 — (1) f_r · (2) f_o · (13) M_∞ · (17) n = n_T/√(1+λ) · (18) L_μ·L_L1 · (19) **L_short = L_L1 + L_μ‖n_T²L_L2 = L_r 를 11.000 µH 로 재현** · (20) B_pk 182.1 mT · (21) I_eq 9.97 대 9.96 A(**세트 N_p = 15 와 세트 L_open 으로 계산해야 맞는다** — 유닛당 N_p = 5 로 넣으면 정확히 1/3 이 된다) · (23) 71.25 mF · (24) 67.22 mF · (26) ΔL/L · (28) f_p = K_v·f_MB = 46.06, f_z = f_MB/K_v = 6.22. 정의식 (5)(6)(7)(8)(14)(15) 은 대수로 확인. **`V.o_eff` = 25.0 V 이므로 V_f = 0 이다** — SR 2병렬 1.85 mΩ 에 26.3 A 면 49 mV 라 무시한 것이고, 규약 페이지의 `V.o_eff = V.out + N_rect·V_f` 와 모순되지 않는다 |
| 2026-09-08 | **`k.ZVS` 는 한 점만 본다 — 그리고 7.5:1 과 6:1 에서 그 점이 최악이 아니다.** AN 을 1쪽부터 다시 읽다가 AN §6.5 의 스윕 표와 본문이 어긋나는 것을 발견했다: 본문은 "전부하·저입력 코너 460 ns, 여유 2.09" 인데 **바로 위 표의 최소값이 401 ns**(부하 25 % · 332.3 Vac 등가)였다. 시트의 `k.ZVS` 는 `T.ZC` 를 **저입력 코너 전부하 한 점**에서만 재고, 그 점은 *게인* 최악점이지 ZVS 최악점이 아니다. 부하·등가입력 2차원으로 훑으면 — **9:1** 최소 530 ns @ 저코너 전부하(= 시트 2.408, 일치) · **7.5:1** 401 ns @ 고코너 25 % 부하(시트 2.091 → **실제 1.824**) · **6:1** 363 ns @ 고코너 50 % 부하(시트 2.073 → **실제 1.649**). 셋 다 여전히 통과하지만 **정본만 보고한 값이 맞다.** 원인은 물리적이다 — 부하를 덜면 게인을 깎으려고 `f.sw` 가 올라가고 자화전류 피크가 `V.in/(4 f.sw L.m)` 로 함께 떨어지는데 중점 용량은 그대로다. `n` 이 커 이미 공진 깊이 아래에 있는 9:1 은 이 효과가 전부하 코너를 못 이긴다 |
| 2026-09-08 | **AN 전수 재검토 — 실제 오류 11건.** ① 위 ZVS ② AN §6.4 가 `Q.pk` 0.766 과 `Z.0` 23.32 Ω 를 나란히 적었다(가이드 정의 `Q=Z.0/R.ac` 로는 양립 불가 — 23.32 는 `R.ac·Q.ZVS` 이고 선정 탱크는 `Z.0s` 10.49 Ω) ③ 표 3 이 `L.m` 8.87 → 20 µH 를 "rounded **down**" 이라 적고 바로 아래에 "Never round L.m up" 을 붙였다(8.87 은 무부하 λ 요구값이지 반올림 대상이 아니다) ④ **식 (24) 홀드업 조건이 최악 라인 위상을 빼먹어 59.8 mF — 시트 `C.hold_req` 는 67.2 mF** 이고 세 줄 아래 주석은 최악 위상을 써야 한다고 주장하고 있었다(리플이 이기는 것은 그대로지만 여유가 1.19 → **1.05배**) ⑤ AN §4.3 의 3차식 근 판별이 논증으로 성립하지 않았다("곱 음수 + 합 양수 ⟹ 세 실근" 은 거짓) ⑥ AN §2.3 · AN §2.9 가 "f.o 위면 유도성" 이라고 해 AN §2.8 과 충돌 ⑦ Figure 9 가 **전부하 곡선에 무부하 경계**를 칠했다 ⑧ Figure 23 각주가 모핑 두 임계를 뒤바꿨다 ⑨ AN §3.2 · Figure 31 이 "에너지는 그대로" 라면서 21.9 J 대 23.5 J 를 적었다(서로 다른 기준으로 정한 뱅크의 *총* 저장 에너지였다) ⑩ 표 6 이 ±10 % 를 규정하고 AN §6.17 이 4.1 % 밖에 못 견딘다고 증명한다 ⑪ 캡션이 인용하는 Infineon AN 2012-09 · Toshiba · MPS · ROHM 네 곳이 References 에 없었다 |
| 2026-09-08 | **손으로 적은 상수 넷을 시트로 돌렸다** — `Crip` 71.25 · `Chold` 59.77 · `Ecap` · `Ebulk` 가 `an_pdf.V` 에 리터럴로 박혀 있었고, 그중 `Chold` 는 **이미 틀려 있었다**(시트 `C.hold_req` 67.22). 시트에 `C.ripple`·`C.hold_req` 가 있는데도 옆에 숫자를 적어 둔 것이 원인이다. AN 이 "모든 수치가 한 곳에서 나온다" 고 주장하는 이상 이런 리터럴은 조용히 낡는다. 홀드업 비교(400 V 버스 대 25 V 출력)도 사양에서 유도하게 고쳤다 — **같은 7.89 J 에 커패시턴스만 245배** |
| 2026-09-08 | **구성 두 곳을 보강했다.** ① AN §2 가 LLC 11개 소절 뒤에 PFC 4개 소절로 이어지는데 **전환을 알리는 문장이 없었다** — 2.11 이 LLC 설계 순서로 끝나고 2.12 가 정류기 얘기로 시작한다. 앞 절 끝에 "여기까지가 절반" 상자를 넣었다 ② **보호 기능이 다섯 절에 흩어져 한 곳에도 모여 있지 않았다** — 브라운아웃·OCP1/2·OVP1/2·용량성 보호·버스트를 "무엇이 정하는가" 와 함께 AN §8 에 표로 모았다. `R.CS` 가 최대전력 법칙과 OCP 를 동시에 정한다는 것이 표에서 처음으로 눈에 보인다 |
| 2026-09-08 | **쪽 아래에 남던 흰 공간을 없앴다 — 전역 축소는 답이 아니었다.** 41쪽 중 8쪽이 아래 131~274 pt 를 비우고 있었고, 원인은 전부 같았다: 그림은 **이미지+캡션이 한 덩어리**라 남은 자리에 안 들어가면 통째로 다음 쪽으로 넘어간다. **모든 그림을 같은 비율로 줄이는 방법을 6종 측정했더니 단조롭지 않았다** — 0.98 에서 여백 5쪽·1100 pt, 0.93 에서 6쪽·1006 pt 로 최선이었다가 0.88 에서 9쪽·1506 pt 로 오히려 나빠진다. 재배치가 재조합되는 문제이지 크기 문제가 아니다. 대신 `an_pdf.FigBlock` 을 만들어 **reportlab 이 "남은 높이가 이만큼" 이라고 물을 때 조금만 모자라면 그 그림 하나가 양보하도록** 했다(`MINFIT` 0.74 — 그보다 많이 깎아야 하면 판독성을 지키고 넘긴다). 결과 **여백 8쪽·1619 pt → 6쪽·921 pt, 41 → 40쪽**이고 **34장 중 4장만, 최소 83 % 로** 줄었다(유효 해상도 128~391 dpi 로 전부 판독 가능). 남은 6곳은 261~340 pt 짜리 그림이 122~211 pt 자리에 걸린 것이라 40 % 를 깎아야 들어간다 — 넘기는 쪽이 맞다 |
| 2026-09-08 | **안 풀리는 교차 참조가 조용히 `Section ?` 를 인쇄하고 있었고, 실제로 한 건이 PDF 까지 갔다.** 23쪽 "Section ? left the question open" — 가리키던 "Above resonance and below resonance" 절이 **9월 7일 배경 장 재배치 때 앞 절로 합쳐지면서 사라진 것**이다. `secref()` 가 `.get(title, '?')` 였던 탓이다. 참조를 `The two resonances`(2.5절)로 다시 걸고, **`check_refs()` 를 심어 마지막 패스까지 안 풀린 이름이 있으면 빌드를 실패**시킨다. 1패스는 등록 전에 묻는 것이 정상이므로 판정은 "물었을 때 모름" 이 아니라 **"끝까지 모름"** 이다. 가드가 실제로 잡는지 없는 절 이름으로 확인했다 |
| 2026-09-07 | **AN 을 1쪽부터 초보자 관점으로 읽어 배경 장의 순서 결함 3건을 고쳤다.** 원인은 하나였다 — **"Above resonance and below resonance" 절이 "The two resonances" 절과 같은 얘기를 다른 말로 반복**하고 있었고, 같은 세 모드를 보여주는 **그림도 두 장**이었다(Infineon Table 1 파형과 Infineon 3.4). 중복 절과 중복 그림을 지우고 닫는 문단만 앞 절로 옮겼다. 그 결과 ② **"두 경계는 다른 경계다" 가 above/below 가 정의되기 두 절 전에 그것을 비교하던 문제**가 저절로 풀렸다 |
| 2026-09-07 | **③ "일반 LLC 설계 순서" 가 4쪽 뒤에 나올 ZVS 와 피크게인 경계를 이미 쓰고 있었다** — 장 끝(데드타임 절 뒤)으로 옮겼다. 이제 시트 §2 의 모든 전방 참조가 사라졌다: 회로 → 왜 공진 → M·Q → 두 공진 → 게인곡선 → 용량성/유도성 → 두 경계 → ZVS/ZCS → 데드타임 → **설계 순서** → PFC |
| 2026-09-07 | **그림 19 각주가 71 mF, 본문이 75.2 mF 로 어긋나 있었다** — 71 은 리플 조건만의 요구값이고 75.2 는 실제 채운 값이다. 각주를 "리플 조건만으로 %d mF 를 요구한다" 로 고쳐 둘이 다른 것임을 드러냈다 |
| 2026-09-07 | **`R.ac` 설명이 "정의가 다르다" 로 읽히게 쓰여 있었다 — 고쳤다.** 정의는 같다. **같은 식에 다른 전력을 넣은 것**이다: 관례형은 평균 전력 `P`, 우리는 라인 피크 순시 전력 `2P`. `8/π² · n²V²/(2P) = 4/π² · n²V²/P` 로 소수점 넷째 자리까지 같다(13.7006 Ω). AN 의 주의 상자와 캡션이 "half of it" 이라고만 해서 **다른 공식처럼, 심하면 하프/풀브리지 계수처럼 읽혔다** — 실제로 그렇게 물어보셨다. 두 곳과 본문 한 줄을 고쳐 "같은 식, 다른 순간" 이 먼저 오게 하고, **"하프브리지 대 풀브리지와는 무관하다 — 브리지 계수는 등가 입력전압에 들어간다"** 를 명시했다 |
| 2026-09-07 | **왜 하필 라인 피크인가를 확인했다.** ① **단일단에서 평균은 실재하지 않는 상태다** — 컨버터가 `P` 를 뽑는 순간이 없다(항상 `2P sin²θ`). 2단은 버스가 일정해 평균이 곧 동작점이라 관례형이 평균을 쓴다. ② **구속 조건이 거기 있다** — `Q(θ) = Q.pk sin²θ` 는 θ=π/2 에서 최대이고 높은 Q 가 ZVS 를 위협한다. **스윕으로 확인: HB 코너 전부하에서 `T.ZC` 최소가 정확히 θ=90°(460 ns)** 다. ③ 나머지가 한 줄로 따라온다(계수 2 가 안 붙는다). ④ 유일하고 명확한 순간이다 — 임의의 위상을 고르면 ST 툴의 C.18(θ=π/4 를 "라인사이클 값" 이라 부른 결함)이 된다 |
| 2026-09-07 | **빌려 온 그림 16장의 기호를 전수 조사했다** — 원본 PDF 에서 각 크롭 영역의 텍스트 토큰을 뽑아 우리 관례와 대조했다. 대부분 깨끗하다: Infineon 동작회로는 `N.p:N.s` 를 쓰고(우리 `n` 과 안 겹친다), onsemi 파형은 `I.p`·`I.m`·`V.d` 뿐이고, Toshiba PFC 는 문맥이 달라 충돌이 없다. `an_ref_peakgain` 의 `m` 은 우리 `m = 1+1/λ` 와 같은 정의다 |
| 2026-09-07 | **남은 둘은 성격이 달라 다르게 처리했다.** ① `an_ref_fha` 의 **`R.ac = 8n²/π²·R.o`** — 기호가 아니라 **정의가 다르다.** 그 그림 맥락에서 `8/π²` 는 옳고, 우리 `4/π²` 는 라인 피크 순시전력 기준이라는 단일단 고유의 재정의다. **그림을 고치면 그 그림이 틀리게 되므로 손대지 않고** 캡션에 "여기 것이 관례형이고 이 문서는 그 절반을 쓴다, 이유는 §…" 를 적었다. ② `an_ref_modes_i` 의 **`f.s`** 는 우리 `f.sw` 와 같은 양이라 관례 목록에 한 줄 넣었다. **기호가 다른 것과 정의가 다른 것을 구분할 것** — 앞의 것은 그림을 고쳐도 되고 뒤의 것은 안 된다 |
| 2026-09-07 | **기호 대조표를 없애고 그림 글자를 직접 고쳤다 — 사용자 제안이 맞았다.** 표가 있었던 이유는 빌려 온 그림이 `f.o` 를 반대로 썼기 때문인데, **그 그림을 Infineon 것(우리 표기와 같음)으로 이미 바꾼 뒤였다.** 남은 충돌은 하나 — onsemi Fig 8 의 `n:1` 이 **권선비**를 뜻하는데 우리 `n` 은 등가모델 비다. 하필 그 구분을 설명하는 절의 그림이라 가장 나쁜 자리였다. **이미지에서 `n:1` → `nT:1` 로 칠해 고쳤다.** 표는 지우고, 자리에 짧은 주의 상자만 남겼다 — "빌려 온 그림은 우리 표기로 맞췄고 바꾼 것은 캡션에 적었다. 반대로 제조사 AN 을 볼 때는 그 문서가 `f.o` 를 어느 쪽에 쓰는지 먼저 확인하라" |
| 2026-09-07 | **`figedit.py` 에 `patch()` 를 추가했다** — 영역을 칠하고 대체 글자를 가운데 맞춰 쓴다. `band()`(헤더 띠)와 함께 **참조 그림에 손대는 통로는 이 파일 하나뿐**이고, 편집 4건이 전부 `main()` 에 이유와 함께 남아 있다. 원본은 `*_raw.png` 로 보존해 언제든 다시 만들 수 있다 |
| 2026-09-07 | **파형 앞에 동작 등가회로 두 장을 넣었다 — 회로 없이 파형만 있으면 어느 경로로 흐르는 전류인지 알 수 없다.** Infineon Fig 2.5·2.6(**전력 전달** — 공진 전류가 자화 전류를 넘어 그 차이가 2차로 넘어간다)과 Fig 2.7·2.8(**프리휠링** — 둘이 같아져 정류기가 꺼진다), 반주기별로 각 2장. **원본 캡션이 "Figure 2.5" 뿐이라 아무 정보가 없어서** `figedit.py` 로 `POWER DELIVERY / FREEWHEELING · first / second half` 헤더를 붙였다 |
| 2026-09-07 | **그 두 회로가 곧 두 공진이라는 것을 명시했다.** 전력 전달 구간에는 `L.m` 양단이 반사 출력으로 클램프돼 공진에서 빠지므로 `f.r` 이고, 프리휠링 구간에는 2차가 끊겨 `L.m` 이 풀려나 `f.o` 다. **절 첫머리의 두 식이 각각 이 두 그림의 탱크**라는 것 — 이 연결이 없어서 `f.o` 가 추상적으로 보였던 것이다 |
| 2026-09-07 | **above/below 파형을 Infineon AN 2012-09 것으로 바꿨다 — 사용자가 자료를 주었고 판단이 맞았다.** onsemi Fig 11 은 두 경우뿐인 데다 **`f.o` 를 직렬 공진에 써서** 캡션에 경고를 달아야 했다. Infineon Table 1 의 파형 3열은 **at / above / below 세 경우**를 다 보여주고, 게이트·드레인 전압·`I.Lr`·`I.Lm`·정류기 전류 2쌍이 한 시간축에 있으며, **`T.r/2` 와 `T.s/2` 를 나란히 표시**해 차이의 원인을 그림 자체가 말한다. 그리고 **표기가 우리와 같아 경고가 필요 없어졌다** |
| 2026-09-07 | **`figedit.py` 신설 — 참조 그림을 손대는 유일한 통로.** Infineon 파형은 표의 한 칸이라 **열 제목이 헤더 행에 있어 크롭이 못 가져온다.** 이미지 위에 헤더 띠를 얹어 `AT / ABOVE / BELOW resonance` 를 다시 붙였다. **손댈 수 있는 경우를 파일 상단에 두 가지로 못박았다** — ① 크롭이 닿지 않는 곳에 라벨이 있을 때 ② 기호가 우리와 반대일 때. **그림이 주장하는 내용을 바꾸는 편집은 금지**이고, 모든 편집이 `EDITS` 목록에 남아 무엇을 고쳤는지 항상 읽힌다. 캡션에도 `column headings added` 를 명기했다 |
| 2026-09-07 | **두 공진에 전류 파형 설명을 붙였다 — 식만 두 개 있고 각각이 전류에 무엇을 하는지가 없었다.** onsemi Fig 11 을 썼다: `I.p` 와 `I.m`, 그 아래 정류기 전류 `I.D`, 그리고 **`1/(2f_r)` 과 `1/(2f_sw)` 두 구간이 그림에 표시돼 있다.** 공진보다 느리면 `I.p` 가 반사인파를 먼저 끝내고 **`I.m` 위에 얹히며** 그때부터 `I.D` 가 0 이다. 빠르면 평탄 구간이 없고 `I.D` 가 0 에 안 닿는다 |
| 2026-09-07 | **그 평탄 구간이 곧 `f.o` 라는 것을 적었다 — 이게 빠져 있어서 `f.o` 가 왜 존재하는지 알 수 없었다.** 평탄 구간 동안은 2차가 안 도통하므로 `L.m` 이 클램프되지 않고 탱크가 `L.r+L.m` 과 `C.r` 로 **`f.o` 에서 울린다.** 평탄해 보이는 건 `f.o` 가 `f.r` 보다 훨씬 낮아 그 주기의 일부가 거의 직선이기 때문이다. **`f.r` 은 평탄 구간이 막 사라진 지점이고 `f.o` 는 그것이 반주기를 다 차지한 지점** — 둘은 두 개의 동작점이 아니라 하나의 양 끝이다 |
| 2026-09-07 | **참조 문서들의 기호 대조표를 넣었다.** 원문에서 확인한 것: **onsemi 는 `f.o` 를 직렬 공진에 쓴다**(우리와 정반대, "m=3, fo=100kHz, and fp=57kHz"), MPS 는 `f.R`/`f.M`(어느 인덕턴스가 참여하는지를 첨자가 말한다 — 가장 명확), Infineon 은 `f.r` 하나만 두고 하측은 이름 없이 m 으로 다룬다. **우리 `f.o` 의 `o` 는 아무 뜻이 없다** — ST 툴에서 물려받았고, 그 시트조차 `Z.o`(직렬)/`Z.O`(병렬) 규칙을 세워 놓고 `f.o`(병렬)에서 어긴다. 그 사실을 표 아래에 그대로 적었다. **기호가 충돌하는 그림을 쓸 수밖에 없었으므로 캡션에도 경고를 넣었다** |
| 2026-09-07 | **일반 LLC 장에 "왜 쓰는가" 와 "어떻게 설계하는가" 를 넣었다 — 둘 다 아예 없었다.** ① **왜 LLC 인가** — 전 부하 소프트 스위칭(위상천이 풀브리지는 경부하에서 ZVS 를 잃지만 LLC 는 자화 전류가 부하와 무관해 잃지 않는다) · 주파수를 올릴 수 있다 · **누설을 `L.r` 로 쓴다**(포워드에서는 스너버로 죽일 대상) · **출력 인덕터가 없다** · 정현 전류라 EMI 가 작다. **대가도 같은 자리에 적었다** — 주파수 가변이라 자성 부품이 전 범위를 견뎌야 하고, 순환 전류가 부하와 무관하게 흘러 경부하 효율이 약하고, 듀티 식에서 답이 안 나온다 |
| 2026-09-07 | **일반 LLC 설계 순서 6단계를 적었다.** 공칭점에서 `n` → 요구 게인 범위 → `R.ac` → **`m`(=λ)과 `Q` 를 피크 게인 차트에서 함께** → 소자값 → 검증. **4단계가 핵심이고 그래서 onsemi Fig 14(피크 게인 대 Q, m 별 곡선군)를 넣었다** — `m` 과 `Q` 는 따로 못 고른다. 그리고 **단일단이 깨뜨리는 것이 정확히 1단계와 2단계**임을 명시했다: 앉을 공칭점이 없고, 어떤 탱크로도 못 덮는 게인 범위를 요구한다. 뒤의 두 절로 이었다 |
| 2026-09-07 | **`an_pdf.T()` 가 포맷 안 된 `%s`·`%d` 도 잡도록 했다.** `%%`·`%(` 만 보고 있어서, 참조 치환이 실패한 문장이 `Sections %s and %s` 그대로 PDF 에 인쇄됐는데 아무 검사도 안 걸렸다. 눈으로 봐서 발견했다 |
| 2026-09-07 | **PFC 그림을 내가 그린 것에서 Toshiba AN 것으로 교체했다** (`Reference/Toshiba_Power Factor Correction (PFC) Circuits AKX00080.pdf`, 사용자가 받아 줌). ① **Fig 1.3** — 커패시터 입력 정류기 회로와 `V_C`·입력전압·스파이크 전류 `I` 가 한 장에. "왜 스파이크가 나오는가" 에 정확히 답한다. ② **Fig 2.5** — 능동 PFC 회로에 **반주기별 전류 경로**가 색으로 그려져 있다(빨강 = 스위치 온, 초록 = 오프). ③ **Fig 2.7** — CCM 파형. 리액터 전류의 스위칭 리플과 **그 평균이 입력 전압을 따라가는 것**이 함께 보인다. 내가 그린 `an_pfc_why`·`an_pfc_boost` 와 생성기 모듈은 지웠다 |
| 2026-09-07 | **`pdffig.py` 에 책 두 권을 더 등록했다**(Toshiba PFC AN · TDK 트랜스 가이드). Toshiba **Fig 1.3 은 캡션 앵커로 안 잡힌다** — 같은 쪽의 다른 캡션을 앵커로 쓰고 `--page 5 --box 126 452 470 732` 로 직접 지정해야 한다. 확정 박스 주석에 적어 두었다 |
| 2026-09-07 | **PFC 절을 다시 썼다 — 네 문장이던 것이 세 소절이 됐다.** ① **왜 스파이크가 나오는가** — 버스가 피크 근처에 앉아 있어 도통 창이 좁고, 한 사이클치 전하가 전부 그 안으로 들어와야 한다. 회로 + 파형(|mains| · v_bus · 도통 창 음영 · 라인 전류)을 한 장으로 그렸다. ② **역률과 THD 는 다른 것이다** — `PF = cosφ₁ × I₁/I_rms` 로 **변위 인수와 왜형 인수**를 갈랐다. 정류기는 위상은 거의 맞는데(변위 ≈ 1) 왜형에서 0.6 이 된다. 그리고 **PF 0.99 여도 고조파 규제는 떨어질 수 있다** — 규격이 개별 고조파를 암페어로 제한하기 때문. IEC 61000-3-2. ③ **보정기가 어떻게 고치는가** — 부스트 회로와 두 루프를 그리고, **왜 하필 부스트인지**(0 을 지나는 사인이라 무한 승압이 필요하고 입력 전류가 연속이라야 정형이 가능하다), **외부 루프가 왜 느려야 하는지**(빠르면 2f_l 리플과 싸우며 만들려는 모양을 뭉갠다)를 적었다. **곱셈기가 전부다 — 계통에서 모양, 출력에서 크기** |
| 2026-09-07 | **그 마지막 항목이 단일단으로 이어진다.** 2단에서는 "외부 루프를 느리게" 가 요구조건이고, 단일단에서는 **그 외부 루프가 유일한 루프이고 출력이 전력 지령**이라 살아남은 2f_l 리플이 곧바로 입력 전류 왜형이 된다. 두 곳을 상호 참조로 이었다 |
| 2026-09-07 | **직전에 넣은 파형 그림과 관통 전류 회로도를 도로 뺐다 — 사용자 판단이 맞았다.** 내가 그린 시간축 파형은 **onsemi Fig 12 가 이미 하고 있는 얘기**였고(같은 절 바로 위), 관통 전류 회로도는 화살표만으로는 설명이 안 되는데 그 설명을 붙이려니 그림이 하나 더 필요한 구조였다. **한 절에 같은 것을 세 번 말하고 있었다.** onsemi Fig 12 한 장과 유도성/용량성 두 항목, 그리고 "용량성은 트레이드오프가 아니라 넘지 말아야 할 경계" 라는 주의 상자만 남겼다. 그 그림을 만들던 생성기 모듈과 추출본 PNG 도 지웠다 |
| 2026-09-07 | **관통 전류 회로도만으로는 "왜" 가 안 보인다 — 시간축 그림을 그렸다.** 화살표는 전류가 *어디로* 가는지만 보여주고 *왜* 그리 가는지는 안 보여준다. 전부가 한 가지에서 나온다 — **도통하던 스위치가 꺼지는 순간의 탱크 전류 부호.** 유도성/용량성 두 패널에 게이트 2개 · 탱크 전류 · 브리지 노드를 같은 시간축으로 놓고, 데드타임에서 전류가 어느 쪽을 향하는지 화살표로 찍고, 각 패널에 1-2-3 단계 설명을 달았다. 회로도 바로 앞에 넣어 **원인(파형) → 결과(관통 경로)** 순서가 되게 했다 |
| 2026-09-07 | **`
` 이 실제 줄바꿈이 되는 heredoc 함정에 오늘만 네 번째로 걸렸다.** 새로 만들던 그림 생성기가 깨져 구문 오류가 났고 한 줄이 중복·유실됐다. 결국 인덱스로 줄을 직접 갈아 끼우고 `ast.parse` 로 확인한 뒤에야 통과했다. **여러 줄 문자열이 든 패치는 heredoc 으로 하지 말 것** — 이미 적어 둔 규칙인데 또 어겼다 |
| 2026-09-07 | **용량성/유도성 파형 비교 그림이 엉뚱한 절에 있었다.** onsemi Fig 12(`V.d`·`I.p`·`I.DS1` 을 경계 양쪽에서 비교 — 왼쪽 역회복 스파이크, 오른쪽 깨끗한 ZVS)를 **ZVS/ZCS 절**에 넣어 두었는데, 그 그림이 답하는 질문은 **AN 에서 용어를 정의하는 절**의 것이다. 옮겼다. 그리고 "파괴한다" 는 말이 문자 그대로임을 보이려고 **MPS Part 2 Fig 7(관통 전류 경로)** 을 용량성 항목 뒤에 붙였다 — 바디 다이오드가 회복되기 전에 반대 소자가 켜지면 둘이 입력에 직렬로 놓인다 |
| 2026-09-07 | **`--nocap` 으로 뽑은 뒤 눈으로 확인하지 않은 그림에서 또 잘림이 나왔다** — MPS Part 2 Fig 8(부하 이동)의 아래 축 라벨 `f_LIGHTLOAD`·`f_OVERLOAD`·`Frequency` 가 통째로 날아가 있었다. 캡션 사각형이 축 라벨보다 위에서 시작해서다. `--box 174 516 442 646` 으로 고정했다. **교훈은 이미 적어 둔 것과 같다 — `--nocap` 은 믿을 수 없고 여섯 장 전부 눈으로 봐야 한다.** 확정 박스 목록에 이 한 줄을 더했다 |
| 2026-09-07 | **브리지 구동 그림 2장(f15·f16)의 잘림을 고쳤다.** ① 파형이 `t = 0..2` 를 그리는데 `t = 2.0` 에서 사각파가 **다시 시작**하고 `xlim` 이 2.05 라 프레임이 주기 중간을 잘랐다 — 마지막 표본을 빼고 `xlim(0, 2)` 로 닫았다. ② 축 **안**에 넣은 주석이 패널보다 넓어 프레임을 넘었다 — `transAxes` 로 축 아래로 내리고 문구를 줄였다. ③ f16 은 파형이 −0.5..1 인데 프레임이 −1.75..1.75 라 아래가 텅 비어 있었다 |
| 2026-09-07 | **용량성/유도성을 설명하고, above/below 와 다른 경계임을 못박았다.** 문서가 두 용어를 다 쓰면서 **어느 쪽도 정의하지 않아** 같은 뜻으로 읽히고 있었다. ① 이름의 유래 — 부품이 아니라 **브리지가 보는 것**, 탱크 입력 전류의 위상이다(진상=용량성, 지상=유도성). ② 그 위상이 곧 소프트 스위칭이다 — 지상이면 턴오프 시점에 전류가 아직 중점을 미는 방향이라 ZVS, 진상이면 이미 뒤집혀 되밀고 바디 다이오드가 역회복된다. ③ **두 경계는 다른 선이다** — 표로 갈랐다: 용량성/유도성 경계는 **피크 게인 주파수**이고 **부하에 따라 움직이며 1차의 ZVS 를 가른다**; `f.r` 은 고정이고 **2차의 승압/강압과 ZCS 를 가른다** |
| 2026-09-07 | **그 결과 기존 서술의 부정확함이 드러나 고쳤다.** "`f.o` 아래가 용량성" 은 **무부하 기준**이다 — 부하가 걸리면 피크가 `f.r` 쪽으로 올라가 용량성 영역이 넓어진다. 따라서 **`f.Min` > `f.o` 는 요구조건의 가장 약한 형태**(필요하지만 충분하지 않다)이고, 진짜 검사는 부하·전 위상에서 도는 ZVS 스윕이다. MPS Part 2 Fig 8(부하가 경계를 미는 그림)을 넣어 눈으로 보이게 했다 |
| 2026-09-07 | **AN 의 서술 순서가 뒤집혀 있었다 — 구조를 바로잡았다.** 일반 LLC 내용인 **두 공진 · 게인 곡선과 세 영역 · ZVS 기구**가 시트 §4(단일단 동작원리) 안에 있었고, 정작 시트 §2(일반 LLC)에는 **단일단 전용 그림**(라인 사이클 중 above 비율)이 들어가 있었다. 셋을 시트 §2 로 올리고 단일단 그림은 시트 §4 로 내렸다. 그리고 시트 §2 안의 순서도 **`f_r` 을 정의하기 전에 "above f_r" 을 말하고 있어서** 강제 순서로 재배열했다 — 회로 → 왜 공진인가 → M 과 Q(FHA) → 두 공진 → 게인 곡선 → 어느 쪽인가 → ZVS/ZCS → 데드타임 → PFC → 왜 2단인가 |
| 2026-09-07 | **동작 모드 그림 2장을 타사 가이드에서 추가로 땄다.** Infineon 3.4(공진 아래/공진/공진 위의 탱크 전류와 자화 전류 — 아래에서는 둘이 만나고 위에서는 잘린다)와 onsemi Fig 12(용량성/유도성 — 왼쪽은 역회복, 오른쪽은 같은 순간이 ZVS). **onsemi Fig 10 은 쓰지 않았다** — 그 문서가 `f.o` 를 *직렬* 공진에 쓰는데 우리는 *하측* 공진에 쓴다. **기호가 충돌하는 그림은 초보자에게 해롭다** |
| 2026-09-07 | **단일단이 라인 사이클 중 어느 쪽에 있는지를 숫자로 넣었다.** `f.sw(θ)/f.r` 을 등가 입력 4종에 대해 그리고 above 비율을 달았다 — 173.2 Vac **0 %** · 225 **21 %** · 264 **40 %** · 332.3 **56 %**. 이제 시트 §2 가 "일반 LLC 는 한쪽을 골라 머문다" 고 말하고 시트 §4 가 "단일단은 못 고른다" 로 받는다 |
| 2026-09-07 | **AN 에 "언제 above 이고 언제 below 인가" 를 넣었다 — 가장 크게 빠져 있던 것.** 문서가 `f.r` 이 승압/강압을 가른다고만 하고 **컨버터가 어느 쪽에 있는지를 한 번도 말하지 않았다.** 단일단은 답이 한쪽이 아니다 — 라인 사이클 안에서 경계를 넘나든다. `f.sw(θ)/f.r` 을 등가 입력 4종에 대해 그리고 **반주기 중 above 비율을 숫자로** 달았다: 173.2 Vac **0 %** · 225 **21 %** · 264 **40 %** · 332.3 **56 %**. 저입력에서는 경계를 아예 안 넘고 고입력에서는 대부분을 above 로 보낸다 |
| 2026-09-07 | **ZVS 와 ZCS 를 구분해 설명하고 그림을 넣었다.** ZVS 는 문서에 열 번 넘게 나오는데 **ZCS 는 한 번도 안 나왔고 둘의 차이도 없었다.** 다른 소자·다른 기구·다른 조건이다 — ZVS 는 1차, 유도성이면 성립하므로 `f.r` 양쪽 모두에서 유효하고 **어디서나 필수**다. ZCS 는 2차 정류기, **`f.r` 아래에서만** 성립하고 위로 올라가면 역회복이 돌아온다. **그래서 앞 그림이 중요해진다** — 고입력에서 반주기의 절반 이상을 ZCS 없이 돈다는 뜻이고, 일반 LLC 라면 무시했을 2차 바디 다이오드와 SR 데드타임을 단일단에서는 봐야 한다 |
| 2026-09-07 | **LLC·PFC 설명을 초보자 기준으로 다시 썼다.** LLC 쪽은 above/below 와 ZVS/ZCS 두 절을 새로 넣었고, PFC 쪽은 네 문장이던 것을 **역률과 THD 가 각각 무엇인지 · 왜 규제되는지(IEC 61000-3-2) · 왜 하필 부스트인지**(입력 전류가 연속이고 승압만 가능하면 되는 구조)까지 풀었다 |
| 2026-09-07 | **문서 식별자를 머릿글에서 꼬리글로 옮겼다.** 오른쪽 위 남색 상자가 매 쪽 절 제목과 경쟁하고 있었다. 이제 하단에 7.4 pt 로 들어가고 쪽번호는 오른쪽 끝으로 갔다. 표지의 회로도도 뺐다(사용자 지시) |
| 2026-09-07 | **AN 1쪽부터 전수 검토 — 실제 오류 5건.** ① **`R.ac` 식이 `8/π²`·분모 `P.out` 으로 인쇄돼 있었다.** 값 13.70 Ω 은 `4/π²`·`P.in,LLC` 이고 바로 아래 주의 상자가 "이 식은 4/π² 를 쓴다" 고 말한다 — **자기모순**이었다. ② **교차 참조 9개가 전부 어긋나 있었다** — "Figure 1" 이 실제로는 7번, "Section 5.10" 이 6.18번. 그림·절을 추가하면 조용히 어긋나는 구조였다. ③ 이득식이 `F_x` 를, 나머지 전부가 `f_n` 을 써서 **같은 양에 이름이 둘**이었다. ④ "타사 그림은 싣지 않는다" 는 문장이 남아 있었다(지금은 6장을 싣는다). ⑤ 개정 이력에 V1.1 행이 없었다 |
| 2026-09-07 | **교차 참조를 이름으로 푸는 2-pass 빌드를 넣었다.** 1차 통과에서 그림·표·절이 받은 번호를 기록하고 2차 통과에서 `figref`/`tblref`/`secref` 로 해결한다. **번호를 손으로 적는 방식은 그림 하나만 추가해도 전부 깨진다** — 실제로 깨져 있었다 |
| 2026-09-07 | **초보자가 막힐 지점을 보강했다.** `f.n`·`M`·`Z.0`·`Q`·`V.o,eff`·`N.rect` 가 **정의 없이 쓰이고 있었다** — 관례 목록에 넣고, FHA 절에서 M 과 Q 를 각각 한 단락씩 풀었다. "양쪽 포트가 전압원이라 게인이 강제된다" 는 **왜** 그런지를 두 단락으로 늘렸다(게인은 두 전압의 비인데 둘 다 밖에서 못박혀 있다 → 주파수가 움직이는 것은 Q, 즉 전력). 이득식에는 눈으로 하는 검산 둘(`f.n`=1 에서 M=1, `f.n`→∞ 에서 1/(1+λ))을 붙였다 |
| 2026-09-07 | **AN 에 인덕턴스 공차 경고를 넣었다.** `k.floor` 1.021 옆에 "버티는 L 저하 = 1−1/k.floor² = 4.1 %" 와 **"±10 % 사양은 여기 안 들어간다 — 트랜스포머 발주 전에 확인하라"** 를 적었다. 시트와 사양서가 다른 문서라 이 둘이 만나지 않고 있었다. 그리고 표지의 빈 파란 띠에 전력단 회로도를 넣었다 |
| 2026-09-07 | **영문 AN 에 배경 절을 신설했다 — LLC 가 뭐고 PFC 가 뭐고 왜 합칠 수 있는가.** 이전 판은 단일단 이론으로 곧장 들어가 **독자가 LLC 와 PFC 를 이미 만들어 봤다고 가정**하고 있었다. 소절 6개: LLC 가 무엇인가 / 공진이 무엇을 사는가(ZVS) / 회로가 어떻게 `M(f_n, Q)` 가 되는가(FHA 3단계) / PFC 가 무엇인가 / 왜 보통 2단인가. **버스 커패시터가 하는 두 가지 일(버퍼 + 탱크 격리)을 분리해 적은 것이 핵심**이다 — 단일단은 그 둘을 동시에 잃는다 |
| 2026-09-07 | **회로 그림은 직접 그리지 않고 참조 가이드에서 땄다.** 처음엔 `schem.py` 로 직접 그렸는데 상자가 축 밖으로 나가는 등 품질이 안 나왔다. onsemi AN-4151 의 Fig 3(하프브리지 LLC) · Fig 4(파형) · Fig 5(R.ac 유도) · Fig 6(AC 등가회로) · Fig 8(누설 통합 등가회로)과 MPS Part 2 의 Fig 3(AC/DC 블록도)을 썼고, **캡션마다 출처를 적었다.** `pdffig.py` 에 **`--nocap`**(원본 캡션 제외 - 우리 문서가 자기 캡션을 다니까)과 **흰 여백 자동 트림**을 넣었다. 잘림 여부는 여섯 장 전부 눈으로 확인했다 |
| 2026-09-07 | **AN 의 설계점을 7.5:1 로 옮겼다 — 정본 시트·워크북은 9:1 그대로.** `an_pdf.AN_VARIANT` 로 **변형 시트를 직접 지정**한다(`Smath/variants/L6790A_7p5to1.sm`). `figs.py` 도 같은 시트를 따른다. 처음에 정본 시트와 워크북까지 7.5:1 로 바꿨다가 되돌렸다 — **변형별 산출물이 이미 3벌씩 있으므로 정본을 건드릴 이유가 없었다.** 워크북은 `BACKUP_3` 에서 복구했다 |
| 2026-09-07 | **7.5:1 은 무부하 최소 게인 해가 없어서 그 절의 문장이 깨졌다** — `M.∞` 0.6452 대 요구 0.6409 로 근호 안이 음수가 되어 파이썬이 복소수를 돌려준다. 해가 있을 때와 없을 때를 나누어 쓰도록 고쳤고, **없는 쪽이 오히려 그 절이 말하는 바를 보여 준다.** 그리고 권선비 표기를 `n.T : 1` 에서 실제 턴수 `15 : 2` 로 바꿨다 — 2차가 1턴일 때만 맞는 표기였다 |
| 2026-09-07 | **한국어 가이드와 대조해 영문 AN 에 빠져 있던 이론 5건을 채웠다.** ① **`f.sw(θ)` 닫힌형** — AN 은 "주파수 프로파일이 곧 PFC" 라고만 하고 **어떻게 구하는지를 안 적고 있었다.** `x = 1/f_n²` 로 3차식이 되고 근의 곱이 음수라 **항상 실근 3개 중 하나만 음수 → 물리적 근은 언제나 가운데**, 분기 판정이 필요 없다는 것까지 넣었다(가이드 C.20). ② **FB 핀 제어 법칙** `V.FB = 0.5 + 0.167·R.CS·P.in` — 최대전력·버스트·과부하가 한 스케일 위에 있는 이유다. ③ **Venable K-factor Type II 보상 설계법** — AN 은 소자값만 있고 방법이 없었다. ④ **이득 여유와 `f.180` 닫힌형** — 근호 안이 음수면 오류가 아니라 "위상이 −180° 에 안 닿는다" 는 답이라는 것까지. ⑤ **입력 커패시터** `C.in = 3 nF/W × P.in` 과 그것이 필터가 아니라 **THD 항**이라는 것. 그리고 `R.ac` 의 `4/π²` 가 일반 LLC 의 `8/π²` 의 절반인 이유(라인 피크 순시전력 기준)를 주의 상자로 넣었다. AN 25 → **26쪽** · 식 20 → **27** |
| 2026-09-07 | **`figs.py` 도 7.5:1 에 고정돼 있어 그림이 본문과 어긋나 있었다** — 그림은 `f.o` 90.4 kHz · 합성 피크 18.32 A 인데 옆의 표는 85.1 kHz · 17.70 A 였다. `an_pdf.py` 와 같은 방식으로 **시트에서 설계점을 읽게** 하고 AN 그림을 전부 다시 만들었다. 한국어 가이드용 `figures/` 는 건드리지 않았다 — 그쪽 본문이 아직 구 수치라 같이 두어야 맞다 |
| 2026-09-07 | **mathtext 는 `igl`·`\Bigl` 계열을 모른다.** 두 번 걸렸다 — 처음엔 병렬 연산자, 다음엔 3차식. 평범한 괄호로 바꿔야 한다. `an_body.py` 에서 10곳을 치환했다 |
| 2026-09-07 | **영문 AN 을 특정 응용에서 떼어냈다 (v1.0 → v1.1).** ① 트랜스포머 절이 **3분할을 뼈대로** 쓰고 있었다 — 사양표가 "per unit, 3 units per set" 이었다. **트랜스포머 1개 기준**(권선비 9:1 · 개방 35 µH · 단락 11 µH · 2차 30.3 A rms / 116 A pk)으로 다시 쓰고, 분할은 **"한 대로 안 되면" 한 절**로 내렸다. 분할의 결과 3가지(인덕턴스·2차 전류는 /N.x, **1차 전류는 안 나뉜다**, 시험 전류는 안 변한다, 권선비가 N.x/N.s 단위로 양자화)만 남겼다. ② **응용 특정 서술을 전수 제거** — OLED·TV·패널·세트·end product. 리플 5 % 는 이제 "부하의 성질이지 컨버터의 성질이 아니다" 로만 쓴다. ③ 요약표의 트랜스포머 행도 `N.p/N.s in 3 series sections` → 개방/단락 인덕턴스로 바꿨다 |
| 2026-09-07 | **AN 에 무부하 최소 게인 한계 절 신설.** 무부하 게인은 `1/(1+λ)` 아래로 안 내려가므로 **요구 최소 게인이 그보다 낮으면 [26] 에 해가 아예 없다** — 스프레드시트가 오류를 뱉는 것이 수식이 깨진 게 아니라 질문에 답이 없는 것이다. 이 설계는 점근선 0.686 대 요구 0.793 이라 231 kHz 에서 해가 있다. **전부하 검사로는 절대 안 드러나는 성질**이라는 것을 함께 적었다 |
| 2026-09-07 | **핀 배정을 승인원 방식으로 통일했다 — NS2 3→5 · NS3 4→6.** 2026-09-04 에 빌더는 사용자 회로도를 따라 **인접핀 3–4 / 5–6** 으로, `CLAUDE.md` 는 **승인원 엇갈림 3→5 / 4→6** 으로 적혀 두 기록이 갈라져 있었고 맞춰본 적이 없었다. **전기적으로는 완전히 같다** — 어느 쪽이든 4+5 를 이으면 끝이 3·6 인 같은 센터탭 권선이 된다. 다른 것은 각 권선 끝이 보빈 어느 핀에 오느냐뿐이고, 그건 **PCB 풋프린트** 문제다. 승인원 쪽을 택한 이유: 기존품 2차가 `Cu plate 1 Ts` 인데 **1턴 동판은 보빈 한쪽으로 들어가 반대쪽으로 나오므로 두 끝이 반대편 핀 뱅크에 떨어진다** — 인접핀은 동판 양 끝이 같은 쪽으로 나와야 해서 부자연스럽다. 센터탭 문구(4+5)는 어느 쪽이든 그대로 유효하다 |
| 2026-09-07 | **영문 AN 이 7.5:1 과 9:1 을 섞어 쓰고 있었다.** `an_pdf.V` 의 `R` 은 `l6790.design(Lm_sel=20e-6, n_sel=6.)` 로 **7.5:1** 에 고정돼 있었는데 `SH` 는 정본 시트(**9:1**)에서 읽고 있었다. 그래서 같은 표에 `n` = 6.02 와 `n.T` = 9.00 이 나란히 인쇄됐다 — **서로 다른 트랜스포머다.** `R` 이 시트에서 설계점을 읽도록 바꾸고, 손으로 박혀 있던 `Np`·`Ns`·`nser`·`AL`·`Bpk`·`Cr`·`Lr`·`Lm` 도 전부 시트에서 읽게 했다 |
| 2026-09-07 | **영문 AN 에 트랜스포머 절 신설 — 소절 6개.** 두 권선비·두 인덕턴스 / 사양서에 무엇을 적는가 / 자속은 2차가 정한다 / 포화 시험 전류 / 분할 트랜스포머. **일반 가이드로 썼다 — 특정 고객·벤더·기존 보드 언급 없음.** 수치는 전부 `an_pdf.V` 보간이라 손으로 박은 상수가 없다. 사양서 표 1개 추가. AN 20 → **24쪽** |
| 2026-09-07 | **AN 에 회로도 3장을 넣었다** — `bom_power_stage`(전력단 전체, CT/FB 2안), `bom_pin_config`(IC 주변), `comp_network_st`(보상망). **셋 다 `figures/` 에 있었는데 AN 이 안 쓰고 있었다.** `figures/an/` 으로 복사해 AN 의 설계 절차·컨트롤러·루프 세 자리에 배치 |
| 2026-09-07 | **영문 AN 의 그리스 문자와 수학 기호가 통째로 빠져 있었다.** reportlab 내장 Helvetica 는 Latin-1 이라 `λ`·`μ`·`Ω`·`η`·`√`·`≥`·`≈`·`Δ` 가 **조용히 사라지거나 엉뚱한 글리프**로 나왔다(`L.μ` → `L∝`, `20.97 Ω` → `20.97 │`). `use_unicode()` 로 **Arial** 을 등록했다 — Helvetica 와 메트릭 호환이라 레이아웃이 움직이지 않는다. 한국어 문서의 `use_korean()` 경로는 그대로다 |
| 2026-09-07 | **공차를 개방·단락 모두 ±10 % 로 통일했다 — 사용자 결정.** 직전에 `k.floor` 에서 계산한 비대칭 공차(7.5:1 +10/−3 %)를 넣었는데, **양산 공정이 그런 기형 공차를 못 맞춘다.** 되돌렸다. 대가는 명확하다 — **7.5:1 은 개방 인덕턴스가 −10 % 로 나오면 하드 스위칭한다**(버티는 저하 4.1 %). 7.5:1 을 채택한다면 **`R.T` 를 11 → 10 kΩ 으로 내려 `k.floor` 를 1.021 → 1.117(버티는 저하 19.9 %)로 올리는 것이 유일한 방법**이다. 9:1(14.9 %)·6:1(58.9 %)은 ±10 % 로 문제없다 |
| 2026-09-07 | **단락 측정 조건을 잘못 고쳤다가 되돌렸다 — 사용자 지적이 맞았다.** "동작 중에는 레그 하나만 도통하니 하나만 단락해야 한다"고 바꿨는데, **기존 양산품 26OP-LM83W 사양서 4-2 가 `(SECONDARY ALL SHORT)` 로 적고 있다** — 같은 벤더(KH필룩스) · 같은 CT 구조다. 내 논리는 두 2차 반쪽이 자기적으로 분리된다고 가정한 것인데, 실제로는 둘 다 같은 `Secondary SECTION` 에 감기므로 누설 자속 경로가 1차↔2차 구획 사이 기하로 정해져 **어느 쪽이 전류를 흘리든 거의 같다.** 무엇보다 **벤더의 지그와 검사가 그 조건에 맞춰져 있다.** 원복하되 보조는 단락하지 않는다는 것만 명시했다(양산품도 secondary 만 단락한다). **증거 없이 관례를 뒤집지 말 것** |
| 2026-09-07 | **개방 인덕턴스 공차를 평평한 ±10 % 로 만들면서 비대칭 보호를 잃었었다.** 예전에 KH 양식에 채워 넣었던 7.5:1 판은 `10.33uH **+10/-4%**` 였고, 양산품도 `53uH +7/-10 %` 로 비대칭이다. 빌더를 다시 쓰면서 ±10 % 로 평평하게 만들어 **7.5:1 이 자기 공차에 깨지는 상태**가 됐던 것이다. `k.floor` 에서 계산하도록 복원했다 — 버티는 저하 `1−1/k.floor²` 의 80 % 를 쓰고 10 % 로 자른다. 결과: **9:1 ±10 % · 7.5:1 +10/−3 % · 6:1 ±10 %**. 7.5:1 만 비대칭이 필요하고, 그 이유를 사양서에 한 줄 넣었다 |
| 2026-09-07 | **`build_trans_spec.py` 가 Excel 잠금에 걸리면 이유를 말하도록 했다** — `PermissionError` 를 그대로 던지고 있었다 |
| 2026-09-07 | **벤더 사양서의 단락 인덕턴스 측정 조건이 틀려 있었다.** "All other windings SHORTED" 라고 적어 두었는데, 그러면 NS2·NS3·NAUX 를 **동시에** 단락한다. **동작 중에는 정류 레그 하나만 도통하고 보조는 고임피던스 분압**이므로, 권선을 더 단락할수록 측정값은 낮아진다 — 벤더는 그 낮은 값을 3.67 µH 로 맞추려고 **누설을 필요 이상으로 키우게** 되고 실제 `L.r` 은 11 µH 보다 커진다. 조건을 **"Short NS2 (3–4) ONLY. NS3 and NAUX OPEN"** 으로 고치고 이유를 사양서에 한 줄 남겼다. **개방 측정은 원래 맞았다** |
| 2026-09-07 | **`Final Design Check` 의 `L.μ`·`L.L1`·`L.L2` 는 사양으로 주면 안 된다.** 셋은 `L.L1 = n.T²·L.L2` 라는 **대칭 가정**으로 만든 분해이고(결합계수 k = L.mu/L.open = √(L.m/L.open) = n/n.T = 0.8281 로 확인), 실제 누설 배분은 권선 배치가 정한다. **어느 단자쌍에서도 측정되지 않으므로 벤더가 검사할 수 없다.** 우리가 주는 개방·단락은 그 블록을 **측정 가능한 형태로 쓴 같은 값**이다(35.00 / 11.00 µH 세트). 그리고 배분은 우리에게 영향이 없다 — 탱크는 개방·단락만 보고, 자속도 `L.L2·ω·I.sec_pk` = 8.19 V < `V.out` 25 V 라 상호전압이 도통 구간 내내 양(+)이고 누설 볼트-초 적분이 0 이라 **`B.pk` 가 배분과 무관하다** |
| 2026-09-07 | **`check_trans_spec.py` 가 셀 번지를 박아 두어 사양서 행이 한 줄 밀리자 엉뚱한 칸을 읽었다** — `A.e` 를 92 로, `f.sw` 를 빈 칸으로. 고치는 대상과 같은 부류의 실수였다. **라벨로 행을 찾도록** 바꿨다 |
| 2026-09-07 | **3종 열·효율·안정성 비교 — 결론이 갈린다.** 계산 손실은 **7.5:1 이 최저**(33.49 W · η 95.15 %), 9:1 34.87 W, 6:1 39.16 W. 그런데 트랜스포머 자체는 **코어가 지배**한다 — 각자 0.20 T 에 맞춘 코어를 쓰면 필요 `A.e` 가 **103 / 206 / 211 mm²** 라 7.5:1 코어 체적이 절반이고 코어손이 약 **1/2.8** 이다. 2차 피크도 98.6 A 로 9:1 의 115.6 A 보다 15 % 낮다. 반대로 **여유는 9:1 이 최고**다(`k.ZVS` 2.408 · `k.ceil` 1.526 · `k.OCP` 1.295, 그리고 무부하 주파수 해가 있는 유일한 설계점). **6:1 은 둘 다 진다** |
| 2026-09-07 | **7.5:1 은 사양서가 요구하는 ±10 % 인덕턴스 공차를 못 견딘다.** `k.floor` 1.021 은 L 저하 **4.0 %** 까지만 버티는데 사양은 10 % 다. −10 % 에서 `f.o` 90.39 → 95.28 kHz 가 되어 `f.Min` 92.25 kHz 를 넘고 **하드 스위칭한다.** 9:1 은 14.9 %, 6:1 은 58.9 % 를 견딘다. **`R.T` 11 → 10 kΩ 이면 7.5:1 도 19.9 % 로 풀린다.** `CLAUDE.md` 시트 §4.2 5b 갱신 |
| 2026-09-07 | **전면 재검토 ① — 벤더 사양서의 손입력 상수가 낡아 있었다.** `build_trans_spec.py` 의 `VARIANTS` 가 인덕턴스·전류·A.e·주파수를 전부 손으로 박은 상수로 갖고 있었고, **6:1 판의 `f.Min` 이 90 kHz** 로 적혀 있었다 — 참값 110 kHz. 다른 두 판에서 복사된 것이다. 값은 시트에 정확히 있었고 **옮겨 적는 순간 끊겼다.** 빌더가 변형 시트에서 읽도록 바꾸고, **완성된 xlsx 를 다시 읽어 시트와 대조하는** `check_trans_spec.py` 를 새로 만들었다(9항목 × 3변형, 불일치 0). 가이드 C.33 |
| 2026-09-07 | **전면 재검토 ② — 6:1 의 `C.T` 390 pF 가 자기 설계 상한 375 pF 밖이었다.** `k.CTd` = 0.962 로 시트가 정확히 잡고 있었는데 읽지 않았다. 설계 창 193…375 과 DS 창 270…1000 의 교집합 기하중간 318 pF 에서 가장 가까운 E12 인 **330 pF** 로 고쳤다. `k.CTd` 0.962 → **1.136**, `k.floor` 1.332 → **1.559**, `k.ceil` 1.031 → **1.178**, `f.Min` 110.1 → 128.9 kHz. **6:1 의 여유가 세 항목 다 좋아졌다.** 워크북 변형 `Device Setting D21` 도 같이 고쳤다. 가이드 C.34 |
| 2026-09-07 | **전면 재검토 ③ — 시트 주석 6개가 9:1 숫자를 문자열로 갖고 다른 변형에 그대로 복사되고 있었다.** 가장 나쁜 것은 **7.5:1 시트가 `R.ZCD_H` 로 330 kΩ 을 선정해 놓고 바로 옆에 "330 k 로 두면 OVP1 이 14 V 에서 걸려 기동 못 한다"** 고 적어 놓은 것이었다. `C.r`·`L.m`·`k.lam`·`n.aux`·`R.ZCD_H_sel`·`C.T` 여섯 주석을 전부 **값에서 문장을 만들도록** 바꿨다. 가이드 C.33 |
| 2026-09-07 | **발견 — 무부하 최소 게인 조건은 해가 없을 수 있고, 7.5:1 과 6:1 이 그렇다.** 무부하 게인은 점근선 `1/(1+λ.act)` 아래로 내려가지 않는다. 9:1 은 M.FBmax 0.7928 > 점근선 0.6859 라 **231 kHz 에서 해가 있고**, 7.5:1 은 0.6409 대 0.6452(0.7 % 차이로 해 없음), 6:1 은 0.5299 대 0.6893 이다. **워크북 `RTC` 26·27행이 `#NUM!` 이었던 것이 바로 이것** — 계산이 깨진 게 아니라 묻는 질문에 답이 없었던 것이다. 불합격이 아니라 **버스트 모드가 맡는 영역**이지만, **무부하 조절을 주파수로 끝낼 수 있는 설계점은 9:1 뿐**이라는 것은 본사의 7.5:1 권고 어디에도 없던 정보다. 가이드 C.31 |
| 2026-09-07 | **자속을 자화 인덕턴스로 계산하면 √(1+λ) 만큼 낙관적이다.** 시트의 `B.pk` 는 2차 볼트-초 식이라 `V.o_eff/(4·f.r·N.s·A.e)` 로 정리되고 **`N.p` 가 아예 없다** — 1차 턴수를 늘려도 자속은 안 내려간다. 같은 값을 `L.m·I.Lm_pk/(N.p·A.e)` 로 구하면 302 mT 가 나와 **364 mT 보다 17 % 작다**. 모델의 `L.m` 가지 전압이 `n·V.out` 인데 실제 반사 전압은 `n.T·V.out` 이기 때문이다. **시트는 원래 보수적인 쪽을 쓰고 있었다** — 확인만 한 것이지 고친 것은 없다. 가이드 [149][150] |
| 2026-09-07 | **개방 포화 시험 전류는 1차 피크 전류가 아니다.** 벤더 DC 중첩 시험은 다른 권선을 개방하므로 **2차 기자력 상쇄가 전혀 없고**, 같은 전류가 훨씬 큰 자속을 만든다. 기준은 동작 자속을 재현하는 `I.sat_eq = B.pk·N.p·A.e/L.open_x` 이고 9:1 에서 **10.6 A** 다. 1차 피크 17.7 A 를 그대로 적으면 **0.61 T 를 요구하는 셈이라 어떤 페라이트도 통과 못 한다.** 사양서의 16 A 는 여유 51 % 로 정상이었다. 가이드 [152] |
| 2026-09-07 | **시트 §12 에 `A.e_req`·`k.Ae`·`I.sat_eq`·`I.sat_spec` 4행 신설.** 둘 다 벤더에 나가는 값인데 시트에 없었다. `k.Ae` = 0.549 — **코어 미선정을 판정으로 드러낸다**(EE6405 113 mm² 대 필요 206 mm²). 7.5:1 은 1.098 로 통과한다. 사양서의 시험 전류도 이제 `I.sat_spec` 에서 온다 — 여유 계수가 두 곳에 있으면 또 갈라진다 |
| 2026-09-07 | **`k.` 로 시작한다고 전부 판정이 아니다.** `K.Tpri`·`K.Tsec`·`K.ring` 은 **[PICK] 입력**이고 `r.tr` 은 절단 인수(공진 아래에서 1 이 정상), `k.PM`·`k.OVP1`·`k.lam` 은 **한계가 아니라 목표에 대한 비**다. 시트 §17 의 "Not verdicts" 주석과 `snapshot.py` 의 제외 목록에 전부 실었다. 가이드 C.32 |
| 2026-09-07 | **`snapshot.py` 의 `k.PSR` 설명이 낡아 있었다** — "3 병렬이면 1.598" 은 7.5:1 값이고 9:1 에서는 1.39 다. 그리고 `k.PSR`·`k.Ae` 는 **아직 안 닫은 항목인데 "오류가 아니다" 로 끝나 닫힌 것처럼 읽혔다.** `STILL_OPEN` 을 두어 문구를 갈랐다 |
| 2026-09-07 | **6:1 의 `k.NRCS` 0.966 은 열어 둔다.** 1차 라인 실효 12.37 A 라 감지저항이 5.17개분 소산이다. 6개로 올리면 `R.CS` 24 mΩ 유지에 개당 144 mΩ 이 필요한데 E24 에 없고, 150 mΩ 이면 `Pin.max` 가 672 W 로 `P.in` 684.8 W 아래로 떨어진다. **소자 결정이라 임의로 바꾸지 않았다** |
| 2026-09-07 | **설계점 3종을 시트·워크북·사양서 아홉 벌로 만들었다.** 이름을 `nT9/nT7p5/nT6` 에서 **`9to1`·`7p5to1`·`6to1`** 로 바꿨다 — 한눈에 권선비가 읽히게. SMath 는 `L6790_VARIANT` 환경변수, 사양서는 명령행 인자, 워크북은 정본 복사 후 셀 패치 + Excel 전체 재계산이다. 셋 다 `mismatches: 0` · `VERDICT OK` 이고 워크북 `n.T` 가 9.000 / 7.500 / 6.000 으로 정확히 떨어진다 |
| 2026-09-07 | **`C.T` 는 `Device Setting D21` 에 직접 입력이다 — F열 선정 셀이 아니다.** 처음에 `F21` 에 써서 안 먹었고, `k.CT` 만 바뀌고 `f_Min`·`f_Max` 가 그대로라 알아챘다. 6:1 은 `C.T` 390 pF 가 있어야 `f_Max` 372.9 kHz 가 열린다 — 470 pF 의 319.6 kHz 로는 FB 코너 362 kHz 에 못 닿는다 |
| 2026-09-07 | **openpyxl 로 읽은 캐시가 Excel 이 계산한 값보다 낡아 있을 수 있다.** 워크북을 패치하고 COM 으로 `CalculateFullRebuild` + `Save` 를 돌렸는데도 `D27`·`D28` 이 옛 값으로 읽혔다. Excel 을 다시 열어 `Value2` 를 직접 물으니 제대로 나왔고, 그 상태로 저장하니 캐시도 맞았다. **워크북을 고친 뒤에는 COM 으로 값을 직접 확인할 것** - openpyxl 만 믿으면 안 된다 |
| 2026-09-07 | **사고 재발 — 벤더 사양서 정본을 또 덮어썼다(75 KB·그림 2장 → 6.8 KB·그림 0장).** 패치 스크립트가 중간에 실패했는데 그 뒤 빌더를 3번 돌렸고, 그때 빌더의 출력 경로가 아직 정본이었다. **어제 같은 사고를 내고 "셀 단위로만 고치겠다" 고 적어 놓고 반복했다.** 재발 방지로 `build_trans_spec.py` 를 **인자 없이 실행하면 거부**하게 하고 출력을 `variants/` 로만 내보내도록 바꿨다 — 이제 정본 경로를 건드릴 수 없다. 복구는 OneDrive 버전 기록(12:58 이전) |
| 2026-09-07 | **패치 스크립트에서 같은 실수를 두 번 했다 — bash heredoc 안의 `\n` 이 실제 줄바꿈이 된다.** `<<'PY'` 로 인용해도 파이썬 문자열 리터럴 안의 `\n` 이 살아남지 못해 SyntaxError 가 났다. **여러 줄 문자열을 넣는 패치는 heredoc 말고 Edit 로 할 것** |
| 2026-09-07 | **6:1 을 실제로 살려서 3종 비교를 완성했다.** 탱크를 다시 잡았다 — `C_r` **180 nF** · `L_r` **6.4 µH** · `L_m` **14.2 µH**(`Z_o` 10.49 → **5.96 Ω**) · `C_T` 470 → **390 pF**. 세 변형 전부 `mismatches: 0` · `VERDICT OK` 다. 전에 6:1 이 참조 구현과 2건 어긋났던 것도 사라졌다 — 어긋난 원인은 6:1 자체가 아니라 그 탱크가 닫힌형이 못 다루는 영역에 있었기 때문이었다 |
| 2026-09-07 | **`C_T` 를 변형별로 만들어야 했다 — 6:1 은 470 pF 로는 못 돈다.** FB 코너가 **361.9 kHz** 를 요구하는데 `C_T` 470 pF 의 `f_Max` 는 319.6 kHz 다. 390 pF 로 내리면 `f_Max` 373.0 kHz 로 열리고 `f_Min` 이 92.3 → 110.1 kHz 로 올라 `k.floor` 도 1.020 → **1.332** 로 좋아진다. 대신 **영교차 클램프가 넓어져 THD 가 나빠진다** — `f_Min` 이 높을수록 라인 영교차 부근에서 하한에 걸리는 구간이 커진다 |
| 2026-09-07 | **3종 최종 비교 (전부 성립한다).** ZVS·2차·뱅크는 **6:1 이 최고**(`T_ZC` 657 ns · 2차 8.94 A rms / 28.0 A pk · 뱅크 27.33 A), 1차는 **7.5:1 이 최고**(라인 rms 10.93 A · 탱크 피크 18.32 A), 9:1 은 그 사이다. **`n` 이 작으면 2차 전류가 1차로 크게 반사되지만, `Z_o` 를 낮춰 탱크를 무겁게 만들면 자화 전류가 커져 ZVS 와 2차 파형이 좋아진다** — 그 맞바꿈이 6:1 의 성격이다 |
| 2026-09-07 | **6:1 의 대가 셋.** ① `C_r` 100 → 180 nF (400 V 급 필름, 부피·비용) ② **`k.ceil` 1.031 — 여유가 거의 없다**(9:1 1.526 · 7.5:1 1.284). `f_sw` 가 362 kHz 까지 올라가 스위칭 손실과 자성 부품 주파수가 같이 오른다 ③ `f_Min` 110 kHz 로 THD 악화 |
| 2026-09-07 | **코어는 탱크로 못 바꾼다 — 세 변형의 `A.e` 최소가 9:1 206 · 7.5:1 103 · 6:1 211 mm² 다.** 근본은 **1차 턴수가 적으면 자속이 오른다**는 것이고(9:1 은 3턴, 6:1 은 2턴), 탱크 정수는 여기 안 들어온다. **EE6405(113 mm²)에 들어가는 것은 7.5:1 뿐**이고 그것은 2차 2턴을 요구한다. 2차 2턴이 가능한가 하는 질문이 여전히 유일한 갈림길이다 |
| 2026-09-07 | **6:1 이 불가능하다는 앞의 결론을 철회한다 — 탐색을 잘못했다.** `C_r` 100 nF · `L_r` 11 µH 를 고정한 채 `L_m` 만 훑고 "성립 창이 없다" 고 적었는데, 탱크에는 자유도가 셋(`C_r`·`L_r`·`L_m`)이다. (λ, f_r, Z_o) 로 다시 매개화해 **280 조합을 전수 탐색하니 121개가 ZVS 를 통과**한다. 최대 `T_ZC` 1676 ns 로 9:1(530 ns)보다 훨씬 낫다 |
| 2026-09-07 | **ZVS 는 자화 전류가 좌우하고 그것은 닫힌형으로 나온다 — `I_Lm,pk = 39.27·n_T·λ/(Z_o·√(1+λ))`** (V_out = 25 V). 검산: 9:1 → 12.79 A, 7.5:1 → 12.40 A 로 시트값과 일치한다. **핵심은 `Z_o` 에 반비례한다는 것**이다. 6:1 은 `n` 이 작아 불리하지만 **`Z_o` 를 10.49 → 4~6 Ω 으로 낮추면 그 손해를 덮고도 남는다**(자화 13~24 A). 앞선 실패는 6:1 의 결함이 아니라 `Z_o` 를 안 건드린 탓이었다 |
| 2026-09-07 | **6:1 의 진짜 대가는 ZVS 가 아니라 1차 응력이다.** `Z_o` 를 낮추려면 `C_r` 을 키우고 `L_r` 을 줄여야 한다 — 대표 후보 `λ 0.45 · f_r 150 kHz · Z_o 6 Ω` 이면 `C_r` **177 nF**(현재 100), `L_r` 6.37 µH, `L_m` 14.1 µH, `T_ZC` 646 ns. `C_r` 이 1.3~3.3배로 커져 400 V 급 필름 커패시터의 부피·비용이 붙고, 탱크 순환 전류가 커지며(`I_pk` 18~24 A 대 현재 17.7), `L_m` 이 작아 자화 전류와 코어 포화 요구가 올라간다. **"불가능" 이 아니라 "1차 응력을 더 지불하는 설계" 다** |
| 2026-09-07 | **교훈 — 설계 후보를 탈락시킬 때 자유도를 다 훑었는지 확인할 것.** 6:1 을 세 번(λ 하한·ZVS·Cardano) 실패시키고 "우연이 아니다" 라고 적었는데, 세 실패가 전부 같은 1차원 절단면 위에 있었다. `l6790.py` 로 (λ, f_r, Z_o) 3차원을 도는 데 2분이면 됐다 |
| 2026-09-07 | **`R.CFG` 판정 두 개 신설 — `k.CFG_lo` · `k.CFG_hi`.** 선정 30 kΩ 이 **모핑이 살아 있는 15~47 kΩ 창** 안에 있는지 아무 데서도 안 봤다. 이 창을 벗어나면 IC 가 모핑을 멈추고 고정 하프브리지로 도는데, **탱크가 2:1 구동 계단에 맞춰 설계돼 있어서 그러면 성립하지 않는다.** 지금 2.00 / 1.57 로 통과. 한편 `audit_sm` 이 함께 지적한 `R.CFG_max`·`C.T_max/min`·`N.RCS_min`·`R.CS_single_max`·`V.out_min` 은 **오탐이었다** — 각각 `k.BO`(`V.BO_act ≤ V.AC_min` 과 `R.CFG_sel ≤ R.CFG_max` 는 같은 부등식이다) · `k.CTd` · `k.NRCS` · `k.RCS` · `k.hold` 가 이미 등가로 검사한다. 이름이 달라 감사기가 못 알아본 것이다 |
| 2026-09-07 | **`audit_fet.py` 의 낡은 셀 참조를 고쳤다** — `kT.pri` 를 `D80` 에서 찾고 있었으나 FET 블록 재배치 때 `D58` 로 옮겨졌다. 이제 "두 식 모두 Tj,max 기준으로 평가된다" 로 통과한다. **1차 손실 과소평가 경고는 오탐이었다** |
| 2026-09-07 | **변형 3종의 트랜스포머 값을 참조 구현으로 독립 재계산해 사양서와 대조했다 — 전부 일치.** 9:1 기준 `L_open` 11.67 · `L_short` 3.667 µH · 1차 11.06 A rms / 17.70 A pk · 2차 10.14 / 38.5 A · 포화(OVP2) 15.68 A · `Ae` 206 mm². 세 변형 모두 `L_short` 가 3.667 µH 로 같다(`L_r` 11 µH 고정). `Ae` 최소는 **9:1 206 · 7.5:1 103 · 6:1 206 mm²** — 7.5:1 만 EE6405(113)에 들어간다 |
| 2026-09-07 | **`λ` 하한을 틀린 코너에서 계산하고 있었다 — 전면 재검토에서 나온 유일한 실질 결함.** `λ.min.1 = 1/M.Vmax − 1` 이 `M.Vmax` 를 **V.AC_max = 264 V** 에서 평가했다. 그런데 이 식은 *탱크가 가장 낮은 요구 게인까지 내려갈 수 있는가* 를 묻고, 그 게인은 **등가입력이 가장 높은 FB 모핑 코너 332.34 V** 에서 나온다(무부하 게인 하한이 1/(1+λ) 이므로 λ ≥ 1/M − 1). **`RTC!C9` 는 원인 E 에서 이미 같은 코너로 옮겼는데 λ 사슬만 빠져 있었다.** 시트 자신이 `V.in_FBmax` 주석에 "ST 툴은 여기서 V.AC_max 를 평가한다" 고 적어 놓고도 정작 λ.1 은 안 고쳤다. **λ.min.1 0.0019 → 0.2613 · 요구 λ 0.125 → 0.5785** (137배 과소평가였다). `l6790.py` · 빌더 · 워크북 `Res. Tank Design D12` 세 곳을 같이 고쳤다(CHANGELOG 94행) |
| 2026-09-07 | **그 결함이 안 보였던 이유 — `λ.act` 를 `λ` 와 비교하는 판정이 아예 없었다.** 시트가 요구치를 계산해 놓고 선정값과 대조하지 않으니 틀린 값이어도 티가 안 났다. **`k.lam = λ.act/λ` 를 신설**했다. 지금 0.79 로 나오는데 **불합격이 아니다** — `λ` 는 `f.sw_max_spec` 225 kHz 에서 묻는 **무부하** 요구이고, 선정 λ 0.4583 이면 무부하 FB 코너가 **231 kHz** 를 필요로 한다. 목표를 3 % 넘을 뿐 **VCO 상한 319.6 kHz 안**이고, 전부하에서는 같은 코너를 209.5 kHz 에 도달한다(`f.sw.b`). 판정 행 주석에 그렇게 적었다 |
| 2026-09-07 | **전면 재검토 — 나머지는 전부 통과.** `audit_rpn` 512식 불일치 0(RPN 을 되읽어 독립 계산) · `audit_dim` 차원 충돌 0 · `check_sm` VERDICT OK · `bom_compare` 29항목 차이 0 · `xl_sm_compare` **145항목 전부 일치** · `rtc_check` 18점 편차 0.0000 % · `audit_md` 통과 |
| 2026-09-07 | **도구 두 개가 낡아 거짓 경보를 내고 있었다.** ① `rtc_check.py` 에 설계점 `n=6.0 · L_m=20 µH` 가 박혀 있어 9:1 에서 `M_req` 가 통째로 −19.5 % 어긋나 **거짓 FAIL 54건**. 워크북 `Res. Tank Design F9·F45` 에서 읽도록 고쳤다 — **도구에 설계점을 박아 두면 설계가 바뀔 때 조용히 낡는다.** ② `audit_fet.py` 는 `kT.pri` 를 `D80` 에서 찾는데 실제로는 `D58` 로 옮겨졌다(FET 블록 재배치 때). "1차 손실 과소평가" 경고는 **오탐**이다 — `D79 = D70*D58*(...)²` 로 온도계수가 제대로 곱해져 있다. 도구는 아직 안 고쳤다 |
| 2026-09-07 | **본사가 7.5:1 을 추천하는 근거를 검증했다 — 방향은 맞고 크기는 작다.** 공진 아래에서는 2차 전류가 불연속이라 같은 출력에 첨두·실효가 커진다: `I_sec,pk` 98.6 → 115.6 A(+17 %) · `I_diode` 28.4 → 30.3 A · `I_Cout` 30.2 → 33.9 A. 손실로는 2차 도통 5.64 → 6.48 W 를 포함해 **합계 20.24 → 21.52 W, 효율 0.19 포인트**. **다만 본사가 안 말한 반대편이 있다** — 공진 아래는 SR 이 영전류에서 꺼진다(ZCS). 9:1 은 항상 ZCS, 7.5:1 은 라인 주기 안에서 두 모드를 오간다. SR 턴오프 손실은 컨트롤러 타이밍에 달려 있어 계산으로 못 가른다 |
| 2026-09-07 | **9:1 이 "거의 전 구간 공진 아래" 라는 본사 설명은 맞다.** `M(θ) = M_pk/sinθ` 이고 `M < 1` 이면 공진 위인데, 라인 반주기 중 공진 위 비율이 — **9:1**: 90~130 Vac(FB)·175~230 Vac(HB) 에서 **0 %**, FB 코너 150~166 Vac 에서 32~42 %, 264 Vac 에서 3.9 %. **7.5:1**: 110 Vac 부터 16 %, 230 Vac 부터 25 %, 264 Vac 에서 40 %. **6:1**: 90 Vac 에서도 이미 21 %. `n` 이 클수록 요구 게인이 커져 더 깊이 아래에 머문다 |
| 2026-09-07 | **~~6:1 은 설계 후보가 아니다 — 세 곳에서 막힌다.~~ 2026-09-07 철회. 아래는 `C_r` 100 nF · `L_r` 11 µH 를 고정한 채 `L_m` 만 훑은 1차원 절단면의 결론이었고, 탱크 자유도 3개 중 하나만 본 것이다.** ① `L_m` 20~22 µH: `Q.ZVS1 = λ/M·√(1/λ + M²/(M²−1))` 의 근호가 음수 — `M.Vmin` 0.98 로 **최저 입력에서도 공진 위**라 게인 피크 기준 ZVS 한계가 성립하지 않는다 ② `L_m` 24~26 µH: 빌드는 되나 **ZVS 실패** — 전 영역 스윕 `T_ZC` **121 ns** 대 데드타임 220 ns(0.55배) ③ `L_m` 30 µH 이상: 시트 §8 Cardano 해 없음 — λ 가 낮아 무부하 점근선 1/(1+λ) 이 고입력 요구 게인 0.51 위로 올라간다. **성립 창이 없다** |
| 2026-09-07 | **SMath 시트를 설계점 3종으로 변형화했다.** `build_l6790_smath.py` 가 `L6790_VARIANT` 환경변수(`9to1`·`7p5to1`·`6to1`)를 받아 `Smath/variants/L6790A_*.sm` 로 쓴다. 인자가 없으면 기존 정본 자리에 쓰므로 **guard 추적 경로는 그대로**다. 세 시트 전부 `VERDICT OK`. 워크북과 벤더 사양서의 3종화는 아직 안 했다 — 6:1 이 탈락했으므로 9:1·7.5:1 두 벌로 줄일지 결정이 필요하다 |
| 2026-09-04 | **설계점을 9:1 로 옮겼다 — 2차를 1턴밖에 못 감는다는 제약에서 출발했다.** 2차가 대전류 구리판이라 1턴이 한계이고, 3코어 직병렬이므로 **감을 수 있는 세트 권선비가 3·N_p/1, 즉 3의 배수뿐**이다. 본사가 지시한 n.T = 7.47 은 2차 2턴이라야 나오므로 실현 불가. 후보 6·9·12 중 **9** 를 골랐다(6:1 은 1차 전류 +11 %·k.Ploss 0.254, 12:1 은 코어가 더 커진다). 바꾼 입력 넷 — **n 6.02 → 7.4527 · L_m 20 → 24 µH · n.aux 1.5 → 3.0 · R.ZCD_H 330 → 680 kΩ**, 그리고 자기 블록 **N_p 5 → 3 · N_s 2 → 1** |
| 2026-09-04 | **λ 를 0.55 → 0.4583 으로 같이 내린 것이 핵심이다.** n.T = n·√(1+λ) 이므로 n 과 λ 는 묶여 있다 — n.T 를 9 로 못박으면 λ 가 n 을 정한다. λ = 11/24 로 잡으면 n = 9/1.2076 = 7.4527 이고, **커진 n 이 올릴 뻔한 1차 전류를 λ 하락이 되돌린다**: 라인 rms 10.9 → 11.04 A, 정적 도통 소자 9.62 → 9.88 W, 요구 Rth 10.40 → 10.12 °C/W 로 원설계와 사실상 같다. **λ 를 안 내리고 n 만 올렸다면 손실이 +17 % 였다** |
| 2026-09-04 | **9:1 로 오면서 좋아진 것 넷.** f_o 가 90.39 → 85.07 kHz 로 내려가 **k.floor 1.021 → 1.084**(인덕턴스 허용저하 4 → 15 %, 그래서 벤더에 ±10 % 를 줄 수 있다 — CLAUDE.md 시트 §4.2 항목 5b 의 R_T 딜레마가 해소됐다), **k.ceil 1.28 → 1.519**, **ZVS T_ZC 457 → 530 ns**, **OCP1 여유 1.251 → 1.295**. 그리고 T_idle 이 250 이든 350 ns 든 둘 다 통과한다(원설계는 350 ns 에서 k.floor 1.002 로 사실상 경계였다) |
| 2026-09-04 | **나빠진 것 셋.** ① **2차 소자 rms 14.15 → 15.16 A · k.PSR 1.065 → 0.926 미달** — n.SR 을 2 → 3 으로 올리면 1.389 로 회복되나 **아직 안 바꿨다** ② **코어 — N_p 가 5 → 3 이라 자속이 오른다. B_pk 가 EE6405(113.1 mm²)에서 364 mT** 로 0.20 T 를 크게 넘는다. **A.e ≥ 206 mm² 필요, EE6405 는 1.8 배 부족** ③ 출력 뱅크 리플 30.19 → 33.85 A(개당 0.19 → 0.21 A), 커패시터 정격 미확인 |
| 2026-09-04 | **ZCD 분압기가 보조권선비를 따라간다는 것을 놓칠 뻔했다.** 보조는 코어당 1턴 × 3 직렬로 **항상 3턴**이므로 n.aux = 3/N_s 다. 2차가 2턴에서 1턴이 되면 n.aux 가 1.5 → 3.0, V_aux 가 37.5 → 75 V 로 **두 배**가 된다. R.ZCD_H 를 330 kΩ 그대로 두면 **OVP1 이 14.08 V 에서 걸려 기동조차 못 한다.** 680 kΩ(E24, 상한 662.5 k 를 2.6 % 초과)으로 올려 OVP1 28.21 V·OVP2 30.66 V 로 복구했다 — 원설계(28.17/30.61)와 거의 같다 |
| 2026-09-04 | **`R.T` 12 kΩ 시도를 되돌렸다.** 사용자가 THD 개선(영교차 클램프 축소)을 노리고 올렸는데, `f_Min = 1/(2(C_T·R_T + T_idle))` 이라 **R_T 가 커지면 f_Min 이 내려간다.** 12 kΩ 에서 f_Min 84.89 kHz 가 f_o 85.07 kHz **아래**로 내려가 용량성 영역이 열렸다(k.RT.ceil 0.998). 상한은 11.973 kΩ 이다. **ST 툴이 이 값을 `RT.min` 이라 부르는 것이 거꾸로**이고, 우리 수정본은 라벨에 `(NOT a minimum)` 을 붙여 놨다. 11 kΩ 으로 복귀(k.RT.ceil 1.089) |
| 2026-09-04 | **보상망에 권선비가 안 들어가는 것이 맞다는 것을 수식으로 확인했다.** `D64 V_FB = (2·K_HV)/(K_M·K_FF)·P_o/η·R_CS`, `D71 G_cto = (P_o/V_out)·(1/V_FB)·(1/C_out)` — **어디에도 n 이 없다.** FB 핀이 전력 지령이고 전력은 권선비와 무관하게 보존되기 때문이다. 실제로 n 을 6.02 → 7.4527 로 바꿨는데 f_cross 17.57 Hz·Φ_M 49.64°·GM 34.18 dB 가 한 자리도 안 움직였다. **간접 경로는 R_CS 하나뿐**이다(D64 에 들어 있다) — R_CS 를 바꾸면 보상망을 다시 봐야 한다 |
| 2026-09-04 | **벤더 사양서를 9:1 로 다시 만들었다**(`Calculation Excel Sheet/LGE_670W_L6790A_Transformer_Spec_rev1.xlsx`). 유닛 환산 — 개방 11.67 µH ±10 % · 단락 3.67 µH ±10 % · 권선비 3:1 · D.C OVERLAP 16 A · 1차 11.0 A rms/17.7 A pk · 2차 각 10.1 A/38.5 A · f_sw 90~210 kHz(전부하) · Ae ≥ 206 mm². **항목명을 승인원 용어로 맞췄다**(Saturation → **D.C OVERLAP**). 핀 배정은 사용자 그림을 따라 **NS2 = 3·4, NS3 = 5·6**(기존 승인원의 엇갈린 3→5, 4→6 과 다름), **센터탭 4+5 는 PCB 에서** 만든다 |
| 2026-09-04 | **사고 — 사양서를 재생성하면서 사용자가 손으로 고친 셀 색과 배치를 덮어썼다.** `build_trans_spec.py` 가 `wb.save()` 로 통째로 다시 만드는데 백업을 안 남겼다. 복구는 OneDrive 버전 기록뿐. **빌더 첫머리에 "요청받았을 때만 실행할 것" 을 박아 두었고, 앞으로 이 파일은 셀 단위로만 고친다.** 결선도를 matplotlib 으로 그려 넣었다가 사용자가 "완전 엉망" 이라 해 생성기째 삭제했다 — **사용자가 직접 그린 그림을 쓴다** |
| 2026-09-04 | **아직 안 닫힌 것 둘.** ① **n.SR 2 → 3**(k.PSR 0.926) ② **코어**(A.e ≥ 206 mm², EE6405 로는 불가). ②는 **2차 2턴으로 가면 저절로 풀린다** — 6:2 도 세트 9:1 이고 턴수가 2배면 자속이 절반이라 **A.e ≥ 103 mm² 로 EE6405 가 들어간다**(여유 +10 %). 대신 유닛당 권선이 6턴 → 11턴이라 **창 면적**이 관건이고, n.aux 가 1.5 로 돌아가 R.ZCD_H 도 330 kΩ 으로 복귀한다. **벤더에게 "EE6405 보빈에 1차 6턴·2차 2턴×2·보조 1턴이 들어가는가" 를 물어야 한다** |
| 2026-09-04 | **2차 병렬 3개의 유닛간 매칭 요구가 사양서에 빠져 있다.** 유닛당 누설 ±10 % 만 걸어 놨는데, 세 유닛의 누설이 서로 다르면 병렬 2차에서 **전류가 한쪽으로 쏠린다**. 그리고 1차 3턴에서 누설 31 %(3.67/11.67)를 ±10 % 로 재현하는 것 자체가 저턴수라 까다롭다(기존품은 26 % 를 **5턴**에서 했다). 둘 다 벤더 회신을 봐야 한다 |
| 2026-09-04 | **트랜스포머 요구사양서를 새로 만들었다**(`Calculation Excel Sheet/LGE_670W_L6790A_Transformer_Spec_rev1.xlsx`, 당시 이름 L6790_Transformer_Spec_rev1 · `build_trans_spec.py`). 벤더 양식(`L6790_Trans_20260901.xlsx`)을 고치다가 버리고 새로 그렸다. 원칙 셋 — ① **검사성적서가 아니라 사양서**이므로 벤더가 채울 빈 칸을 두지 않는다(벤더는 자기 승인원으로 회신한다) ② 회로가 요구하는 값만 적고 코어·전선·권선배치는 넘긴다 ③ 조건 칸에서 다른 행을 참조하지 않는다. **빈 칸에 빈 문자열을 쓰면 숫자 서식 칸에서 `0.00` 으로 그려진다** — `put()` 이 `None` 이면 셀을 아예 안 쓰도록 했다 |
| 2026-09-04 | **2차 1턴 제약이 드러났다 — 세트 권선비가 3의 배수로만 나온다.** 2차가 대전류 구리판이라 1턴이 한계라면 세트 n_T = 3·Np/Ns 가 3·6·9·12 뿐이고, **본사가 지시한 7.47 을 실현할 수 없다**(Np=2.5 가 필요). 후보는 6:1(n 4.82)과 9:1(n 7.23)이고, 게인은 둘 다 성립하나 6:1 은 1차 전류 +11 %·`k.Ploss` 0.254 로 나빠지고 9:1 은 자화전류가 커져 포화 요구가 15 → 18.2 A 로 오른다. **탱크 재설계가 필요하며 아직 안 풀었다** |
| 2026-09-04 | **본사가 트랜스 제조사에 줄 값으로 Final Design 블록(L.μ 24.90 · L.L1 6.10 · L.L2 0.109 µH · n.T 7.47)을 지목했다.** 검산하니 우리 시트와 같은 트랜스포머다(L_open 31.0 · L_short 10.99 · λ 0.549). **n.T = 7.47 을 감으라는 것이므로 n vs n_T 논쟁이 본사 지시로 정리됐다.** 다만 그대로 보내면 안 된다 — **세트 기준 값이라 유닛으로 환산해야** 하고(1차 직렬이라 ÷3, 2차 병렬이라 L.L2 는 ×3), `L.L2` 0.109 µH 는 지그 부유 인덕턴스 수준이라 제작사가 못 잰다 |
| 2026-09-04 | **보상망 시정수 차이의 원인을 확정했다 — `C.out` 이 절반이었다.** 사용자가 ST 원본의 시트 XML 10장을 추출해 주어 전량 대조했다(원본 `sheetN` ↔ 우리 `sheetN+1`, dimension 전수 일치). `CompensationNet&Check` **D5(C.out) 이 ST 0.0376 F 대 우리 0.0752 F — 정확히 2배**다. 그것이 D65·D68(2f_l 리플)을 절반으로 만들고, 거기에 C.13 수정(D73 의 분모를 D65 → D68, 50 → 47 Hz)이 겹쳐 **요구 EA 이득 D73 이 0.2416 → 0.4541 (×1.88 = 2.0 ÷ 1.0638)**, `EA.o_Goal` 19.26 → 36.20 Hz, **계산 C_Fo 146.41 → 77.88 nF** 로 간다. 검증도 따라 움직인다 — 교차 30.41 → 17.57 Hz, 위상여유 46.04 → 49.64°. **구현된 보상망 자체는 두 시트가 똑같다**(F93~F96 = 82 nF/560 nF/47 k/1.55 n 동일 → f_z 6.047 · f_p 47.34 · f_px 999.4 동일). 즉 차이는 *부품* 이 아니라 *목표와 검증* 이다 |
| 2026-09-04 | **ST 원본이 우리 선정값을 이미 반영하고 있다는 것을 확인 — 다만 수식은 자기 것 그대로다.** 들어와 있는 것: `V.Fo` D28 = 1.15 V · `R.P` F41 = 1.3 k · `R.B` F43 = 6.2 k · F93~F96 · D110 = 47 Hz. 안 들어온 것: C.8(D15 CTR 이중적용, 566.04 대 300) · DS 5.2.12(D42·D43 의 분모를 V_F/R_P 로) · **C.13(D73 이 여전히 D65 를 읽는다)** · 이득여유 블록(D130 f.180 = 202.45 Hz, 원본에 없음). **부품 선정은 받아들이고 시트 결함 수정은 안 받은 상태다.** 그래서 ST 시트는 자기 계산(C_Fo 146.41 nF)과 자기 선정값(82 nF)이 1.8배 어긋난 채로 있다 |
| 2026-09-04 | **ST 원본 워크북이 IRM 암호화라 열 수 없다는 것을 확인.** `Calculation Excel Sheet/04092026_LGE_670W_L6790A_spread sheet.xlsx`(당시 이름 01092026…) 는 확장자가 xlsx 인데 헤더가 `d0cf11e0`(OLE2)다. 스트림을 열어 보니 `DataSpaces/TransformInfo/DRMEncryptedTransform/Primary`(Microsoft.Metadata.DRMTransform)와 `EncryptedPackage` 1.32 MB — **구형 .xls 가 아니라 IRM 으로 보호된 OOXML** 이다. openpyxl(BadZipFile) · xlrd(`Can't find workbook in OLE2`) · Excel COM(권한 대화상자에서 2 분 초과) 전부 실패. **복호화는 시도하지 않았다.** 사용자가 Excel 에서 보호를 풀어 사본을 남기면 `xl_sm_compare.py` 로 전량 대조할 수 있다. 시트 §2 자료 지도에 "읽기 불가" 로 등재 |
| 2026-09-04 | **보상망 시정수가 원본과 다른 이유를 우리 변경이력에서 특정했다.** `CHANGELOG_rev0_6` 64행(C.13) 하나가 계산된 시정수를 움직인다 — D73 이 "@2f_L,min" 이라고 라벨돼 있으면서 분모로 공칭 50 Hz 리플(D65)을 읽고 있었고, 최소 47 Hz 리플(D68)은 아무 데서도 참조되지 않았다. 50/47 = 1.064 배 낙관적이고 오차가 `EA_o → f_MB → f_z → C_Fo → R_F` 로 전파돼 **보상망 값을 3~6 % 움직인다.** 63행(f_px 196칸)·67행(이득여유 신설)·77행(D110 50→47)은 *검증* 만 바꾸고 추천값은 안 바꾼다. 90행(V.Fo 1.45→1.15 V, R_P 1.3 k, R_B 6.2 k)은 옵토 바이어싱이라 시정수와 무관하다. **다만 3~6 % 로 설명이 안 되는 크기라면 원인은 사양 차이(C_out 18.8 vs 75.2 mF)이고, 그것은 원본을 열어야 판정된다** |
| 2026-09-04 | **트랜스포머 설계 정리 PDF에 시트 §1 "설계 사양" 절 신설, 시트 §8 "보조 권선은 λ 보정이 필요 없다" 절 신설(7 → 9쪽).** 사용자 지적: "설계 사양을 모른 채로 시작하니 헷갈린다" — 입력·출력·용도·컨트롤러 표를 시트 §1로 앞에 두고 "단일단이라 벌크 커패시터가 없다"는 한 문장에서 시작하게 고쳤다. 보조권선 λ 보정 여부 질문에 대한 답도 넣었다 — **필요 없다.** n→n_T 보정은 *부하 전류가 누설을 지나갈 때*의 얘기인데, 보조권선은 ZCD 감지 전용이라 전류를 거의 안 끌어가고, 실제 시트 식 [94] `V_aux=(n_aux/n_sec)·V_out`도 이미 물리 턴수비를 보정 없이 그대로 쓴다 — 주파수 제어 루프가 누설과 무관하게 목표 출력을 만들어 놓은 뒤, 보조권선은 그 결과(코어 자속)를 그냥 재는 것이기 때문이다. 시트 §7(유닛당 인덕턴스)의 "λ는 유닛과 전체에서 같다"는 서술에도 "2차가 병렬이어도 성립한다(1차 직렬 배수가 Lm·Lr에 똑같이 걸리므로)"를 보강했다. 이 PC에 없던 `reportlab`을 설치해 직접 재빌드·9쪽 전부 렌더링 확인 |
| 2026-09-04 | **TI SDAA419 "Turn Ratio Correction Factor for Integrated Transformer LLC Design"(2026-06)가 결정적 검증이었다.** 자사 EVM(UCC25661EVM-128, 390 VDC/12 V/15 A, 실측 $L_P$=510 µH·$L_K$=85 µH·물리 권선비 33:2:2=16.5)에서 우리와 똑같은 증상("the turn ratio is not matched our target")을 보고한다. 2차 누설까지 반영해 유도한 보정 인수 $L_P/L_M$($L_M=L_P\sqrt{1-L_K/L_P}$)이 대수적으로 우리 $n_T/n=\sqrt{1+\lambda}$와 **정확히 같음**을 확인했다 — $L_P/L_M=\sqrt{L_P/(L_P-L_K)}$, TI 예제 1.095 = $\sqrt{1.2}$. 무보정 권선비(15.35)보다 보정 권선비(16.8)가 실제 보드 물리비(16.5)에 더 가깝고, SIMPLIS 시뮬레이션(무보정 115.53 kHz vs 보정 100.42 kHz)과 오실로스코프 실측(97.66 kHz)이 전부 보정 쪽을 확인한다. 가이드 §3.10 권선비 절에 검증 콜아웃으로 반영했다 |
| 2026-09-04 | **참고문헌 5종 대조 — TDK(SRX/SRV LLC 트랜스포머 가이드) · onsemi AN-4151 · Infineon AN 2013-03(LLC) · Infineon ICL5102(LCC) · TI SDAA419.** 업계가 물리 권선비와 게인식 권선비를 두 기호로 안 나누는 이유를 확인했다 — 그들 λ(0.14~0.25)에서는 보정 인수 √(1+λ)(10~11 %)가 2차 턴수(3~4턴) 반올림에 묻혀서 넣으나 안 넣으나 결과가 같다(TDK: 3.98/3.58→둘 다 4, onsemi: 4.00/3.61→둘 다 4, 직접 역산해 확인). 유일하게 Infineon LLC 노트(2013-03)만 권선비 자체에 √(m/(m−1))=√(1+λ) 보정을 넣는다고 명시하지만, 중간 계산이 안 보여 스펙표(5쪽)로 직접 역산해도 재현이 안 됐다 — **이 항목은 미확정으로 남긴다** |
| 2026-09-04 | **본사(ST)와 트랜스포머 권선비(n=6 vs n_T=7.47) 이메일 왕복.** 본사가 "n=6이 실제로 감을 물리 권선비, n_T=7.47은 프로브로 재는 값"이라 주장 → 실측 물리로 반박(15:2로 감으면 프로브는 오히려 9.34가 나온다, 시트 D57·D59 자체가 7.47로 임피던스를 옮기고 있다) → 본사가 다음 답변에서 "n=6은 APR/FHA 모델값, n_T가 물리 T-model, 우리 자신도 실측하면 재계산값(7.47 쪽)에 가깝게 나온다"로 정정 — **사실상 우리 편으로 태도가 바뀌었다.** — **2026-09-04 정정: 이 서술은 낙관적이었다. 사용자 확인 결과 논쟁은 아직 안 끝났다.** 그래서 벤더 사양서에서 턴수와 권선비를 둘 다 빼고 개방·누설·상호 인덕턴스 셋으로만 보내기로 했다 — 등가모델을 하나도 쓰지 않는 형태다. 15:2 유지, 벤더 사양은 인덕턴스로 고정, 최종 판정은 1차 시제품 실측으로 미룬다 |
| 2026-09-04 | **출력 리플(4.74 %)과 "±5 % Regulation" 관계를 확인하다가, 시트에 없는 계산 하나를 발견했다.** 리플만 보면 25 V 대비 ±2.37 %로 여유가 크지만, 실제 규제 지점은 25 V가 아니라 분압기 실현값 25.18 V다([143], R_I=100 k/R_o=11 k). 리플을 25.18 V 중심으로 얹으면 최악(피크)이 목표 25 V 대비 +3.1 %로, ±5 % 안에는 들어오지만 리플 하나만 볼 때보다 여유가 줄어든다. **이 "리플+분압기 오프셋" 합산 판정이 시트에 없다** — 손으로 조합한 것이라 판정 지표로 넣을지는 보류. `k.RN`(1.724)은 이것과 무관한 별개의 스위칭 리플&노이즈 여유다 |
| 2026-09-03 | **CLAUDE.md·HISTORY.md 전수 대조 — 낡은 서술 발견·정정.** (1) `CLAUDE.md` 시트 §4.1 Phase 6·시트 §5, `HISTORY.md` 시트 §3.2가 `CC`의 3점 θ 평가와 `Vin_max` 264 Vac를 "구조적 한계라 못 고친다"고 여전히 적고 있었다 — 둘 다 2026-08-23에 이미 닫혔다(`HISTORY.md` 시트 §3.7). `CLAUDE.md` 두 곳에 실제 내용(θ 6점+Simpson, `RTC!C9=MAX(D79,D81)`)을 채우고, `HISTORY.md` 시트 §3.2 목록에 취소선+철회 표기를 추가했다. (2) `CLAUDE.md` 시트 §4.1의 Phase 5 지시가 "240 W 예제는 부록으로 이동"이라 적혀 있었는데 `HISTORY.md` 시트 §1.5(2026-08-24)의 사용자 결정은 "완전히 삭제"다 — Phase 5 항목을 그 결정대로 정정. (3) C.25~C.27이 `CLAUDE.md` 시트 §4 Phase 4b 목록에 빠져 있어 "미반영"처럼 보였다. 워크북 CC·BOM 셀을 직접 열어 `CHANGELOG_rev0_6` 72~74행과 함께 이미 반영돼 있음을 확인, 양쪽 문서에 명시했다 |
| 2026-09-03 | **교육자료 PPT 낡은 값 2건 수정.** 08-28 rev 2 빌드 이후(08-24·09-01) 바뀐 수치가 반영 안 돼 있었다 — 31번 슬라이드 정적 도통 소자 손실 5.7 W(자리표시자 24 mΩ 시절 값) → **9.62 W**, 52번 슬라이드 R_ZCD,H 162 kΩ·OVP1/OVP2 27.4/29.8 V → **330 kΩ·28.2/30.6 V**. python-pptx가 이 PC에 없어 zip 안의 slide31.xml·slide52.xml 텍스트 런을 직접 치환했다. 원본은 세션 스크래치패드에 백업 |
| 2026-09-03 | **Altium 스키매틱 폴더(`LGE_670W_L6790A...`)를 앞으로 참조하지 않기로 확정**(사용자 지시). 시트 §2 자료 지도에도 추가하지 않는다 — 사용자 본인의 진행 중 작업이고, TL431 분압기 5칸이 물음표로 비어 있는 등 미완성 값이라 시트와 비교하면 거짓 불일치만 만든다 |
| 2026-09-01 | **포화전류 15 A 의 근거를 바꿨다 — 임의 마진에서 OVP2 로.** 처음에는 "설계 자속 182 mT 를 재현하는 9.96 A × 1.5" 라고 적었는데 **1.5 는 내가 정한 수**였다. 사용자가 세 번 되물어서 다시 봤다. 자화전류는 공진 아래에서 도통 구간이 T_r/2 로 고정되므로 **주파수와 무관하고 출력 전압에만 비례**한다. 그러면 최악은 컨트롤러가 스위칭을 멈추기 직전까지 허용하는 출력 = **OVP2 30.61 V**, 그때 자화전류가 **15.13 A**. 자속 검산도 같은 곳으로 온다(OVP2 자속 223 mT → 시험전류 12.20 A × 1.2 = 14.6 A). **같은 15 A 인데 근거가 임의값에서 회로 한계로 바뀌었다** |
| 2026-09-01 | **본사 검토용 트랜스포머 사양(영문) 확정.** 유닛당 5T:2T:2T:1T · 개방 10.33 µH · 단락 3.67 µH · 1차 10.9 A rms/18.3 A pk · 2차 9.44 A rms/32.8 A pk · 90~250 kHz · 설계 자속 182 mT · 포화 15 A 이상. **코어·보빈·전선·권선배치는 지정하지 않는다.** 사용자 요청으로 절연·온도 항목과 시험 방법 명칭은 뺐다 — 기존 승인원(26OP-LM83W)과 동일하므로 |
| 2026-09-01 | **워크북에 `I.Lm_pk` 가 없다는 것을 확인.** 대조표 145항목 중 전류는 `I.trafo_pk`·`I.sec_pk`·`I.Lr_pk` 뿐이고 자화 성분이 없다. 워크북은 ZVS 를 닫힌형 근사로 하고 자속을 아예 계산하지 않으므로 필요가 없었던 것이다. **포화전류 근거가 이 값에 걸려 있으므로 CLAUDE.md 시트 §4.1 에 명시**했다 |
| 2026-09-01 | **트랜스포머 설계 정리 PDF(한국어) 신설** — 8쪽, 식 7 · 표 10. 대화에서 λ·결합도·권선비를 여러 번 되짚은 것을 하나의 사슬로 정리했다: 입력이 넓다 → 게인 범위 → λ 0.55 → k 80 % → 전압비 6 vs 감을 비 7.5 → 5 T : 2 T → 벤더 사양 → 남은 문제. **수치는 전부 `an_pdf.V` 보간**이라 시트가 바뀌면 문서도 따라온다 |
| 2026-09-01 | **`an_pdf.py` 를 문서 2종 공용 엔진으로.** 하드코딩되어 있던 세 가지를 뺐다 — 서체(`FONT`/`FONTB`/`FONTI` + `use_korean()`), 표지 문구(`COVER`), 그리고 `main()` 안에 섞여 있던 `build()`. 캡션 낱말(`FIGWORD`/`TBLWORD`)과 머리글 크기(`HEADSIZE`)도 변수로. **레이아웃 코드를 복사하지 않았다** |
| 2026-09-01 | 그 과정에서 **중복된 `st()`/`S` 블록**(이전 되돌리기의 잔재, 두 벌 중 뒤엣것만 살아 있었다)을 제거했다. 다만 그 사이에 끼어 있던 `T()` 가 같이 지워져 AN 빌드가 깨졌고 되살렸다 — **파일이 CRLF/LF 혼재라 여러 줄짜리 치환이 조용히 실패**하는 것이 원인의 절반이었다. 이제 CRLF 로 통일 |
| 2026-09-01 | 한국어 PDF 에서 **U+2212(MINUS SIGN)가 임베드된 맑은고딕에서 렌더링되지 않았다** — "−20 %" 가 "20 %" 로 나왔다. 4곳을 ASCII 하이픈으로. 영문 AN 은 영향 없다 |
| 2026-09-01 | **시트에 부품 공차 해석이 없다는 것이 드러났다.** "벤더에게 인덕턴스 공차를 얼마나 줄 수 있나" 를 처음 물었더니, 가장 얇은 판정 `k.floor` = 1.021 이 **공차 예산 전체**여야 한다는 것이 나왔다 — L_r+L_m 이 5 % 낮으면 f_o 가 VCO 하한 f_Min 을 넘어 용량성 영역이 열린다. `R.T` 는 시트가 제대로 골랐다(상한 11.24 kΩ 아래 최대 표준값 11 kΩ, THD 를 살리는 선택). **문제는 선택이 아니라 그 선택을 공칭 한 점에서만 검증한 것이다.** 판정 36개가 전부 공칭 기준이다. 사용자가 공차를 보류했으므로 CLAUDE.md 시트 §4.2 항목 5b 로 남긴다 |
| 2026-09-01 | **회로도를 벡터에서 넷리스트로 추출해 구조를 확정했다.** PNG 로는 배선이 겹쳐 계속 오독했다 — 결정적인 이유는 **점프(hop) 아크 13개**가 배선을 두 토막으로 끊어 놓는 것이었다. 곡선 덩어리를 세어 코일 12개(좌 6 · 우 6)와 코어 바 3개를 찾고, 점프의 양끝을 이어 준 뒤 union-find 로 노드를 병합하니 배선이 한 번에 나왔다. `pymupdf.get_drawings()` |
| 2026-09-01 | **확정된 구조.** 1차: 브리지 중점 → Np(T1) → Np(T2) → Np(T3) → C_r → 반대쪽 중점(3직렬). 2차: 세 코어의 Ns1 끼리 · 센터탭끼리 · Ns2 끼리 병렬, **센터탭이 VOUT(+) 이고 정류기는 저측**(다이오드 캐소드가 권선 쪽) — N 채널 SR 소스 접지에 맞는 배치다. **시트 가정(N_x 3 · n_T 7.5 · N_rect 1)과 전부 일치** |
| 2026-09-01 | **보조권선이 3직렬 하나였다 — 내가 끝까지 못 본 것.** 코어마다 보조가 하나씩 있어서 "라벨 없는 여분 권선"으로 읽었는데, 실제로는 **1차 GND → aux(T1) → aux(T2) → aux(T3) → ZCD_AUX 로 직렬인 한 권선**이다. 1차와 같은 3분할 논리이고, 세 코어의 평균 자속을 본다 |
| 2026-09-01 | **그래서 `n.aux` 는 0.8 도 0.5 도 아니고 1.5 다.** 코어당 12.5 V/턴 × 3턴 = 37.5 V. **코어당 1턴이 감을 수 있는 최소이므로 1.5 는 선택이 아니라 하한**이다. 0.8 은 코어당 0.53턴을 요구해 애초에 감을 수 없었다 |
| 2026-09-01 | **VCC 는 외부 12 V 전원에서 온다(사용자 확정).** 보조권선은 ZCD 감지 전용이므로 `n.aux_max`(= OVP2 에서 V.aux ≤ VCC 정격 25 V)가 **적용되지 않는다.** 계산은 워크북과 맞추려고 그대로 두고 `k.auxr` 0.558 · `k.VCC` 0.544 를 `snapshot.ACCEPTED` 에 사유와 함께 등록했다 — `k.Ploss` 와 같은 처리다. **VCC 를 이 권선에서 뽑는 순간 되살려야 한다** |
| 2026-09-01 | **`R.ZCD_H` 162 → 330 kΩ**(사용자 선정, E24). 계산값 321.8 k 보다 위라 OVP1 이 목표 27.5 → **28.17 V** 로 올라간다. 리플 꼭대기(25.59 V) 대비 여유가 7 → **10 %** 로 늘고, OVP2 30.61 V 에서 SR 이 보는 전압은 61.2 V 로 80 V 등급 안이다. `k.OVP1` 0.996 → **1.024**. 기동 종료 출력전압은 16.08 → 16.53 V — 콜드 스타트 리스크가 아주 조금 무거워진다 |
| 2026-09-01 | **LGE 트랜스포머 개수 — 내가 4개로 잘못 셌다.** 사용자가 코어를 붉게 표시해 다시 보내 주어 확인했다. **3개가 맞다.** 1차측 코일이 6개라 전부 전력 권선으로 묶었는데, 실제로는 코어 3개에 각각 2개씩이다. 시트의 3 × 5 T = 15 T, n_T = 7.5 는 그대로 유효하다. **아직 확인이 필요한 것 하나** — 3코어면 1차측이 Np 3개 + 보조 2개 = 5개여야 하는데 6개가 보인다 |
| 2026-08-28 | **배포용 영문 Application Note 신설** — 배포용 영문 AN 20쪽 (지금은 v1.1). Infineon AN 2013-03 의 **레이아웃**만 따르고 브랜딩은 우리 것이다(표지에 "Not an ST publication" 명시). 8개 절 · 식 14 · 그림 14 · 표 10. reportlab platypus + matplotlib mathtext(수식은 PNG). **한국어 설계 가이드와 역할이 다르다** — 가이드는 식 [1]–[148] 전체를 담은 참조서이고, AN 은 배포 가능한 자립 문서다 |
| 2026-08-28 | **AN 의 모든 수치는 `an_pdf.V` 보간이다.** 본문(`an_body.py`)에 손으로 적은 상수가 없고, V 는 `l6790.py` + SMath 시트에서 만들어진다. ZVS 격자(표 3)는 옮겨 적지 않고 `l6790.sweep()` 으로 **매 빌드 재계산**한다 |
| 2026-08-28 | **`T()` 에 가드 두 개.** 포맷 안 된 `%%` 와 `%(…)` 가 그대로 인쇄되고 있었다 — "34 %%" 와 "(%(Icomp).2f A)". 후자는 `%%` 를 고치다 내가 `% V` 를 같이 지워서 만든 것이다. 이제 T() 가 그런 문자열을 만나면 빌드를 세운다 |
| 2026-08-28 | **자기 정정 — 그리스 문자가 안 나온다고 잘못 진단했다.** 페이지를 이미지로 보니 λ·θ·Ω 이 빠져 있어 WinAnsi 한계라 보고 Symbol 폰트 매핑을 넣었는데, 그 매핑이 **검은 네모**를 그렸다. 실제로는 reportlab 이 `&lambda;` 를 정상 렌더링하며 **빠져 보인 것은 pymupdf 래스터라이저**였다. PDF 텍스트를 추출해 확인했어야 했다. 매핑을 걷어냈고 T() 는 `&thinsp;` 만 처리한다 |
| 2026-08-28 | `g_tank_current` · `g_loop_bode` 도 `_tank_current(T)` · `_loop_bode(T)` 로 분리 — 한국어(가이드)·영문(AN) 두 라벨셋이 같은 그리기 코드를 쓴다. **라벨 6개가 `% fc` 같은 지역변수 포맷 표현식이라 dict 로 옮기니 NameError** 가 났다. 포맷은 호출부에 남기고 dict 에는 포맷 문자열만 둔다 |
| 2026-08-28 | `figs.py --plain` 신설 — `ask()` 제목을 그리지 않고 `figures/an/` 에 쓴다. 슬라이드에는 질문형 제목이 맞지만 번호 붙은 그림에는 캡션이 제목이다 |
| 2026-08-28 | **교육자료 rev 2 — 55 → 61장.** 240 W / 60 V 예제 수치를 25 V / 26.3 A 로 교체(글 18곳 + 표 60칸). **표는 그때까지 한 번도 훑은 적이 없었다** — 첫 스캔이 텍스트만 보고 "9장" 이라 했는데, 실제로 예제가 살던 곳은 41·43·44·45·46 의 표 다섯 개였다. 44번 ZVS 격자는 치환이 아니라 `l6790.py` 로 **재계산**했다(전부하 173.2 Vac eq. 457 ns) |
| 2026-08-28 | **38번(현 42번)은 결론이 뒤집혔다** — "홀드업이 이긴다(4364 vs 2258 µF)" 였는데 우리 사양에서는 **리플이 이긴다**(71.3 vs 59.8 mF) |
| 2026-08-28 | **그 자리에서 내가 틀린 문장을 썼다가 되잡았다** — "어느 조건이 이기는지는 출력 전압이 정한다(25 V 면 리플, 60 V 면 홀드업)". **아니다. 두 조건 다 1/V_out² 이라 비는 전압과 무관하고 사양만이 정한다:** 리플이 이길 조건은 `1 − (V_o,min/V_out)² > 4π·f_l·Δv·T_hold`. 우리 0.422 > 0.354(1.19배), 240 W 예제 0.306 < 0.628(홀드업 2.06배). 60 V 가 아니라 **Δv 10 % 와 droop 17 %** 가 원인이었다. 그림 `f19_cout_criterion` 으로 그려 넣었다 |
| 2026-08-28 | **46번(현 50번)이 정반대를 말하고 있었다** — "R_T 13 kΩ 이면 f_Min 이 77.4 kHz 로 **하한 아래**라 ACP 만 믿는 셈" 은 240 W 예제 얘기다. 우리는 92.3 kHz 로 **위**에 있고, 다만 **2.1 % 뿐**이며 설계 전체에서 가장 얇은 여유다. T_idle 700 ns 면 음수가 된다 — 그 문장으로 바꿨다 |
| 2026-08-28 | **모핑을 1장에서 5장으로**(사용자 요구: "딸랑 한 페이지 설명하고 끝이 아니라 브리지 회로가 어떻게 다르게 동작하는지 그림으로"). 29 왜 필요한가 · 30 풀브리지 도통 · 31 하프브리지 도통 · 32 게이트 신호 · 33 레벨과 히스테리시스. 그림 3장 신설(`f15`·`f16`·`f17`) — 반주기별 도통 경로, v_tank 파형, HOUT/LOUT 4선. **31번에 정적 도통 소자를 명시**했다: Q4 는 스위칭하지 않지만 탱크 전류 전체(10.9 A)를 계속 흘려 5.70 W 로 컨버터에서 가장 뜨겁다 |
| 2026-08-28 | **출력 뱅크를 1장에서 3장으로** — 42 두 가지 역할 · 43 어느 조건이 이기나(위 판정식) · 44 75 mF 의 하드웨어 실체(160개 · 개당 0.19 A · ESR 0.1875 mΩ · **콜드 스타트 신규 리스크** · 부피). `f19` 오른쪽 패널에 **에너지는 사라지지 않는다**를 그렸다: 2단의 400 V 벌크 274 µF = 21.9 J, 우리 75.2 mF = 23.5 J. 같은 줄이 25 V 로 내려오면서 274배의 정전용량이 된 것뿐이다 |
| 2026-08-28 | `morphing_levels` 그림을 `_morph_levels(T)` 로 분리해 한국어(가이드)·영문(교육자료 `f18`) 두 라벨셋이 **같은 그리기 코드**를 쓰게 했다. 복사했으면 갈라졌을 것이다 |
| 2026-08-28 | 잔여 정정 — 7번 f_o 88.1 → 90.4 kHz · f_r 150.3 → 151.7, 45번 루프 교차 "20 Hz" → 17.6 Hz(제목 포함), 49번 검증 수치(101 → 145항목 · f_cross 17.57 · Φ_M 49.64° · 3차 4.23 %), 56번 정오표의 "187 → 241 kHz" 를 우리 값(189.2 → 249.6)으로. `check_deck.py` 문제 33건은 전부 **손대지 않은 슬라이드**의 기존 것이고 신설 8장은 0건 |
| 2026-08-19 이전 | 가이드 rev 1.1 · SMath 시트 §3 시트 · PPT 51장 · 스프레드시트 rev 0.6 완성. 식 [1]–[148] 5회 대조 |
| 2026-08-21 | 사양 확정(25 V 직출력, 리플 5 %, 홀드업 12 ms @100 Vac 최악 위상) |
| 2026-08-21 | LGE 기존 보드 분석(시트 §5.2) · EVB TV 용도 확인(시트 §5.1) · [70a] CT 결함 발견(시트 §3.5) |
| 2026-08-21 | 닫힌형 해 발견(시트 §2) · T_idle 8칸 역산(시트 §4) |
| 2026-08-21 | **SMath 정본 시트 rev 1 완성** — 19개 절, A3, 보드선도 4장 |
| 2026-08-21 | 보상망 26식 전수 대조 → C.12·C.13 발견 · ISEN 블록 전수 대조 → C.14~C.17 발견, 스프레드시트 반영 |
| 2026-08-21 | 엑셀↔SMath 101값 대조(차이 0) · BOM 29항목 대조 도구 작성 |
| 2026-08-21 | xlsx 손상 사고 → `set_cell()` + 사전 검사기(시트 §6.1) |
| 2026-08-21 | 사용자가 SMath에서 F9 실행·저장 확인 (revision 8) |
| 2026-08-23 | **워크북 정리 완료 — 대조 134/134 · 차이 0.** 원인 A–F 전부 해소. `RTC` 격자 탐색을 **Cardano 닫힌형 18블록**으로 대체(편차 0.0000 %), `Vin_max` 를 FB 코너로. **"E 는 못 고친다" 는 앞선 기록 2건 철회.** 남은 흠은 `RTC` 행 26·27 의 `#NUM!` 164칸뿐이고 **차트·설계값에 영향 없음**(읽는 곳이 `CG25` 하나) |
| 2026-08-23 | **워크북에 B 적용**(`fix_xl_cc.py`, 셀 50개) — `CC` 에 θ 3열과 Simpson 적분을 넣어 **C.18 을 닫았다.** 뱅크 리플전류 21.77 → **30.17 A**(SMath 30.19). **"스프레드시트로는 적분 불가" 라는 앞선 판단은 틀렸다** — RTC 가 이미 θ 6점을 풀고 있었다. 대조 차이 8 → **5건** |
| 2026-08-23 | **워크북에 A·C·D 적용**(`fix_xl_causes.py`, 셀 6개) — ZVS 를 선정 후 RTC 실현값으로 재검산 · 검증 블록 라인주파수 50 → 47 Hz · 홀드업 시작 전압을 리플 골로. `CHANGELOG_rev0_6` 76–78행에 기록. **E 는 적용 불가로 판정** — `RTC!C9` 를 FB 코너로 돌리면 M_req 가 무부하 게인 하한 아래로 내려가 `fn.max` 가 #NUM! 이 되고 격자 전체가 무너진다 |
| 2026-08-23 | **차이 18건을 수식 수준에서 전수 재대조 → 근본 원인은 6개**(시트 §3.7). 그룹 안에서 두 식은 글자까지 같고 값 하나만 다르며 뒤 행은 첫 행의 하류다. `xl_sm_compare.py` 가 원인 태그와 그룹 요약을 인쇄하도록 고쳤다. **"B 는 구조적으로 불가능" 이라는 앞선 기록도 철회** — `RTC` 의 θ 6점에 사다리꼴을 쓰면 오차 0.35 % 다 |
| 2026-08-23 | **대조표가 없는 결함을 하나 만들어냈다.** `ΔV.loop` 를 `CC!D65`("@2fL", 공칭 50 Hz 참고 행)에 매핑해 6.4 % 차이를 C.13 결함으로 보고했으나, 설계가 실제로 쓰는 것은 `D68`("@2fLmin", 47 Hz)이고 그 값 **1.18430 은 시트와 정확히 같다**(`D73`·`D83` 도 47 Hz 를 쓴다). 재매핑 후 일치 116 → **117**. 남는 것은 성격이 다르다 — **검증 블록(D110~D132)에 별도 입력 `D110 = 50`** 이 있어 워크북이 47 로 설계하고 50 으로 검증한다. 수식이 아니라 입력 셀 하나 |
| 2026-08-24 | **FET 선정 파라미터 감사 신설**(`audit_fet.py`) — LLC 가 요구하는 항목마다 워크북 셀과 SMath 변수가 *있는지* 와 *수식에 쓰이는지* 를 따로 본다. 결함 4건이 나왔고 전부 닫았다. 시트 §9.1 |
| 2026-08-24 | **① 1차 전류 정격이 잘못된 피크를 쓰고 있었다** — `Power Components!D65 = 1.3×CC!E27` 인데 `CC!E27` 은 **반사 부하전류**(I_trafo,pk 16.39 A)다. 브리지 스위치가 흘리는 것은 **합성 탱크 전류** i_Lr 이다. 워크북은 그 값을 이미 `Device Setting!D32 = 18.319 A` 에 갖고 있었다(OCP1 설정용). 정격이 12 % 부족했다. 가이드 [115] 도 같은 오류이며 SMath 는 이미 덮어쓰고 있었다 |
| 2026-08-24 | **② 소자 1개당 전류가 어디에도 없었다** — 전류는 전부 스위치 *위치*당(1차) · *레그*당(2차) 인데 손실·정격식이 소자 1개가 그걸 다 흘리는 것처럼 썼다. 병렬 소자 수가 아예 없어 SMath 의 R_DS(on)(소자당)과 워크북의 R_DS(on)(레그당)은 **비교조차 불가능**했다. 양쪽에 `n.par`=2 · `n.SR`=3 과 소자 1개당 실효·피크 4행씩 신설 |
| 2026-08-24 | **③ 온도가 앞뒤로 안 맞았다** — `D78`·`D100` 은 25 °C R_DS(on) 로 손실을 내고 `D79`·`D101` 은 그 손실로 "Tj 125 °C 까지 가는 Rth" 를 물었다. 같은 식의 분자와 분모가 다른 온도. `kT.pri`=2.0 · `kT.sec`=1.5 신설. 요구 Rth 가 약 2배 후했다 |
| 2026-08-24 | **④ 2차 손실이 다이오드 모델이었다** — `Vf·I_avg + rd·I_rms²` 이고 선정 부품도 쇼트키였는데 본 설계는 SR 이다. `Conf.SR` 신설(1 = 다이오드 · 2 = SR). SMath 는 SR 전용이라 반대쪽 흠이 있었다 — 데드타임 바디 다이오드 도통을 아예 안 본다. **양쪽 다 "안 본다" 고 명시**했다 |
| 2026-08-24 | **⑤ SMath 시트 §13.3 자리표시자가 시트 §13.1 요구를 6배 위반** — `R.dson_p` 150 mΩ vs 요구 25.3 mΩ. 그 결과 `P.pri_tot = 35.64 W`(출력의 5.4 %)가 설계값처럼 인쇄됐다. 25 °C 기준 pick(`R.dson_p25` 24 mΩ)에서 `K.Tpri` 로 열간값을 유도하는 형태로 바꿔 **요구를 구조적으로 만족**하게 했다. 5.70 W. 예산 대조 `k.Ploss`·`k.PSR` 신설 — **예산은 있는데 비교가 없던 자리** |
| 2026-08-24 | **⑥ [64] 가 [63] 과 다른 파형을 제곱하고 있었다** — [63] 의 `1−cos(π f_r/f_sw)` 는 공진 위에서 **사인이 잘린다**는 뜻인데, [64] 는 `d` 를 1 로 고정해 **온전한 반사인파**의 실효값을 쓴다. 두 식이 다른 파형을 말한다. 절단 인수 `k_tr = 1 − sin2φ/2φ` 를 [64a] 로 신설하고 `l6790.py` · SMath · 워크북 `CC` · 가이드 넷 다 반영. **저입력에서는 φ=π 라 인수가 정확히 1** — 두 설계 모두 2차 실효 최악이 저입력이므로 **소자 선정도 커패시터 개수도 안 바뀐다**. FB 코너에서는 **+8.0 %**(240 W 예제 +8.4 %). 부록 C.30 |
| 2026-08-24 | **유니버설 동작 전 구간 검산 — 히스테리시스 밴드가 빠져 있었다.** 가이드는 등가 범위를 173.2–332.3 Vac(1.92:1)로 쓴다. 각 임계값을 *그 모드가 보장되는* 쪽에 배정한 값이다. 그런데 235–245 V_pk 사이는 **이력이 모드를 정한다** — 올라오는 중이면 245 V_pk 까지 FB, 내려가는 중이면 235 V_pk 까지 HB. 진짜 범위는 **166.2–346.5 Vac (2.08:1)**. 여유 게인 15.6 → 10.9 % · ZVS 2.08 → 1.70배 · f_sw,max 249.6 → 263.2 kHz. **전 항목 통과하므로 설계는 안 바꾼다** — 가이드 §3.3 에 절을 신설해 적어 두었다. 가장 얇은 곳은 입력 범위가 아니라 `k.floor` 1.021(VCO 하한 92.25 kHz 대 f_o 90.39 kHz)이고, 그것은 미확정인 `T_idle` 에 직접 걸려 있다 |
| 2026-08-24 | **1차·2차 소자 구성 확정 — 현 엑셀 값 그대로.** `n.par` = 1 · `n.SR` = 2, 병렬 증설도 GaN 도 쓰지 않는다. 잠정 3 W 예산은 1차에서 미달(9.62 W, `k.Ploss` 0.312)이고 2차도 거의 소진(`k.PSR` 1.065)이지만 **감수하기로 했고, 방열판 사용 여부는 시제품 열 실측에서 판정한다.** 두 판정이 1 미만인 것은 설계 결정의 결과이지 결함이 아니다 — CLAUDE.md 시트 §3 판정표와 시트 §4.2 항목 5·3b 양쪽에 명시했다. 실측 항목 11 신설 |
| 2026-08-24 | **`kT.sec` 확정 1.9.** 2차 SR 데이터시트의 R_DS(on)–T_a 곡선 (V_GS = 10 V)에서 25 °C 3.2 mΩ · 125 °C 6.1 mΩ. **축이 T_a 인데도 쓸 수 있는 이유는 "Pulse test" 라 다이가 자기발열하지 않아 T_j = T_a 이기 때문** — 펄스가 아닌 곡선은 이렇게 못 쓴다. 내 추정 1.5 는 낙관적이었다. 열간 5.55 → **7.03 mΩ**, 2차 총손실 4.45 → **5.63 W**, `k.PSR` 1.349 → **1.065** 로 예산을 거의 다 썼다. 레그당 3병렬이면 회복된다 |
| 2026-08-24 | **`kT.pri` 확정 1.8.** 사용자가 STO60N045DM9 의 normalized R_DS(on) vs T_j 곡선에서 125 °C 배수를 읽어 왔다(내 추정은 2.0). 열간 90 → **81 mΩ**, 정적 도통 소자 10.69 → **9.62 W**, 요구 Rth 9.36 → **10.40 °C/W**, `k.Ploss` 0.281 → **0.312**. **`kT.sec` = 1.5 는 아직 추정으로 남는다** |
| 2026-08-24 | **SMath 에 실제 부품값 반영.** 엑셀엔 STO60N045DM9(45 mΩ)·2차 3.1 mΩ 을 넣으셨는데 **SMath 는 내 자리표시자 24 mΩ·2 mΩ 그대로였다** — 엑셀이 아직 재계산 전이라 대조표가 이를 가리고 있었다. 45 / 3.1 mΩ 으로 맞춤. 1차 정적 도통 소자 5.70 → **10.69 W**, 요구 Rth 17.5 → **9.35 °C/W**, `k.Ploss` 0.526 → **0.281**. 2차는 레그당 2병렬로 총 3.73 W, `k.PSR` **1.610** 통과. 아울러 `Power Components` 배치 정정 — 1차 정적 손실이 **2차 섹션 제목(B81) 아래**에 있었고 `kT.pri` 입력이 결과 블록 한가운데 있었다. 77–80 = Ctot · 스위칭손실 · 정적손실 · Rth 로 정리하고 `kT.pri` 는 섹션 제목 바로 아래(D58)로. 그 행을 82행에 만들던 임시 스크립트는 다시 돌면 배치를 되돌리므로 삭제했다 |
| 2026-08-24 | **소자·보상망 확정 반영.** 1차 STO60N045DM9(45 mΩ max, n.par 1) · 2차 SR 레그당 2개(`n.SR` = 2). **`kT` 가 1 로 꺼져 있던 것을 2 / 1.5 로** — 데이터시트 max 는 **25 °C 공정산포**이지 온도가 아니다. 1차 정적 도통 소자 5.34 → **10.69 W**, 요구 Rth 18.7 → **9.36 °C/W**. 보상망은 `V.Z` 25 → 12 V 로 바뀌어 있었고, **`V.Fo` 가 엑셀 1.45 V · SMath 1.15 V 로 갈라져 두 문서의 R_B 창이 아예 겹치지 않았다**(5.873–6.652 vs 6.934–8.001 k). 1.15 V 로 통일. **R_B 창이 R_P 에 의존**(V.Fo/R.P 가 분모)하므로 R_P 를 1.8 → 1.3 k 로 내리자 창이 5.759–6.477 k 로 이동했고 R_B = 6.2 k 로 수렴. `C.Fo` 82 n · `C.F` 560 n · `R.F` 47 k 는 **사용자 선정이 이미 정확**했다. 판정 33 → **35 / 36**, 남은 미달은 보류 결정한 `k.Ploss` 뿐 |
| 2026-08-24 | **도구 4개가 `<result>` 를 잘못 읽고 있었다.** `<result>` 도 RPN 인데 첫 토큰만 읽어 **단항 마이너스와 지수가 사라졌다.** 넷이 같은 가정을 독립적으로 해서 서로 못 잡았다. `n.SR` = 1 재계산 뒤 `D37`(필요 세라믹) 불일치로 드러났는데 **엑셀도 SMath 도 맞았다** — `C.cer_min = MAX(−64.14 µF, 0)` = 0 을 시트는 −3.87e-15 µF 로 쓰고 나는 3.87 로 읽었다. `smresult.py` 신설. 525개 중 53개가 다중 토큰, 그중 16개가 부호를 잃고 있었다. 시트 §6.6 |
| 2026-08-24 | **사양 변경 — 1차·2차 모두 소자 1개씩**(사용자 결정). `n.par` 2 → 1 · `n.SR` 3 → 1. **2차는 통과** — 레그당 1개가 28.3 A rms · 98.4 A peak 를 전부 흘리고 2.41 W, `k.PSR` 1.248. **1차는 미달** — HB 모핑에서 정적 도통 소자가 5.70 W 로 잠정 예산 3 W 의 1.9 배, `k.Ploss` 0.526. 3 W 를 맞추려면 25 °C 기준 12.6 mΩ 이 필요한데 600 V 슈퍼정션에 없다(최선 17–24 mΩ → 4.04–5.70 W). **예산 인상 또는 그 한 자리만 2 병렬**이 선택지다. 이 변경으로 워크북의 알려진 한계가 실제 문제가 되어 함께 닫았다 — `D82` 신설(정적 도통 소자 손실)과 `D79 = (125−Ta)/MAX(D78,D82)`. 요구 Rth 35.1 → **17.5 °C/W** |
| 2026-08-24 | **⑦ 살아 있는 참조 위에 손으로 적은 상수가 얹혀 있었다** — C.18 이 `CC` 에 Simpson J열을 만들면서 `Res. Tank Design` D65·D67 과 `Power Components` D28 세 셀만 연결하고 **D60·D61·D84·D85 를 빠뜨렸다.** 그 넷은 θ=π/4 한 점을 읽은 채 **F열 노란 손입력**(10.9 · 7.707 · 28.31 · 28.31)으로 덮여 있었다. C.27 과 같은 구조 — 입력을 바꾸면 J열은 따라오고 손입력은 안 따라온다. J29·J30·J25·J26 으로 연결하고 손입력 4칸 삭제. **오늘 값은 안 움직인다**(적을 당시엔 맞는 값이었다). 나머지 `CC!C..H` 참조 10건은 전부 피크(E열 = θ=π/2)이거나 의도된 최악 스위칭주기 값이라 그대로 둔다 |
| 2026-08-24 | **`RTC` 26·27행 `#NUM!` 164칸을 닫았다.** 2026-08-23 에 "차트·설계값 영향 없음" 으로 남겨 둔 것. `fn.max = √(λ/(1+λ−1/M))` 은 무부하 게인 점근선 1/(1+λ)=0.6452 아래에서 해가 없고, 원인 E 가 M 을 0.6383 으로 내렸다. `CG26` 이 이미 갖고 있던 3중 가드를 전 열에 이식. **읽는 곳이 없어 수치는 하나도 안 움직인다** |
| 2026-08-24 | **워크북을 한 언어로** — 한글 57칸(CHANGELOG 50 + 라벨 7)을 영문화. 영문 산출물에 읽을 수 없는 변경 이력이 붙어 있었다 |
| 2026-08-24 | **SMath 시트 §19.6 에서 부품번호 삭제** — 사양이 바뀔 때마다 손으로 고쳐야 하고, 시트에서 **어떤 계산도 검증할 수 없는 유일한 항목**이다. 소자 등급·개수와 살아 있는 요구값으로 대체 |
| 2026-08-24 | **엑셀↔SMath 대조에 반도체 블록 11항목 추가**(134 → 145). 이 블록은 지금까지 **한 번도 대조된 적이 없었고**, 그래서 두 문서가 조용히 갈라져 있었다 |
| 2026-08-24 | **어제의 "규칙 8 정정"을 철회한다.** `design()` 을 선정값 없이 불러 *계산* 탱크로 쟀던 것이라 측정 자체가 틀렸다. 예제가 실제로 쓰는 선정 탱크(33 nF/34 µH/65 µH)로 재면 스윕 246.5 ns · 닫힌형 302.8 ns = **+22.9 % 낙관적**이다. 본 설계(선정 탱크)는 −34.4 % 보수적. **부호가 탱크마다 바뀐다** — 그래서 근사식은 어느 방향으로도 못 믿는다. 원래 기록이 방향은 맞았다 |
| 2026-08-23 | ~~규칙 8의 부호가 뒤집혀 있었다~~ **(2026-08-24 철회)** "닫힌형 [36]–[38] 은 약 10 % **낙관적**" 이라고 적어뒀으나 실측은 반대다 — 본 설계 **−34.4 %**, 240 W 예제 **−5.5 %** 로 **보수적**(여유를 과소평가)이다. 원인 둘의 기여가 비슷하다: 근사식 [36] 만으로 −10.7 %(기록된 10 % 는 이 크기였다), 설계 `Q_ZVS` 를 쓴 것만으로 −20.6 %. **툴 자신의 `RTC` 격자는 f_n 0.84006 으로 SMath 와 5자리까지 같다** — 워크북은 정답을 갖고도 ZVS 검사에만 근사식을 쓴다. 조언 자체(스윕을 쓰라)는 유효 |
| 2026-08-23 | **재계산이 예측 4건을 반증했다.** "재계산하면 일치한다"고 달아둔 주석 10건 중 6건은 맞았고 4건은 틀렸다 — `C_T` 상한은 SMath 가 FB 코너 249.6 kHz, 워크북이 사양치 225 kHz 를 쓰는 **영구적** 차이였고(역산 249.65 kHz 로 확인), `D126`·`D128`·`D129` 는 C.13 기준 차이의 하류로 비 1.0638 × 1.0512 = 1.1183 이 정확히 설명한다. 주석 10건 전부 실제 결과로 교체 |
| 2026-08-23 | **사용자가 워크북을 재계산**(열었다 저장). 잠정이던 대조가 확정됐다 — 135항목 중 **일치 101 → 116 · 불일치 3 → 0**. C.12(196셀 f_px)·C.13 수정이 실제로 동작함이 이로써 확인됐다. 오류값은 54개(RTC 행 29 의도적 마스킹)로 변동 없고, 한계를 벗어난 선정값도 0 |
| 2026-08-23 | **`RTC` 격자를 셀이 아니라 답으로 검증**(`rtc_check.py`) — 18점을 Cardano·이분법과 대조, 최대 편차 0.197 %. 동시에 **RTC 가 설계에 미치는 경로는 셀 9개뿐**임을 확인. 그리고 **내 기록이 틀렸음**을 발견: "f_n 격자 상한 1.3703" 은 격자가 아니라 `fn.max` 였고 실제 격자는 2.0127(305.4 kHz)까지 간다 — 문서 4곳 정정 |
| 2026-08-23 | **검사 범위를 문서화**(시트 §1.4) — 전수 확인이 끝난 것과 안 본 것을 구분. 값 대조를 101 → **135항목**으로 확대하다 오늘 넣은 `C.T_min` 의 주파수 기준 오류를 즉시 발견 |
| 2026-08-23 | **반복 구조 감사 10라운드** — C.25~C.27 발견. RPN 왕복 475식·차원 해석 475식 전수 통과. SMath 에 없던 식 4개(ZVS 천이시간 [40][41][42] 포함) 구현. 판정 23 → 33개 |
| 2026-08-23 | **구조 감사기 `audit_xl.py` · `audit_sm.py`** — C.21~C.24 발견. 선정값이 자기 한계를 벗어난 곳 2건, 라벨이 정반대인 곳 1건, 비교 검사 자체가 없던 곳 5건 |
| 2026-08-23 | **이득 여유를 양쪽에 신설** — `f.180 = √(f_p·f_px − f_z·(f_p+f_px))` 닫힌형. GM 34.26 dB. 위상 여유는 원래 양쪽에 있었다 |
| 2026-08-23 | `R_P` 1.5 → **1.4 kΩ**(상한 위반), `C_in` 2000 → **2200 nF**(하한 미달) |
| 2026-08-23 | **C.12·C.13 워크북 반영** — f_px 극점을 196셀에 삽입(공유수식 마스터 7개), D73 분모를 f_l,min 리플로. 입력 c_HB 800 pF·t_D 220 ns·T_idle 250 ns 로 SMath 와 통일 |
| 2026-08-23 | **설계 가이드에 정오표 반영** — C.12–C.20 신설, [70a] 센터탭 경고, C.1 8칸 역산표. 1562 → 1723줄 |
| 2026-08-23 | **교육자료에 반영** — 정오표 슬라이드 신설(51 → 52장), T_idle·LOUT 행 갱신, 규칙 9에 노드 실효값 |
| 2026-08-23 | **자기 정정** — I_Cout 차이를 [70a] CT 결함으로 잘못 귀속했었다. 실제 원인은 C.18 |
| 2026-08-23 | **C.18 발견·반영** — 워크북이 θ=π/4 한 점을 "mains cycle" 실효값이라 부르고 있었다. 2차 정류기 손실 1.38 배 과소평가. 라벨 정정 + 라인사이클 입력 셀 신설 |
| 2026-08-23 | 엑셀↔SMath **전량 대조 도구 `xl_sm_compare.py`** — 101 항목, 불일치 0 |
| 2026-08-23 | SMath `R.ZCD_H` 상한을 **선정된** 하단 저항 기준으로 정정 (계산값을 쓰고 있었다) |
| 2026-08-23 | CLAUDE.md 시트 §3을 **생성물**로 전환(`snapshot.py`) — 손으로 옮겨 적은 값이 낡는 사고가 이미 한 번 있었다(시트 71.4 mF, 문서 80 mF) |
| 2026-08-23 | xlsx 수술 도구를 스크래치패드에서 `generators/xlsx_patch.py`로 영구화 · Stop hook `guard.py` 도입 |
| 2026-08-23 | 문서를 `CLAUDE.md`(작업용) / `HISTORY.md`(근거)로 분리. `README.txt`·`HANDOFF` 폐기 |
| 2026-08-23 | **출력 커패시터 선정 변경** — 사용자가 엑셀에서 680 µF × 105 → **470 µF × 160 = 75.2 mF**. 리플 4.99 → 4.74 %, 홀드업 여유 1.055 → 1.119. 양쪽 다 개선 |
| 2026-08-23 | 중간 상태 백업 5개 삭제(xlsx 4 + sm 1) — 전부 내 수정 이력의 중간판이고 현행 파일이 검증을 통과했다. **ST 원본 r1.0은 이 폴더에 없다** |

---

## 9. FET 선정 — 무엇을 보고 무엇을 계산하는가

2026-08-24. `audit_fet.py` 가 항목마다 두 가지를 따로 묻는다:
**있는가**(셀·변수가 존재하는가) 와 **쓰이는가**(어떤 수식이 그걸 읽는가).
**있는데 안 쓰이는 쪽이 더 위험하다 — 검토한 것처럼 보인다.**

### 9.1 감사 결과 (수정 후)

| 항목 | 워크북 | SMath | 비고 |
| :--- | :--- | :--- | :--- |
| V_DS 내압 | 표시 D64 · D89 | 표시 `V.DS_pri` · `V.DS_sec` | 요구조건 행이므로 "표시만" 이 정상 |
| I_D 정격 (위치/레그당) | 표시 D65 · D90 | 표시 `I.pri_rating` · `I.sec_rating` | **D65 는 틀린 피크를 쓰고 있었다 — 시트 §9.2** |
| 병렬 소자 수 | D68 = 2 · D103 = 3 | `n.par` · `n.SR` | **신설.** 없으면 전류가 나뉠 곳이 없다 |
| 소자 1개당 피크 | D66 · D91 | `I.pk_dev` · `I.SR_pk_dev` | **신설** |
| 소자 1개당 실효 | D62 · D87 | `I.mos_dev` · `I.SR_dev` | **신설.** 손실식이 이걸 읽는다 |
| R_DS(on) @25 °C | D70 = 24 mΩ · D96 = 2 mΩ | `R.dson_p25` · `R.dson_s25` | 소자당 |
| R_DS(on) 온도계수 | D80 = 2.0 · D98 = 1.5 | `K.Tpri` · `K.Tsec` | **신설 — 시트 §9.3** |
| C_oss | D71 → D77 | `c.HB` → `Q.ZVS2` | **어느 C_oss 인지가 빠져 있다 — 시트 §9.5** |
| 다이오드/SR 선택 | D93 = 2 | SR 전용 (구조상) | **신설 — 시트 §9.4** |
| 바디 다이오드 조건 | D74 · D75 | `V.bd_rr` · `I.bd_rr` | 373.4 V · 12.36 A. Q_rr 은 가정 안 함 |
| 손실 예산 대조 | 없음 | `k.Ploss` 1.052 · `k.PSR` 3.743 | **신설 — 예산은 있는데 비교가 없었다** |
| R_th 요구 | D79 · D101 | 없음 | 워크북 전용 |
| T_j(max) | D79·D101 안에 리터럴 125 | 없음 | **양쪽 다 입력 셀 없음 (남은 항목)** |
| Q_g · E_off · SR Q_rr | 없음 | 요구조건만 명시 | **소자 값이라 계산 불가 — 시트 §9.5** |

### 9.2 1차 전류 정격이 틀린 피크를 쓰고 있었다

`Power Components!D65 = 1.3 × CC!E27` 이고 `CC!E27 = Itrafo.pk.tsw = I_sec,pk/n` —
**반사 부하 전류**다(16.39 A). 브리지 스위치가 실제로 흘리는 것은 **합성 탱크 전류**
`i_Lr = i_trafo + i_Lm` 이고, 둘은 서로 다른 순간에 피크를 친다. 그 합성 피크는
**워크북이 이미 갖고 있었다** — `Device Setting!D32 = Ilr.pk.max = 18.3189 A`, OCP1
설정점을 잡으려고 계산해 둔 것이고 SMath `I.Lr_pk` 와 5자리까지 같다.

정격이 **12 % 부족**했다. 가이드 [115] 도 같은 식으로 쓰여 있고, SMath 는 시트 §13.1 에서
이미 주석과 함께 덮어쓰고 있었다. 이제 **보호 임계값과 소자 정격이 같은 수에서 나온다.**

### 9.3 온도가 앞뒤로 안 맞았다 — 양쪽 모두

```
D78  손실   = R_DS(on) @ 25 °C × I²
D79  요구 Rth = (125 − Ta) / D78          ← Tj = 125 °C 를 가정
```

같은 계산의 분자와 분모가 다른 온도다. 600 V 슈퍼정션은 25 → 125 °C 에서 R_DS(on) 이
대략 **2배**, 저전압 SR FET 은 **1.5배**가 된다. 그만큼 손실이 과소평가되고 **요구 Rth 가
약 2배 후하게** 나온다. **틀린 방향이 위험한 쪽이다.**

SMath 도 같은 함정이 있었다 — `R.dson_req_dc` = 25.3 mΩ 은 *동작 온도* 기준 요구값인데,
사람은 그걸 데이터시트 25 °C 값과 비교한다. 그래서 **25 °C 로 환산한 행**
(`R.dson_req_25_p` · `R.dson_req_25_s`)을 나란히 두고 "이걸로 소싱하라" 고 명시했다.

> **남은 것:** `T_j(max)` 는 `D79`·`D101` 안에 리터럴 125 로 박혀 있다. 입력 셀이 아니다.

### 9.4 2차 손실 모델이 서로 달랐다

워크북은 `Vf·I_avg + rd·I_rms²` — **다이오드 모델**이고 선정 부품도 쇼트키였다.
SMath 는 R 만 쓰는 **SR 모델**이다. 본 설계는 SR(CLAUDE.md 시트 §1.1)이므로 워크북 쪽이
우리 구성이 아니었다. `Conf.SR` 로 둘 다 표현할 수 있게 했다.

**반대쪽 흠도 있다.** SMath 는 SR 전용이라 **데드타임 바디 다이오드 도통을 아예 안 본다.**
센터탭에서는 그 Q_rr 이 스위칭 주기마다 **두 번** 회복된다. 둘 다 SR 데드타임이 있어야
계산되는데 그건 SR 컨트롤러가 정하는 값이라 이 시트의 입력이 아니다.
공진 아래에서는 2차 전류가 스스로 0 이 되어 SR 이 영전류에서 꺼지므로 작지만,
공진 이상에서는 아니다. **양쪽 시트에 "안 본다" 고 적었다. 실측 대상이다.**

> **그리고 공진 위에서는 두 레그가 쉬는 구간 없이 직접 넘겨받는다.** 그 과도 구간에서 누설 인덕턴스 때문에 **둘이 동시에 도통한다.** 겹침 시간은 SR 데드타임과 2차 누설로 정해지므로 어느 식에도 없고, 그 지점이 바로 SR 바디 다이오드가 강제 역회복되는 곳이다. 관련해서 [64] 자체의 절단 오류(부록 C.30, FB 코너 +8 %)도 이번에 함께 고쳤다 — 저입력이 최악이라 수치는 안 움직였다.

### 9.5 값을 만들지 않은 것들

| 항목 | 왜 없나 | 어떻게 쓰나 |
| :--- | :--- | :--- |
| **어느 C_oss 인가** | 데이터시트가 같은 이름으로 셋을 준다 — 표제 `Coss @ 작은 V_DS`, 에너지 등가 `Co(er)`, 시간 등가 `Co(tr)`(= `Qoss`). **3–5배 차이가 난다** | ZVS 는 데드타임에 **전하**를 옮기는 문제다. 써야 할 것은 `Qoss/V_DS`, 즉 `Co(tr)`. 표제 Coss 를 넣으면 데드타임 요구가 몇 배 쉬워 보인다. **어느 것을 넣었는지 부품 옆에 적을 것** |
| **Q_g** | 소자 값 | HOUTx·LOUTx 는 **V_OH = 4 V 로직 출력**이지 드라이버가 아니다. 외부 하프브리지 드라이버가 필수고, `N.mos × n.par × Q_g × V_drv × f.sw` 를 무는 것은 L6790A 가 아니라 **그 드라이버**다 |
| **E_off** | 소자 값 | ZVS 가 없애는 것은 **턴온 손실뿐**이다. 턴오프는 매 사이클 탱크를 상대로 그대로 일어난다. `I.Lm_pk` = 12.36 A 에서 평가할 것 |
| **Q_rr (1차 바디 다이오드)** | 소자 값 | 조건만 명시: **373.4 V · 12.36 A.** 데이터시트 기본값(보통 25 °C, 낮은 dI/dt)이 아니라 이 조건에서 규정할 것. Q_rr 은 25 → 125 °C 에서 대략 2배 |
| **전류 분담 편차** | 부품 편차 | 병렬 행은 **이상적 분담**을 가정한다. 1차(슈퍼정션)는 R_DS(on) 산포로 가장 뜨거운 소자가 더 먹으므로 **약 20 % 여유**를 두거나 binning 할 것. 2차 SR 은 R_DS(on) 이 온도와 함께 올라가 **스스로 분담한다** |

### 9.6 병렬 개수는 전류가 아니라 예산이 정했다

1차 `n.par = 2` 는 전류 정격 때문이 아니다. 소자 1개당 실효는 3.85 A 뿐이다.
결정한 것은 **손실 예산**이다 — HB 모핑 모드에서 LOUT2 가 계속 High 라 **레그 2 로우사이드가
탱크 전류 전체(10.9 A rms)를 연속으로 흘린다.** 600 V 슈퍼정션이 오늘 줄 수 있는 최선인
24 mΩ(25 °C, 열간 48 mΩ)로도 1개면 **5.7 W** — 3 W 예산을 넘는다. 2 병렬이면 2.85 W.

> **워크북에는 이 정적 소자 행이 없다.** `D78`·`D79` 는 *스위칭* 소자를 설명하므로
> 워크북의 열 답은 **더 시원한 쪽**에 대한 것이다. SMath `P.mos_dev` = 1.426 W 가
> 방열 설계의 기준이다. 이 한계는 워크북 CHANGELOG 82행에 적어 두었다.
